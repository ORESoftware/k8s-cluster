// AUTOGENERATED by remote/libs/nats/subject-defs/src/generate.mjs
// Do not edit by hand; edit JSON Schema under schema/ and regenerate.
// Source schemas: remote/libs/nats/subject-defs/schema/*.schema.json
//
// Pure ESM JavaScript (.mjs) variant of the TypeScript output, intended for
// plain Node consumers (nats-bridge .mjs files, lambda child runtimes) that
// would otherwise need a TS toolchain to import the .ts module. The values
// here are kept byte-for-byte identical to the TS output - only the type
// annotations are removed.

// ---------- Static subjects ----------

/**
 * Generic container pool request subject (legacy default; specific pools usually use ContainerPoolLanguageRequests with their own runtime prefix).
 * Service: dd-container-pool
 */
export const CONTAINER_POOL_REQUESTS_SUBJECT = "dd.remote.container_pool.requests";

/**
 * Generic container pool result subject.
 * Service: dd-container-pool
 */
export const CONTAINER_POOL_RESULTS_SUBJECT = "dd.remote.container_pool.results";

/**
 * Published validation/simulation results. Default for CONTRACT_RESULT_SUBJECT.
 * Service: dd-contract-service
 */
export const CONTRACTS_SOLANA_RESULTS_SUBJECT = "dd.remote.contracts.solana.results";

/**
 * Validation requests for solana.contract.v1 instruction envelopes. Default for CONTRACT_VALIDATE_SUBJECT.
 * Service: dd-contract-service
 */
export const CONTRACTS_SOLANA_VALIDATE_SUBJECT = "dd.remote.contracts.solana.validate";
export const CONTRACTS_SOLANA_VALIDATE_QUEUE_GROUP = "dd-contract-service";

/**
 * Scheduled prompts published by dd-chron-service. A producer converts these into normal per-thread task messages so cron doesn't bypass idempotency or affinity.
 * Service: dd-remote-rest-api
 */
export const CRON_PROMPTS_SUBJECT = "dd.remote.cron.prompts";
export const CRON_PROMPTS_STREAM = "DD_REMOTE_CRON";

/**
 * Discrete-event simulation results. Default for DES_RESULT_SUBJECT.
 * Service: dd-ai-ml-pipeline
 */
export const DES_RESULTS_SUBJECT = "dd.remote.des.results";

/**
 * Discrete-event simulation job requests. Default for DES_SIMULATE_SUBJECT.
 * Service: dd-ai-ml-pipeline
 */
export const DES_SIMULATE_SUBJECT = "dd.remote.des.simulate";
export const DES_SIMULATE_QUEUE_GROUP = "dd-des-simulator";

/**
 * Design input conversion requests for CAD, mesh, slicer, CAM setup, and neutral export workers.
 * Service: dd-fabrication-server
 */
export const FABRICATION_DESIGN_CONVERSION_REQUESTS_SUBJECT = "dd.remote.fabrication.design.conversion.requests";
export const FABRICATION_DESIGN_CONVERSION_REQUESTS_QUEUE_GROUP = "dd-fabrication-design-converters";

/**
 * Design input conversion results carrying neutral exports, translator evidence, blockers, and review metadata.
 * Service: dd-fabrication-server
 */
export const FABRICATION_DESIGN_CONVERSION_RESULTS_SUBJECT = "dd.remote.fabrication.design.conversion.results";

/**
 * Instruction generation requests for slicer, CAM, postprocess, setup-sheet, and machine-code workers.
 * Service: dd-fabrication-server
 */
export const FABRICATION_INSTRUCTION_GENERATION_REQUESTS_SUBJECT = "dd.remote.fabrication.instructions.generation.requests";
export const FABRICATION_INSTRUCTION_GENERATION_REQUESTS_QUEUE_GROUP = "dd-fabrication-instruction-generators";

/**
 * Instruction generation results carrying machine code, setup sheets, simulation evidence, blockers, and release metadata.
 * Service: dd-fabrication-server
 */
export const FABRICATION_INSTRUCTION_GENERATION_RESULTS_SUBJECT = "dd.remote.fabrication.instructions.generation.results";

/**
 * Fabrication planning requests. Default for FABRICATION_REQUEST_SUBJECT.
 * Service: dd-fabrication-server
 */
export const FABRICATION_REQUESTS_SUBJECT = "dd.remote.fabrication.requests";
export const FABRICATION_REQUESTS_QUEUE_GROUP = "dd-fabrication-server";

/**
 * Fabrication planning and validation results. Default for FABRICATION_RESULT_SUBJECT.
 * Service: dd-fabrication-server
 */
export const FABRICATION_RESULTS_SUBJECT = "dd.remote.fabrication.results";

/**
 * Coalesced fan-out of known_git_repos row changes derived from the WAL/CDC stream. Published by dd-remote-rest-api so downstream services (lambda runner, build pipeline) react to git-repo metadata edits without polling.
 * Service: shared
 */
export const GIT_REPOS_CHANGES_SUBJECT = "dd.remote.git-repos.changes";

/**
 * Functions metadata broadcast subject. Default for NATS_LAMBDA_FUNCTIONS_SUBJECT.
 * Service: dd-gleam-lambda-runner
 */
export const LAMBDAS_FUNCTIONS_SUBJECT = "dd.remote.lambdas.functions";

/**
 * Results fan-out from lambda runs. Default for NATS_LAMBDA_RESULT_SUBJECT.
 * Service: dd-gleam-lambda-runner
 */
export const LAMBDAS_RESULTS_SUBJECT = "dd.remote.lambdas.results";

/**
 * MDP/POMDP optimization job requests. Default for MDP_OPTIMIZE_SUBJECT.
 * Service: dd-ai-ml-pipeline
 */
export const MDP_OPTIMIZE_SUBJECT = "dd.remote.mdp.optimize";
export const MDP_OPTIMIZE_QUEUE_GROUP = "dd-mdp-optimizer";

/**
 * MDP optimization results published by the optimizer. Default for MDP_RESULT_SUBJECT.
 * Service: dd-ai-ml-pipeline
 */
export const MDP_RESULTS_SUBJECT = "dd.remote.mdp.results";

/**
 * Control-plane commands for distributed in-house MIP solves, including solve cancellation and revision invalidation.
 * Service: dd-ai-ml-pipeline
 */
export const MIP_SOLVER_CONTROL_SUBJECT = "dd.remote.mip_solver.control";
export const MIP_SOLVER_CONTROL_STREAM = "DD_REMOTE_MIP_SOLVER";

/**
 * Lifecycle and progress events from distributed in-house LP/MIP/IP solver nodes.
 * Service: dd-ai-ml-pipeline
 */
export const MIP_SOLVER_EVENTS_SUBJECT = "dd.remote.mip_solver.events";
export const MIP_SOLVER_EVENTS_STREAM = "DD_REMOTE_MIP_SOLVER";

/**
 * Distributed in-house LP/MIP/IP subproblem jobs. Masters publish branch-and-bound relaxation/subtree work; slave pods consume through the worker queue group.
 * Service: dd-ai-ml-pipeline
 */
export const MIP_SOLVER_JOBS_SUBJECT = "dd.remote.mip_solver.jobs";
export const MIP_SOLVER_JOBS_QUEUE_GROUP = "dd-in-house-mip-solver-node-workers";
export const MIP_SOLVER_JOBS_STREAM = "DD_REMOTE_MIP_SOLVER";

/**
 * Distributed in-house LP/MIP/IP subproblem results. Slaves publish solved LP relaxation/subtree summaries and masters aggregate them by solveId.
 * Service: dd-ai-ml-pipeline
 */
export const MIP_SOLVER_RESULTS_SUBJECT = "dd.remote.mip_solver.results";
export const MIP_SOLVER_RESULTS_STREAM = "DD_REMOTE_MIP_SOLVER";

/**
 * Dead-letter target for ai-ml-pipeline messages that failed validation/processing. Default for ML_DEAD_LETTER_SUBJECT.
 * Service: dd-ai-ml-pipeline
 */
export const ML_DEAD_LETTER_SUBJECT = "dd.remote.ml.deadletter";

/**
 * Derived ML features (z-scores, EWMA baselines) published by ai-ml-pipeline. Default for ML_FEATURE_SUBJECT.
 * Service: dd-ai-ml-pipeline
 */
export const ML_FEATURES_SUBJECT = "dd.remote.ml.features";

/**
 * Wakeup signal published whenever a new task is enqueued for a thread, so the orchestrator can prepare/scale the matching worker deployment without polling.
 * Service: dd-remote-rest-api
 */
export const ORCHESTRATOR_WAKEUP_SUBJECT = "dd.remote.orchestrator.wakeup";
export const ORCHESTRATOR_WAKEUP_STREAM = "DD_REMOTE_CONTROL";

/**
 * Critical operational event bus for compact alert-worthy runtime failures. JetStream-backed by DD_REMOTE_CRITICAL_EVENTS so dd-remote-queue-consumer can log/alert without losing events during restarts. Payloads should carry a dd.log.v1-compatible envelope and must not contain secrets.
 * Service: shared
 */
export const RUNTIME_CRITICAL_EVENTS_SUBJECT = "dd.remote.events.critical";
export const RUNTIME_CRITICAL_EVENTS_QUEUE_GROUP = "dd-runtime-critical-events";
export const RUNTIME_CRITICAL_EVENTS_STREAM = "DD_REMOTE_CRITICAL_EVENTS";

/**
 * Generic runtime event bus. Every deployment publishes lifecycle, error, telemetry-style events here. The default for NATS_EVENT_SUBJECT across the codebase.
 * Service: shared
 */
export const RUNTIME_EVENTS_SUBJECT = "dd.remote.events";

/**
 * MDP telemetry requests emitted by ai-ml-pipeline and consumed by the MDP optimizer. Default for ML_MDP_TELEMETRY_SUBJECT / MDP_TELEMETRY_SUBJECT.
 * Service: dd-ai-ml-pipeline
 */
export const TELEMETRY_MDP_SUBJECT = "dd.remote.telemetry.mdp";

/**
 * Raw runtime telemetry samples ingested by the ai-ml-pipeline. Default for ML_RAW_TELEMETRY_SUBJECT.
 * Service: dd-ai-ml-pipeline
 */
export const TELEMETRY_RAW_SUBJECT = "dd.remote.telemetry.raw";
export const TELEMETRY_RAW_QUEUE_GROUP = "dd-ai-ml-pipeline";

/**
 * Risk-gated buy/sell/hold decisions emitted by the trading server. Default for TRADING_DECISION_SUBJECT.
 * Service: dd-trading-server
 */
export const TRADING_DECISIONS_SUBJECT = "dd.remote.trading.decisions";

/**
 * Concrete order intents derived from decisions. Default for TRADING_ORDER_INTENT_SUBJECT.
 * Service: dd-trading-server
 */
export const TRADING_ORDER_INTENTS_SUBJECT = "dd.remote.trading.order_intents";

/**
 * Inbound market signals consumed by the trading server. Default for TRADING_SIGNAL_SUBJECT.
 * Service: dd-trading-server
 */
export const TRADING_SIGNALS_SUBJECT = "dd.remote.trading.signals";
export const TRADING_SIGNALS_QUEUE_GROUP = "dd-trading-server";

/**
 * WebSocket event bridge used by gleamlang-server and gleamlang-ws-server. Defaults to NATS_PUBLISH_SUBJECT='dd.remote.websocket.events'.
 * Service: shared
 */
export const WEBSOCKET_EVENTS_SUBJECT = "dd.remote.websocket.events";

// ---------- Parameterized subjects ----------

/**
 * Per-row change emitted by wal-gateway. Subject pattern is '<prefix>.<schema>.<table>.<op>'. The default prefix is 'cdc' and the default stream name is 'CDC'. Consumers usually subscribe to the prefix tail wildcard ('cdc.>').
 * Service: dd-wal-gateway
 */
export const CDC_ROW_CHANGE_PATTERN = "{prefix}.{schema}.{table}.{op}";
export const CDC_ROW_CHANGE_WILDCARD = "{prefix}.>";
export const CDC_ROW_CHANGE_STREAM = "CDC";
export function cdcRowChangeSubject(prefix, schema, table, op) {
  return `${prefix}.${schema}.${table}.${op}`;
}
export function parseCdcRowChangeSubject(subject) {
  const patternTokens = ["{prefix}","{schema}","{table}","{op}"];
  const subjectTokens = subject.split(".");
  const result = {};
  let si = 0;
  for (let pi = 0; pi < patternTokens.length; pi += 1) {
    const tok = patternTokens[pi];
    const phMatch = /^\{([a-zA-Z_][a-zA-Z0-9_]*)\}$/.exec(tok);
    if (phMatch) {
      if (si >= subjectTokens.length) return null;
      result[phMatch[1]] = subjectTokens[si];
      si += 1;
      continue;
    }
    if (si >= subjectTokens.length || subjectTokens[si] !== tok) return null;
    si += 1;
  }
  if (si !== subjectTokens.length) return null;
  return result;
}

/**
 * Per-table JetStream filter subject ('<prefix>.<schema>.<table>.>') used by CDC consumers (e.g. dd-remote-rest-api) to subscribe to every op for one Postgres table. Not a publish target; producers publish per-row via CdcRowChange.
 * Service: dd-wal-gateway
 */
export const CDC_TABLE_FILTER_PATTERN = "{prefix}.{schema}.{table}.>";
export const CDC_TABLE_FILTER_WILDCARD = "{prefix}.>";
export const CDC_TABLE_FILTER_STREAM = "CDC";
export function cdcTableFilterSubject(prefix, schema, table) {
  return `${prefix}.${schema}.${table}.>`;
}
export function parseCdcTableFilterSubject(subject) {
  const patternTokens = ["{prefix}","{schema}","{table}",">"];
  const subjectTokens = subject.split(".");
  const result = {};
  let si = 0;
  for (let pi = 0; pi < patternTokens.length; pi += 1) {
    const tok = patternTokens[pi];
    const phMatch = /^\{([a-zA-Z_][a-zA-Z0-9_]*)\}$/.exec(tok);
    if (phMatch) {
      if (si >= subjectTokens.length) return null;
      result[phMatch[1]] = subjectTokens[si];
      si += 1;
      continue;
    }
    if (si >= subjectTokens.length || subjectTokens[si] !== tok) return null;
    si += 1;
  }
  if (si !== subjectTokens.length) return null;
  return result;
}

/**
 * Per-pool event subject (built from pool slug at runtime).
 * Service: dd-container-pool
 */
export const CONTAINER_POOL_EVENTS_PATTERN = "dd.remote.container_pool.{pool_slug}.events";
export const CONTAINER_POOL_EVENTS_WILDCARD = "dd.remote.container_pool.*.events";
export function containerPoolEventsSubject(poolSlug) {
  return `dd.remote.container_pool.${poolSlug}.events`;
}
export function parseContainerPoolEventsSubject(subject) {
  const patternTokens = ["dd","remote","container_pool","{pool_slug}","events"];
  const subjectTokens = subject.split(".");
  const result = {};
  let si = 0;
  for (let pi = 0; pi < patternTokens.length; pi += 1) {
    const tok = patternTokens[pi];
    const phMatch = /^\{([a-zA-Z_][a-zA-Z0-9_]*)\}$/.exec(tok);
    if (phMatch) {
      if (si >= subjectTokens.length) return null;
      result[phMatch[1]] = subjectTokens[si];
      si += 1;
      continue;
    }
    if (si >= subjectTokens.length || subjectTokens[si] !== tok) return null;
    si += 1;
  }
  if (si !== subjectTokens.length) return null;
  return result;
}

/**
 * Per-pool heartbeat subject (built from pool slug at runtime).
 * Service: dd-container-pool
 */
export const CONTAINER_POOL_HEARTBEATS_PATTERN = "dd.remote.container_pool.{pool_slug}.heartbeats";
export const CONTAINER_POOL_HEARTBEATS_WILDCARD = "dd.remote.container_pool.*.heartbeats";
export function containerPoolHeartbeatsSubject(poolSlug) {
  return `dd.remote.container_pool.${poolSlug}.heartbeats`;
}
export function parseContainerPoolHeartbeatsSubject(subject) {
  const patternTokens = ["dd","remote","container_pool","{pool_slug}","heartbeats"];
  const subjectTokens = subject.split(".");
  const result = {};
  let si = 0;
  for (let pi = 0; pi < patternTokens.length; pi += 1) {
    const tok = patternTokens[pi];
    const phMatch = /^\{([a-zA-Z_][a-zA-Z0-9_]*)\}$/.exec(tok);
    if (phMatch) {
      if (si >= subjectTokens.length) return null;
      result[phMatch[1]] = subjectTokens[si];
      si += 1;
      continue;
    }
    if (si >= subjectTokens.length || subjectTokens[si] !== tok) return null;
    si += 1;
  }
  if (si !== subjectTokens.length) return null;
  return result;
}

/**
 * Per-runtime pool request subject (e.g. nodejs, rust, golang, dart, gleamlang, erlang). Seeded by container-pool-app-config.
 * Service: dd-container-pool
 */
export const CONTAINER_POOL_LANGUAGE_REQUESTS_PATTERN = "dd.remote.container_pool.{language}.requests";
export const CONTAINER_POOL_LANGUAGE_REQUESTS_WILDCARD = "dd.remote.container_pool.*.requests";
export function containerPoolLanguageRequestsSubject(language) {
  return `dd.remote.container_pool.${language}.requests`;
}
export function parseContainerPoolLanguageRequestsSubject(subject) {
  const patternTokens = ["dd","remote","container_pool","{language}","requests"];
  const subjectTokens = subject.split(".");
  const result = {};
  let si = 0;
  for (let pi = 0; pi < patternTokens.length; pi += 1) {
    const tok = patternTokens[pi];
    const phMatch = /^\{([a-zA-Z_][a-zA-Z0-9_]*)\}$/.exec(tok);
    if (phMatch) {
      if (si >= subjectTokens.length) return null;
      result[phMatch[1]] = subjectTokens[si];
      si += 1;
      continue;
    }
    if (si >= subjectTokens.length || subjectTokens[si] !== tok) return null;
    si += 1;
  }
  if (si !== subjectTokens.length) return null;
  return result;
}

/**
 * Invocation subject for a single lambda. Consumers subscribe to the wildcard using the runner's queue group; producers publish to the specific function token. Default for NATS_LAMBDA_INVOKE_SUBJECT='dd.remote.lambdas.invoke.*'.
 * Service: dd-gleam-lambda-runner
 */
export const LAMBDAS_INVOKE_PATTERN = "dd.remote.lambdas.invoke.{function_name}";
export const LAMBDAS_INVOKE_WILDCARD = "dd.remote.lambdas.invoke.*";
export const LAMBDAS_INVOKE_QUEUE_GROUP = "dd-gleam-lambda-runner";
export function lambdasInvokeSubject(functionName) {
  return `dd.remote.lambdas.invoke.${functionName}`;
}
export function parseLambdasInvokeSubject(subject) {
  const patternTokens = ["dd","remote","lambdas","invoke","{function_name}"];
  const subjectTokens = subject.split(".");
  const result = {};
  let si = 0;
  for (let pi = 0; pi < patternTokens.length; pi += 1) {
    const tok = patternTokens[pi];
    const phMatch = /^\{([a-zA-Z_][a-zA-Z0-9_]*)\}$/.exec(tok);
    if (phMatch) {
      if (si >= subjectTokens.length) return null;
      result[phMatch[1]] = subjectTokens[si];
      si += 1;
      continue;
    }
    if (si >= subjectTokens.length || subjectTokens[si] !== tok) return null;
    si += 1;
  }
  if (si !== subjectTokens.length) return null;
  return result;
}

/**
 * Broadcast a websocket frame to every connection in a conv. Consumers subscribe via the wildcard 'presence.broadcast.conv.>' so they receive all conv broadcasts and dispatch locally.
 * Service: dd-gleamlang-presence-server
 */
export const PRESENCE_BROADCAST_CONV_PATTERN = "presence.broadcast.conv.{conv_id}";
export const PRESENCE_BROADCAST_CONV_WILDCARD = "presence.broadcast.conv.>";
export function presenceBroadcastConvSubject(convId) {
  return `presence.broadcast.conv.${convId}`;
}
export function parsePresenceBroadcastConvSubject(subject) {
  const patternTokens = ["presence","broadcast","conv","{conv_id}"];
  const subjectTokens = subject.split(".");
  const result = {};
  let si = 0;
  for (let pi = 0; pi < patternTokens.length; pi += 1) {
    const tok = patternTokens[pi];
    const phMatch = /^\{([a-zA-Z_][a-zA-Z0-9_]*)\}$/.exec(tok);
    if (phMatch) {
      if (si >= subjectTokens.length) return null;
      result[phMatch[1]] = subjectTokens[si];
      si += 1;
      continue;
    }
    if (si >= subjectTokens.length || subjectTokens[si] !== tok) return null;
    si += 1;
  }
  if (si !== subjectTokens.length) return null;
  return result;
}

/**
 * Optional per-conv membership-change channel (reserved; today membership changes go through PG NOTIFY).
 * Service: dd-gleamlang-presence-server
 */
export const PRESENCE_MEMBER_CHANGE_CONV_PATTERN = "presence.member_change.conv.{conv_id}";
export const PRESENCE_MEMBER_CHANGE_CONV_WILDCARD = "presence.member_change.conv.>";
export function presenceMemberChangeConvSubject(convId) {
  return `presence.member_change.conv.${convId}`;
}
export function parsePresenceMemberChangeConvSubject(subject) {
  const patternTokens = ["presence","member_change","conv","{conv_id}"];
  const subjectTokens = subject.split(".");
  const result = {};
  let si = 0;
  for (let pi = 0; pi < patternTokens.length; pi += 1) {
    const tok = patternTokens[pi];
    const phMatch = /^\{([a-zA-Z_][a-zA-Z0-9_]*)\}$/.exec(tok);
    if (phMatch) {
      if (si >= subjectTokens.length) return null;
      result[phMatch[1]] = subjectTokens[si];
      si += 1;
      continue;
    }
    if (si >= subjectTokens.length || subjectTokens[si] !== tok) return null;
    si += 1;
  }
  if (si !== subjectTokens.length) return null;
  return result;
}

/**
 * Per-thread control-plane commands (scale, evict, reload). Short-retention JetStream control stream.
 * Service: dd-remote-rest-api
 */
export const THREAD_CONTROL_PATTERN = "dd.remote.thread.{thread_id}.control";
export const THREAD_CONTROL_WILDCARD = "dd.remote.thread.*.control";
export const THREAD_CONTROL_STREAM = "DD_REMOTE_CONTROL";
export function threadControlSubject(threadId) {
  return `dd.remote.thread.${threadId}.control`;
}
export function parseThreadControlSubject(subject) {
  const patternTokens = ["dd","remote","thread","{thread_id}","control"];
  const subjectTokens = subject.split(".");
  const result = {};
  let si = 0;
  for (let pi = 0; pi < patternTokens.length; pi += 1) {
    const tok = patternTokens[pi];
    const phMatch = /^\{([a-zA-Z_][a-zA-Z0-9_]*)\}$/.exec(tok);
    if (phMatch) {
      if (si >= subjectTokens.length) return null;
      result[phMatch[1]] = subjectTokens[si];
      si += 1;
      continue;
    }
    if (si >= subjectTokens.length || subjectTokens[si] !== tok) return null;
    si += 1;
  }
  if (si !== subjectTokens.length) return null;
  return result;
}

/**
 * Per-thread worker events (progress, status, output). Longer-retention JetStream events stream.
 * Service: dd-remote-rest-api
 */
export const THREAD_EVENTS_PATTERN = "dd.remote.thread.{thread_id}.events";
export const THREAD_EVENTS_WILDCARD = "dd.remote.thread.*.events";
export const THREAD_EVENTS_STREAM = "DD_REMOTE_EVENTS";
export function threadEventsSubject(threadId) {
  return `dd.remote.thread.${threadId}.events`;
}
export function parseThreadEventsSubject(subject) {
  const patternTokens = ["dd","remote","thread","{thread_id}","events"];
  const subjectTokens = subject.split(".");
  const result = {};
  let si = 0;
  for (let pi = 0; pi < patternTokens.length; pi += 1) {
    const tok = patternTokens[pi];
    const phMatch = /^\{([a-zA-Z_][a-zA-Z0-9_]*)\}$/.exec(tok);
    if (phMatch) {
      if (si >= subjectTokens.length) return null;
      result[phMatch[1]] = subjectTokens[si];
      si += 1;
      continue;
    }
    if (si >= subjectTokens.length || subjectTokens[si] !== tok) return null;
    si += 1;
  }
  if (si !== subjectTokens.length) return null;
  return result;
}

/**
 * Per-thread worker liveness pings.
 * Service: dd-remote-rest-api
 */
export const THREAD_HEARTBEAT_PATTERN = "dd.remote.thread.{thread_id}.heartbeat";
export const THREAD_HEARTBEAT_WILDCARD = "dd.remote.thread.*.heartbeat";
export const THREAD_HEARTBEAT_STREAM = "DD_REMOTE_EVENTS";
export function threadHeartbeatSubject(threadId) {
  return `dd.remote.thread.${threadId}.heartbeat`;
}
export function parseThreadHeartbeatSubject(subject) {
  const patternTokens = ["dd","remote","thread","{thread_id}","heartbeat"];
  const subjectTokens = subject.split(".");
  const result = {};
  let si = 0;
  for (let pi = 0; pi < patternTokens.length; pi += 1) {
    const tok = patternTokens[pi];
    const phMatch = /^\{([a-zA-Z_][a-zA-Z0-9_]*)\}$/.exec(tok);
    if (phMatch) {
      if (si >= subjectTokens.length) return null;
      result[phMatch[1]] = subjectTokens[si];
      si += 1;
      continue;
    }
    if (si >= subjectTokens.length || subjectTokens[si] !== tok) return null;
    si += 1;
  }
  if (si !== subjectTokens.length) return null;
  return result;
}

/**
 * Per-thread task queue. JetStream-backed (DD_REMOTE_TASKS). Producers publish per-thread; consumers either subscribe to the exact subject (the worker for that thread) or to the wildcard via a queue group (the preparer).
 * Service: dd-remote-rest-api
 */
export const THREAD_TASKS_PATTERN = "dd.remote.thread.{thread_id}.tasks";
export const THREAD_TASKS_WILDCARD = "dd.remote.thread.*.tasks";
export const THREAD_TASKS_QUEUE_GROUP = "dd-remote-thread-preparer";
export const THREAD_TASKS_STREAM = "DD_REMOTE_TASKS";
export function threadTasksSubject(threadId) {
  return `dd.remote.thread.${threadId}.tasks`;
}
export function parseThreadTasksSubject(subject) {
  const patternTokens = ["dd","remote","thread","{thread_id}","tasks"];
  const subjectTokens = subject.split(".");
  const result = {};
  let si = 0;
  for (let pi = 0; pi < patternTokens.length; pi += 1) {
    const tok = patternTokens[pi];
    const phMatch = /^\{([a-zA-Z_][a-zA-Z0-9_]*)\}$/.exec(tok);
    if (phMatch) {
      if (si >= subjectTokens.length) return null;
      result[phMatch[1]] = subjectTokens[si];
      si += 1;
      continue;
    }
    if (si >= subjectTokens.length || subjectTokens[si] !== tok) return null;
    si += 1;
  }
  if (si !== subjectTokens.length) return null;
  return result;
}

// ---------- Standalone queue groups ----------

/**
 * Durable queue group used by dd-remote-queue-consumer replicas for critical runtime event logging and future alert fan-out.
 * Service: shared
 */
export const CRITICAL_EVENTS_LOGGER_QUEUE_GROUP = "dd-runtime-critical-events";

/**
 * Shared queue group used by lambda-runner replicas.
 * Service: dd-gleam-lambda-runner
 */
export const LAMBDA_RUNNER_QUEUE_GROUP = "dd-gleam-lambda-runner";

/**
 * Shared queue group used by slave solver pods so each branch-and-bound subproblem is solved once.
 * Service: dd-ai-ml-pipeline
 */
export const MIP_SOLVER_WORKERS_QUEUE_GROUP = "dd-in-house-mip-solver-node-workers";

/**
 * Shared queue group used by dd-remote-queue-consumer replicas so each task is only prepared once.
 * Service: dd-remote-rest-api
 */
export const THREAD_PREPARER_QUEUE_GROUP = "dd-remote-thread-preparer";

// ---------- JetStream streams ----------

/**
 * Default JetStream stream for the wal-gateway publish path. Overridable via WAL_GATEWAY_STREAM_NAME.
 * Service: dd-wal-gateway
 */
export const CDC_STREAM_NAME = "CDC";
export const CDC_STREAM_SUBJECTS = Object.freeze(["cdc.>"]);
export const CDC_STREAM_RETENTION = "limits";
export const CDC_STREAM_STORAGE = "file";
export const CDC_STREAM_ACK = "explicit";

/**
 * Short-retention control plane stream.
 * Service: dd-remote-rest-api
 */
export const DD_REMOTE_CONTROL_STREAM_NAME = "DD_REMOTE_CONTROL";
export const DD_REMOTE_CONTROL_STREAM_SUBJECTS = Object.freeze(["dd.remote.thread.*.control","dd.remote.orchestrator.wakeup"]);
export const DD_REMOTE_CONTROL_STREAM_RETENTION = "limits";
export const DD_REMOTE_CONTROL_STREAM_STORAGE = "file";
export const DD_REMOTE_CONTROL_STREAM_ACK = "explicit";

/**
 * Durable file-backed stream for alert-worthy runtime failures. Consumers log and alert from this stream; payloads must stay compact and redacted.
 * Service: shared
 */
export const DD_REMOTE_CRITICAL_EVENTS_STREAM_NAME = "DD_REMOTE_CRITICAL_EVENTS";
export const DD_REMOTE_CRITICAL_EVENTS_STREAM_SUBJECTS = Object.freeze(["dd.remote.events.critical"]);
export const DD_REMOTE_CRITICAL_EVENTS_STREAM_RETENTION = "limits";
export const DD_REMOTE_CRITICAL_EVENTS_STREAM_STORAGE = "file";
export const DD_REMOTE_CRITICAL_EVENTS_STREAM_ACK = "explicit";

/**
 * Cron-initiated jobs.
 * Service: dd-remote-rest-api
 */
export const DD_REMOTE_CRON_STREAM_NAME = "DD_REMOTE_CRON";
export const DD_REMOTE_CRON_STREAM_SUBJECTS = Object.freeze(["dd.remote.cron.prompts"]);
export const DD_REMOTE_CRON_STREAM_RETENTION = "limits";
export const DD_REMOTE_CRON_STREAM_STORAGE = "file";
export const DD_REMOTE_CRON_STREAM_ACK = "explicit";

/**
 * Longer-retention event stream for replay/debugging, mirrored into Postgres.
 * Service: dd-remote-rest-api
 */
export const DD_REMOTE_EVENTS_STREAM_NAME = "DD_REMOTE_EVENTS";
export const DD_REMOTE_EVENTS_STREAM_SUBJECTS = Object.freeze(["dd.remote.thread.*.events","dd.remote.thread.*.heartbeat"]);
export const DD_REMOTE_EVENTS_STREAM_RETENTION = "limits";
export const DD_REMOTE_EVENTS_STREAM_STORAGE = "file";
export const DD_REMOTE_EVENTS_STREAM_ACK = "explicit";

/**
 * JetStream stream for distributed in-house LP/MIP/IP solver work, results, control, and progress events.
 * Service: dd-ai-ml-pipeline
 */
export const DD_REMOTE_MIP_SOLVER_STREAM_NAME = "DD_REMOTE_MIP_SOLVER";
export const DD_REMOTE_MIP_SOLVER_STREAM_SUBJECTS = Object.freeze(["dd.remote.mip_solver.jobs","dd.remote.mip_solver.results","dd.remote.mip_solver.control","dd.remote.mip_solver.events"]);
export const DD_REMOTE_MIP_SOLVER_STREAM_RETENTION = "limits";
export const DD_REMOTE_MIP_SOLVER_STREAM_STORAGE = "file";
export const DD_REMOTE_MIP_SOLVER_STREAM_ACK = "explicit";

/**
 * JetStream file storage, explicit ack, message dedupe by Nats-Msg-Id ('remote-task:<taskId>'). Postgres remains the real idempotency guard.
 * Service: dd-remote-rest-api
 */
export const DD_REMOTE_TASKS_STREAM_NAME = "DD_REMOTE_TASKS";
export const DD_REMOTE_TASKS_STREAM_SUBJECTS = Object.freeze(["dd.remote.thread.*.tasks"]);
export const DD_REMOTE_TASKS_STREAM_RETENTION = "limits";
export const DD_REMOTE_TASKS_STREAM_STORAGE = "file";
export const DD_REMOTE_TASKS_STREAM_ACK = "explicit";
