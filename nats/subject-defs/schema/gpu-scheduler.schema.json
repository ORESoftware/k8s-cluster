{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dd.dev/schemas/nats/gpu-scheduler.schema.json",
  "title": "GpuSchedulerNats",
  "description": "NATS subjects owned by dd-gpu-rs (GPU job scheduler: assign submitted jobs to GPUs respecting per-GPU VRAM capacity over time, minimising makespan). Submit work, get result; AI/ML training and inference workloads fit naturally. The server subscribes to GPU job requests under the dd-gpu-rs queue group, publishes computed placements plus runtime events. Payloads carry a gpu.schedule.v1 schemaVersion envelope and must not contain secrets.",
  "type": "object",
  "$dd:nats": {
    "service": "dd-gpu-rs",
    "summary": "GPU job scheduling (VRAM-aware placement of AI/ML jobs onto GPUs) request/result pipeline.",
    "subjects": [
      {
        "name": "GpuJobRequests",
        "description": "Inbound GPU job-scheduling requests (a fleet of GPUs plus jobs needing VRAM and an estimated duration) consumed by the scheduler. Subscribed with the dd-gpu-rs queue group so requests load-balance across replicas. Default for GPU_JOB_SUBJECT.",
        "kind": "static",
        "subject": "dd.remote.gpu.jobs.requests",
        "queueGroup": "dd-gpu-rs",
        "direction": "both"
      },
      {
        "name": "GpuJobResults",
        "description": "Computed GPU placements (per-job gpu assignment, start/finish times, makespan, per-GPU memory utilisation, rejected jobs) emitted by the scheduler. Carries a gpu.schedule.v1 envelope. Default for GPU_RESULT_SUBJECT.",
        "kind": "static",
        "subject": "dd.remote.gpu.jobs.results",
        "direction": "both"
      }
    ],
    "queueGroups": [
      {
        "name": "GpuSchedulerQueueGroup",
        "value": "dd-gpu-rs",
        "description": "Shared queue group used by dd-gpu-rs replicas consuming GPU job requests."
      }
    ]
  }
}
