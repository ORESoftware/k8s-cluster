# Application pins

Each entry is a gitlink to one independently versioned Fiducia repository. The
superproject records reviewed integration commits; edit and validate the child
repository first, then advance its pin here without copying source into `apps/`.
