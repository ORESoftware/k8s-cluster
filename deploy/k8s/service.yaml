apiVersion: v1
kind: Service
metadata:
  name: threefa-sync-server
  namespace: threefa
  labels:
    app.kubernetes.io/name: threefa-sync-server
    app.kubernetes.io/part-of: threefa
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: threefa-sync-server
  ports:
    - name: http
      port: 8080
      targetPort: http
