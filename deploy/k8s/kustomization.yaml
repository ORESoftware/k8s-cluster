apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespace-scoped only — the `shared-auth` Namespace is Layer-1 (k8s-cluster).
namespace: shared-auth

resources:
  - deployment.yaml
  - service.yaml
  - externalsecret.yaml
  - networkpolicy.yaml
