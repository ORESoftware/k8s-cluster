apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespace-scoped resources only — the `shared-auth` Namespace itself is Layer-1
# (owned by ORESoftware/k8s-cluster). See DESIGN.md / k8s-cluster
# docs/app-deploy-contract.md.
namespace: shared-auth

resources:
  - deployment.yaml
  - service.yaml
  - externalsecret.yaml
  - networkpolicy.yaml
