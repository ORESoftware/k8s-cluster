name: monorepo-inventory

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  inventory:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Validate apps-only submodule inventory
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t paths < <(git config -f .gitmodules --get-regexp '^submodule\..*\.path$' | awk '{print $2}' | sort)
          expected_paths=(
            apps/shared-auth-clients
            apps/shared-auth-e2e
            apps/shared-auth-infra
            apps/shared-auth-interfaces
            apps/shared-auth-lib
            apps/shared-auth-mcp-server.rs
            apps/shared-auth-server.rs
            apps/shared-auth-sync
          )
          test "${paths[*]}" = "${expected_paths[*]}"
          for path in "${paths[@]}"; do
            case "$path" in
              apps/*) ;;
              *) echo "submodule outside apps/: $path" >&2; exit 1 ;;
            esac
            test "$(git ls-files --stage -- "$path" | awk '{print $1}')" = 160000
          done
          test -z "$(git config -f .gitmodules --get-regexp '^submodule\..*\.url$' | awk '$2 !~ /^git@github\.com:shared-auth\// {print}')"
          test "$(git config -f .gitmodules --get-regexp '^submodule\..*\.branch$' | wc -l | tr -d ' ')" -eq "${#expected_paths[@]}"
          test -z "$(git config -f .gitmodules --get-regexp '^submodule\..*\.branch$' | awk '$2 != "main" {print}')"
