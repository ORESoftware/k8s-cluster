name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --check
      - name: Run Clippy and preserve final Web Push diagnostics
        run: |
          set +e
          cargo clippy --locked --all-targets --all-features -- -D warnings > web-push-final-clippy.log 2>&1
          status=$?
          echo "$status" > web-push-final-clippy.exit
          exit 0
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: web-push-final-clippy-diagnostics
          path: web-push-final-clippy.log
          if-no-files-found: error
      - name: Enforce Clippy result
        run: exit "$(cat web-push-final-clippy.exit)"
      - run: cargo test --locked --all-features

  container:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          tags: push-notification-server:test
