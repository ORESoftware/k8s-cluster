name: Deprecated PR 636 finalizer executor

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'This temporary branch-writing executor is intentionally disabled.'
          exit 1
