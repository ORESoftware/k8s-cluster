name: agent-format-den-280-batch

on:
  push:
    branches:
      - agent/den-280-batch-ack-postgres

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/den-280-batch-ack-postgres
          fetch-depth: 0
      - run: cargo fmt --all
      - name: Commit formatting and remove one-shot workflow
        run: |
          rm .github/workflows/agent-format-den-280-batch.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src .github/workflows
          if ! git diff --cached --quiet; then
            git commit -m "Format DEN-280 batch acknowledgement slice"
            git push origin "HEAD:${GITHUB_REF_NAME}"
          fi
