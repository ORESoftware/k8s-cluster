name: dependency lock refresh

on:
  pull_request:
    paths:
      - Cargo.toml
      - Cargo.lock
      - .github/workflows/dependency-lock-refresh.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: dependency-lock-refresh-${{ github.ref }}
  cancel-in-progress: true

jobs:
  prometheus:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
          show-progress: false

      - name: Install pinned Rust
        uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c
        with:
          toolchain: 1.88.0

      - name: Refresh only the Prometheus dependency graph
        run: cargo update -p prometheus@0.13.4 --precise 0.14.0

      - name: Verify the vulnerable protobuf release is gone
        run: |
          if grep -A3 '^name = "protobuf"$' Cargo.lock | grep -Fq 'version = "2.28.0"'; then
            echo 'vulnerable protobuf 2.28.0 remains in Cargo.lock' >&2
            exit 1
          fi
          cargo tree -i protobuf

      - name: Upload reviewed Cargo lock
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: prometheus-0.14-cargo-lock
          path: Cargo.lock
          retention-days: 1
          if-no-files-found: error
