name: apply public runtime OpenAPI hardening

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: apply-public-runtime-openapi
  cancel-in-progress: true

jobs:
  apply:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.head_ref == 'agent/openapi-contract-hardening'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout writable feature branch
        uses: actions/checkout@v7
        with:
          ref: agent/openapi-contract-hardening
          fetch-depth: 0
          show-progress: false

      - name: Apply public-runtime contract hardening
        run: node scripts/api-contract/make-runtime-docs-public.mjs

      - name: Regenerate public and internal contracts
        run: node remote/tools/generate-api-docs.mjs

      - name: Validate runtime privacy and source parity
        run: |
          node --check remote/tools/generate-api-docs.mjs
          node --check remote/tools/validate-openapi-contracts.mjs
          node --check remote/tests/check-rest-api-route-parity.mjs
          node remote/tools/validate-openapi-contracts.mjs
          node remote/tests/check-rest-api-route-parity.mjs
          git diff --check

      - name: Remove one-shot patch script
        run: node -e "require('node:fs').unlinkSync('scripts/api-contract/make-runtime-docs-public.mjs')"

      - name: Commit public-runtime contract hardening
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A AGENTS.md docs remote scripts
          git diff --cached --exit-code && exit 1 || true
          git commit -m "Serve only public OpenAPI contracts at runtime [api-contract-generated]"
          git push origin HEAD:agent/openapi-contract-hardening
