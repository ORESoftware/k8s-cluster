name: Dispatch authorized sustainable-bike bundle import from exact carrier
run-name: Dispatch authorized sustainable-bike bundle import from ${{ github.event_name }} @ ${{ github.sha }}

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/ops-dispatch-sustainable-bike-authorized-bundle-pr-target.yml
      - .github/workflows/ops-publish-missing-org-repositories-dispatch.yml
      - scripts/ops/import_sustainable_bike_bundle_via_ssm.sh
  pull_request_target:
    types: [opened, reopened, synchronize, ready_for_review]
    branches: [main]
    paths:
      - .github/import/sustainable-bike-authorized-bundle-dispatch

permissions:
  actions: write
  contents: read
  issues: write
  pull-requests: write
  statuses: write

concurrency:
  group: den-3843-authorized-bundle-carrier-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: false

jobs:
  validate-workflow:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out exact PR head without credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Validate scripts and immutable route
        shell: bash
        run: |
          set -euo pipefail
          test '${{ github.repository }}' = 'ORESoftware/k8s-cluster'
          bash -n scripts/ops/import_sustainable_bike_bundle_via_ssm.sh
          grep -qF "inputs.request_id == 'den-3843-sustainable-bike-bundle-v1'" \
            .github/workflows/ops-publish-missing-org-repositories-dispatch.yml
          grep -qF 'bash scripts/ops/import_sustainable_bike_bundle_via_ssm.sh' \
            .github/workflows/ops-publish-missing-org-repositories-dispatch.yml
          printf 'request_id=%s bundle=%s target=%s\n' \
            'den-3843-sustainable-bike-bundle-v1' \
            'ba05db2234aa7fb51d3784d3b725cca757b5d2a4b3399b4b60f8fc62982ab756' \
            'ORESoftware/sustainable-bike'

  dispatch-and-monitor:
    if: >-
      github.event_name == 'pull_request_target' &&
      github.event.pull_request.draft == true &&
      github.event.pull_request.user.login == 'ORESoftware' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'agent/den-3843-authorized-bundle-dispatch-carrier' &&
      github.event.pull_request.title == 'DO NOT MERGE: dispatch authorized sustainable-bike bundle import'
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    env:
      REPOSITORY: ORESoftware/k8s-cluster
      PR_NUMBER: ${{ github.event.pull_request.number }}
      MARKER_PATH: .github/import/sustainable-bike-authorized-bundle-dispatch
      WORKFLOW_FILE: ops-publish-missing-org-repositories-dispatch.yml
      REQUEST_ID: den-3843-sustainable-bike-bundle-v1
      EXPECTED_RUN_TITLE: Publish missing organization repositories (den-3843-sustainable-bike-bundle-v1)
    steps:
      - name: Validate exact same-repository metadata carrier
        id: carrier
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -Eeuo pipefail
          pull="$(gh api "repos/${REPOSITORY}/pulls/${PR_NUMBER}")"
          head_sha="$(jq -er '.head.sha | select(test("^[0-9a-f]{40}$"))' <<<"$pull")"
          main_sha="$(gh api "repos/${REPOSITORY}/git/ref/heads/main" --jq '.object.sha')"
          [[ "$main_sha" =~ ^[0-9a-f]{40}$ ]]

          jq -e '
            .draft == true and
            .user.login == "ORESoftware" and
            .base.ref == "main" and
            .head.repo.full_name == "ORESoftware/k8s-cluster" and
            .head.ref == "agent/den-3843-authorized-bundle-dispatch-carrier" and
            .title == "DO NOT MERGE: dispatch authorized sustainable-bike bundle import" and
            .commits == 1 and
            .changed_files == 1 and
            .additions == 5 and
            .deletions == 0
          ' <<<"$pull" >/dev/null

          commits="$(gh api "repos/${REPOSITORY}/pulls/${PR_NUMBER}/commits?per_page=100")"
          test "$(jq 'length' <<<"$commits")" -eq 1
          parent_sha="$(jq -er '.[0].parents[0].sha | select(test("^[0-9a-f]{40}$"))' <<<"$commits")"

          files="$(gh api "repos/${REPOSITORY}/pulls/${PR_NUMBER}/files?per_page=100")"
          jq -e --arg path "$MARKER_PATH" '
            length == 1 and
            .[0].filename == $path and
            .[0].status == "added" and
            .[0].changes == 5
          ' <<<"$files" >/dev/null

          encoded="$(gh api "repos/${REPOSITORY}/contents/${MARKER_PATH}?ref=${head_sha}" --jq '.content')"
          marker="$(printf '%s' "$encoded" | tr -d '\n' | base64 --decode)"
          expected="$(printf '%s\n' \
            'target=ORESoftware/sustainable-bike' \
            'tracking=DEN-3843' \
            'request_id=den-3843-sustainable-bike-bundle-v1' \
            'bundle_sha256=ba05db2234aa7fb51d3784d3b725cca757b5d2a4b3399b4b60f8fc62982ab756' \
            'action=dispatch-authorized-oidc-import')"
          test "$marker" = "$expected"

          comparison="$(gh api "repos/${REPOSITORY}/compare/${parent_sha}...${main_sha}")"
          jq -e '(.status == "ahead" or .status == "identical") and .behind_by == 0' <<<"$comparison" >/dev/null
          printf 'head_sha=%s\nmain_sha=%s\n' "$head_sha" "$main_sha" >> "$GITHUB_OUTPUT"

      - name: Dispatch the already-authorized workflow path
        id: dispatch
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -Eeuo pipefail
          endpoint="repos/${REPOSITORY}/actions/workflows/${WORKFLOW_FILE}/runs?event=workflow_dispatch&branch=main&per_page=30"
          before_id="$(gh api "$endpoint" --jq '.workflow_runs[0].id // 0')"
          [[ "$before_id" =~ ^[0-9]+$ ]]

          gh workflow run "$WORKFLOW_FILE" \
            --repo "$REPOSITORY" \
            --ref main \
            -f request_id="$REQUEST_ID"

          run_json=''
          for _ in $(seq 1 120); do
            runs="$(gh api "$endpoint")"
            run_json="$(
              jq -c \
                --argjson before "$before_id" \
                --arg main "${{ steps.carrier.outputs.main_sha }}" \
                --arg title "$EXPECTED_RUN_TITLE" '
                [.workflow_runs[]
                  | select(.id > $before)
                  | select(.event == "workflow_dispatch")
                  | select(.head_branch == "main")
                  | select(.head_sha == $main)
                  | select(.display_title == $title)]
                | sort_by(.id)
                | first // empty
              ' <<<"$runs"
            )"
            test -n "$run_json" && break
            sleep 2
          done
          test -n "$run_json"

          run_id="$(jq -er '.id | select(type == "number" and . > 0)' <<<"$run_json")"
          run_url="$(jq -er '.html_url | select(startswith("https://github.com/ORESoftware/k8s-cluster/actions/runs/"))' <<<"$run_json")"
          head_sha="$(jq -er '.head_sha | select(test("^[0-9a-f]{40}$"))' <<<"$run_json")"
          test "$head_sha" = '${{ steps.carrier.outputs.main_sha }}'
          printf 'run_id=%s\nrun_url=%s\nhead_sha=%s\n' "$run_id" "$run_url" "$head_sha" >> "$GITHUB_OUTPUT"

          gh pr comment "$PR_NUMBER" --repo "$REPOSITORY" \
            --body "Exact authorized carrier accepted. Dispatched ${run_url} at trusted main \`${head_sha}\` with request \`${REQUEST_ID}\`." >/dev/null

      - name: Monitor authorized import to terminal state
        id: monitor
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_ID: ${{ steps.dispatch.outputs.run_id }}
          RUN_URL: ${{ steps.dispatch.outputs.run_url }}
          HEAD_SHA: ${{ steps.dispatch.outputs.head_sha }}
          CARRIER_HEAD_SHA: ${{ steps.carrier.outputs.head_sha }}
        shell: bash
        run: |
          set -Eeuo pipefail
          [[ "$RUN_ID" =~ ^[0-9]+$ ]]
          status=queued
          conclusion=''
          for _ in $(seq 1 1500); do
            run="$(gh api "repos/${REPOSITORY}/actions/runs/${RUN_ID}")"
            status="$(jq -er .status <<<"$run")"
            conclusion="$(jq -r '.conclusion // empty' <<<"$run")"
            if [[ "$status" == completed ]]; then
              break
            fi
            sleep 2
          done
          test "$status" = completed
          test -n "$conclusion"

          gh pr comment "$PR_NUMBER" --repo "$REPOSITORY" \
            --body "Authorized sustainable-bike bundle import completed with **${conclusion}** at \`${HEAD_SHA}\`: ${RUN_URL}" >/dev/null

          state=failure
          description='authorized sustainable-bike bundle import failed'
          if [[ "$conclusion" == success ]]; then
            state=success
            description='all sustainable-bike bundle commits and refs verified'
          fi
          gh api --method POST "repos/${REPOSITORY}/statuses/${CARRIER_HEAD_SHA}" \
            -f state="$state" \
            -f context='ops/den-3843-authorized-bundle-import' \
            -f description="$description" \
            -f target_url="$RUN_URL" >/dev/null
          printf 'conclusion=%s\n' "$conclusion" >> "$GITHUB_OUTPUT"
          test "$conclusion" = success

      - name: Close successful carrier without merge
        if: steps.monitor.outputs.conclusion == 'success'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          gh pr close "$PR_NUMBER" --repo "$REPOSITORY" \
            --comment 'Authorized protected import succeeded; closing metadata-only carrier without merge. Refs DEN-3843.' >/dev/null
