name: Deprecated Messaging Intel applicator repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'This temporary branch-writing repair workflow is intentionally disabled.'
          exit 1
