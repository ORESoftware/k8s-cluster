name: Deprecated Messaging Intel branch finalizer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'This temporary branch-writing finalizer is intentionally disabled.'
          exit 1
