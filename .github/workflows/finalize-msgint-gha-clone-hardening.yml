name: Finalize Messaging Intel GHA clone hardening

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'The branch finalizer is disabled while its deterministic patch is reviewed.'
