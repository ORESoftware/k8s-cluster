# Rust build output
/target
.direnv/
**/*.rs.bk

# Secrets — never commit real credentials. Only .env.example is tracked.
.env
.env.*
!.env.example

# Editor / OS
.DS_Store

# Cloudflare Wrangler's local account/build cache (may contain account metadata).
.wrangler/
tmp
temp
tmp/worktrees
temp/worktrees
