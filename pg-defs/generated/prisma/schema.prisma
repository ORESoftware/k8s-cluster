// Generated by @dd/pg-defs. Do not edit by hand.
// SOURCE OF TRUTH: schema/schema.sql defines the database contract.
// Generated ORM/client code is an adapter only; do not infer migrations from it.
// MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model AppConfig {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  scope String @default("default") @db.VarChar(120)
  key String @db.VarChar(200)
  value Json
  version Int @default(1)
  status String @default("active") @db.VarChar(32)
  labels Json @default("[]")
  metaData Json @map("meta_data") @default("{}")
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  @@unique([scope, key], map: "app_config_scope_key_uq")
  // app_config_status_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // app_config_updated_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // app_config_labels_gin_idx is defined in schema.sql because Prisma cannot fully model gin partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("app_config")
}

model VapiPhoneCallEvents {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  callId String @map("call_id") @db.VarChar(160)
  eventType String @map("event_type") @db.VarChar(80)
  payloadHash String @map("payload_hash") @db.VarChar(64)
  callerHash String? @map("caller_hash") @db.VarChar(64)
  calledNumberHash String? @map("called_number_hash") @db.VarChar(64)
  endedReason String? @map("ended_reason") @db.VarChar(160)
  durationSeconds Int? @map("duration_seconds")
  summary String? @db.Text
  payload Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@unique([payloadHash], map: "vapi_phone_call_events_payload_hash_uq")
  @@index([callId, createdAt], map: "vapi_phone_call_events_call_id_created_at_idx")
  // vapi_phone_call_events_caller_hash_created_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([eventType, createdAt], map: "vapi_phone_call_events_event_type_created_at_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("vapi_phone_call_events")
}

model MusicSongs {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  title String @db.VarChar(200)
  slug String @db.VarChar(220)
  status String @default("generated") @db.VarChar(32)
  seed BigInt @db.BigInt
  generationDate String @map("generation_date") @default(dbgenerated("to_char(current_date, 'YYYY-MM-DD')")) @db.VarChar(10)
  storageProvider String? @map("storage_provider") @db.VarChar(32)
  storageBucket String? @map("storage_bucket") @db.VarChar(200)
  storageKey String? @map("storage_key") @db.Text
  audioUrl String? @map("audio_url") @db.Text
  contentType String? @map("content_type") @db.VarChar(120)
  durationMillis Int @map("duration_millis") @default(180000)
  sampleRate Int @map("sample_rate") @default(44100)
  bpmMillis Int @map("bpm_millis") @default(128000)
  genre String @default("electronica") @db.VarChar(80)
  peakMicros Int @map("peak_micros") @default(0)
  rmsMicros Int @map("rms_micros") @default(0)
  spectralCentroidMillihz BigInt @map("spectral_centroid_millihz") @default(0) @db.BigInt
  listenabilityScoreMicros Int @map("listenability_score_micros") @default(0)
  voteScore Int @map("vote_score") @default(0)
  upVotes Int @map("up_votes") @default(0)
  downVotes Int @map("down_votes") @default(0)
  playCount Int @map("play_count") @default(0)
  summary Json @default("{}")
  metaData Json @map("meta_data") @default("{}")
  publishedAt DateTime? @map("published_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([slug], map: "music_songs_slug_uq")
  // music_songs_published_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([generationDate, status], map: "music_songs_generation_date_status_idx")
  // music_songs_vote_score_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("music_songs")
}

model MusicSongVotes {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  songId String @map("song_id") @db.Uuid
  visitorHash String @map("visitor_hash") @db.VarChar(64)
  userAgentHash String? @map("user_agent_hash") @db.VarChar(64)
  voteValue Int @map("vote_value")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([songId, visitorHash], map: "music_song_votes_song_visitor_uq")
  @@index([songId, createdAt], map: "music_song_votes_song_created_at_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("music_song_votes")
}

model SoundRecorderAccounts {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  status String @default("active") @db.VarChar(32)
  externalSubject String? @map("external_subject") @db.VarChar(240)
  displayName String? @map("display_name") @db.VarChar(160)
  legalRegion String? @map("legal_region") @db.VarChar(64)
  retentionHours Int @map("retention_hours") @default(500)
  retentionPolicyVersion String @map("retention_policy_version") @default("sound-recorder-retention-v1") @db.VarChar(80)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  // sound_recorder_accounts_external_subject_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([status, updatedAt], map: "sound_recorder_accounts_status_updated_at_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("sound_recorder_accounts")
}

model SoundRecorderDevices {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  accountId String @map("account_id") @db.Uuid
  platform String @db.VarChar(24)
  status String @default("active") @db.VarChar(32)
  installId String @map("install_id") @db.VarChar(160)
  deviceLabel String? @map("device_label") @db.VarChar(160)
  appVersion String? @map("app_version") @db.VarChar(80)
  osVersion String? @map("os_version") @db.VarChar(80)
  tokenHash String @map("token_hash") @db.VarChar(64)
  tokenLast4 String @map("token_last4") @db.VarChar(4)
  consentVersion String @map("consent_version") @db.VarChar(80)
  consentAcceptedAt DateTime @map("consent_accepted_at") @db.Timestamptz(6)
  recordingIndicatorAcknowledged Boolean @map("recording_indicator_acknowledged") @default(false)
  lastSeenAt DateTime? @map("last_seen_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([tokenHash], map: "sound_recorder_devices_token_hash_uq")
  @@unique([accountId, installId], map: "sound_recorder_devices_account_install_uq")
  @@index([accountId, status, updatedAt], map: "sound_recorder_devices_account_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("sound_recorder_devices")
}

model SoundRecorderUploadSessions {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  accountId String @map("account_id") @db.Uuid
  deviceId String @map("device_id") @db.Uuid
  status String @default("active") @db.VarChar(32)
  storageProvider String @map("storage_provider") @default("s3") @db.VarChar(32)
  storageBucket String @map("storage_bucket") @db.VarChar(200)
  storagePrefix String @map("storage_prefix") @db.Text
  contentType String @map("content_type") @default("audio/mp4") @db.VarChar(120)
  codec String? @db.VarChar(80)
  sampleRate Int? @map("sample_rate")
  channelCount Int @map("channel_count") @default(1)
  segmentDurationSeconds Int @map("segment_duration_seconds") @default(60)
  maxSegmentBytes Int @map("max_segment_bytes") @default(10485760)
  startedAt DateTime @map("started_at") @default(now()) @db.Timestamptz(6)
  lastHeartbeatAt DateTime? @map("last_heartbeat_at") @db.Timestamptz(6)
  closedAt DateTime? @map("closed_at") @db.Timestamptz(6)
  expiresAt DateTime? @map("expires_at") @db.Timestamptz(6)
  clientTimezone String? @map("client_timezone") @db.VarChar(80)
  legalRegion String? @map("legal_region") @db.VarChar(64)
  useCase String @map("use_case") @default("security") @db.VarChar(32)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([storagePrefix], map: "sound_recorder_upload_sessions_storage_prefix_uq")
  @@index([accountId, startedAt], map: "sound_recorder_upload_sessions_account_started_idx")
  @@index([deviceId, status, startedAt], map: "sound_recorder_upload_sessions_device_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("sound_recorder_upload_sessions")
}

model SoundRecorderSegments {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  accountId String @map("account_id") @db.Uuid
  deviceId String @map("device_id") @db.Uuid
  sessionId String @map("session_id") @db.Uuid
  sequenceNumber Int @map("sequence_number")
  status String @default("pending") @db.VarChar(32)
  storageProvider String @map("storage_provider") @default("s3") @db.VarChar(32)
  storageBucket String @map("storage_bucket") @db.VarChar(200)
  storageKey String @map("storage_key") @db.Text
  contentType String @map("content_type") @default("audio/mp4") @db.VarChar(120)
  codec String? @db.VarChar(80)
  capturedStartedAt DateTime @map("captured_started_at") @db.Timestamptz(6)
  capturedEndedAt DateTime? @map("captured_ended_at") @db.Timestamptz(6)
  durationMillis Int @map("duration_millis")
  byteCount Int? @map("byte_count")
  sha256Hex String? @map("sha256_hex") @db.VarChar(64)
  uploadUrlExpiresAt DateTime? @map("upload_url_expires_at") @db.Timestamptz(6)
  etag String? @db.VarChar(160)
  uploadedAt DateTime? @map("uploaded_at") @db.Timestamptz(6)
  expiresAt DateTime @map("expires_at") @db.Timestamptz(6)
  pinnedAt DateTime? @map("pinned_at") @db.Timestamptz(6)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([sessionId, sequenceNumber], map: "sound_recorder_segments_session_sequence_uq")
  @@unique([storageKey], map: "sound_recorder_segments_storage_key_uq")
  // sound_recorder_segments_account_capture_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // sound_recorder_segments_expiry_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("sound_recorder_segments")
}

model SoundRecorderEvidenceExports {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  accountId String @map("account_id") @db.Uuid
  deviceId String? @map("device_id") @db.Uuid
  createdByDeviceId String? @map("created_by_device_id") @db.Uuid
  status String @default("requested") @db.VarChar(32)
  requestedFrom DateTime @map("requested_from") @db.Timestamptz(6)
  requestedTo DateTime @map("requested_to") @db.Timestamptz(6)
  segmentCount Int @map("segment_count") @default(0)
  manifest Json @default("{}")
  downloadUrlExpiresAt DateTime? @map("download_url_expires_at") @db.Timestamptz(6)
  requestedAt DateTime @map("requested_at") @default(now()) @db.Timestamptz(6)
  readyAt DateTime? @map("ready_at") @db.Timestamptz(6)
  expiresAt DateTime? @map("expires_at") @db.Timestamptz(6)
  metaData Json @map("meta_data") @default("{}")
  @@index([accountId, requestedAt], map: "sound_recorder_evidence_exports_account_requested_idx")
  @@index([status, requestedAt], map: "sound_recorder_evidence_exports_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("sound_recorder_evidence_exports")
}

model SoundRecorderAuditEvents {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  accountId String? @map("account_id") @db.Uuid
  deviceId String? @map("device_id") @db.Uuid
  eventType String @map("event_type") @db.VarChar(80)
  eventHash String @map("event_hash") @db.VarChar(64)
  payload Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@unique([eventHash], map: "sound_recorder_audit_events_event_hash_uq")
  // sound_recorder_audit_events_account_created_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([eventType, createdAt], map: "sound_recorder_audit_events_type_created_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("sound_recorder_audit_events")
}

model SoundRecorderOauthStates {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  accountId String @map("account_id") @db.Uuid
  deviceId String @map("device_id") @db.Uuid
  provider String @db.VarChar(32)
  stateHash String @map("state_hash") @db.VarChar(64)
  redirectUri String @map("redirect_uri") @db.VarChar(512)
  folderPath String? @map("folder_path") @db.VarChar(512)
  status String @default("pending") @db.VarChar(32)
  expiresAt DateTime @map("expires_at") @db.Timestamptz(6)
  consumedAt DateTime? @map("consumed_at") @db.Timestamptz(6)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([stateHash], map: "sound_recorder_oauth_states_hash_uq")
  @@index([accountId, provider, createdAt], map: "sound_recorder_oauth_states_account_provider_idx")
  // sound_recorder_oauth_states_expiry_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("sound_recorder_oauth_states")
}

model SoundRecorderCloudConnections {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  accountId String @map("account_id") @db.Uuid
  createdByDeviceId String? @map("created_by_device_id") @db.Uuid
  provider String @db.VarChar(32)
  linkMode String @map("link_mode") @default("server_oauth") @db.VarChar(32)
  status String @default("active") @db.VarChar(32)
  displayName String? @map("display_name") @db.VarChar(160)
  providerAccountId String? @map("provider_account_id") @db.VarChar(240)
  providerSubjectHash String? @map("provider_subject_hash") @db.VarChar(64)
  rootFolderId String? @map("root_folder_id") @db.VarChar(512)
  folderPath String @map("folder_path") @default("sound-recorder") @db.VarChar(512)
  oauthScope String? @map("oauth_scope") @db.Text
  tokenCiphertext String? @map("token_ciphertext") @db.Text
  tokenNonce String? @map("token_nonce") @db.VarChar(64)
  tokenAad String? @map("token_aad") @db.VarChar(512)
  tokenVersion Int? @map("token_version")
  tokenExpiresAt DateTime? @map("token_expires_at") @db.Timestamptz(6)
  lastSyncAt DateTime? @map("last_sync_at") @db.Timestamptz(6)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  // sound_recorder_cloud_connections_active_account_provider_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([accountId, status, updatedAt], map: "sound_recorder_cloud_connections_account_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("sound_recorder_cloud_connections")
}

model SoundRecorderCloudCopyJobs {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  accountId String @map("account_id") @db.Uuid
  connectionId String @map("connection_id") @db.Uuid
  segmentId String @map("segment_id") @db.Uuid
  provider String @db.VarChar(32)
  status String @default("pending") @db.VarChar(32)
  destinationKey String @map("destination_key") @db.VarChar(2048)
  providerFileId String? @map("provider_file_id") @db.VarChar(512)
  attempts Int @default(0)
  lockedUntil DateTime? @map("locked_until") @db.Timestamptz(6)
  startedAt DateTime? @map("started_at") @db.Timestamptz(6)
  completedAt DateTime? @map("completed_at") @db.Timestamptz(6)
  lastError String? @map("last_error") @db.VarChar(500)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([connectionId, segmentId], map: "sound_recorder_cloud_copy_jobs_connection_segment_uq")
  @@index([accountId, status, updatedAt], map: "sound_recorder_cloud_copy_jobs_account_status_idx")
  @@index([connectionId, status, updatedAt], map: "sound_recorder_cloud_copy_jobs_connection_status_idx")
  @@index([segmentId], map: "sound_recorder_cloud_copy_jobs_segment_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("sound_recorder_cloud_copy_jobs")
}

model ContainerPoolConfigs {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  slug String @db.VarChar(120)
  displayName String @map("display_name") @db.VarChar(200)
  image String @db.Text
  command Json @default("[]")
  env Json @default("{}")
  requestPath String @map("request_path") @default("/invoke") @db.VarChar(256)
  healthPath String @map("health_path") @default("/healthz") @db.VarChar(256)
  containerPort Int @map("container_port") @default(8080)
  minWarm Int @map("min_warm") @default(1)
  maxWarm Int @map("max_warm") @default(2)
  maxConcurrencyPerContainer Int @map("max_concurrency_per_container") @default(1)
  requestTimeoutMs Int @map("request_timeout_ms") @default(30000)
  idleTtlSeconds Int @map("idle_ttl_seconds") @default(900)
  natsSubject String? @map("nats_subject") @db.Text
  status String @default("active") @db.VarChar(32)
  labels Json @default("[]")
  metaData Json @map("meta_data") @default("{}")
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  // container_pool_configs_slug_active_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // container_pool_configs_status_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // container_pool_configs_updated_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // container_pool_configs_labels_gin_idx is defined in schema.sql because Prisma cannot fully model gin partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("container_pool_configs")
}

model KnownGitRepo {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  repoUrl String @map("repo_url") @db.Text
  displayName String @map("display_name") @db.VarChar(200)
  provider String @default("github") @db.VarChar(40)
  defaultBranch String @map("default_branch") @default("dev") @db.VarChar(120)
  status String @default("active") @db.VarChar(32)
  lastVerifiedAt DateTime? @map("last_verified_at") @db.Timestamptz(6)
  metaData Json @map("meta_data") @default("{}")
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  // known_git_repos_repo_url_active_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // known_git_repos_status_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // known_git_repos_updated_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("known_git_repos")
}

model AgentContextBlobs {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  projectId String @map("project_id") @default("default") @db.VarChar(120)
  repoId String? @map("repo_id") @db.Uuid
  contextId String @map("context_id") @db.VarChar(200)
  contextTitle String @map("context_title") @db.VarChar(300)
  contextBlob String @map("context_blob") @db.Text
  status String @default("active") @db.VarChar(32)
  labels Json @default("[]")
  metaData Json @map("meta_data") @default("{}")
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  // agent_context_blobs_project_repo_context_active_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // agent_context_blobs_repo_id_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // agent_context_blobs_project_id_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // agent_context_blobs_updated_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("agent_context_blobs")
}

model AgentContextEmbeddings {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  contextBlobId String @map("context_blob_id") @db.Uuid
  embeddingModel String @map("embedding_model") @db.VarChar(120)
  embedding Json
  embeddingDimensions Int @map("embedding_dimensions")
  contentSha256 String @map("content_sha256") @db.VarChar(64)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@unique([contextBlobId, embeddingModel, contentSha256], map: "agent_context_embeddings_blob_model_sha_uq")
  @@index([contextBlobId], map: "agent_context_embeddings_blob_id_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("agent_context_embeddings")
}

model AgentRemoteDevThread {
  id String @id @db.Uuid
  userId String @map("user_id") @db.Uuid
  knownGitRepoId String? @map("known_git_repo_id") @db.Uuid
  title String @default("New thread") @db.Text
  repo String @db.Text
  baseBranch String @map("base_branch") @default("dev") @db.VarChar(120)
  meta Json @default("{}")
  archivedAt DateTime? @map("archived_at") @db.Timestamptz(6)
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  // agent_remote_dev_threads_user_id_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // agent_remote_dev_threads_known_git_repo_id_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // agent_remote_dev_threads_repo_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // agent_remote_dev_threads_updated_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // agent_remote_dev_threads_created_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("agent_remote_dev_threads")
}

model AgentRemoteDevTask {
  id String @id @db.Uuid
  threadId String @map("thread_id") @db.Uuid
  userId String @map("user_id") @db.Uuid
  dockerTaskId String @map("docker_task_id") @db.Uuid
  prompt String @db.Text
  status String @default("queued") @db.VarChar(32)
  branch String? @db.VarChar(200)
  prUrl String? @map("pr_url") @db.Text
  prState String? @map("pr_state") @db.VarChar(32)
  exitReason String? @map("exit_reason") @db.VarChar(32)
  errorMessage String? @map("error_message") @db.Text
  lastEventSeq Int @map("last_event_seq") @default(-1)
  meta Json @default("{}")
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  startedAt DateTime? @map("started_at") @db.Timestamptz(6)
  finishedAt DateTime? @map("finished_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  @@unique([dockerTaskId], map: "agent_remote_dev_tasks_docker_task_id_uq")
  // agent_remote_dev_tasks_thread_id_created_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // agent_remote_dev_tasks_user_id_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // agent_remote_dev_tasks_status_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // agent_remote_dev_tasks_updated_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // agent_remote_dev_tasks_created_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("agent_remote_dev_tasks")
}

model AgentRemoteDevEvent {
  id BigInt @id @default(autoincrement()) @db.BigInt
  taskId String @map("task_id") @db.Uuid
  threadId String? @map("thread_id") @db.Uuid
  seq Int
  eventKind String @map("event_kind") @db.VarChar(80)
  payload Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@unique([taskId, seq], map: "agent_remote_dev_events_task_seq_uq")
  @@index([taskId, createdAt], map: "agent_remote_dev_events_task_id_created_at_idx")
  // agent_remote_dev_events_thread_id_created_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([createdAt], map: "agent_remote_dev_events_created_at_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("agent_remote_dev_events")
}

model AgentRemoteDevBreadcrumb {
  id BigInt @id @default(autoincrement()) @db.BigInt
  threadId String @map("thread_id") @db.Uuid
  taskId String? @map("task_id") @db.Uuid
  kind String @db.VarChar(80)
  payload Json @default("{}")
  emittedAt DateTime @map("emitted_at") @default(now()) @db.Timestamptz(6)
  podName String? @map("pod_name") @db.VarChar(253)
  branch String? @db.VarChar(120)
  provider String? @db.VarChar(60)
  @@index([threadId, emittedAt], map: "agent_remote_dev_breadcrumbs_thread_id_emitted_at_idx")
  // agent_remote_dev_breadcrumbs_task_id_emitted_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([emittedAt], map: "agent_remote_dev_breadcrumbs_emitted_at_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("agent_remote_dev_breadcrumbs")
}

model AgentRemoteDevArtifact {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  taskId String @map("task_id") @db.Uuid
  threadId String @map("thread_id") @db.Uuid
  filename String @db.Text
  contentType String? @map("content_type") @db.VarChar(200)
  sizeBytes BigInt? @map("size_bytes") @db.BigInt
  storageProvider String @map("storage_provider") @db.VarChar(32)
  storageBucket String? @map("storage_bucket") @db.VarChar(200)
  storageKey String? @map("storage_key") @db.Text
  url String @db.Text
  signedUrlExpiresAt DateTime? @map("signed_url_expires_at") @db.Timestamptz(6)
  sha256 String? @db.VarChar(64)
  meta Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@index([taskId, createdAt], map: "agent_remote_dev_artifacts_task_id_created_at_idx")
  @@index([threadId, createdAt], map: "agent_remote_dev_artifacts_thread_id_created_at_idx")
  @@index([createdAt], map: "agent_remote_dev_artifacts_created_at_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("agent_remote_dev_artifacts")
}

model AgentRemoteDevRuntimeLock {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  threadId String @map("thread_id") @db.Uuid
  owner String @db.VarChar(200)
  status String @default("active") @db.VarChar(32)
  fencingToken Int @map("fencing_token") @default(0)
  leaseExpiresAt DateTime @map("lease_expires_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  // agent_remote_dev_runtime_locks_thread_active_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([leaseExpiresAt], map: "agent_remote_dev_runtime_locks_lease_expires_at_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("agent_remote_dev_runtime_locks")
}

model MipSolverSessions {
  sessionId String @map("session_id") @id @db.VarChar(200)
  revision BigInt @default(0) @db.BigInt
  problem Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([updatedAt], map: "mip_solver_sessions_updated_at_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("mip_solver_sessions")
}

model MipSolverSolves {
  solveId String @map("solve_id") @id @db.VarChar(160)
  requestId String @map("request_id") @db.VarChar(200)
  revision BigInt @default(0) @db.BigInt
  status String @default("running") @db.VarChar(64)
  nodeId String @map("node_id") @db.VarChar(253)
  nodeRole String @map("node_role") @db.VarChar(32)
  problem Json @default("{}")
  options Json @default("{}")
  response Json @default("{}")
  jobsExpected Int @map("jobs_expected") @default(0)
  jobsPublished Int @map("jobs_published") @default(0)
  jobsCompleted Int @map("jobs_completed") @default(0)
  jobsRedelegated Int @map("jobs_redelegated") @default(0)
  jobsSplit Int @map("jobs_split") @default(0)
  timedOut Boolean @map("timed_out") @default(false)
  distributed Boolean @default(false)
  warnings Json @default("[]")
  startedAt DateTime @map("started_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  finishedAt DateTime? @map("finished_at") @db.Timestamptz(6)
  @@index([requestId, updatedAt], map: "mip_solver_solves_request_id_idx")
  @@index([status, updatedAt], map: "mip_solver_solves_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("mip_solver_solves")
}

model MipSolverJobs {
  jobId String @map("job_id") @id @db.VarChar(240)
  solveId String @map("solve_id") @db.VarChar(160)
  rootJobId String @map("root_job_id") @db.VarChar(240)
  retryIndex Int @map("retry_index") @default(0)
  depth Int @default(0)
  status String @default("submitted") @db.VarChar(64)
  workerNode String? @map("worker_node") @db.VarChar(253)
  jobPayload Json @map("job_payload") @default("{}")
  resultPayload Json @map("result_payload") @default("{}")
  submittedAt DateTime @map("submitted_at") @default(now()) @db.Timestamptz(6)
  finishedAt DateTime? @map("finished_at") @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([solveId, status, updatedAt], map: "mip_solver_jobs_solve_status_idx")
  @@index([solveId, rootJobId, retryIndex], map: "mip_solver_jobs_root_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("mip_solver_jobs")
}

model MipSolverEvents {
  id BigInt @id @default(autoincrement()) @db.BigInt
  solveId String? @map("solve_id") @db.VarChar(160)
  sessionId String? @map("session_id") @db.VarChar(200)
  jobId String? @map("job_id") @db.VarChar(240)
  eventKind String @map("event_kind") @db.VarChar(80)
  payload Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  // mip_solver_events_solve_created_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // mip_solver_events_session_created_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("mip_solver_events")
}

model LambdaFunction {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  slug String @db.VarChar(120)
  displayName String @map("display_name") @db.VarChar(200)
  description String @default("") @db.Text
  runtime String @default("nodejs") @db.VarChar(40)
  entryCommand String @map("entry_command") @default("env -i PATH=\"$PATH\" NODE_ENV=production NODE_NO_WARNINGS=1 node --permission --allow-net child-runtimes/js-function-runner.mjs") @db.Text
  functionBody String @map("function_body") @db.Text
  reuseKey String? @map("reuse_key") @db.VarChar(200)
  idleTimeoutSeconds Int @map("idle_timeout_seconds") @default(300)
  maxRunMs Int @map("max_run_ms") @default(30000)
  containerized Boolean @default(false)
  containerImage String? @map("container_image") @db.Text
  containerBuildStatus String @map("container_build_status") @default("not_requested") @db.VarChar(32)
  containerBuildError String? @map("container_build_error") @db.Text
  containerBuiltAt DateTime? @map("container_built_at") @db.Timestamptz(6)
  status String @default("draft") @db.VarChar(32)
  env Json @default("{}")
  labels Json @default("[]")
  metaData Json @map("meta_data") @default("{}")
  lastInvokedAt DateTime? @map("last_invoked_at") @db.Timestamptz(6)
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  // lambda_functions_slug_active_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // lambda_functions_status_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // lambda_functions_updated_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // lambda_functions_labels_gin_idx is defined in schema.sql because Prisma cannot fully model gin partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("lambda_functions")
}

model WorkflowDefinitions {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  slug String @db.VarChar(120)
  displayName String @map("display_name") @db.VarChar(200)
  description String @default("") @db.Text
  steps Json
  defaultRetry Json @map("default_retry") @default("{}")
  status String @default("draft") @db.VarChar(32)
  labels Json @default("[]")
  metaData Json @map("meta_data") @default("{}")
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  // workflow_definitions_slug_active_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // workflow_definitions_status_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // workflow_definitions_updated_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("workflow_definitions")
}

model WorkflowRuns {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  definitionId String @map("definition_id") @db.Uuid
  definitionSlug String @map("definition_slug") @db.VarChar(120)
  status String @default("pending") @db.VarChar(32)
  currentStepIndex Int @map("current_step_index") @default(0)
  attempt Int @default(0)
  input Json @default("{}")
  context Json @default("{}")
  output Json?
  lastError String? @map("last_error") @db.Text
  wakeAt DateTime? @map("wake_at") @db.Timestamptz(6)
  waitDeadline DateTime? @map("wait_deadline") @db.Timestamptz(6)
  leaseUntil DateTime? @map("lease_until") @db.Timestamptz(6)
  signals Json @default("[]")
  idempotencyKey String? @map("idempotency_key") @db.VarChar(200)
  startedAt DateTime? @map("started_at") @db.Timestamptz(6)
  finishedAt DateTime? @map("finished_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  @@index([definitionId], map: "workflow_runs_definition_id_idx")
  @@index([status], map: "workflow_runs_status_idx")
  // workflow_runs_due_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // workflow_runs_idempotency_key_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("workflow_runs")
}

model WorkflowStepRuns {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  runId String @map("run_id") @db.Uuid
  stepIndex Int @map("step_index")
  stepName String @map("step_name") @db.VarChar(200)
  stepType String @map("step_type") @default("activity") @db.VarChar(32)
  functionRef String @map("function_ref") @default("") @db.VarChar(200)
  attempt Int
  status String @db.VarChar(32)
  input Json?
  output Json?
  error String? @db.Text
  durationMs Int? @map("duration_ms")
  startedAt DateTime @map("started_at") @default(now()) @db.Timestamptz(6)
  finishedAt DateTime? @map("finished_at") @db.Timestamptz(6)
  @@index([runId, stepIndex, attempt], map: "workflow_step_runs_run_id_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("workflow_step_runs")
}

model ContainerPoolImageRevisions {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  imageSlug String @map("image_slug") @db.VarChar(120)
  imageRef String @map("image_ref") @db.Text
  dockerfilePath String @map("dockerfile_path") @db.Text
  buildContext String @map("build_context") @db.Text
  dockerfileText String @map("dockerfile_text") @db.Text
  dockerfileSha256 String @map("dockerfile_sha256") @db.VarChar(64)
  source String @default("user") @db.VarChar(32)
  notes String @default("") @db.Text
  status String @default("candidate") @db.VarChar(32)
  metaData Json @map("meta_data") @default("{}")
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  // container_pool_image_revisions_slug_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // container_pool_image_revisions_slug_sha_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("container_pool_image_revisions")
}

model ContainerPoolBuildRuns {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  imageSlug String @map("image_slug") @db.VarChar(120)
  revisionId String @map("revision_id") @db.Uuid
  imageRef String @map("image_ref") @db.Text
  candidateTag String @map("candidate_tag") @db.Text
  buildStatus String @map("build_status") @default("queued") @db.VarChar(32)
  testStatus String @map("test_status") @default("not_started") @db.VarChar(32)
  overallStatus String @map("overall_status") @default("queued") @db.VarChar(32)
  testCommand String @map("test_command") @default("") @db.Text
  buildStartedAt DateTime? @map("build_started_at") @db.Timestamptz(6)
  buildFinishedAt DateTime? @map("build_finished_at") @db.Timestamptz(6)
  testStartedAt DateTime? @map("test_started_at") @db.Timestamptz(6)
  testFinishedAt DateTime? @map("test_finished_at") @db.Timestamptz(6)
  buildLogExcerpt String @map("build_log_excerpt") @default("") @db.Text
  testLogExcerpt String @map("test_log_excerpt") @default("") @db.Text
  errorMessage String? @map("error_message") @db.Text
  triggeredBy String? @map("triggered_by") @db.Uuid
  metaData Json @map("meta_data") @default("{}")
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  // container_pool_build_runs_slug_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // container_pool_build_runs_overall_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("container_pool_build_runs")
}

model PresenceConvs {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  slug String @db.VarChar(120)
  displayName String @map("display_name") @default("") @db.VarChar(200)
  status String @default("active") @db.VarChar(32)
  metaData Json @map("meta_data") @default("{}")
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  // presence_convs_slug_active_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // presence_convs_status_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // presence_convs_updated_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("presence_convs")
}

model PresenceConvMembers {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  convId String @map("conv_id") @db.Uuid
  userId String @map("user_id") @db.Uuid
  role String @default("member") @db.VarChar(32)
  status String @default("active") @db.VarChar(32)
  metaData Json @map("meta_data") @default("{}")
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  joinedAt DateTime @map("joined_at") @default(now()) @db.Timestamptz(6)
  leftAt DateTime? @map("left_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  // presence_conv_members_conv_user_active_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // presence_conv_members_user_id_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // presence_conv_members_conv_id_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // presence_conv_members_updated_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("presence_conv_members")
}

model PresenceUsers {
  id String @id @db.Uuid
  slug String @db.Text
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([slug], map: "presence_users_slug_uq")
  @@map("presence_users")
}

model PresenceEvents {
  seq BigInt @id @default(autoincrement()) @db.BigInt
  eventAt DateTime @map("event_at") @default(now()) @db.Timestamptz(6)
  op String @db.Text
  convId String @map("conv_id") @db.Uuid
  userId String @map("user_id") @db.Uuid
  convSlug String @map("conv_slug") @db.Text
  userSlug String @map("user_slug") @db.Text
  convShard Int @map("conv_shard")
  userShard Int @map("user_shard")
  softDeleted Boolean @map("soft_deleted") @default(false)
  @@index([convShard, seq], map: "presence_events_conv_shard_seq_idx")
  @@index([userShard, seq], map: "presence_events_user_shard_seq_idx")
  @@index([eventAt], map: "presence_events_event_at_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("presence_events")
}

model PresenceConsumerCheckpoints {
  consumerId String @map("consumer_id") @id @db.Text
  lastSeq BigInt @map("last_seq") @default(0) @db.BigInt
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@map("presence_consumer_checkpoints")
}

model DesSoccerLearningExperiments {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  slug String @db.VarChar(160)
  displayName String @map("display_name") @db.VarChar(240)
  description String @default("") @db.Text
  status String @default("active") @db.VarChar(32)
  config Json @default("{}")
  labels Json @default("[]")
  metaData Json @map("meta_data") @default("{}")
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  // des_soccer_learning_experiments_slug_active_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // des_soccer_learning_experiments_status_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // des_soccer_learning_experiments_updated_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("des_soccer_learning_experiments")
}

model DesSoccerLearningPolicyVersions {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  experimentId String @map("experiment_id") @db.Uuid
  parentPolicyVersionId String? @map("parent_policy_version_id") @db.Uuid
  generation Int @default(0)
  versionLabel String @map("version_label") @db.VarChar(160)
  sourceKind String @map("source_kind") @default("seed") @db.VarChar(40)
  status String @default("candidate") @db.VarChar(32)
  options Json @default("{}")
  config Json @default("{}")
  lineage Json @default("[]")
  metrics Json @default("{}")
  entryCount Int @map("entry_count") @default(0)
  targetEntryCount Int @map("target_entry_count") @default(0)
  visitCount BigInt @map("visit_count") @default(0) @db.BigInt
  fitnessMicros BigInt @map("fitness_micros") @default(0) @db.BigInt
  branchKey String @map("branch_key") @db.Uuid
  retentionKind String @map("retention_kind") @default("branch_tip") @db.VarChar(32)
  fullEntriesRetained Boolean @map("full_entries_retained") @default(true)
  fullEntriesPrunedAt DateTime? @map("full_entries_pruned_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  @@unique([experimentId, versionLabel], map: "des_soccer_learning_policy_versions_label_uq")
  // des_soccer_learning_policy_versions_active_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // des_soccer_learning_policy_versions_fitness_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // des_soccer_learning_policy_versions_branch_tip_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // des_soccer_learning_policy_versions_single_active_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("des_soccer_learning_policy_versions")
}

model DesSoccerLearningPolicyEntries {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  policyVersionId String @map("policy_version_id") @db.Uuid
  team String @db.VarChar(8)
  entryKind String @map("entry_kind") @db.VarChar(16)
  stateHash String @map("state_hash") @db.VarChar(32)
  stateKey Json @map("state_key")
  action String @db.VarChar(80)
  targetFineCellId Int @map("target_fine_cell_id") @default(-1)
  targetTacticalCellId Int @map("target_tactical_cell_id") @default(-1)
  targetMacroCellId Int @map("target_macro_cell_id") @default(-1)
  targetRootCellId Int @map("target_root_cell_id") @default(-1)
  valueMicros BigInt @map("value_micros") @db.BigInt
  visits Int @default(0)
  sourceRunId String? @map("source_run_id") @db.Uuid
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@unique([policyVersionId, team, entryKind, stateHash, action, targetFineCellId, targetTacticalCellId, targetMacroCellId, targetRootCellId], map: "des_soccer_learning_policy_entries_key_uq")
  @@index([policyVersionId, team, entryKind, stateHash], map: "des_soccer_learning_policy_entries_lookup_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("des_soccer_learning_policy_entries")
}

model DesSoccerLearningJobs {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  experimentId String @map("experiment_id") @db.Uuid
  basePolicyVersionId String? @map("base_policy_version_id") @db.Uuid
  spawnStrategy String @map("spawn_strategy") @default("latest") @db.VarChar(32)
  status String @default("queued") @db.VarChar(32)
  priority Int @default(0)
  seed BigInt @db.BigInt
  attempt Int @default(0)
  maxAttempts Int @map("max_attempts") @default(1)
  leaseOwner String? @map("lease_owner") @db.VarChar(200)
  leaseExpiresAt DateTime? @map("lease_expires_at") @db.Timestamptz(6)
  startedAt DateTime? @map("started_at") @db.Timestamptz(6)
  finishedAt DateTime? @map("finished_at") @db.Timestamptz(6)
  config Json @default("{}")
  runnerConfig Json @map("runner_config") @default("{}")
  resultRunId String? @map("result_run_id") @db.Uuid
  error String? @db.Text
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  // des_soccer_learning_jobs_claim_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([basePolicyVersionId, createdAt], map: "des_soccer_learning_jobs_base_policy_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("des_soccer_learning_jobs")
}

model DesSoccerLearningRuns {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  jobId String? @map("job_id") @db.Uuid
  experimentId String @map("experiment_id") @db.Uuid
  basePolicyVersionId String? @map("base_policy_version_id") @db.Uuid
  outputPolicyVersionId String? @map("output_policy_version_id") @db.Uuid
  runnerId String @map("runner_id") @db.VarChar(200)
  seed BigInt @db.BigInt
  episodeIndex Int @map("episode_index") @default(0)
  status String @default("completed") @db.VarChar(32)
  scoreHome Int @map("score_home") @default(0)
  scoreAway Int @map("score_away") @default(0)
  homeGoalDiff Int @map("home_goal_diff") @default(0)
  awayGoalDiff Int @map("away_goal_diff") @default(0)
  homeOutcome String @map("home_outcome") @default("draw") @db.VarChar(16)
  awayOutcome String @map("away_outcome") @default("draw") @db.VarChar(16)
  homeMergeWeightMicros BigInt @map("home_merge_weight_micros") @default(0) @db.BigInt
  awayMergeWeightMicros BigInt @map("away_merge_weight_micros") @default(0) @db.BigInt
  fitnessMicros BigInt @map("fitness_micros") @default(0) @db.BigInt
  durationTicks BigInt @map("duration_ticks") @default(0) @db.BigInt
  simulatedSecondsMicros BigInt @map("simulated_seconds_micros") @default(0) @db.BigInt
  elapsedMillis BigInt @map("elapsed_millis") @default(0) @db.BigInt
  transitions Int @default(0)
  summary Json @default("{}")
  stats Json @default("{}")
  error String? @db.Text
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([experimentId, createdAt], map: "des_soccer_learning_runs_experiment_idx")
  @@index([basePolicyVersionId, fitnessMicros, createdAt], map: "des_soccer_learning_runs_policy_fitness_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("des_soccer_learning_runs")
}

model DesSoccerLearningRunDeltas {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  runId String @map("run_id") @db.Uuid
  team String @db.VarChar(8)
  entryKind String @map("entry_kind") @db.VarChar(16)
  stateHash String @map("state_hash") @db.VarChar(32)
  stateKey Json @map("state_key")
  action String @db.VarChar(80)
  targetFineCellId Int @map("target_fine_cell_id") @default(-1)
  targetTacticalCellId Int @map("target_tactical_cell_id") @default(-1)
  targetMacroCellId Int @map("target_macro_cell_id") @default(-1)
  targetRootCellId Int @map("target_root_cell_id") @default(-1)
  beforeValueMicros BigInt @map("before_value_micros") @default(0) @db.BigInt
  afterValueMicros BigInt @map("after_value_micros") @default(0) @db.BigInt
  valueDeltaMicros BigInt @map("value_delta_micros") @default(0) @db.BigInt
  visitDelta Int @map("visit_delta") @default(0)
  mergeWeightMicros BigInt @map("merge_weight_micros") @default(0) @db.BigInt
  effectiveVisitMicros BigInt @map("effective_visit_micros") @default(0) @db.BigInt
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@unique([runId, team, entryKind, stateHash, action, targetFineCellId, targetTacticalCellId, targetMacroCellId, targetRootCellId], map: "des_soccer_learning_run_deltas_key_uq")
  @@index([team, entryKind, stateHash, action], map: "des_soccer_learning_run_deltas_merge_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("des_soccer_learning_run_deltas")
}

model DesSoccerLearningMergeEvents {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  experimentId String @map("experiment_id") @db.Uuid
  basePolicyVersionId String? @map("base_policy_version_id") @db.Uuid
  outputPolicyVersionId String @map("output_policy_version_id") @db.Uuid
  strategy String @default("outcome_weighted_average") @db.VarChar(40)
  inputRunCount Int @map("input_run_count") @default(0)
  inputDeltaCount Int @map("input_delta_count") @default(0)
  decayMicros BigInt @map("decay_micros") @default(1000000) @db.BigInt
  metrics Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@index([experimentId, createdAt], map: "des_soccer_learning_merge_events_experiment_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("des_soccer_learning_merge_events")
}

model DesFelElevatorLearningRuns {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  runLabel String @map("run_label") @db.VarChar(200)
  scenarioSlug String @map("scenario_slug") @db.VarChar(160)
  status String @default("completed") @db.VarChar(32)
  dispatchPolicy String @map("dispatch_policy") @db.VarChar(40)
  seed BigInt @db.BigInt
  floors Int
  shafts Int
  capacity Int
  travelSecondsMicros BigInt @map("travel_seconds_micros") @default(0) @db.BigInt
  dwellSecondsMicros BigInt @map("dwell_seconds_micros") @default(0) @db.BigInt
  arrivalRateMicros BigInt @map("arrival_rate_micros") @default(0) @db.BigInt
  horizonSecondsMicros BigInt @map("horizon_seconds_micros") @default(0) @db.BigInt
  events BigInt @default(0) @db.BigInt
  arrivals BigInt @default(0) @db.BigInt
  boarded BigInt @default(0) @db.BigInt
  served BigInt @default(0) @db.BigInt
  meanWaitMicros BigInt @map("mean_wait_micros") @default(0) @db.BigInt
  dispatchDecisions Int @map("dispatch_decisions") @default(0)
  pomdpBeliefUpdates Int @map("pomdp_belief_updates") @default(0)
  onlineLearningUpdates BigInt @map("online_learning_updates") @default(0) @db.BigInt
  onlineLearningLossLastMicros BigInt? @map("online_learning_loss_last_micros") @db.BigInt
  config Json @default("{}")
  metrics Json @default("{}")
  artifact Json
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([scenarioSlug, createdAt], map: "des_fel_elevator_learning_runs_scenario_idx")
  @@index([dispatchPolicy, createdAt], map: "des_fel_elevator_learning_runs_policy_idx")
  @@index([scenarioSlug, meanWaitMicros, createdAt], map: "des_fel_elevator_learning_runs_mean_wait_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("des_fel_elevator_learning_runs")
}

model DesFelElevatorPolicyStates {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  runId String @map("run_id") @db.Uuid
  policyKind String @map("policy_kind") @db.VarChar(40)
  sourceKind String @map("source_kind") @default("run-final") @db.VarChar(40)
  featureDim Int @map("feature_dim") @default(0)
  outputDim Int @map("output_dim") @default(0)
  parameterCount Int @map("parameter_count") @default(0)
  onlineLearningUpdates BigInt @map("online_learning_updates") @default(0) @db.BigInt
  lossHistory Json @map("loss_history") @default("[]")
  state Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@unique([runId, sourceKind, policyKind], map: "des_fel_elevator_policy_states_run_source_uq")
  @@index([policyKind, createdAt], map: "des_fel_elevator_policy_states_policy_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("des_fel_elevator_policy_states")
}

model DesFelElevatorDispatchDecisions {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  runId String @map("run_id") @db.Uuid
  decisionIndex Int @map("decision_index")
  simTimeMicros BigInt @map("sim_time_micros") @default(0) @db.BigInt
  callFloor Int @map("call_floor")
  carIndex Int @map("car_index")
  policyKind String @map("policy_kind") @db.VarChar(40)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@unique([runId, decisionIndex], map: "des_fel_elevator_dispatch_decisions_run_index_uq")
  @@index([runId, simTimeMicros], map: "des_fel_elevator_dispatch_decisions_time_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("des_fel_elevator_dispatch_decisions")
}

model DesFelElevatorPomdpBeliefs {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  runId String @map("run_id") @db.Uuid
  beliefIndex Int @map("belief_index")
  simTimeMicros BigInt @map("sim_time_micros") @default(0) @db.BigInt
  floor Int
  action String @db.VarChar(32)
  observation String @db.VarChar(32)
  emptyProbMicros Int @map("empty_prob_micros") @default(0)
  waitingProbMicros Int @map("waiting_prob_micros") @default(0)
  crowdedProbMicros Int @map("crowded_prob_micros") @default(0)
  belief Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@unique([runId, beliefIndex], map: "des_fel_elevator_pomdp_beliefs_run_index_uq")
  @@index([runId, floor, simTimeMicros], map: "des_fel_elevator_pomdp_beliefs_floor_time_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("des_fel_elevator_pomdp_beliefs")
}

model BenefactorMarketingClients {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  status String @default("onboarding") @db.VarChar(32)
  name String @db.VarChar(200)
  slug String @db.VarChar(220)
  industry String? @db.VarChar(120)
  websiteUrl String? @map("website_url") @db.Text
  billingEmail String? @map("billing_email") @db.VarChar(240)
  ownerUserId String? @map("owner_user_id") @db.Uuid
  servicePackage String? @map("service_package") @db.VarChar(120)
  onboardingStage String @map("onboarding_stage") @default("intake") @db.VarChar(80)
  portalEnabled Boolean @map("portal_enabled") @default(true)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([slug], map: "benefactor_marketing_clients_slug_uq")
  @@index([status, updatedAt], map: "benefactor_marketing_clients_status_updated_at_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_clients")
}

model BenefactorMarketingContacts {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  status String @default("active") @db.VarChar(32)
  firstName String? @map("first_name") @db.VarChar(120)
  lastName String? @map("last_name") @db.VarChar(120)
  email String? @db.VarChar(240)
  phone String? @db.VarChar(80)
  jobTitle String? @map("job_title") @db.VarChar(160)
  lifecycleRole String @map("lifecycle_role") @default("other") @db.VarChar(40)
  consentStatus String @map("consent_status") @default("unknown") @db.VarChar(32)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, updatedAt], map: "benefactor_marketing_contacts_client_status_idx")
  // benefactor_marketing_contacts_client_email_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_contacts")
}

model BenefactorMarketingServicePackages {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  status String @default("active") @db.VarChar(32)
  code String @db.VarChar(120)
  name String @db.VarChar(200)
  channelMix Json @map("channel_mix") @default("[]")
  deliverables Json @default("[]")
  monthlyBudgetCents Int @map("monthly_budget_cents") @default(0)
  retainerCents Int @map("retainer_cents") @default(0)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([code], map: "benefactor_marketing_service_packages_code_uq")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_service_packages")
}

model BenefactorMarketingContracts {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  packageId String? @map("package_id") @db.Uuid
  status String @default("draft") @db.VarChar(32)
  contractNumber String? @map("contract_number") @db.VarChar(120)
  startsOn String? @map("starts_on") @db.VarChar(10)
  endsOn String? @map("ends_on") @db.VarChar(10)
  billingTerms Json @map("billing_terms") @default("{}")
  totalValueCents Int @map("total_value_cents") @default(0)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, updatedAt], map: "benefactor_marketing_contracts_client_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_contracts")
}

model BenefactorMarketingInvoices {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  contractId String? @map("contract_id") @db.Uuid
  status String @default("draft") @db.VarChar(32)
  invoiceNumber String? @map("invoice_number") @db.VarChar(120)
  dueOn String? @map("due_on") @db.VarChar(10)
  amountCents Int @map("amount_cents") @default(0)
  paidAt DateTime? @map("paid_at") @db.Timestamptz(6)
  lineItems Json @map("line_items") @default("[]")
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, updatedAt], map: "benefactor_marketing_invoices_client_status_idx")
  // benefactor_marketing_invoices_client_number_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_invoices")
}

model BenefactorMarketingIntegrations {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String? @map("client_id") @db.Uuid
  platform String @db.VarChar(64)
  status String @default("connected") @db.VarChar(32)
  authKind String @map("auth_kind") @default("manual") @db.VarChar(32)
  externalAccountId String? @map("external_account_id") @db.VarChar(200)
  syncCursor String? @map("sync_cursor") @db.Text
  config Json @default("{}")
  lastSyncAt DateTime? @map("last_sync_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, platform, status], map: "benefactor_marketing_integrations_client_platform_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_integrations")
}

model BenefactorMarketingLeads {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  sourceIntegrationId String? @map("source_integration_id") @db.Uuid
  status String @default("new") @db.VarChar(32)
  companyName String @map("company_name") @db.VarChar(240)
  domain String? @db.VarChar(240)
  contactName String? @map("contact_name") @db.VarChar(200)
  contactEmail String? @map("contact_email") @db.VarChar(240)
  contactTitle String? @map("contact_title") @db.VarChar(160)
  countryCode String? @map("country_code") @db.VarChar(8)
  leadScore Int @map("lead_score") @default(0)
  icpFitScore Int @map("icp_fit_score") @default(0)
  verificationStatus String @map("verification_status") @default("unknown") @db.VarChar(32)
  enrichmentStatus String @map("enrichment_status") @default("pending") @db.VarChar(32)
  companyProfile Json @map("company_profile") @default("{}")
  signals Json @default("[]")
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, leadScore, updatedAt], map: "benefactor_marketing_leads_client_status_score_idx")
  // benefactor_marketing_leads_domain_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_leads")
}

model BenefactorMarketingEnrichmentJobs {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  leadId String? @map("lead_id") @db.Uuid
  jobKind String @map("job_kind") @db.VarChar(48)
  status String @default("queued") @db.VarChar(32)
  externalJobId String? @map("external_job_id") @db.VarChar(200)
  scraperHandoffUrl String? @map("scraper_handoff_url") @db.Text
  input Json @default("{}")
  result Json @default("{}")
  errorSummary String? @map("error_summary") @db.Text
  queuedAt DateTime @map("queued_at") @default(now()) @db.Timestamptz(6)
  startedAt DateTime? @map("started_at") @db.Timestamptz(6)
  completedAt DateTime? @map("completed_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, queuedAt], map: "benefactor_marketing_enrichment_jobs_client_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_enrichment_jobs")
}

model BenefactorMarketingCampaigns {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  status String @default("draft") @db.VarChar(32)
  campaignKind String @map("campaign_kind") @default("multi_channel") @db.VarChar(48)
  name String @db.VarChar(220)
  objective String? @db.Text
  budgetCents Int @map("budget_cents") @default(0)
  startsOn String? @map("starts_on") @db.VarChar(10)
  endsOn String? @map("ends_on") @db.VarChar(10)
  targetSegments Json @map("target_segments") @default("[]")
  kpis Json @default("{}")
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, updatedAt], map: "benefactor_marketing_campaigns_client_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_campaigns")
}

model BenefactorMarketingCampaignChannels {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  campaignId String @map("campaign_id") @db.Uuid
  channel String @db.VarChar(48)
  status String @default("draft") @db.VarChar(32)
  externalCampaignId String? @map("external_campaign_id") @db.VarChar(200)
  strategy Json @default("{}")
  schedule Json @default("{}")
  metricsSnapshot Json @map("metrics_snapshot") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([campaignId, channel, status], map: "benefactor_marketing_campaign_channels_campaign_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_campaign_channels")
}

model BenefactorMarketingCampaignExperiments {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  campaignId String @map("campaign_id") @db.Uuid
  status String @default("draft") @db.VarChar(32)
  experimentKind String @map("experiment_kind") @db.VarChar(48)
  hypothesis String? @db.Text
  variants Json @default("[]")
  winningVariant String? @map("winning_variant") @db.VarChar(120)
  resultSummary Json @map("result_summary") @default("{}")
  startedAt DateTime? @map("started_at") @db.Timestamptz(6)
  endedAt DateTime? @map("ended_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([campaignId, status, updatedAt], map: "benefactor_marketing_campaign_experiments_campaign_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_campaign_experiments")
}

model BenefactorMarketingAutomationWorkflows {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  status String @default("draft") @db.VarChar(32)
  name String @db.VarChar(220)
  triggerKind String @map("trigger_kind") @db.VarChar(64)
  triggerConfig Json @map("trigger_config") @default("{}")
  actionGraph Json @map("action_graph") @default("{}")
  lastRunAt DateTime? @map("last_run_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, updatedAt], map: "benefactor_marketing_automation_workflows_client_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_automation_workflows")
}

model BenefactorMarketingAutomationEvents {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  workflowId String? @map("workflow_id") @db.Uuid
  leadId String? @map("lead_id") @db.Uuid
  eventKind String @map("event_kind") @db.VarChar(80)
  status String @default("received") @db.VarChar(32)
  payload Json @default("{}")
  errorSummary String? @map("error_summary") @db.Text
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, createdAt], map: "benefactor_marketing_automation_events_client_created_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_automation_events")
}

model BenefactorMarketingReports {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  reportKind String @map("report_kind") @default("dashboard") @db.VarChar(48)
  status String @default("draft") @db.VarChar(32)
  periodStart String? @map("period_start") @db.VarChar(10)
  periodEnd String? @map("period_end") @db.VarChar(10)
  metrics Json @default("{}")
  narrative String? @db.Text
  deliveryTargets Json @map("delivery_targets") @default("[]")
  generatedAt DateTime? @map("generated_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, reportKind, updatedAt], map: "benefactor_marketing_reports_client_kind_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_reports")
}

model BenefactorMarketingAttributionEvents {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  leadId String? @map("lead_id") @db.Uuid
  eventType String @map("event_type") @db.VarChar(64)
  sourcePlatform String? @map("source_platform") @db.VarChar(64)
  sourceEventId String? @map("source_event_id") @db.VarChar(200)
  occurredAt DateTime @map("occurred_at") @default(now()) @db.Timestamptz(6)
  valueCents Int @map("value_cents") @default(0)
  payload Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, eventType, occurredAt], map: "benefactor_marketing_attribution_events_client_type_idx")
  // benefactor_marketing_attribution_events_source_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_attribution_events")
}

model BenefactorMarketingOpportunities {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  leadId String? @map("lead_id") @db.Uuid
  status String @default("open") @db.VarChar(32)
  stage String @default("prospecting") @db.VarChar(48)
  name String @db.VarChar(220)
  amountCents Int @map("amount_cents") @default(0)
  probabilityMicros Int @map("probability_micros") @default(0)
  expectedCloseOn String? @map("expected_close_on") @db.VarChar(10)
  ownerUserId String? @map("owner_user_id") @db.Uuid
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, stage, updatedAt], map: "benefactor_marketing_opportunities_client_stage_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_opportunities")
}

model BenefactorMarketingContentAssets {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  status String @default("draft") @db.VarChar(32)
  assetKind String @map("asset_kind") @db.VarChar(48)
  title String @db.VarChar(240)
  channel String? @db.VarChar(64)
  body String? @db.Text
  assetUri String? @map("asset_uri") @db.Text
  seoKeywords Json @map("seo_keywords") @default("[]")
  approvalStatus String @map("approval_status") @default("pending") @db.VarChar(32)
  publishAt DateTime? @map("publish_at") @db.Timestamptz(6)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, updatedAt], map: "benefactor_marketing_content_assets_client_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_content_assets")
}

model BenefactorMarketingProjectTasks {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  contentAssetId String? @map("content_asset_id") @db.Uuid
  status String @default("todo") @db.VarChar(32)
  priority String @default("normal") @db.VarChar(32)
  title String @db.VarChar(240)
  description String? @db.Text
  assignedTo String? @map("assigned_to") @db.Uuid
  dueOn String? @map("due_on") @db.VarChar(10)
  slaDueAt DateTime? @map("sla_due_at") @db.Timestamptz(6)
  timeSpentMinutes Int @map("time_spent_minutes") @default(0)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, priority, updatedAt], map: "benefactor_marketing_project_tasks_client_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_project_tasks")
}

model BenefactorMarketingClientApprovals {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  contentAssetId String? @map("content_asset_id") @db.Uuid
  requestedBy String? @map("requested_by") @db.Uuid
  status String @default("pending") @db.VarChar(32)
  approvalKind String @map("approval_kind") @db.VarChar(48)
  title String @db.VarChar(240)
  requestPayload Json @map("request_payload") @default("{}")
  responseNote String? @map("response_note") @db.Text
  dueAt DateTime? @map("due_at") @db.Timestamptz(6)
  decidedAt DateTime? @map("decided_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, updatedAt], map: "benefactor_marketing_client_approvals_client_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_client_approvals")
}

model BenefactorMarketingTickets {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  status String @default("open") @db.VarChar(32)
  priority String @default("normal") @db.VarChar(32)
  subject String @db.VarChar(240)
  description String? @db.Text
  source String @default("portal") @db.VarChar(32)
  assignedTo String? @map("assigned_to") @db.Uuid
  lastActivityAt DateTime @map("last_activity_at") @default(now()) @db.Timestamptz(6)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, priority, updatedAt], map: "benefactor_marketing_tickets_client_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_tickets")
}

model BenefactorMarketingMeetings {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  leadId String? @map("lead_id") @db.Uuid
  opportunityId String? @map("opportunity_id") @db.Uuid
  status String @default("scheduled") @db.VarChar(32)
  meetingKind String @map("meeting_kind") @db.VarChar(48)
  title String @db.VarChar(240)
  scheduledAt DateTime @map("scheduled_at") @db.Timestamptz(6)
  durationMinutes Int @map("duration_minutes") @default(30)
  notes String? @db.Text
  recordingUri String? @map("recording_uri") @db.Text
  transcriptSummary Json @map("transcript_summary") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, scheduledAt], map: "benefactor_marketing_meetings_client_scheduled_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_meetings")
}

model BenefactorMarketingTeamAllocations {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String? @map("client_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  userId String @map("user_id") @db.Uuid
  role String @db.VarChar(48)
  allocationPercent Int @map("allocation_percent") @default(100)
  startsOn String? @map("starts_on") @db.VarChar(10)
  endsOn String? @map("ends_on") @db.VarChar(10)
  billable Boolean @default(true)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([userId, startsOn, endsOn], map: "benefactor_marketing_team_allocations_user_idx")
  // benefactor_marketing_team_allocations_client_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_team_allocations")
}

model BenefactorMarketingIntegrationSyncRuns {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  integrationId String @map("integration_id") @db.Uuid
  clientId String? @map("client_id") @db.Uuid
  syncKind String @map("sync_kind") @default("incremental") @db.VarChar(48)
  direction String @default("import") @db.VarChar(24)
  status String @default("queued") @db.VarChar(32)
  recordsSeen Int @map("records_seen") @default(0)
  recordsChanged Int @map("records_changed") @default(0)
  cursorBefore String? @map("cursor_before") @db.Text
  cursorAfter String? @map("cursor_after") @db.Text
  payload Json @default("{}")
  errorSummary String? @map("error_summary") @db.Text
  startedAt DateTime? @map("started_at") @db.Timestamptz(6)
  completedAt DateTime? @map("completed_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([integrationId, status, createdAt], map: "benefactor_marketing_integration_sync_runs_integration_idx")
  // benefactor_marketing_integration_sync_runs_client_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_integration_sync_runs")
}

model BenefactorMarketingOutreachSequences {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  status String @default("draft") @db.VarChar(32)
  channel String @default("email") @db.VarChar(32)
  name String @db.VarChar(220)
  audienceFilter Json @map("audience_filter") @default("{}")
  cadence Json @default("{}")
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, updatedAt], map: "benefactor_marketing_outreach_sequences_client_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_outreach_sequences")
}

model BenefactorMarketingOutreachSteps {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sequenceId String @map("sequence_id") @db.Uuid
  status String @default("active") @db.VarChar(32)
  stepOrder Int @map("step_order")
  channel String @db.VarChar(32)
  delayMinutes Int @map("delay_minutes") @default(0)
  subject String? @db.VarChar(240)
  bodyTemplate String? @map("body_template") @db.Text
  personalizationHints Json @map("personalization_hints") @default("[]")
  experimentKey String? @map("experiment_key") @db.VarChar(120)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([sequenceId, stepOrder], map: "benefactor_marketing_outreach_steps_sequence_order_uq")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_outreach_steps")
}

model BenefactorMarketingOutreachEnrollments {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  sequenceId String @map("sequence_id") @db.Uuid
  leadId String? @map("lead_id") @db.Uuid
  contactId String? @map("contact_id") @db.Uuid
  status String @default("active") @db.VarChar(32)
  currentStepOrder Int @map("current_step_order") @default(1)
  enrollmentContext Json @map("enrollment_context") @default("{}")
  lastTouchAt DateTime? @map("last_touch_at") @db.Timestamptz(6)
  nextTouchAt DateTime? @map("next_touch_at") @db.Timestamptz(6)
  outcome String? @db.VarChar(64)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([sequenceId, status, nextTouchAt], map: "benefactor_marketing_outreach_enrollments_sequence_idx")
  @@index([clientId, status, updatedAt], map: "benefactor_marketing_outreach_enrollments_client_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_outreach_enrollments")
}

model BenefactorMarketingOutreachTouchpoints {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  sequenceId String? @map("sequence_id") @db.Uuid
  enrollmentId String? @map("enrollment_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  leadId String? @map("lead_id") @db.Uuid
  contactId String? @map("contact_id") @db.Uuid
  channel String @db.VarChar(32)
  direction String @default("outbound") @db.VarChar(24)
  status String @default("planned") @db.VarChar(32)
  subject String? @db.VarChar(240)
  bodyExcerpt String? @map("body_excerpt") @db.Text
  externalMessageId String? @map("external_message_id") @db.VarChar(200)
  occurredAt DateTime @map("occurred_at") @default(now()) @db.Timestamptz(6)
  payload Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, occurredAt], map: "benefactor_marketing_outreach_touchpoints_client_idx")
  // benefactor_marketing_outreach_touchpoints_external_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_outreach_touchpoints")
}

model BenefactorMarketingProspectResearchBriefs {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  leadId String? @map("lead_id") @db.Uuid
  status String @default("draft") @db.VarChar(32)
  researchKind String @map("research_kind") @default("account_research") @db.VarChar(48)
  source String @default("ai_assisted") @db.VarChar(48)
  summary String? @db.Text
  findings Json @default("[]")
  recommendedActions Json @map("recommended_actions") @default("[]")
  confidenceMicros Int @map("confidence_micros") @default(0)
  modelName String? @map("model_name") @db.VarChar(120)
  generatedAt DateTime? @map("generated_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, updatedAt], map: "benefactor_marketing_prospect_research_briefs_client_idx")
  // benefactor_marketing_prospect_research_briefs_lead_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_prospect_research_briefs")
}

model BenefactorMarketingConversionEvents {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  leadId String? @map("lead_id") @db.Uuid
  contentAssetId String? @map("content_asset_id") @db.Uuid
  eventType String @map("event_type") @db.VarChar(64)
  sourcePlatform String? @map("source_platform") @db.VarChar(64)
  sourceEventId String? @map("source_event_id") @db.VarChar(200)
  sessionId String? @map("session_id") @db.VarChar(200)
  visitorKey String? @map("visitor_key") @db.VarChar(200)
  occurredAt DateTime @map("occurred_at") @default(now()) @db.Timestamptz(6)
  valueCents Int @map("value_cents") @default(0)
  utm Json @default("{}")
  payload Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, eventType, occurredAt], map: "benefactor_marketing_conversion_events_client_type_idx")
  // benefactor_marketing_conversion_events_source_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_conversion_events")
}

model BenefactorMarketingPortalMembers {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  contactId String? @map("contact_id") @db.Uuid
  userId String? @map("user_id") @db.Uuid
  email String @db.VarChar(240)
  status String @default("invited") @db.VarChar(32)
  role String @default("viewer") @db.VarChar(32)
  accessScope Json @map("access_scope") @default("{}")
  lastSeenAt DateTime? @map("last_seen_at") @db.Timestamptz(6)
  invitedAt DateTime @map("invited_at") @default(now()) @db.Timestamptz(6)
  acceptedAt DateTime? @map("accepted_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([clientId, email], map: "benefactor_marketing_portal_members_client_email_uq")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_portal_members")
}

model BenefactorMarketingSharedDocuments {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  contentAssetId String? @map("content_asset_id") @db.Uuid
  status String @default("active") @db.VarChar(32)
  documentKind String @map("document_kind") @db.VarChar(48)
  title String @db.VarChar(240)
  storageUri String @map("storage_uri") @db.Text
  mimeType String? @map("mime_type") @db.VarChar(120)
  visibility String @default("client_portal") @db.VarChar(32)
  uploadedBy String? @map("uploaded_by") @db.Uuid
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, updatedAt], map: "benefactor_marketing_shared_documents_client_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_shared_documents")
}

model BenefactorMarketingCollaborationComments {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  parentCommentId String? @map("parent_comment_id") @db.Uuid
  resourceType String @map("resource_type") @db.VarChar(64)
  resourceId String? @map("resource_id") @db.Uuid
  authorUserId String? @map("author_user_id") @db.Uuid
  authorContactId String? @map("author_contact_id") @db.Uuid
  body String @db.Text
  status String @default("open") @db.VarChar(32)
  visibility String @default("client_portal") @db.VarChar(32)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, resourceType, updatedAt], map: "benefactor_marketing_collaboration_comments_client_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_collaboration_comments")
}

model BenefactorMarketingNotifications {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  recipientUserId String? @map("recipient_user_id") @db.Uuid
  recipientContactId String? @map("recipient_contact_id") @db.Uuid
  channel String @default("email") @db.VarChar(32)
  status String @default("queued") @db.VarChar(32)
  notificationKind String @map("notification_kind") @db.VarChar(64)
  title String @db.VarChar(240)
  body String? @db.Text
  payload Json @default("{}")
  scheduledAt DateTime? @map("scheduled_at") @db.Timestamptz(6)
  sentAt DateTime? @map("sent_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, status, scheduledAt], map: "benefactor_marketing_notifications_client_status_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_notifications")
}

model BenefactorMarketingTimeEntries {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String? @map("client_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  projectTaskId String? @map("project_task_id") @db.Uuid
  userId String @map("user_id") @db.Uuid
  entryDate String @map("entry_date") @db.VarChar(10)
  minutes Int
  billable Boolean @default(true)
  rateCents Int @map("rate_cents") @default(0)
  costCents Int @map("cost_cents") @default(0)
  notes String? @db.Text
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  // benefactor_marketing_time_entries_client_date_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_time_entries")
}

model BenefactorMarketingVendorCosts {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String? @map("client_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  vendorName String @map("vendor_name") @db.VarChar(200)
  category String @db.VarChar(64)
  status String @default("planned") @db.VarChar(32)
  amountCents Int @map("amount_cents")
  incurredOn String? @map("incurred_on") @db.VarChar(10)
  invoiceRef String? @map("invoice_ref") @db.VarChar(120)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  // benefactor_marketing_vendor_costs_client_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_vendor_costs")
}

model BenefactorMarketingCommissionEntries {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String? @map("client_id") @db.Uuid
  opportunityId String? @map("opportunity_id") @db.Uuid
  userId String @map("user_id") @db.Uuid
  status String @default("pending") @db.VarChar(32)
  commissionKind String @map("commission_kind") @default("deal") @db.VarChar(48)
  basisCents Int @map("basis_cents") @default(0)
  rateMicros Int @map("rate_micros") @default(0)
  amountCents Int @map("amount_cents") @default(0)
  earnedOn String? @map("earned_on") @db.VarChar(10)
  paidAt DateTime? @map("paid_at") @db.Timestamptz(6)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([userId, status, updatedAt], map: "benefactor_marketing_commission_entries_user_idx")
  // benefactor_marketing_commission_entries_client_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_commission_entries")
}

model BenefactorMarketingBudgetForecasts {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  campaignId String? @map("campaign_id") @db.Uuid
  forecastKind String @map("forecast_kind") @default("monthly") @db.VarChar(48)
  periodStart String @map("period_start") @db.VarChar(10)
  periodEnd String @map("period_end") @db.VarChar(10)
  status String @default("draft") @db.VarChar(32)
  revenueCents Int @map("revenue_cents") @default(0)
  mediaSpendCents Int @map("media_spend_cents") @default(0)
  laborCostCents Int @map("labor_cost_cents") @default(0)
  vendorCostCents Int @map("vendor_cost_cents") @default(0)
  grossMarginCents Int @map("gross_margin_cents") @default(0)
  assumptions Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, periodStart, status], map: "benefactor_marketing_budget_forecasts_client_period_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_budget_forecasts")
}

model BenefactorMarketingCallInsights {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  clientId String @map("client_id") @db.Uuid
  meetingId String? @map("meeting_id") @db.Uuid
  leadId String? @map("lead_id") @db.Uuid
  opportunityId String? @map("opportunity_id") @db.Uuid
  status String @default("ready") @db.VarChar(32)
  provider String? @db.VarChar(64)
  transcriptUri String? @map("transcript_uri") @db.Text
  summary String? @db.Text
  sentiment String? @db.VarChar(32)
  actionItems Json @map("action_items") @default("[]")
  objections Json @default("[]")
  nextSteps Json @map("next_steps") @default("[]")
  confidenceMicros Int @map("confidence_micros") @default(0)
  analyzedAt DateTime @map("analyzed_at") @default(now()) @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([clientId, analyzedAt], map: "benefactor_marketing_call_insights_client_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_marketing_call_insights")
}

model UsaccUsers {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  externalSubject String? @map("external_subject") @db.VarChar(240)
  emailHash String? @map("email_hash") @db.VarChar(64)
  displayName String @map("display_name") @db.VarChar(200)
  userKind String @map("user_kind") @default("natural_person") @db.VarChar(48)
  status String @default("active") @db.VarChar(32)
  kycLevel String @map("kyc_level") @default("none") @db.VarChar(32)
  roles Json @default("{}")
  isLegalEntity Boolean @map("is_legal_entity") @default(false)
  legalRegion String? @map("legal_region") @db.VarChar(64)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  // usacc_users_external_subject_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // usacc_users_email_hash_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([status, updatedAt], map: "usacc_users_status_updated_at_idx")
  // usacc_users_roles_gin_idx is defined in schema.sql because Prisma cannot fully model gin partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("usacc_users")
}

model UsaccCases {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  caseNumber String @map("case_number") @db.VarChar(80)
  title String @db.VarChar(240)
  status String @default("draft") @db.VarChar(40)
  filingTier String @map("filing_tier") @default("screen") @db.VarChar(40)
  plaintiffUserId String? @map("plaintiff_user_id") @db.Uuid
  defendantSummary String @map("defendant_summary") @db.Text
  conductSummary String @map("conduct_summary") @db.Text
  conductFingerprint String? @map("conduct_fingerprint") @db.VarChar(128)
  conductWindowStart String? @map("conduct_window_start") @db.VarChar(10)
  conductWindowEnd String? @map("conduct_window_end") @db.VarChar(10)
  priorityScoreMicros Int @map("priority_score_micros") @default(0)
  metaData Json @map("meta_data") @default("{}")
  openedAt DateTime? @map("opened_at") @db.Timestamptz(6)
  closedAt DateTime? @map("closed_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([caseNumber], map: "usacc_cases_case_number_uq")
  @@index([status, updatedAt], map: "usacc_cases_status_updated_at_idx")
  // usacc_cases_plaintiff_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("usacc_cases")
}

model UsaccCaseParticipants {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  caseId String @map("case_id") @db.Uuid
  userId String @map("user_id") @db.Uuid
  role String @db.VarChar(48)
  status String @default("active") @db.VarChar(32)
  grantedBy String? @map("granted_by") @db.Uuid
  grantedByPolicyVersion String? @map("granted_by_policy_version") @db.VarChar(120)
  endedAt DateTime? @map("ended_at") @db.Timestamptz(6)
  endedReason String? @map("ended_reason") @db.VarChar(240)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([caseId, userId, role], map: "usacc_case_participants_case_user_role_uq")
  @@index([userId, status, updatedAt], map: "usacc_case_participants_user_idx")
  @@index([caseId, role, status], map: "usacc_case_participants_case_role_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("usacc_case_participants")
}

model UsaccCaseStages {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  caseId String @map("case_id") @db.Uuid
  stageKey String @map("stage_key") @db.VarChar(64)
  stageOrder Int @map("stage_order")
  title String @db.VarChar(200)
  status String @default("pending") @db.VarChar(32)
  assignedUserId String? @map("assigned_user_id") @db.Uuid
  openedAt DateTime? @map("opened_at") @db.Timestamptz(6)
  dueAt DateTime? @map("due_at") @db.Timestamptz(6)
  closedAt DateTime? @map("closed_at") @db.Timestamptz(6)
  decisionSummary String? @map("decision_summary") @db.Text
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([caseId, stageKey], map: "usacc_case_stages_case_stage_key_uq")
  @@index([caseId, stageOrder], map: "usacc_case_stages_case_order_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("usacc_case_stages")
}

model UsaccElections {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  caseId String? @map("case_id") @db.Uuid
  stageId String? @map("stage_id") @db.Uuid
  electionKind String @map("election_kind") @db.VarChar(48)
  title String @db.VarChar(220)
  status String @default("draft") @db.VarChar(32)
  quorumCount Int @map("quorum_count") @default(1)
  thresholdMicros Int @map("threshold_micros") @default(500000)
  opensAt DateTime? @map("opens_at") @db.Timestamptz(6)
  closesAt DateTime? @map("closes_at") @db.Timestamptz(6)
  sealedUntil DateTime? @map("sealed_until") @db.Timestamptz(6)
  tally Json @default("{}")
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  // usacc_elections_case_status_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // usacc_elections_stage_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("usacc_elections")
}

model UsaccVotes {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  electionId String @map("election_id") @db.Uuid
  caseId String? @map("case_id") @db.Uuid
  voterUserId String @map("voter_user_id") @db.Uuid
  voteKind String @map("vote_kind") @default("choice") @db.VarChar(48)
  voteValue String @map("vote_value") @db.VarChar(80)
  weightMicros Int @map("weight_micros") @default(1000000)
  commitmentHash String? @map("commitment_hash") @db.VarChar(128)
  sealedPayload Json? @map("sealed_payload")
  revealedAt DateTime? @map("revealed_at") @db.Timestamptz(6)
  contractDigest String? @map("contract_digest") @db.VarChar(160)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([electionId, voterUserId], map: "usacc_votes_election_voter_uq")
  // usacc_votes_case_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([voterUserId, createdAt], map: "usacc_votes_voter_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("usacc_votes")
}

model UsaccEscrowAccounts {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  caseId String @map("case_id") @db.Uuid
  status String @default("pending") @db.VarChar(32)
  provider String @default("stripe_treasury") @db.VarChar(48)
  providerAccountRef String? @map("provider_account_ref") @db.VarChar(240)
  currency String @default("USD") @db.VarChar(12)
  targetAmountCents BigInt @map("target_amount_cents") @default(0) @db.BigInt
  committedAmountCents BigInt @map("committed_amount_cents") @default(0) @db.BigInt
  capturedAmountCents BigInt @map("captured_amount_cents") @default(0) @db.BigInt
  disbursedAmountCents BigInt @map("disbursed_amount_cents") @default(0) @db.BigInt
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([caseId, provider], map: "usacc_escrow_accounts_case_provider_uq")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("usacc_escrow_accounts")
}

model UsaccLedgerEntries {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  caseId String? @map("case_id") @db.Uuid
  escrowAccountId String? @map("escrow_account_id") @db.Uuid
  userId String? @map("user_id") @db.Uuid
  entryKind String @map("entry_kind") @db.VarChar(48)
  direction String @db.VarChar(16)
  amountCents BigInt @map("amount_cents") @db.BigInt
  currency String @default("USD") @db.VarChar(12)
  providerRef String? @map("provider_ref") @db.VarChar(240)
  contractDigest String? @map("contract_digest") @db.VarChar(160)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  // usacc_ledger_entries_case_created_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // usacc_ledger_entries_user_created_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("usacc_ledger_entries")
}

model UsaccContractOperations {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  caseId String? @map("case_id") @db.Uuid
  electionId String? @map("election_id") @db.Uuid
  voteId String? @map("vote_id") @db.Uuid
  requestId String @map("request_id") @db.VarChar(160)
  operationKind String @map("operation_kind") @db.VarChar(48)
  status String @default("pending") @db.VarChar(32)
  programId String? @map("program_id") @db.VarChar(128)
  digest String? @db.VarChar(160)
  envelope Json @default("{}")
  response Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([requestId], map: "usacc_contract_operations_request_id_uq")
  // usacc_contract_operations_case_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("usacc_contract_operations")
}

model UsaccSimulationRuns {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  caseId String? @map("case_id") @db.Uuid
  status String @default("queued") @db.VarChar(32)
  mode String @default("sim") @db.VarChar(32)
  seed BigInt @db.BigInt
  horizonDays Int @map("horizon_days") @default(180)
  actorCount Int @map("actor_count") @default(0)
  eventCount Int @map("event_count") @default(0)
  metrics Json @default("{}")
  trace Json @default("[]")
  input Json @default("{}")
  startedAt DateTime? @map("started_at") @db.Timestamptz(6)
  finishedAt DateTime? @map("finished_at") @db.Timestamptz(6)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  // usacc_simulation_runs_case_created_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([status, createdAt], map: "usacc_simulation_runs_status_created_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("usacc_simulation_runs")
}

model UsaccAuditEvents {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  caseId String? @map("case_id") @db.Uuid
  actorUserId String? @map("actor_user_id") @db.Uuid
  eventType String @map("event_type") @db.VarChar(96)
  eventHash String @map("event_hash") @db.VarChar(128)
  source String @default("usacc-rest-api-backend-rs") @db.VarChar(80)
  payload Json @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  @@unique([eventHash], map: "usacc_audit_events_hash_uq")
  // usacc_audit_events_case_created_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("usacc_audit_events")
}

model BenefactorLeads {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessName String @map("business_name") @default("") @db.VarChar(300)
  ownerFirstName String @map("owner_first_name") @default("") @db.VarChar(120)
  ownerLastName String @map("owner_last_name") @default("") @db.VarChar(130)
  primaryEmail String @map("primary_email") @default("") @db.VarChar(255)
  secondaryEmail String? @map("secondary_email") @db.VarChar(255)
  primaryPhone String? @map("primary_phone") @db.VarChar(100)
  websiteUrl String? @map("website_url") @db.VarChar(500)
  serviceCategory String @map("service_category") @default("other") @db.VarChar(60)
  serviceSubcategories Json @map("service_subcategories") @default("[]")
  city String? @db.VarChar(120)
  state String? @db.VarChar(80)
  zipCode String? @map("zip_code") @db.VarChar(20)
  country String @default("US") @db.VarChar(80)
  serviceArea String? @map("service_area") @db.VarChar(500)
  leadStatus String @map("lead_status") @default("new") @db.VarChar(30)
  outreachStatus String @map("outreach_status") @default("pending") @db.VarChar(30)
  totalOutreachAttempts Int @map("total_outreach_attempts") @default(0)
  lastOutreachAt DateTime? @map("last_outreach_at") @db.Timestamptz(6)
  contactAttempts Json @map("contact_attempts") @default("[]")
  sourceUrl String? @map("source_url") @db.VarChar(1000)
  sourceQuery String? @map("source_query") @db.VarChar(500)
  sourceTool String? @map("source_tool") @db.VarChar(60)
  sourceEngine String? @map("source_engine") @db.VarChar(30)
  isVerified Boolean @map("is_verified") @default(false)
  tags Json @default("[]")
  metaData Json @map("meta_data") @default("{}")
  notes String? @db.Text
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  // benefactor_leads_email_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  @@index([businessName], map: "benefactor_leads_business_name_idx")
  @@index([serviceCategory], map: "benefactor_leads_category_idx")
  @@index([city], map: "benefactor_leads_city_idx")
  @@index([state], map: "benefactor_leads_state_idx")
  @@index([zipCode], map: "benefactor_leads_zip_idx")
  @@index([leadStatus], map: "benefactor_leads_status_idx")
  @@index([outreachStatus], map: "benefactor_leads_outreach_idx")
  @@index([lastOutreachAt], map: "benefactor_leads_last_outreach_idx")
  @@index([createdAt], map: "benefactor_leads_created_at_idx")
  @@index([isSoftDeleted], map: "benefactor_leads_soft_deleted_idx")
  @@index([isVerified], map: "benefactor_leads_verified_idx")
  @@index([serviceCategory, city], map: "benefactor_leads_category_city_idx")
  @@index([serviceCategory, state], map: "benefactor_leads_category_state_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_leads")
}

model BenefactorLeadsDomains {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  domain String @db.VarChar(255)
  domainKind String @map("domain_kind") @default("email") @db.VarChar(32)
  status String @default("allowed") @db.VarChar(40)
  reason String? @db.Text
  source String @default("manual") @db.VarChar(80)
  isBlacklisted Boolean @map("is_blacklisted") @default(false)
  isBlocked Boolean @map("is_blocked") @default(false)
  isPermanentlyBlocked Boolean @map("is_permanently_blocked") @default(false)
  blockedReason String? @map("blocked_reason") @db.Text
  blockedUntil DateTime? @map("blocked_until") @db.Timestamptz(6)
  skipUntil DateTime? @map("skip_until") @db.Timestamptz(6)
  scrapeCount Int @map("scrape_count") @default(0)
  skipCount Int @map("skip_count") @default(0)
  skippedCount Int @map("skipped_count") @default(0)
  emailFoundCount Int @map("email_found_count") @default(0)
  leadInsertedCount Int @map("lead_inserted_count") @default(0)
  lastSeenAt DateTime? @map("last_seen_at") @db.Timestamptz(6)
  lastScrapedAt DateTime? @map("last_scraped_at") @db.Timestamptz(6)
  lastSkippedAt DateTime? @map("last_skipped_at") @db.Timestamptz(6)
  lastEmailFoundAt DateTime? @map("last_email_found_at") @db.Timestamptz(6)
  lastLeadInsertedAt DateTime? @map("last_lead_inserted_at") @db.Timestamptz(6)
  lastSeenUrl String? @map("last_seen_url") @db.Text
  metaData Json @map("meta_data") @default("{}")
  isActive Boolean @map("is_active") @default(true)
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  @@unique([domain, domainKind], map: "benefactor_leads_domains_domain_kind_uq")
  @@index([domain], map: "benefactor_leads_domains_domain_idx")
  @@index([domainKind], map: "benefactor_leads_domains_kind_idx")
  @@index([status], map: "benefactor_leads_domains_status_idx")
  @@index([isBlacklisted], map: "benefactor_leads_domains_blacklisted_idx")
  @@index([domainKind, status], map: "benefactor_leads_domains_kind_status_idx")
  @@index([isBlocked], map: "benefactor_leads_domains_blocked_idx")
  @@index([blockedUntil], map: "benefactor_leads_domains_blocked_until_idx")
  @@index([skipUntil], map: "benefactor_leads_domains_skip_until_idx")
  @@index([lastScrapedAt], map: "benefactor_leads_domains_last_scraped_idx")
  @@index([isActive], map: "benefactor_leads_domains_active_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_leads_domains")
}

model BenefactorSearchLocations {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  slug String @db.VarChar(160)
  city String @db.VarChar(120)
  state String @db.VarChar(80)
  stateCode String? @map("state_code") @db.VarChar(10)
  country String @default("US") @db.VarChar(80)
  metroArea String? @map("metro_area") @db.VarChar(220)
  militaryArea String? @map("military_area") @db.VarChar(220)
  primaryInstallation String? @map("primary_installation") @db.VarChar(220)
  installationAliases Json @map("installation_aliases") @default("[]")
  locationType String @map("location_type") @default("military_town") @db.VarChar(80)
  priority Int @default(5)
  searchWeight Int @map("search_weight") @default(5)
  totalQueryRuns Int @map("total_query_runs") @default(0)
  totalEmailsInserted Int @map("total_emails_inserted") @default(0)
  successCount Int @map("success_count") @default(0)
  failureCount Int @map("failure_count") @default(0)
  lastRunAt DateTime? @map("last_run_at") @db.Timestamptz(6)
  lastSuccessAt DateTime? @map("last_success_at") @db.Timestamptz(6)
  lastFailureAt DateTime? @map("last_failure_at") @db.Timestamptz(6)
  cooldownUntil DateTime? @map("cooldown_until") @db.Timestamptz(6)
  metaData Json @map("meta_data") @default("{}")
  isActive Boolean @map("is_active") @default(true)
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  @@unique([slug], map: "benefactor_search_locations_slug_uq")
  @@unique([city, state, country], map: "benefactor_search_locations_city_state_country_uq")
  @@index([state], map: "benefactor_search_locations_state_idx")
  @@index([militaryArea], map: "benefactor_search_locations_military_area_idx")
  @@index([locationType], map: "benefactor_search_locations_type_idx")
  @@index([priority], map: "benefactor_search_locations_priority_idx")
  @@index([cooldownUntil], map: "benefactor_search_locations_cooldown_idx")
  @@index([isActive], map: "benefactor_search_locations_active_idx")
  @@index([isSoftDeleted], map: "benefactor_search_locations_soft_deleted_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_search_locations")
}

model BenefactorScrapeQueries {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  queryText String @map("query_text") @db.Text
  queryHash String @map("query_hash") @db.VarChar(64)
  benefactorIcpSlug String? @map("benefactor_icp_slug") @db.VarChar(160)
  benefactorIcpName String? @map("benefactor_icp_name") @db.VarChar(220)
  benefactorSearchLocationId String? @map("benefactor_search_location_id") @db.Uuid
  serviceCategory String @map("service_category") @default("other") @db.VarChar(60)
  targetCity String? @map("target_city") @db.VarChar(120)
  targetState String? @map("target_state") @db.VarChar(80)
  targetCountry String @map("target_country") @default("US") @db.VarChar(80)
  targetMilitaryArea String? @map("target_military_area") @db.VarChar(220)
  targetInstallation String? @map("target_installation") @db.VarChar(220)
  queryVariant String @map("query_variant") @default("email_contact") @db.VarChar(80)
  searchPageDepth Int @map("search_page_depth") @default(4)
  priority Int @default(5)
  totalRuns Int @map("total_runs") @default(0)
  totalUrlsVisited Int @map("total_urls_visited") @default(0)
  totalEmailsFound Int @map("total_emails_found") @default(0)
  totalEmailsInserted Int @map("total_emails_inserted") @default(0)
  totalEmailsDuplicate Int @map("total_emails_duplicate") @default(0)
  totalErrors Int @map("total_errors") @default(0)
  successCount Int @map("success_count") @default(0)
  failureCount Int @map("failure_count") @default(0)
  lastRunAt DateTime? @map("last_run_at") @db.Timestamptz(6)
  lastSuccessAt DateTime? @map("last_success_at") @db.Timestamptz(6)
  lastFailureAt DateTime? @map("last_failure_at") @db.Timestamptz(6)
  lastRunEmailsFound Int @map("last_run_emails_found") @default(0)
  lastRunEmailsInserted Int @map("last_run_emails_inserted") @default(0)
  lastRunSuccess Boolean @map("last_run_success") @default(false)
  lastRunDurationMs Int @map("last_run_duration_ms") @default(0)
  lastRunError String? @map("last_run_error") @db.Text
  cooldownUntil DateTime? @map("cooldown_until") @db.Timestamptz(6)
  consecutiveZeroNewRuns Int @map("consecutive_zero_new_runs") @default(0)
  lastZeroNewRunAt DateTime? @map("last_zero_new_run_at") @db.Timestamptz(6)
  metaData Json @map("meta_data") @default("{}")
  isActive Boolean @map("is_active") @default(true)
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  @@unique([queryHash], map: "benefactor_scrape_queries_hash_uq")
  @@index([benefactorIcpSlug], map: "benefactor_scrape_queries_icp_slug_idx")
  @@index([benefactorSearchLocationId], map: "benefactor_scrape_queries_location_id_idx")
  @@index([serviceCategory], map: "benefactor_scrape_queries_category_idx")
  @@index([targetCity], map: "benefactor_scrape_queries_city_idx")
  @@index([targetState], map: "benefactor_scrape_queries_state_idx")
  @@index([targetMilitaryArea], map: "benefactor_scrape_queries_military_area_idx")
  @@index([queryVariant], map: "benefactor_scrape_queries_variant_idx")
  @@index([priority], map: "benefactor_scrape_queries_priority_idx")
  @@index([lastRunAt], map: "benefactor_scrape_queries_last_run_idx")
  @@index([successCount], map: "benefactor_scrape_queries_success_count_idx")
  @@index([failureCount], map: "benefactor_scrape_queries_failure_count_idx")
  @@index([isActive], map: "benefactor_scrape_queries_active_idx")
  @@index([cooldownUntil], map: "benefactor_scrape_queries_cooldown_idx")
  @@index([consecutiveZeroNewRuns], map: "benefactor_scrape_queries_zero_new_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_scrape_queries")
}

model BenefactorDomainSearchTracking {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  domain String @db.VarChar(255)
  forWhat String @map("for_what") @default("benefactor_lead_scrape") @db.VarChar(80)
  searchResultAppearances Int @map("search_result_appearances") @default(0)
  queuedVisitCount Int @map("queued_visit_count") @default(0)
  visitCount Int @map("visit_count") @default(0)
  goodResultCount Int @map("good_result_count") @default(0)
  badResultCount Int @map("bad_result_count") @default(0)
  emailFoundCount Int @map("email_found_count") @default(0)
  leadInsertedCount Int @map("lead_inserted_count") @default(0)
  lastQueuedAt DateTime? @map("last_queued_at") @db.Timestamptz(6)
  lastVisitedAt DateTime? @map("last_visited_at") @db.Timestamptz(6)
  lastGoodResultAt DateTime? @map("last_good_result_at") @db.Timestamptz(6)
  lastBadResultAt DateTime? @map("last_bad_result_at") @db.Timestamptz(6)
  lastEmailFoundAt DateTime? @map("last_email_found_at") @db.Timestamptz(6)
  lastLeadInsertedAt DateTime? @map("last_lead_inserted_at") @db.Timestamptz(6)
  lastGoodUrl String? @map("last_good_url") @db.Text
  lastBadUrl String? @map("last_bad_url") @db.Text
  blockedUntil DateTime? @map("blocked_until") @db.Timestamptz(6)
  isPermanentlyBlocked Boolean @map("is_permanently_blocked") @default(false)
  blockedReason String? @map("blocked_reason") @db.Text
  metaData Json @map("meta_data") @default("{}")
  isActive Boolean @map("is_active") @default(true)
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  @@unique([domain, forWhat], map: "benefactor_domain_search_tracking_domain_for_what_uq")
  @@index([forWhat], map: "benefactor_domain_search_tracking_for_what_idx")
  @@index([visitCount], map: "benefactor_domain_search_tracking_visit_count_idx")
  @@index([goodResultCount], map: "benefactor_domain_search_tracking_good_result_idx")
  @@index([badResultCount], map: "benefactor_domain_search_tracking_bad_result_idx")
  @@index([emailFoundCount], map: "benefactor_domain_search_tracking_email_found_idx")
  @@index([blockedUntil], map: "benefactor_domain_search_tracking_blocked_until_idx")
  @@index([isActive], map: "benefactor_domain_search_tracking_active_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_domain_search_tracking")
}

model BenefactorIcps {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  slug String @db.VarChar(160)
  name String @db.VarChar(220)
  category String @default("local_services") @db.VarChar(120)
  serviceCategory String @map("service_category") @default("other") @db.VarChar(120)
  description String @default("") @db.Text
  outcallFitScore Int @map("outcall_fit_score") @default(5)
  priority Int @default(5)
  searchTerms Json @map("search_terms") @default("[]")
  searchSignals Json @map("search_signals") @default("[]")
  targetHomeServices Boolean @map("target_home_services") @default(false)
  targetMedical Boolean @map("target_medical") @default(false)
  targetLegal Boolean @map("target_legal") @default(false)
  targetEvents Boolean @map("target_events") @default(false)
  targetCorporate Boolean @map("target_corporate") @default(false)
  targetIndustrial Boolean @map("target_industrial") @default(false)
  metaData Json @map("meta_data") @default("{}")
  isActive Boolean @map("is_active") @default(true)
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  @@unique([slug], map: "benefactor_icps_slug_uq")
  @@index([category], map: "benefactor_icps_category_idx")
  @@index([serviceCategory], map: "benefactor_icps_service_category_idx")
  @@index([priority], map: "benefactor_icps_priority_idx")
  @@index([outcallFitScore], map: "benefactor_icps_outcall_fit_idx")
  @@index([isActive], map: "benefactor_icps_active_idx")
  @@index([isSoftDeleted], map: "benefactor_icps_soft_deleted_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("benefactor_icps")
}

model VcsRepositories {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  slug String @db.VarChar(120)
  displayName String @map("display_name") @db.VarChar(200)
  vcsKind String @map("vcs_kind") @default("git") @db.VarChar(20)
  remoteUrl String @map("remote_url") @db.Text
  defaultBranch String @map("default_branch") @default("main") @db.VarChar(160)
  mirrorPath String? @map("mirror_path") @db.Text
  mirrorStatus String @map("mirror_status") @default("pending") @db.VarChar(32)
  visibility String @default("private") @db.VarChar(20)
  lastSyncedAt DateTime? @map("last_synced_at") @db.Timestamptz(6)
  lastError String? @map("last_error") @db.Text
  sizeBytes BigInt @map("size_bytes") @default(0) @db.BigInt
  refCount Int @map("ref_count") @default(0)
  metaData Json @map("meta_data") @default("{}")
  isSoftDeleted Boolean @map("is_soft_deleted") @default(false)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  createdBy String? @map("created_by") @db.Uuid
  updatedBy String? @map("updated_by") @db.Uuid
  // vcs_repositories_slug_active_uq is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // vcs_repositories_vcs_kind_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // vcs_repositories_mirror_status_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // vcs_repositories_updated_at_idx is defined in schema.sql because Prisma cannot fully model partial/expression indexes.
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("vcs_repositories")
}

model VcsRefs {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  repositoryId String @map("repository_id") @db.Uuid
  refName String @map("ref_name") @db.VarChar(255)
  refType String @map("ref_type") @default("branch") @db.VarChar(20)
  targetRevision String @map("target_revision") @db.VarChar(120)
  isDefault Boolean @map("is_default") @default(false)
  metaData Json @map("meta_data") @default("{}")
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@unique([repositoryId, refName], map: "vcs_refs_repo_name_uq")
  @@index([repositoryId], map: "vcs_refs_repository_id_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("vcs_refs")
}

model VcsOperations {
  id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  repositoryId String? @map("repository_id") @db.Uuid
  vcsKind String @map("vcs_kind") @default("git") @db.VarChar(20)
  opType String @map("op_type") @db.VarChar(32)
  status String @default("pending") @db.VarChar(20)
  params Json @default("{}")
  resultSummary Json @map("result_summary") @default("{}")
  error String? @db.Text
  durationMs Int? @map("duration_ms")
  requestedBy String? @map("requested_by") @db.VarChar(200)
  createdAt DateTime @map("created_at") @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @map("updated_at") @default(now()) @db.Timestamptz(6)
  @@index([repositoryId], map: "vcs_operations_repository_id_idx")
  @@index([opType], map: "vcs_operations_op_type_idx")
  @@index([createdAt], map: "vcs_operations_created_at_idx")
  // Check constraints live in schema.sql and must be migrated from the SQL contract.
  @@map("vcs_operations")
}
