// Generated by @dd/pg-defs. Do not edit by hand.
// SOURCE OF TRUTH: schema/schema.sql defines the database contract.
// Generated ORM/client code is an adapter only; do not infer migrations from it.
// MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.
#pragma once

#include <cstdint>
#include <functional>
#include <optional>
#include <regex>
#include <string>
#include <vector>

namespace dd_pg_defs {

inline const char* accounts_table = "threefa.accounts";
inline const std::vector<std::string> accounts_columns = { "id", "username", "auth_secret", "created_at" };
inline const char* accounts_select_sql = R"SQL(select
      id::text as id,
      username,
      auth_secret,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from threefa.accounts)SQL";

struct AccountsRow {
    std::string id;
    std::string username;
    std::string auth_secret;
    std::string created_at;
};

inline AccountsRow accounts_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    AccountsRow row;
    (void)is_null;
    row.id = get(0);
    row.username = get(1);
    row.auth_secret = get(2);
    row.created_at = get(3);
    return row;
}

inline const char* devices_table = "threefa.devices";
inline const std::vector<std::string> devices_columns = { "id", "account_id", "device_name", "sync_token_hash", "revoked", "created_at" };
inline const char* devices_select_sql = R"SQL(select
      id::text as id,
      account_id::text as account_id,
      device_name,
      sync_token_hash,
      revoked,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from threefa.devices)SQL";

struct DevicesRow {
    std::string id;
    std::string account_id;
    std::string device_name;
    std::string sync_token_hash;
    bool revoked;
    std::string created_at;
};

inline DevicesRow devices_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DevicesRow row;
    (void)is_null;
    row.id = get(0);
    row.account_id = get(1);
    row.device_name = get(2);
    row.sync_token_hash = get(3);
    row.revoked = (get(4) == "t");
    row.created_at = get(5);
    return row;
}
inline std::optional<std::string> validate_devices_sync_token_hash(const std::string& value) {
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("devices.sync_token_hash does not match the required pattern");
    return std::nullopt;
}

inline const char* vault_blobs_table = "threefa.vault_blobs";
inline const std::vector<std::string> vault_blobs_columns = { "account_id", "ciphertext", "nonce", "kdf_salt", "kdf_params", "version", "updated_at" };
inline const char* vault_blobs_select_sql = R"SQL(select
      account_id::text as account_id,
      ciphertext,
      nonce,
      kdf_salt,
      kdf_params::text as kdf_params_json,
      version::text as version_json,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from threefa.vault_blobs)SQL";

struct VaultBlobsRow {
    std::string account_id;
    std::string ciphertext;
    std::string nonce;
    std::string kdf_salt;
    std::string kdf_params;
    std::string version;
    std::string updated_at;
};

inline VaultBlobsRow vault_blobs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    VaultBlobsRow row;
    (void)is_null;
    row.account_id = get(0);
    row.ciphertext = get(1);
    row.nonce = get(2);
    row.kdf_salt = get(3);
    row.kdf_params = get(4);
    row.version = get(5);
    row.updated_at = get(6);
    return row;
}

inline const char* app_config_table = "app_config";
inline const std::vector<std::string> app_config_columns = { "id", "scope", "key", "value", "version", "status", "labels", "meta_data", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* app_config_select_sql = R"SQL(select
      id::text as id,
      scope,
      key,
      value::text as value_json,
      version,
      status,
      labels::text as labels_json,
      meta_data::text as meta_data_json,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from app_config)SQL";

enum class AppConfigStatus { Active, Paused, Archived };
inline std::string app_config_status_to_string(AppConfigStatus value) {
    switch (value) {
        case AppConfigStatus::Active: return "active";
        case AppConfigStatus::Paused: return "paused";
        case AppConfigStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<AppConfigStatus> parse_app_config_status(const std::string& value) {
    if (value == "active") return AppConfigStatus::Active;
    if (value == "paused") return AppConfigStatus::Paused;
    if (value == "archived") return AppConfigStatus::Archived;
    return std::nullopt;
}

struct AppConfigRow {
    std::string id;
    std::string scope;
    std::string key;
    std::string value;
    int32_t version;
    std::string status;
    std::string labels;
    std::string meta_data;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline AppConfigRow app_config_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    AppConfigRow row;
    (void)is_null;
    row.id = get(0);
    row.scope = get(1);
    row.key = get(2);
    row.value = get(3);
    row.version = std::stoi(get(4));
    row.status = get(5);
    row.labels = get(6);
    row.meta_data = get(7);
    row.is_soft_deleted = (get(8) == "t");
    row.created_at = get(9);
    row.updated_at = get(10);
    row.created_by = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.updated_by = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    return row;
}
inline std::optional<std::string> validate_app_config_scope(const std::string& value) {
    if (value.size() > 120) return std::string("app_config.scope must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._/-]{1,120}$)RX"))) return std::string("app_config.scope does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_app_config_key(const std::string& value) {
    if (value.size() > 200) return std::string("app_config.key must be at most 200 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,200}$)RX"))) return std::string("app_config.key does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_app_config_version(int32_t value) {
    if (value < 1) return std::string("app_config.version is below the minimum");
    return std::nullopt;
}

inline const char* vapi_phone_call_events_table = "vapi_phone_call_events";
inline const std::vector<std::string> vapi_phone_call_events_columns = { "id", "call_id", "event_type", "payload_hash", "caller_hash", "called_number_hash", "ended_reason", "duration_seconds", "summary", "payload", "created_at" };
inline const char* vapi_phone_call_events_select_sql = R"SQL(select
      id::text as id,
      call_id,
      event_type,
      payload_hash,
      caller_hash,
      called_number_hash,
      ended_reason,
      duration_seconds,
      summary,
      payload::text as payload_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from vapi_phone_call_events)SQL";

struct VapiPhoneCallEventsRow {
    std::string id;
    std::string call_id;
    std::string event_type;
    std::string payload_hash;
    std::optional<std::string> caller_hash;
    std::optional<std::string> called_number_hash;
    std::optional<std::string> ended_reason;
    std::optional<int32_t> duration_seconds;
    std::optional<std::string> summary;
    std::string payload;
    std::string created_at;
};

inline VapiPhoneCallEventsRow vapi_phone_call_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    VapiPhoneCallEventsRow row;
    (void)is_null;
    row.id = get(0);
    row.call_id = get(1);
    row.event_type = get(2);
    row.payload_hash = get(3);
    row.caller_hash = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.called_number_hash = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.ended_reason = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.duration_seconds = is_null(7) ? std::nullopt : std::optional<int32_t>(std::stoi(get(7)));
    row.summary = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.payload = get(9);
    row.created_at = get(10);
    return row;
}
inline std::optional<std::string> validate_vapi_phone_call_events_call_id(const std::string& value) {
    if (value.size() > 160) return std::string("vapi_phone_call_events.call_id must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_vapi_phone_call_events_event_type(const std::string& value) {
    if (value.size() > 80) return std::string("vapi_phone_call_events.event_type must be at most 80 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,80}$)RX"))) return std::string("vapi_phone_call_events.event_type does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_vapi_phone_call_events_payload_hash(const std::string& value) {
    if (value.size() > 64) return std::string("vapi_phone_call_events.payload_hash must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("vapi_phone_call_events.payload_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_vapi_phone_call_events_caller_hash(const std::string& value) {
    if (value.size() > 64) return std::string("vapi_phone_call_events.caller_hash must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("vapi_phone_call_events.caller_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_vapi_phone_call_events_called_number_hash(const std::string& value) {
    if (value.size() > 64) return std::string("vapi_phone_call_events.called_number_hash must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("vapi_phone_call_events.called_number_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_vapi_phone_call_events_ended_reason(const std::string& value) {
    if (value.size() > 160) return std::string("vapi_phone_call_events.ended_reason must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_vapi_phone_call_events_duration_seconds(int32_t value) {
    if (value < 0) return std::string("vapi_phone_call_events.duration_seconds is below the minimum");
    if (value > 86400) return std::string("vapi_phone_call_events.duration_seconds is above the maximum");
    return std::nullopt;
}

inline const char* music_songs_table = "music_songs";
inline const std::vector<std::string> music_songs_columns = { "id", "title", "slug", "status", "seed", "generation_date", "storage_provider", "storage_bucket", "storage_key", "audio_url", "content_type", "duration_millis", "sample_rate", "bpm_millis", "genre", "peak_micros", "rms_micros", "spectral_centroid_millihz", "listenability_score_micros", "vote_score", "up_votes", "down_votes", "play_count", "summary", "meta_data", "published_at", "created_at", "updated_at" };
inline const char* music_songs_select_sql = R"SQL(select
      id::text as id,
      title,
      slug,
      status,
      seed,
      generation_date,
      storage_provider,
      storage_bucket,
      storage_key,
      audio_url,
      content_type,
      duration_millis,
      sample_rate,
      bpm_millis,
      genre,
      peak_micros,
      rms_micros,
      spectral_centroid_millihz,
      listenability_score_micros,
      vote_score,
      up_votes,
      down_votes,
      play_count,
      summary::text as summary_json,
      meta_data::text as meta_data_json,
      to_char(published_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as published_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from music_songs)SQL";

enum class MusicSongsStatus { Generated, Published, Discarded, Failed, Archived };
inline std::string music_songs_status_to_string(MusicSongsStatus value) {
    switch (value) {
        case MusicSongsStatus::Generated: return "generated";
        case MusicSongsStatus::Published: return "published";
        case MusicSongsStatus::Discarded: return "discarded";
        case MusicSongsStatus::Failed: return "failed";
        case MusicSongsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<MusicSongsStatus> parse_music_songs_status(const std::string& value) {
    if (value == "generated") return MusicSongsStatus::Generated;
    if (value == "published") return MusicSongsStatus::Published;
    if (value == "discarded") return MusicSongsStatus::Discarded;
    if (value == "failed") return MusicSongsStatus::Failed;
    if (value == "archived") return MusicSongsStatus::Archived;
    return std::nullopt;
}

enum class MusicSongsStorageProvider { S3, R2, Gcs, Drive, Local };
inline std::string music_songs_storage_provider_to_string(MusicSongsStorageProvider value) {
    switch (value) {
        case MusicSongsStorageProvider::S3: return "s3";
        case MusicSongsStorageProvider::R2: return "r2";
        case MusicSongsStorageProvider::Gcs: return "gcs";
        case MusicSongsStorageProvider::Drive: return "drive";
        case MusicSongsStorageProvider::Local: return "local";
    }
    return "";
}
inline std::optional<MusicSongsStorageProvider> parse_music_songs_storage_provider(const std::string& value) {
    if (value == "s3") return MusicSongsStorageProvider::S3;
    if (value == "r2") return MusicSongsStorageProvider::R2;
    if (value == "gcs") return MusicSongsStorageProvider::Gcs;
    if (value == "drive") return MusicSongsStorageProvider::Drive;
    if (value == "local") return MusicSongsStorageProvider::Local;
    return std::nullopt;
}

struct MusicSongsRow {
    std::string id;
    std::string title;
    std::string slug;
    std::string status;
    int64_t seed;
    std::string generation_date;
    std::optional<std::string> storage_provider;
    std::optional<std::string> storage_bucket;
    std::optional<std::string> storage_key;
    std::optional<std::string> audio_url;
    std::optional<std::string> content_type;
    int32_t duration_millis;
    int32_t sample_rate;
    int32_t bpm_millis;
    std::string genre;
    int32_t peak_micros;
    int32_t rms_micros;
    int64_t spectral_centroid_millihz;
    int32_t listenability_score_micros;
    int32_t vote_score;
    int32_t up_votes;
    int32_t down_votes;
    int32_t play_count;
    std::string summary;
    std::string meta_data;
    std::optional<std::string> published_at;
    std::string created_at;
    std::string updated_at;
};

inline MusicSongsRow music_songs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    MusicSongsRow row;
    (void)is_null;
    row.id = get(0);
    row.title = get(1);
    row.slug = get(2);
    row.status = get(3);
    row.seed = std::stoll(get(4));
    row.generation_date = get(5);
    row.storage_provider = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.storage_bucket = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.storage_key = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.audio_url = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.content_type = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.duration_millis = std::stoi(get(11));
    row.sample_rate = std::stoi(get(12));
    row.bpm_millis = std::stoi(get(13));
    row.genre = get(14);
    row.peak_micros = std::stoi(get(15));
    row.rms_micros = std::stoi(get(16));
    row.spectral_centroid_millihz = std::stoll(get(17));
    row.listenability_score_micros = std::stoi(get(18));
    row.vote_score = std::stoi(get(19));
    row.up_votes = std::stoi(get(20));
    row.down_votes = std::stoi(get(21));
    row.play_count = std::stoi(get(22));
    row.summary = get(23);
    row.meta_data = get(24);
    row.published_at = is_null(25) ? std::nullopt : std::optional<std::string>(get(25));
    row.created_at = get(26);
    row.updated_at = get(27);
    return row;
}
inline std::optional<std::string> validate_music_songs_title(const std::string& value) {
    if (value.size() > 200) return std::string("music_songs.title must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_slug(const std::string& value) {
    if (value.size() > 220) return std::string("music_songs.slug must be at most 220 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9-]{0,218}[a-z0-9]$)RX"))) return std::string("music_songs.slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_generation_date(const std::string& value) {
    if (value.size() > 10) return std::string("music_songs.generation_date must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("music_songs.generation_date does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_storage_bucket(const std::string& value) {
    if (value.size() > 200) return std::string("music_songs.storage_bucket must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_content_type(const std::string& value) {
    if (value.size() > 120) return std::string("music_songs.content_type must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_duration_millis(int32_t value) {
    if (value < 1) return std::string("music_songs.duration_millis is below the minimum");
    if (value > 1800000) return std::string("music_songs.duration_millis is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_sample_rate(int32_t value) {
    if (value < 8000) return std::string("music_songs.sample_rate is below the minimum");
    if (value > 192000) return std::string("music_songs.sample_rate is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_bpm_millis(int32_t value) {
    if (value < 1) return std::string("music_songs.bpm_millis is below the minimum");
    if (value > 300000) return std::string("music_songs.bpm_millis is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_genre(const std::string& value) {
    if (value.size() > 80) return std::string("music_songs.genre must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_peak_micros(int32_t value) {
    if (value < 0) return std::string("music_songs.peak_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_rms_micros(int32_t value) {
    if (value < 0) return std::string("music_songs.rms_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_spectral_centroid_millihz(int64_t value) {
    if (value < 0) return std::string("music_songs.spectral_centroid_millihz is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_listenability_score_micros(int32_t value) {
    if (value < 0) return std::string("music_songs.listenability_score_micros is below the minimum");
    if (value > 1000000) return std::string("music_songs.listenability_score_micros is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_up_votes(int32_t value) {
    if (value < 0) return std::string("music_songs.up_votes is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_down_votes(int32_t value) {
    if (value < 0) return std::string("music_songs.down_votes is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_songs_play_count(int32_t value) {
    if (value < 0) return std::string("music_songs.play_count is below the minimum");
    return std::nullopt;
}

inline const char* music_song_votes_table = "music_song_votes";
inline const std::vector<std::string> music_song_votes_columns = { "id", "song_id", "visitor_hash", "user_agent_hash", "vote_value", "created_at", "updated_at" };
inline const char* music_song_votes_select_sql = R"SQL(select
      id::text as id,
      song_id::text as song_id,
      visitor_hash,
      user_agent_hash,
      vote_value,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from music_song_votes)SQL";

struct MusicSongVotesRow {
    std::string id;
    std::string song_id;
    std::string visitor_hash;
    std::optional<std::string> user_agent_hash;
    int32_t vote_value;
    std::string created_at;
    std::string updated_at;
};

inline MusicSongVotesRow music_song_votes_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    MusicSongVotesRow row;
    (void)is_null;
    row.id = get(0);
    row.song_id = get(1);
    row.visitor_hash = get(2);
    row.user_agent_hash = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.vote_value = std::stoi(get(4));
    row.created_at = get(5);
    row.updated_at = get(6);
    return row;
}
inline std::optional<std::string> validate_music_song_votes_visitor_hash(const std::string& value) {
    if (value.size() > 64) return std::string("music_song_votes.visitor_hash must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("music_song_votes.visitor_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_song_votes_user_agent_hash(const std::string& value) {
    if (value.size() > 64) return std::string("music_song_votes.user_agent_hash must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("music_song_votes.user_agent_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_music_song_votes_vote_value(int32_t value) {
    if (value < -1) return std::string("music_song_votes.vote_value is below the minimum");
    if (value > 1) return std::string("music_song_votes.vote_value is above the maximum");
    return std::nullopt;
}

inline const char* sound_recorder_accounts_table = "sound_recorder_accounts";
inline const std::vector<std::string> sound_recorder_accounts_columns = { "id", "status", "external_subject", "display_name", "legal_region", "retention_hours", "retention_policy_version", "created_at", "updated_at" };
inline const char* sound_recorder_accounts_select_sql = R"SQL(select
      id::text as id,
      status,
      external_subject,
      display_name,
      legal_region,
      retention_hours,
      retention_policy_version,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from sound_recorder_accounts)SQL";

enum class SoundRecorderAccountsStatus { Active, Paused, Locked, Deleted };
inline std::string sound_recorder_accounts_status_to_string(SoundRecorderAccountsStatus value) {
    switch (value) {
        case SoundRecorderAccountsStatus::Active: return "active";
        case SoundRecorderAccountsStatus::Paused: return "paused";
        case SoundRecorderAccountsStatus::Locked: return "locked";
        case SoundRecorderAccountsStatus::Deleted: return "deleted";
    }
    return "";
}
inline std::optional<SoundRecorderAccountsStatus> parse_sound_recorder_accounts_status(const std::string& value) {
    if (value == "active") return SoundRecorderAccountsStatus::Active;
    if (value == "paused") return SoundRecorderAccountsStatus::Paused;
    if (value == "locked") return SoundRecorderAccountsStatus::Locked;
    if (value == "deleted") return SoundRecorderAccountsStatus::Deleted;
    return std::nullopt;
}

struct SoundRecorderAccountsRow {
    std::string id;
    std::string status;
    std::optional<std::string> external_subject;
    std::optional<std::string> display_name;
    std::optional<std::string> legal_region;
    int32_t retention_hours;
    std::string retention_policy_version;
    std::string created_at;
    std::string updated_at;
};

inline SoundRecorderAccountsRow sound_recorder_accounts_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SoundRecorderAccountsRow row;
    (void)is_null;
    row.id = get(0);
    row.status = get(1);
    row.external_subject = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.display_name = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.legal_region = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.retention_hours = std::stoi(get(5));
    row.retention_policy_version = get(6);
    row.created_at = get(7);
    row.updated_at = get(8);
    return row;
}
inline std::optional<std::string> validate_sound_recorder_accounts_external_subject(const std::string& value) {
    if (value.size() > 240) return std::string("sound_recorder_accounts.external_subject must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_accounts_display_name(const std::string& value) {
    if (value.size() > 160) return std::string("sound_recorder_accounts.display_name must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_accounts_legal_region(const std::string& value) {
    if (value.size() > 64) return std::string("sound_recorder_accounts.legal_region must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,64}$)RX"))) return std::string("sound_recorder_accounts.legal_region does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_accounts_retention_hours(int32_t value) {
    if (value < 1) return std::string("sound_recorder_accounts.retention_hours is below the minimum");
    if (value > 500) return std::string("sound_recorder_accounts.retention_hours is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_accounts_retention_policy_version(const std::string& value) {
    if (value.size() > 80) return std::string("sound_recorder_accounts.retention_policy_version must be at most 80 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,80}$)RX"))) return std::string("sound_recorder_accounts.retention_policy_version does not match the required pattern");
    return std::nullopt;
}

inline const char* sound_recorder_devices_table = "sound_recorder_devices";
inline const std::vector<std::string> sound_recorder_devices_columns = { "id", "account_id", "platform", "status", "install_id", "device_label", "app_version", "os_version", "token_hash", "token_last4", "consent_version", "consent_accepted_at", "recording_indicator_acknowledged", "last_seen_at", "transfer_paused", "transfer_pause_reason", "network_policy", "battery_level", "charging", "transfer_state_updated_at", "created_at", "updated_at" };
inline const char* sound_recorder_devices_select_sql = R"SQL(select
      id::text as id,
      account_id::text as account_id,
      platform,
      status,
      install_id,
      device_label,
      app_version,
      os_version,
      token_hash,
      token_last4,
      consent_version,
      to_char(consent_accepted_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as consent_accepted_at,
      recording_indicator_acknowledged,
      to_char(last_seen_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_seen_at,
      transfer_paused,
      transfer_pause_reason,
      network_policy,
      battery_level,
      charging,
      to_char(transfer_state_updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as transfer_state_updated_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from sound_recorder_devices)SQL";

enum class SoundRecorderDevicesPlatform { Ios, Android, Macos, Windows, Linux };
inline std::string sound_recorder_devices_platform_to_string(SoundRecorderDevicesPlatform value) {
    switch (value) {
        case SoundRecorderDevicesPlatform::Ios: return "ios";
        case SoundRecorderDevicesPlatform::Android: return "android";
        case SoundRecorderDevicesPlatform::Macos: return "macos";
        case SoundRecorderDevicesPlatform::Windows: return "windows";
        case SoundRecorderDevicesPlatform::Linux: return "linux";
    }
    return "";
}
inline std::optional<SoundRecorderDevicesPlatform> parse_sound_recorder_devices_platform(const std::string& value) {
    if (value == "ios") return SoundRecorderDevicesPlatform::Ios;
    if (value == "android") return SoundRecorderDevicesPlatform::Android;
    if (value == "macos") return SoundRecorderDevicesPlatform::Macos;
    if (value == "windows") return SoundRecorderDevicesPlatform::Windows;
    if (value == "linux") return SoundRecorderDevicesPlatform::Linux;
    return std::nullopt;
}

enum class SoundRecorderDevicesStatus { Active, Revoked, Lost, Replaced, Deleted };
inline std::string sound_recorder_devices_status_to_string(SoundRecorderDevicesStatus value) {
    switch (value) {
        case SoundRecorderDevicesStatus::Active: return "active";
        case SoundRecorderDevicesStatus::Revoked: return "revoked";
        case SoundRecorderDevicesStatus::Lost: return "lost";
        case SoundRecorderDevicesStatus::Replaced: return "replaced";
        case SoundRecorderDevicesStatus::Deleted: return "deleted";
    }
    return "";
}
inline std::optional<SoundRecorderDevicesStatus> parse_sound_recorder_devices_status(const std::string& value) {
    if (value == "active") return SoundRecorderDevicesStatus::Active;
    if (value == "revoked") return SoundRecorderDevicesStatus::Revoked;
    if (value == "lost") return SoundRecorderDevicesStatus::Lost;
    if (value == "replaced") return SoundRecorderDevicesStatus::Replaced;
    if (value == "deleted") return SoundRecorderDevicesStatus::Deleted;
    return std::nullopt;
}

enum class SoundRecorderDevicesTransferPauseReason { LowBattery, NetworkConstraint, Offline, Manual };
inline std::string sound_recorder_devices_transfer_pause_reason_to_string(SoundRecorderDevicesTransferPauseReason value) {
    switch (value) {
        case SoundRecorderDevicesTransferPauseReason::LowBattery: return "low_battery";
        case SoundRecorderDevicesTransferPauseReason::NetworkConstraint: return "network_constraint";
        case SoundRecorderDevicesTransferPauseReason::Offline: return "offline";
        case SoundRecorderDevicesTransferPauseReason::Manual: return "manual";
    }
    return "";
}
inline std::optional<SoundRecorderDevicesTransferPauseReason> parse_sound_recorder_devices_transfer_pause_reason(const std::string& value) {
    if (value == "low_battery") return SoundRecorderDevicesTransferPauseReason::LowBattery;
    if (value == "network_constraint") return SoundRecorderDevicesTransferPauseReason::NetworkConstraint;
    if (value == "offline") return SoundRecorderDevicesTransferPauseReason::Offline;
    if (value == "manual") return SoundRecorderDevicesTransferPauseReason::Manual;
    return std::nullopt;
}

enum class SoundRecorderDevicesNetworkPolicy { Any, WifiOnly, CellularOnly };
inline std::string sound_recorder_devices_network_policy_to_string(SoundRecorderDevicesNetworkPolicy value) {
    switch (value) {
        case SoundRecorderDevicesNetworkPolicy::Any: return "any";
        case SoundRecorderDevicesNetworkPolicy::WifiOnly: return "wifi_only";
        case SoundRecorderDevicesNetworkPolicy::CellularOnly: return "cellular_only";
    }
    return "";
}
inline std::optional<SoundRecorderDevicesNetworkPolicy> parse_sound_recorder_devices_network_policy(const std::string& value) {
    if (value == "any") return SoundRecorderDevicesNetworkPolicy::Any;
    if (value == "wifi_only") return SoundRecorderDevicesNetworkPolicy::WifiOnly;
    if (value == "cellular_only") return SoundRecorderDevicesNetworkPolicy::CellularOnly;
    return std::nullopt;
}

struct SoundRecorderDevicesRow {
    std::string id;
    std::string account_id;
    std::string platform;
    std::string status;
    std::string install_id;
    std::optional<std::string> device_label;
    std::optional<std::string> app_version;
    std::optional<std::string> os_version;
    std::string token_hash;
    std::string token_last4;
    std::string consent_version;
    std::string consent_accepted_at;
    bool recording_indicator_acknowledged;
    std::optional<std::string> last_seen_at;
    bool transfer_paused;
    std::optional<std::string> transfer_pause_reason;
    std::string network_policy;
    std::optional<int32_t> battery_level;
    std::optional<bool> charging;
    std::optional<std::string> transfer_state_updated_at;
    std::string created_at;
    std::string updated_at;
};

inline SoundRecorderDevicesRow sound_recorder_devices_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SoundRecorderDevicesRow row;
    (void)is_null;
    row.id = get(0);
    row.account_id = get(1);
    row.platform = get(2);
    row.status = get(3);
    row.install_id = get(4);
    row.device_label = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.app_version = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.os_version = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.token_hash = get(8);
    row.token_last4 = get(9);
    row.consent_version = get(10);
    row.consent_accepted_at = get(11);
    row.recording_indicator_acknowledged = (get(12) == "t");
    row.last_seen_at = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    row.transfer_paused = (get(14) == "t");
    row.transfer_pause_reason = is_null(15) ? std::nullopt : std::optional<std::string>(get(15));
    row.network_policy = get(16);
    row.battery_level = is_null(17) ? std::nullopt : std::optional<int32_t>(std::stoi(get(17)));
    row.charging = is_null(18) ? std::nullopt : std::optional<bool>((get(18) == "t"));
    row.transfer_state_updated_at = is_null(19) ? std::nullopt : std::optional<std::string>(get(19));
    row.created_at = get(20);
    row.updated_at = get(21);
    return row;
}
inline std::optional<std::string> validate_sound_recorder_devices_install_id(const std::string& value) {
    if (value.size() > 160) return std::string("sound_recorder_devices.install_id must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_devices_device_label(const std::string& value) {
    if (value.size() > 160) return std::string("sound_recorder_devices.device_label must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_devices_app_version(const std::string& value) {
    if (value.size() > 80) return std::string("sound_recorder_devices.app_version must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_devices_os_version(const std::string& value) {
    if (value.size() > 80) return std::string("sound_recorder_devices.os_version must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_devices_token_hash(const std::string& value) {
    if (value.size() > 64) return std::string("sound_recorder_devices.token_hash must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("sound_recorder_devices.token_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_devices_token_last4(const std::string& value) {
    if (value.size() > 4) return std::string("sound_recorder_devices.token_last4 must be at most 4 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9_-]{4}$)RX"))) return std::string("sound_recorder_devices.token_last4 does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_devices_consent_version(const std::string& value) {
    if (value.size() > 80) return std::string("sound_recorder_devices.consent_version must be at most 80 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,80}$)RX"))) return std::string("sound_recorder_devices.consent_version does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_devices_battery_level(int32_t value) {
    if (value < 0) return std::string("sound_recorder_devices.battery_level is below the minimum");
    if (value > 100) return std::string("sound_recorder_devices.battery_level is above the maximum");
    return std::nullopt;
}

inline const char* sound_recorder_upload_sessions_table = "sound_recorder_upload_sessions";
inline const std::vector<std::string> sound_recorder_upload_sessions_columns = { "id", "account_id", "device_id", "status", "storage_provider", "storage_bucket", "storage_prefix", "content_type", "codec", "sample_rate", "channel_count", "segment_duration_seconds", "max_segment_bytes", "started_at", "last_heartbeat_at", "closed_at", "expires_at", "client_timezone", "legal_region", "use_case", "meta_data", "created_at", "updated_at" };
inline const char* sound_recorder_upload_sessions_select_sql = R"SQL(select
      id::text as id,
      account_id::text as account_id,
      device_id::text as device_id,
      status,
      storage_provider,
      storage_bucket,
      storage_prefix,
      content_type,
      codec,
      sample_rate,
      channel_count,
      segment_duration_seconds,
      max_segment_bytes,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(last_heartbeat_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_heartbeat_at,
      to_char(closed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as closed_at,
      to_char(expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as expires_at,
      client_timezone,
      legal_region,
      use_case,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from sound_recorder_upload_sessions)SQL";

enum class SoundRecorderUploadSessionsStatus { Active, Closed, Revoked, Expired };
inline std::string sound_recorder_upload_sessions_status_to_string(SoundRecorderUploadSessionsStatus value) {
    switch (value) {
        case SoundRecorderUploadSessionsStatus::Active: return "active";
        case SoundRecorderUploadSessionsStatus::Closed: return "closed";
        case SoundRecorderUploadSessionsStatus::Revoked: return "revoked";
        case SoundRecorderUploadSessionsStatus::Expired: return "expired";
    }
    return "";
}
inline std::optional<SoundRecorderUploadSessionsStatus> parse_sound_recorder_upload_sessions_status(const std::string& value) {
    if (value == "active") return SoundRecorderUploadSessionsStatus::Active;
    if (value == "closed") return SoundRecorderUploadSessionsStatus::Closed;
    if (value == "revoked") return SoundRecorderUploadSessionsStatus::Revoked;
    if (value == "expired") return SoundRecorderUploadSessionsStatus::Expired;
    return std::nullopt;
}

enum class SoundRecorderUploadSessionsStorageProvider { S3 };
inline std::string sound_recorder_upload_sessions_storage_provider_to_string(SoundRecorderUploadSessionsStorageProvider value) {
    switch (value) {
        case SoundRecorderUploadSessionsStorageProvider::S3: return "s3";
    }
    return "";
}
inline std::optional<SoundRecorderUploadSessionsStorageProvider> parse_sound_recorder_upload_sessions_storage_provider(const std::string& value) {
    if (value == "s3") return SoundRecorderUploadSessionsStorageProvider::S3;
    return std::nullopt;
}

enum class SoundRecorderUploadSessionsUseCase { Security, Music, Meeting, VoiceNote, Ambient };
inline std::string sound_recorder_upload_sessions_use_case_to_string(SoundRecorderUploadSessionsUseCase value) {
    switch (value) {
        case SoundRecorderUploadSessionsUseCase::Security: return "security";
        case SoundRecorderUploadSessionsUseCase::Music: return "music";
        case SoundRecorderUploadSessionsUseCase::Meeting: return "meeting";
        case SoundRecorderUploadSessionsUseCase::VoiceNote: return "voice_note";
        case SoundRecorderUploadSessionsUseCase::Ambient: return "ambient";
    }
    return "";
}
inline std::optional<SoundRecorderUploadSessionsUseCase> parse_sound_recorder_upload_sessions_use_case(const std::string& value) {
    if (value == "security") return SoundRecorderUploadSessionsUseCase::Security;
    if (value == "music") return SoundRecorderUploadSessionsUseCase::Music;
    if (value == "meeting") return SoundRecorderUploadSessionsUseCase::Meeting;
    if (value == "voice_note") return SoundRecorderUploadSessionsUseCase::VoiceNote;
    if (value == "ambient") return SoundRecorderUploadSessionsUseCase::Ambient;
    return std::nullopt;
}

struct SoundRecorderUploadSessionsRow {
    std::string id;
    std::string account_id;
    std::string device_id;
    std::string status;
    std::string storage_provider;
    std::string storage_bucket;
    std::string storage_prefix;
    std::string content_type;
    std::optional<std::string> codec;
    std::optional<int32_t> sample_rate;
    int32_t channel_count;
    int32_t segment_duration_seconds;
    int32_t max_segment_bytes;
    std::string started_at;
    std::optional<std::string> last_heartbeat_at;
    std::optional<std::string> closed_at;
    std::optional<std::string> expires_at;
    std::optional<std::string> client_timezone;
    std::optional<std::string> legal_region;
    std::string use_case;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline SoundRecorderUploadSessionsRow sound_recorder_upload_sessions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SoundRecorderUploadSessionsRow row;
    (void)is_null;
    row.id = get(0);
    row.account_id = get(1);
    row.device_id = get(2);
    row.status = get(3);
    row.storage_provider = get(4);
    row.storage_bucket = get(5);
    row.storage_prefix = get(6);
    row.content_type = get(7);
    row.codec = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.sample_rate = is_null(9) ? std::nullopt : std::optional<int32_t>(std::stoi(get(9)));
    row.channel_count = std::stoi(get(10));
    row.segment_duration_seconds = std::stoi(get(11));
    row.max_segment_bytes = std::stoi(get(12));
    row.started_at = get(13);
    row.last_heartbeat_at = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.closed_at = is_null(15) ? std::nullopt : std::optional<std::string>(get(15));
    row.expires_at = is_null(16) ? std::nullopt : std::optional<std::string>(get(16));
    row.client_timezone = is_null(17) ? std::nullopt : std::optional<std::string>(get(17));
    row.legal_region = is_null(18) ? std::nullopt : std::optional<std::string>(get(18));
    row.use_case = get(19);
    row.meta_data = get(20);
    row.created_at = get(21);
    row.updated_at = get(22);
    return row;
}
inline std::optional<std::string> validate_sound_recorder_upload_sessions_storage_bucket(const std::string& value) {
    if (value.size() > 200) return std::string("sound_recorder_upload_sessions.storage_bucket must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_upload_sessions_content_type(const std::string& value) {
    if (value.size() > 120) return std::string("sound_recorder_upload_sessions.content_type must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_upload_sessions_codec(const std::string& value) {
    if (value.size() > 80) return std::string("sound_recorder_upload_sessions.codec must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_upload_sessions_sample_rate(int32_t value) {
    if (value < 8000) return std::string("sound_recorder_upload_sessions.sample_rate is below the minimum");
    if (value > 192000) return std::string("sound_recorder_upload_sessions.sample_rate is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_upload_sessions_channel_count(int32_t value) {
    if (value < 1) return std::string("sound_recorder_upload_sessions.channel_count is below the minimum");
    if (value > 8) return std::string("sound_recorder_upload_sessions.channel_count is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_upload_sessions_segment_duration_seconds(int32_t value) {
    if (value < 1) return std::string("sound_recorder_upload_sessions.segment_duration_seconds is below the minimum");
    if (value > 600) return std::string("sound_recorder_upload_sessions.segment_duration_seconds is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_upload_sessions_max_segment_bytes(int32_t value) {
    if (value < 1) return std::string("sound_recorder_upload_sessions.max_segment_bytes is below the minimum");
    if (value > 209715200) return std::string("sound_recorder_upload_sessions.max_segment_bytes is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_upload_sessions_client_timezone(const std::string& value) {
    if (value.size() > 80) return std::string("sound_recorder_upload_sessions.client_timezone must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_upload_sessions_legal_region(const std::string& value) {
    if (value.size() > 64) return std::string("sound_recorder_upload_sessions.legal_region must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,64}$)RX"))) return std::string("sound_recorder_upload_sessions.legal_region does not match the required pattern");
    return std::nullopt;
}

inline const char* sound_recorder_segments_table = "sound_recorder_segments";
inline const std::vector<std::string> sound_recorder_segments_columns = { "id", "account_id", "device_id", "session_id", "sequence_number", "status", "storage_provider", "storage_bucket", "storage_key", "content_type", "codec", "captured_started_at", "captured_ended_at", "duration_millis", "byte_count", "sha256_hex", "upload_url_expires_at", "etag", "uploaded_at", "expires_at", "pinned_at", "meta_data", "created_at", "updated_at" };
inline const char* sound_recorder_segments_select_sql = R"SQL(select
      id::text as id,
      account_id::text as account_id,
      device_id::text as device_id,
      session_id::text as session_id,
      sequence_number,
      status,
      storage_provider,
      storage_bucket,
      storage_key,
      content_type,
      codec,
      to_char(captured_started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as captured_started_at,
      to_char(captured_ended_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as captured_ended_at,
      duration_millis,
      byte_count,
      sha256_hex,
      to_char(upload_url_expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as upload_url_expires_at,
      etag,
      to_char(uploaded_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as uploaded_at,
      to_char(expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as expires_at,
      to_char(pinned_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as pinned_at,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from sound_recorder_segments)SQL";

enum class SoundRecorderSegmentsStatus { Pending, Uploaded, Failed, Expired, Deleted };
inline std::string sound_recorder_segments_status_to_string(SoundRecorderSegmentsStatus value) {
    switch (value) {
        case SoundRecorderSegmentsStatus::Pending: return "pending";
        case SoundRecorderSegmentsStatus::Uploaded: return "uploaded";
        case SoundRecorderSegmentsStatus::Failed: return "failed";
        case SoundRecorderSegmentsStatus::Expired: return "expired";
        case SoundRecorderSegmentsStatus::Deleted: return "deleted";
    }
    return "";
}
inline std::optional<SoundRecorderSegmentsStatus> parse_sound_recorder_segments_status(const std::string& value) {
    if (value == "pending") return SoundRecorderSegmentsStatus::Pending;
    if (value == "uploaded") return SoundRecorderSegmentsStatus::Uploaded;
    if (value == "failed") return SoundRecorderSegmentsStatus::Failed;
    if (value == "expired") return SoundRecorderSegmentsStatus::Expired;
    if (value == "deleted") return SoundRecorderSegmentsStatus::Deleted;
    return std::nullopt;
}

enum class SoundRecorderSegmentsStorageProvider { S3 };
inline std::string sound_recorder_segments_storage_provider_to_string(SoundRecorderSegmentsStorageProvider value) {
    switch (value) {
        case SoundRecorderSegmentsStorageProvider::S3: return "s3";
    }
    return "";
}
inline std::optional<SoundRecorderSegmentsStorageProvider> parse_sound_recorder_segments_storage_provider(const std::string& value) {
    if (value == "s3") return SoundRecorderSegmentsStorageProvider::S3;
    return std::nullopt;
}

struct SoundRecorderSegmentsRow {
    std::string id;
    std::string account_id;
    std::string device_id;
    std::string session_id;
    int32_t sequence_number;
    std::string status;
    std::string storage_provider;
    std::string storage_bucket;
    std::string storage_key;
    std::string content_type;
    std::optional<std::string> codec;
    std::string captured_started_at;
    std::optional<std::string> captured_ended_at;
    int32_t duration_millis;
    std::optional<int32_t> byte_count;
    std::optional<std::string> sha256_hex;
    std::optional<std::string> upload_url_expires_at;
    std::optional<std::string> etag;
    std::optional<std::string> uploaded_at;
    std::string expires_at;
    std::optional<std::string> pinned_at;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline SoundRecorderSegmentsRow sound_recorder_segments_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SoundRecorderSegmentsRow row;
    (void)is_null;
    row.id = get(0);
    row.account_id = get(1);
    row.device_id = get(2);
    row.session_id = get(3);
    row.sequence_number = std::stoi(get(4));
    row.status = get(5);
    row.storage_provider = get(6);
    row.storage_bucket = get(7);
    row.storage_key = get(8);
    row.content_type = get(9);
    row.codec = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.captured_started_at = get(11);
    row.captured_ended_at = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.duration_millis = std::stoi(get(13));
    row.byte_count = is_null(14) ? std::nullopt : std::optional<int32_t>(std::stoi(get(14)));
    row.sha256_hex = is_null(15) ? std::nullopt : std::optional<std::string>(get(15));
    row.upload_url_expires_at = is_null(16) ? std::nullopt : std::optional<std::string>(get(16));
    row.etag = is_null(17) ? std::nullopt : std::optional<std::string>(get(17));
    row.uploaded_at = is_null(18) ? std::nullopt : std::optional<std::string>(get(18));
    row.expires_at = get(19);
    row.pinned_at = is_null(20) ? std::nullopt : std::optional<std::string>(get(20));
    row.meta_data = get(21);
    row.created_at = get(22);
    row.updated_at = get(23);
    return row;
}
inline std::optional<std::string> validate_sound_recorder_segments_sequence_number(int32_t value) {
    if (value < 0) return std::string("sound_recorder_segments.sequence_number is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_segments_storage_bucket(const std::string& value) {
    if (value.size() > 200) return std::string("sound_recorder_segments.storage_bucket must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_segments_content_type(const std::string& value) {
    if (value.size() > 120) return std::string("sound_recorder_segments.content_type must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_segments_codec(const std::string& value) {
    if (value.size() > 80) return std::string("sound_recorder_segments.codec must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_segments_duration_millis(int32_t value) {
    if (value < 1) return std::string("sound_recorder_segments.duration_millis is below the minimum");
    if (value > 600000) return std::string("sound_recorder_segments.duration_millis is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_segments_byte_count(int32_t value) {
    if (value < 0) return std::string("sound_recorder_segments.byte_count is below the minimum");
    if (value > 209715200) return std::string("sound_recorder_segments.byte_count is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_segments_sha256_hex(const std::string& value) {
    if (value.size() > 64) return std::string("sound_recorder_segments.sha256_hex must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("sound_recorder_segments.sha256_hex does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_segments_etag(const std::string& value) {
    if (value.size() > 160) return std::string("sound_recorder_segments.etag must be at most 160 characters");
    return std::nullopt;
}

inline const char* sound_recorder_evidence_exports_table = "sound_recorder_evidence_exports";
inline const std::vector<std::string> sound_recorder_evidence_exports_columns = { "id", "account_id", "device_id", "created_by_device_id", "status", "requested_from", "requested_to", "segment_count", "manifest", "download_url_expires_at", "requested_at", "ready_at", "expires_at", "meta_data" };
inline const char* sound_recorder_evidence_exports_select_sql = R"SQL(select
      id::text as id,
      account_id::text as account_id,
      device_id::text as device_id,
      created_by_device_id::text as created_by_device_id,
      status,
      to_char(requested_from at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as requested_from,
      to_char(requested_to at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as requested_to,
      segment_count,
      manifest::text as manifest_json,
      to_char(download_url_expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as download_url_expires_at,
      to_char(requested_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as requested_at,
      to_char(ready_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as ready_at,
      to_char(expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as expires_at,
      meta_data::text as meta_data_json
    from sound_recorder_evidence_exports)SQL";

enum class SoundRecorderEvidenceExportsStatus { Requested, Ready, Expired, Revoked };
inline std::string sound_recorder_evidence_exports_status_to_string(SoundRecorderEvidenceExportsStatus value) {
    switch (value) {
        case SoundRecorderEvidenceExportsStatus::Requested: return "requested";
        case SoundRecorderEvidenceExportsStatus::Ready: return "ready";
        case SoundRecorderEvidenceExportsStatus::Expired: return "expired";
        case SoundRecorderEvidenceExportsStatus::Revoked: return "revoked";
    }
    return "";
}
inline std::optional<SoundRecorderEvidenceExportsStatus> parse_sound_recorder_evidence_exports_status(const std::string& value) {
    if (value == "requested") return SoundRecorderEvidenceExportsStatus::Requested;
    if (value == "ready") return SoundRecorderEvidenceExportsStatus::Ready;
    if (value == "expired") return SoundRecorderEvidenceExportsStatus::Expired;
    if (value == "revoked") return SoundRecorderEvidenceExportsStatus::Revoked;
    return std::nullopt;
}

struct SoundRecorderEvidenceExportsRow {
    std::string id;
    std::string account_id;
    std::optional<std::string> device_id;
    std::optional<std::string> created_by_device_id;
    std::string status;
    std::string requested_from;
    std::string requested_to;
    int32_t segment_count;
    std::string manifest;
    std::optional<std::string> download_url_expires_at;
    std::string requested_at;
    std::optional<std::string> ready_at;
    std::optional<std::string> expires_at;
    std::string meta_data;
};

inline SoundRecorderEvidenceExportsRow sound_recorder_evidence_exports_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SoundRecorderEvidenceExportsRow row;
    (void)is_null;
    row.id = get(0);
    row.account_id = get(1);
    row.device_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.created_by_device_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.status = get(4);
    row.requested_from = get(5);
    row.requested_to = get(6);
    row.segment_count = std::stoi(get(7));
    row.manifest = get(8);
    row.download_url_expires_at = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.requested_at = get(10);
    row.ready_at = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.expires_at = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.meta_data = get(13);
    return row;
}
inline std::optional<std::string> validate_sound_recorder_evidence_exports_segment_count(int32_t value) {
    if (value < 0) return std::string("sound_recorder_evidence_exports.segment_count is below the minimum");
    return std::nullopt;
}

inline const char* sound_recorder_audit_events_table = "sound_recorder_audit_events";
inline const std::vector<std::string> sound_recorder_audit_events_columns = { "id", "account_id", "device_id", "event_type", "event_hash", "payload", "created_at" };
inline const char* sound_recorder_audit_events_select_sql = R"SQL(select
      id::text as id,
      account_id::text as account_id,
      device_id::text as device_id,
      event_type,
      event_hash,
      payload::text as payload_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from sound_recorder_audit_events)SQL";

struct SoundRecorderAuditEventsRow {
    std::string id;
    std::optional<std::string> account_id;
    std::optional<std::string> device_id;
    std::string event_type;
    std::string event_hash;
    std::string payload;
    std::string created_at;
};

inline SoundRecorderAuditEventsRow sound_recorder_audit_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SoundRecorderAuditEventsRow row;
    (void)is_null;
    row.id = get(0);
    row.account_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.device_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.event_type = get(3);
    row.event_hash = get(4);
    row.payload = get(5);
    row.created_at = get(6);
    return row;
}
inline std::optional<std::string> validate_sound_recorder_audit_events_event_type(const std::string& value) {
    if (value.size() > 80) return std::string("sound_recorder_audit_events.event_type must be at most 80 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,80}$)RX"))) return std::string("sound_recorder_audit_events.event_type does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_audit_events_event_hash(const std::string& value) {
    if (value.size() > 64) return std::string("sound_recorder_audit_events.event_hash must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("sound_recorder_audit_events.event_hash does not match the required pattern");
    return std::nullopt;
}

inline const char* sound_recorder_oauth_states_table = "sound_recorder_oauth_states";
inline const std::vector<std::string> sound_recorder_oauth_states_columns = { "id", "account_id", "device_id", "provider", "state_hash", "redirect_uri", "folder_path", "status", "expires_at", "consumed_at", "meta_data", "created_at", "updated_at" };
inline const char* sound_recorder_oauth_states_select_sql = R"SQL(select
      id::text as id,
      account_id::text as account_id,
      device_id::text as device_id,
      provider,
      state_hash,
      redirect_uri,
      folder_path,
      status,
      to_char(expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as expires_at,
      to_char(consumed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as consumed_at,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from sound_recorder_oauth_states)SQL";

enum class SoundRecorderOauthStatesProvider { GoogleDrive, MicrosoftOnedrive, AppleIcloud, Dropbox };
inline std::string sound_recorder_oauth_states_provider_to_string(SoundRecorderOauthStatesProvider value) {
    switch (value) {
        case SoundRecorderOauthStatesProvider::GoogleDrive: return "google_drive";
        case SoundRecorderOauthStatesProvider::MicrosoftOnedrive: return "microsoft_onedrive";
        case SoundRecorderOauthStatesProvider::AppleIcloud: return "apple_icloud";
        case SoundRecorderOauthStatesProvider::Dropbox: return "dropbox";
    }
    return "";
}
inline std::optional<SoundRecorderOauthStatesProvider> parse_sound_recorder_oauth_states_provider(const std::string& value) {
    if (value == "google_drive") return SoundRecorderOauthStatesProvider::GoogleDrive;
    if (value == "microsoft_onedrive") return SoundRecorderOauthStatesProvider::MicrosoftOnedrive;
    if (value == "apple_icloud") return SoundRecorderOauthStatesProvider::AppleIcloud;
    if (value == "dropbox") return SoundRecorderOauthStatesProvider::Dropbox;
    return std::nullopt;
}

enum class SoundRecorderOauthStatesStatus { Pending, Consumed, Expired, Revoked };
inline std::string sound_recorder_oauth_states_status_to_string(SoundRecorderOauthStatesStatus value) {
    switch (value) {
        case SoundRecorderOauthStatesStatus::Pending: return "pending";
        case SoundRecorderOauthStatesStatus::Consumed: return "consumed";
        case SoundRecorderOauthStatesStatus::Expired: return "expired";
        case SoundRecorderOauthStatesStatus::Revoked: return "revoked";
    }
    return "";
}
inline std::optional<SoundRecorderOauthStatesStatus> parse_sound_recorder_oauth_states_status(const std::string& value) {
    if (value == "pending") return SoundRecorderOauthStatesStatus::Pending;
    if (value == "consumed") return SoundRecorderOauthStatesStatus::Consumed;
    if (value == "expired") return SoundRecorderOauthStatesStatus::Expired;
    if (value == "revoked") return SoundRecorderOauthStatesStatus::Revoked;
    return std::nullopt;
}

struct SoundRecorderOauthStatesRow {
    std::string id;
    std::string account_id;
    std::string device_id;
    std::string provider;
    std::string state_hash;
    std::string redirect_uri;
    std::optional<std::string> folder_path;
    std::string status;
    std::string expires_at;
    std::optional<std::string> consumed_at;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline SoundRecorderOauthStatesRow sound_recorder_oauth_states_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SoundRecorderOauthStatesRow row;
    (void)is_null;
    row.id = get(0);
    row.account_id = get(1);
    row.device_id = get(2);
    row.provider = get(3);
    row.state_hash = get(4);
    row.redirect_uri = get(5);
    row.folder_path = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.status = get(7);
    row.expires_at = get(8);
    row.consumed_at = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.meta_data = get(10);
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}
inline std::optional<std::string> validate_sound_recorder_oauth_states_state_hash(const std::string& value) {
    if (value.size() > 64) return std::string("sound_recorder_oauth_states.state_hash must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("sound_recorder_oauth_states.state_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_oauth_states_redirect_uri(const std::string& value) {
    if (value.size() > 512) return std::string("sound_recorder_oauth_states.redirect_uri must be at most 512 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_oauth_states_folder_path(const std::string& value) {
    if (value.size() > 512) return std::string("sound_recorder_oauth_states.folder_path must be at most 512 characters");
    return std::nullopt;
}

inline const char* sound_recorder_cloud_connections_table = "sound_recorder_cloud_connections";
inline const std::vector<std::string> sound_recorder_cloud_connections_columns = { "id", "account_id", "created_by_device_id", "provider", "link_mode", "status", "display_name", "provider_account_id", "provider_subject_hash", "root_folder_id", "folder_path", "oauth_scope", "token_ciphertext", "token_nonce", "token_aad", "token_version", "token_expires_at", "last_sync_at", "meta_data", "created_at", "updated_at" };
inline const char* sound_recorder_cloud_connections_select_sql = R"SQL(select
      id::text as id,
      account_id::text as account_id,
      created_by_device_id::text as created_by_device_id,
      provider,
      link_mode,
      status,
      display_name,
      provider_account_id,
      provider_subject_hash,
      root_folder_id,
      folder_path,
      oauth_scope,
      token_ciphertext,
      token_nonce,
      token_aad,
      token_version,
      to_char(token_expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as token_expires_at,
      to_char(last_sync_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_sync_at,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from sound_recorder_cloud_connections)SQL";

enum class SoundRecorderCloudConnectionsProvider { GoogleDrive, MicrosoftOnedrive, AppleIcloud, Dropbox, AmazonS3, CloudflareR2 };
inline std::string sound_recorder_cloud_connections_provider_to_string(SoundRecorderCloudConnectionsProvider value) {
    switch (value) {
        case SoundRecorderCloudConnectionsProvider::GoogleDrive: return "google_drive";
        case SoundRecorderCloudConnectionsProvider::MicrosoftOnedrive: return "microsoft_onedrive";
        case SoundRecorderCloudConnectionsProvider::AppleIcloud: return "apple_icloud";
        case SoundRecorderCloudConnectionsProvider::Dropbox: return "dropbox";
        case SoundRecorderCloudConnectionsProvider::AmazonS3: return "amazon_s3";
        case SoundRecorderCloudConnectionsProvider::CloudflareR2: return "cloudflare_r2";
    }
    return "";
}
inline std::optional<SoundRecorderCloudConnectionsProvider> parse_sound_recorder_cloud_connections_provider(const std::string& value) {
    if (value == "google_drive") return SoundRecorderCloudConnectionsProvider::GoogleDrive;
    if (value == "microsoft_onedrive") return SoundRecorderCloudConnectionsProvider::MicrosoftOnedrive;
    if (value == "apple_icloud") return SoundRecorderCloudConnectionsProvider::AppleIcloud;
    if (value == "dropbox") return SoundRecorderCloudConnectionsProvider::Dropbox;
    if (value == "amazon_s3") return SoundRecorderCloudConnectionsProvider::AmazonS3;
    if (value == "cloudflare_r2") return SoundRecorderCloudConnectionsProvider::CloudflareR2;
    return std::nullopt;
}

enum class SoundRecorderCloudConnectionsLinkMode { ServerOauth, ClientManaged };
inline std::string sound_recorder_cloud_connections_link_mode_to_string(SoundRecorderCloudConnectionsLinkMode value) {
    switch (value) {
        case SoundRecorderCloudConnectionsLinkMode::ServerOauth: return "server_oauth";
        case SoundRecorderCloudConnectionsLinkMode::ClientManaged: return "client_managed";
    }
    return "";
}
inline std::optional<SoundRecorderCloudConnectionsLinkMode> parse_sound_recorder_cloud_connections_link_mode(const std::string& value) {
    if (value == "server_oauth") return SoundRecorderCloudConnectionsLinkMode::ServerOauth;
    if (value == "client_managed") return SoundRecorderCloudConnectionsLinkMode::ClientManaged;
    return std::nullopt;
}

enum class SoundRecorderCloudConnectionsStatus { Active, Paused, Revoked, Failed };
inline std::string sound_recorder_cloud_connections_status_to_string(SoundRecorderCloudConnectionsStatus value) {
    switch (value) {
        case SoundRecorderCloudConnectionsStatus::Active: return "active";
        case SoundRecorderCloudConnectionsStatus::Paused: return "paused";
        case SoundRecorderCloudConnectionsStatus::Revoked: return "revoked";
        case SoundRecorderCloudConnectionsStatus::Failed: return "failed";
    }
    return "";
}
inline std::optional<SoundRecorderCloudConnectionsStatus> parse_sound_recorder_cloud_connections_status(const std::string& value) {
    if (value == "active") return SoundRecorderCloudConnectionsStatus::Active;
    if (value == "paused") return SoundRecorderCloudConnectionsStatus::Paused;
    if (value == "revoked") return SoundRecorderCloudConnectionsStatus::Revoked;
    if (value == "failed") return SoundRecorderCloudConnectionsStatus::Failed;
    return std::nullopt;
}

struct SoundRecorderCloudConnectionsRow {
    std::string id;
    std::string account_id;
    std::optional<std::string> created_by_device_id;
    std::string provider;
    std::string link_mode;
    std::string status;
    std::optional<std::string> display_name;
    std::optional<std::string> provider_account_id;
    std::optional<std::string> provider_subject_hash;
    std::optional<std::string> root_folder_id;
    std::string folder_path;
    std::optional<std::string> oauth_scope;
    std::optional<std::string> token_ciphertext;
    std::optional<std::string> token_nonce;
    std::optional<std::string> token_aad;
    std::optional<int32_t> token_version;
    std::optional<std::string> token_expires_at;
    std::optional<std::string> last_sync_at;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline SoundRecorderCloudConnectionsRow sound_recorder_cloud_connections_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SoundRecorderCloudConnectionsRow row;
    (void)is_null;
    row.id = get(0);
    row.account_id = get(1);
    row.created_by_device_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.provider = get(3);
    row.link_mode = get(4);
    row.status = get(5);
    row.display_name = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.provider_account_id = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.provider_subject_hash = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.root_folder_id = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.folder_path = get(10);
    row.oauth_scope = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.token_ciphertext = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.token_nonce = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    row.token_aad = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.token_version = is_null(15) ? std::nullopt : std::optional<int32_t>(std::stoi(get(15)));
    row.token_expires_at = is_null(16) ? std::nullopt : std::optional<std::string>(get(16));
    row.last_sync_at = is_null(17) ? std::nullopt : std::optional<std::string>(get(17));
    row.meta_data = get(18);
    row.created_at = get(19);
    row.updated_at = get(20);
    return row;
}
inline std::optional<std::string> validate_sound_recorder_cloud_connections_display_name(const std::string& value) {
    if (value.size() > 160) return std::string("sound_recorder_cloud_connections.display_name must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_cloud_connections_provider_account_id(const std::string& value) {
    if (value.size() > 240) return std::string("sound_recorder_cloud_connections.provider_account_id must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_cloud_connections_provider_subject_hash(const std::string& value) {
    if (value.size() > 64) return std::string("sound_recorder_cloud_connections.provider_subject_hash must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("sound_recorder_cloud_connections.provider_subject_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_cloud_connections_root_folder_id(const std::string& value) {
    if (value.size() > 512) return std::string("sound_recorder_cloud_connections.root_folder_id must be at most 512 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_cloud_connections_folder_path(const std::string& value) {
    if (value.size() > 512) return std::string("sound_recorder_cloud_connections.folder_path must be at most 512 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_cloud_connections_token_nonce(const std::string& value) {
    if (value.size() > 64) return std::string("sound_recorder_cloud_connections.token_nonce must be at most 64 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_cloud_connections_token_aad(const std::string& value) {
    if (value.size() > 512) return std::string("sound_recorder_cloud_connections.token_aad must be at most 512 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_cloud_connections_token_version(int32_t value) {
    if (value < 1) return std::string("sound_recorder_cloud_connections.token_version is below the minimum");
    return std::nullopt;
}

inline const char* sound_recorder_cloud_connection_projection_outbox_table = "sound_recorder_cloud_connection_projection_outbox";
inline const std::vector<std::string> sound_recorder_cloud_connection_projection_outbox_columns = { "seq", "connection_id", "attempts", "available_at", "locked_until", "processed_at", "last_error", "created_at", "updated_at" };
inline const char* sound_recorder_cloud_connection_projection_outbox_select_sql = R"SQL(select
      seq,
      connection_id::text as connection_id,
      attempts,
      to_char(available_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as available_at,
      to_char(locked_until at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as locked_until,
      to_char(processed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as processed_at,
      last_error,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from sound_recorder_cloud_connection_projection_outbox)SQL";

struct SoundRecorderCloudConnectionProjectionOutboxRow {
    int64_t seq;
    std::string connection_id;
    int32_t attempts;
    std::string available_at;
    std::optional<std::string> locked_until;
    std::optional<std::string> processed_at;
    std::optional<std::string> last_error;
    std::string created_at;
    std::string updated_at;
};

inline SoundRecorderCloudConnectionProjectionOutboxRow sound_recorder_cloud_connection_projection_outbox_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SoundRecorderCloudConnectionProjectionOutboxRow row;
    (void)is_null;
    row.seq = std::stoll(get(0));
    row.connection_id = get(1);
    row.attempts = std::stoi(get(2));
    row.available_at = get(3);
    row.locked_until = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.processed_at = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.last_error = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.created_at = get(7);
    row.updated_at = get(8);
    return row;
}
inline std::optional<std::string> validate_sound_recorder_cloud_connection_projection_outbox_attempts(int32_t value) {
    if (value < 0) return std::string("sound_recorder_cloud_connection_projection_outbox.attempts is below the minimum");
    if (value > 50) return std::string("sound_recorder_cloud_connection_projection_outbox.attempts is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_cloud_connection_projection_outbox_last_error(const std::string& value) {
    if (value.size() > 500) return std::string("sound_recorder_cloud_connection_projection_outbox.last_error must be at most 500 characters");
    return std::nullopt;
}

inline const char* sound_recorder_cloud_copy_jobs_table = "sound_recorder_cloud_copy_jobs";
inline const std::vector<std::string> sound_recorder_cloud_copy_jobs_columns = { "id", "account_id", "connection_id", "segment_id", "provider", "status", "destination_key", "provider_file_id", "attempts", "locked_until", "started_at", "completed_at", "last_error", "meta_data", "created_at", "updated_at" };
inline const char* sound_recorder_cloud_copy_jobs_select_sql = R"SQL(select
      id::text as id,
      account_id::text as account_id,
      connection_id::text as connection_id,
      segment_id::text as segment_id,
      provider,
      status,
      destination_key,
      provider_file_id,
      attempts,
      to_char(locked_until at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as locked_until,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(completed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as completed_at,
      last_error,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from sound_recorder_cloud_copy_jobs)SQL";

enum class SoundRecorderCloudCopyJobsProvider { GoogleDrive, MicrosoftOnedrive, AppleIcloud, Dropbox, AmazonS3, CloudflareR2 };
inline std::string sound_recorder_cloud_copy_jobs_provider_to_string(SoundRecorderCloudCopyJobsProvider value) {
    switch (value) {
        case SoundRecorderCloudCopyJobsProvider::GoogleDrive: return "google_drive";
        case SoundRecorderCloudCopyJobsProvider::MicrosoftOnedrive: return "microsoft_onedrive";
        case SoundRecorderCloudCopyJobsProvider::AppleIcloud: return "apple_icloud";
        case SoundRecorderCloudCopyJobsProvider::Dropbox: return "dropbox";
        case SoundRecorderCloudCopyJobsProvider::AmazonS3: return "amazon_s3";
        case SoundRecorderCloudCopyJobsProvider::CloudflareR2: return "cloudflare_r2";
    }
    return "";
}
inline std::optional<SoundRecorderCloudCopyJobsProvider> parse_sound_recorder_cloud_copy_jobs_provider(const std::string& value) {
    if (value == "google_drive") return SoundRecorderCloudCopyJobsProvider::GoogleDrive;
    if (value == "microsoft_onedrive") return SoundRecorderCloudCopyJobsProvider::MicrosoftOnedrive;
    if (value == "apple_icloud") return SoundRecorderCloudCopyJobsProvider::AppleIcloud;
    if (value == "dropbox") return SoundRecorderCloudCopyJobsProvider::Dropbox;
    if (value == "amazon_s3") return SoundRecorderCloudCopyJobsProvider::AmazonS3;
    if (value == "cloudflare_r2") return SoundRecorderCloudCopyJobsProvider::CloudflareR2;
    return std::nullopt;
}

enum class SoundRecorderCloudCopyJobsStatus { Pending, Running, WaitingClient, Completed, Failed, Skipped };
inline std::string sound_recorder_cloud_copy_jobs_status_to_string(SoundRecorderCloudCopyJobsStatus value) {
    switch (value) {
        case SoundRecorderCloudCopyJobsStatus::Pending: return "pending";
        case SoundRecorderCloudCopyJobsStatus::Running: return "running";
        case SoundRecorderCloudCopyJobsStatus::WaitingClient: return "waiting_client";
        case SoundRecorderCloudCopyJobsStatus::Completed: return "completed";
        case SoundRecorderCloudCopyJobsStatus::Failed: return "failed";
        case SoundRecorderCloudCopyJobsStatus::Skipped: return "skipped";
    }
    return "";
}
inline std::optional<SoundRecorderCloudCopyJobsStatus> parse_sound_recorder_cloud_copy_jobs_status(const std::string& value) {
    if (value == "pending") return SoundRecorderCloudCopyJobsStatus::Pending;
    if (value == "running") return SoundRecorderCloudCopyJobsStatus::Running;
    if (value == "waiting_client") return SoundRecorderCloudCopyJobsStatus::WaitingClient;
    if (value == "completed") return SoundRecorderCloudCopyJobsStatus::Completed;
    if (value == "failed") return SoundRecorderCloudCopyJobsStatus::Failed;
    if (value == "skipped") return SoundRecorderCloudCopyJobsStatus::Skipped;
    return std::nullopt;
}

struct SoundRecorderCloudCopyJobsRow {
    std::string id;
    std::string account_id;
    std::string connection_id;
    std::string segment_id;
    std::string provider;
    std::string status;
    std::string destination_key;
    std::optional<std::string> provider_file_id;
    int32_t attempts;
    std::optional<std::string> locked_until;
    std::optional<std::string> started_at;
    std::optional<std::string> completed_at;
    std::optional<std::string> last_error;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline SoundRecorderCloudCopyJobsRow sound_recorder_cloud_copy_jobs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SoundRecorderCloudCopyJobsRow row;
    (void)is_null;
    row.id = get(0);
    row.account_id = get(1);
    row.connection_id = get(2);
    row.segment_id = get(3);
    row.provider = get(4);
    row.status = get(5);
    row.destination_key = get(6);
    row.provider_file_id = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.attempts = std::stoi(get(8));
    row.locked_until = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.started_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.completed_at = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.last_error = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.meta_data = get(13);
    row.created_at = get(14);
    row.updated_at = get(15);
    return row;
}
inline std::optional<std::string> validate_sound_recorder_cloud_copy_jobs_destination_key(const std::string& value) {
    if (value.size() > 2048) return std::string("sound_recorder_cloud_copy_jobs.destination_key must be at most 2048 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_cloud_copy_jobs_provider_file_id(const std::string& value) {
    if (value.size() > 512) return std::string("sound_recorder_cloud_copy_jobs.provider_file_id must be at most 512 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_cloud_copy_jobs_attempts(int32_t value) {
    if (value < 0) return std::string("sound_recorder_cloud_copy_jobs.attempts is below the minimum");
    if (value > 50) return std::string("sound_recorder_cloud_copy_jobs.attempts is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_sound_recorder_cloud_copy_jobs_last_error(const std::string& value) {
    if (value.size() > 500) return std::string("sound_recorder_cloud_copy_jobs.last_error must be at most 500 characters");
    return std::nullopt;
}

inline const char* container_pool_configs_table = "container_pool_configs";
inline const std::vector<std::string> container_pool_configs_columns = { "id", "slug", "display_name", "image", "command", "env", "request_path", "health_path", "container_port", "min_warm", "max_warm", "max_concurrency_per_container", "request_timeout_ms", "idle_ttl_seconds", "nats_subject", "status", "labels", "meta_data", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* container_pool_configs_select_sql = R"SQL(select
      id::text as id,
      slug,
      display_name,
      image,
      command::text as command_json,
      env::text as env_json,
      request_path,
      health_path,
      container_port,
      min_warm,
      max_warm,
      max_concurrency_per_container,
      request_timeout_ms,
      idle_ttl_seconds,
      nats_subject,
      status,
      labels::text as labels_json,
      meta_data::text as meta_data_json,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from container_pool_configs)SQL";

enum class ContainerPoolConfigsStatus { Active, Paused, Archived };
inline std::string container_pool_configs_status_to_string(ContainerPoolConfigsStatus value) {
    switch (value) {
        case ContainerPoolConfigsStatus::Active: return "active";
        case ContainerPoolConfigsStatus::Paused: return "paused";
        case ContainerPoolConfigsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<ContainerPoolConfigsStatus> parse_container_pool_configs_status(const std::string& value) {
    if (value == "active") return ContainerPoolConfigsStatus::Active;
    if (value == "paused") return ContainerPoolConfigsStatus::Paused;
    if (value == "archived") return ContainerPoolConfigsStatus::Archived;
    return std::nullopt;
}

struct ContainerPoolConfigsRow {
    std::string id;
    std::string slug;
    std::string display_name;
    std::string image;
    std::string command;
    std::string env;
    std::string request_path;
    std::string health_path;
    int32_t container_port;
    int32_t min_warm;
    int32_t max_warm;
    int32_t max_concurrency_per_container;
    int32_t request_timeout_ms;
    int32_t idle_ttl_seconds;
    std::optional<std::string> nats_subject;
    std::string status;
    std::string labels;
    std::string meta_data;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline ContainerPoolConfigsRow container_pool_configs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    ContainerPoolConfigsRow row;
    (void)is_null;
    row.id = get(0);
    row.slug = get(1);
    row.display_name = get(2);
    row.image = get(3);
    row.command = get(4);
    row.env = get(5);
    row.request_path = get(6);
    row.health_path = get(7);
    row.container_port = std::stoi(get(8));
    row.min_warm = std::stoi(get(9));
    row.max_warm = std::stoi(get(10));
    row.max_concurrency_per_container = std::stoi(get(11));
    row.request_timeout_ms = std::stoi(get(12));
    row.idle_ttl_seconds = std::stoi(get(13));
    row.nats_subject = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.status = get(15);
    row.labels = get(16);
    row.meta_data = get(17);
    row.is_soft_deleted = (get(18) == "t");
    row.created_at = get(19);
    row.updated_at = get(20);
    row.created_by = is_null(21) ? std::nullopt : std::optional<std::string>(get(21));
    row.updated_by = is_null(22) ? std::nullopt : std::optional<std::string>(get(22));
    return row;
}
inline std::optional<std::string> validate_container_pool_configs_slug(const std::string& value) {
    if (value.size() > 120) return std::string("container_pool_configs.slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9-]{0,118}[a-z0-9]$)RX"))) return std::string("container_pool_configs.slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_container_pool_configs_display_name(const std::string& value) {
    if (value.size() > 200) return std::string("container_pool_configs.display_name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_container_pool_configs_request_path(const std::string& value) {
    if (value.size() > 256) return std::string("container_pool_configs.request_path must be at most 256 characters");
    if (!std::regex_match(value, std::regex(R"RX(^/[A-Za-z0-9._~!$&'()*+,;=:@%/-]{0,255}$)RX"))) return std::string("container_pool_configs.request_path does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_container_pool_configs_health_path(const std::string& value) {
    if (value.size() > 256) return std::string("container_pool_configs.health_path must be at most 256 characters");
    if (!std::regex_match(value, std::regex(R"RX(^/[A-Za-z0-9._~!$&'()*+,;=:@%/-]{0,255}$)RX"))) return std::string("container_pool_configs.health_path does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_container_pool_configs_container_port(int32_t value) {
    if (value < 1) return std::string("container_pool_configs.container_port is below the minimum");
    if (value > 65535) return std::string("container_pool_configs.container_port is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_container_pool_configs_min_warm(int32_t value) {
    if (value < 0) return std::string("container_pool_configs.min_warm is below the minimum");
    if (value > 64) return std::string("container_pool_configs.min_warm is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_container_pool_configs_max_warm(int32_t value) {
    if (value < 1) return std::string("container_pool_configs.max_warm is below the minimum");
    if (value > 128) return std::string("container_pool_configs.max_warm is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_container_pool_configs_max_concurrency_per_container(int32_t value) {
    if (value < 1) return std::string("container_pool_configs.max_concurrency_per_container is below the minimum");
    if (value > 128) return std::string("container_pool_configs.max_concurrency_per_container is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_container_pool_configs_request_timeout_ms(int32_t value) {
    if (value < 100) return std::string("container_pool_configs.request_timeout_ms is below the minimum");
    if (value > 900000) return std::string("container_pool_configs.request_timeout_ms is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_container_pool_configs_idle_ttl_seconds(int32_t value) {
    if (value < 10) return std::string("container_pool_configs.idle_ttl_seconds is below the minimum");
    if (value > 86400) return std::string("container_pool_configs.idle_ttl_seconds is above the maximum");
    return std::nullopt;
}

// Git repositories that the remote agent runtime is allowed to clone, run tasks against, and open PRs for.
inline const char* known_git_repos_table = "known_git_repos";
inline const std::vector<std::string> known_git_repos_columns = { "id", "repo_url", "display_name", "provider", "default_branch", "status", "last_verified_at", "meta_data", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* known_git_repos_select_sql = R"SQL(select
      id::text as id,
      repo_url,
      display_name,
      provider,
      default_branch,
      status,
      to_char(last_verified_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_verified_at,
      meta_data::text as meta_data_json,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from known_git_repos)SQL";

enum class KnownGitRepoProvider { Github, Gitlab, Bitbucket, Generic };
inline std::string known_git_repos_provider_to_string(KnownGitRepoProvider value) {
    switch (value) {
        case KnownGitRepoProvider::Github: return "github";
        case KnownGitRepoProvider::Gitlab: return "gitlab";
        case KnownGitRepoProvider::Bitbucket: return "bitbucket";
        case KnownGitRepoProvider::Generic: return "generic";
    }
    return "";
}
inline std::optional<KnownGitRepoProvider> parse_known_git_repos_provider(const std::string& value) {
    if (value == "github") return KnownGitRepoProvider::Github;
    if (value == "gitlab") return KnownGitRepoProvider::Gitlab;
    if (value == "bitbucket") return KnownGitRepoProvider::Bitbucket;
    if (value == "generic") return KnownGitRepoProvider::Generic;
    return std::nullopt;
}

enum class KnownGitRepoStatus { Active, Paused, Archived };
inline std::string known_git_repos_status_to_string(KnownGitRepoStatus value) {
    switch (value) {
        case KnownGitRepoStatus::Active: return "active";
        case KnownGitRepoStatus::Paused: return "paused";
        case KnownGitRepoStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<KnownGitRepoStatus> parse_known_git_repos_status(const std::string& value) {
    if (value == "active") return KnownGitRepoStatus::Active;
    if (value == "paused") return KnownGitRepoStatus::Paused;
    if (value == "archived") return KnownGitRepoStatus::Archived;
    return std::nullopt;
}

struct KnownGitRepoRow {
    std::string id;
    std::string repo_url;
    std::string display_name;
    std::string provider;
    std::string default_branch;
    std::string status;
    std::optional<std::string> last_verified_at;
    std::string meta_data;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline KnownGitRepoRow known_git_repos_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    KnownGitRepoRow row;
    (void)is_null;
    row.id = get(0);
    row.repo_url = get(1);
    row.display_name = get(2);
    row.provider = get(3);
    row.default_branch = get(4);
    row.status = get(5);
    row.last_verified_at = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.meta_data = get(7);
    row.is_soft_deleted = (get(8) == "t");
    row.created_at = get(9);
    row.updated_at = get(10);
    row.created_by = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.updated_by = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    return row;
}
inline std::optional<std::string> validate_known_git_repos_repo_url(const std::string& value) {
    if (value.size() < 1) return std::string("known_git_repos.repo_url must be at least 1 characters");
    if (value.size() > 2048) return std::string("known_git_repos.repo_url must be at most 2048 characters");
    if (!std::regex_match(value, std::regex(R"RX(^(git@|ssh://|https://).+)RX"))) return std::string("known_git_repos.repo_url does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_known_git_repos_display_name(const std::string& value) {
    if (value.size() < 1) return std::string("known_git_repos.display_name must be at least 1 characters");
    if (value.size() > 200) return std::string("known_git_repos.display_name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_known_git_repos_default_branch(const std::string& value) {
    if (value.size() < 1) return std::string("known_git_repos.default_branch must be at least 1 characters");
    if (value.size() > 120) return std::string("known_git_repos.default_branch must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._/-]{1,120}$)RX"))) return std::string("known_git_repos.default_branch does not match the required pattern");
    return std::nullopt;
}

inline const char* agent_context_blobs_table = "agent_context_blobs";
inline const std::vector<std::string> agent_context_blobs_columns = { "id", "project_id", "repo_id", "context_id", "context_title", "context_blob", "status", "labels", "meta_data", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* agent_context_blobs_select_sql = R"SQL(select
      id::text as id,
      project_id,
      repo_id::text as repo_id,
      context_id,
      context_title,
      context_blob,
      status,
      labels::text as labels_json,
      meta_data::text as meta_data_json,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from agent_context_blobs)SQL";

enum class AgentContextBlobsStatus { Active, Paused, Archived };
inline std::string agent_context_blobs_status_to_string(AgentContextBlobsStatus value) {
    switch (value) {
        case AgentContextBlobsStatus::Active: return "active";
        case AgentContextBlobsStatus::Paused: return "paused";
        case AgentContextBlobsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<AgentContextBlobsStatus> parse_agent_context_blobs_status(const std::string& value) {
    if (value == "active") return AgentContextBlobsStatus::Active;
    if (value == "paused") return AgentContextBlobsStatus::Paused;
    if (value == "archived") return AgentContextBlobsStatus::Archived;
    return std::nullopt;
}

struct AgentContextBlobsRow {
    std::string id;
    std::string project_id;
    std::optional<std::string> repo_id;
    std::string context_id;
    std::string context_title;
    std::string context_blob;
    std::string status;
    std::string labels;
    std::string meta_data;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline AgentContextBlobsRow agent_context_blobs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    AgentContextBlobsRow row;
    (void)is_null;
    row.id = get(0);
    row.project_id = get(1);
    row.repo_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.context_id = get(3);
    row.context_title = get(4);
    row.context_blob = get(5);
    row.status = get(6);
    row.labels = get(7);
    row.meta_data = get(8);
    row.is_soft_deleted = (get(9) == "t");
    row.created_at = get(10);
    row.updated_at = get(11);
    row.created_by = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.updated_by = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    return row;
}
inline std::optional<std::string> validate_agent_context_blobs_project_id(const std::string& value) {
    if (value.size() > 120) return std::string("agent_context_blobs.project_id must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,120}$)RX"))) return std::string("agent_context_blobs.project_id does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_context_blobs_context_id(const std::string& value) {
    if (value.size() > 200) return std::string("agent_context_blobs.context_id must be at most 200 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,200}$)RX"))) return std::string("agent_context_blobs.context_id does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_context_blobs_context_title(const std::string& value) {
    if (value.size() > 300) return std::string("agent_context_blobs.context_title must be at most 300 characters");
    return std::nullopt;
}

inline const char* agent_context_embeddings_table = "agent_context_embeddings";
inline const std::vector<std::string> agent_context_embeddings_columns = { "id", "context_blob_id", "embedding_model", "embedding", "embedding_dimensions", "content_sha256", "created_at" };
inline const char* agent_context_embeddings_select_sql = R"SQL(select
      id::text as id,
      context_blob_id::text as context_blob_id,
      embedding_model,
      embedding::text as embedding_json,
      embedding_dimensions,
      content_sha256,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from agent_context_embeddings)SQL";

struct AgentContextEmbeddingsRow {
    std::string id;
    std::string context_blob_id;
    std::string embedding_model;
    std::string embedding;
    int32_t embedding_dimensions;
    std::string content_sha256;
    std::string created_at;
};

inline AgentContextEmbeddingsRow agent_context_embeddings_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    AgentContextEmbeddingsRow row;
    (void)is_null;
    row.id = get(0);
    row.context_blob_id = get(1);
    row.embedding_model = get(2);
    row.embedding = get(3);
    row.embedding_dimensions = std::stoi(get(4));
    row.content_sha256 = get(5);
    row.created_at = get(6);
    return row;
}
inline std::optional<std::string> validate_agent_context_embeddings_embedding_model(const std::string& value) {
    if (value.size() > 120) return std::string("agent_context_embeddings.embedding_model must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,120}$)RX"))) return std::string("agent_context_embeddings.embedding_model does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_context_embeddings_embedding_dimensions(int32_t value) {
    if (value < 1) return std::string("agent_context_embeddings.embedding_dimensions is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_context_embeddings_content_sha256(const std::string& value) {
    if (value.size() > 64) return std::string("agent_context_embeddings.content_sha256 must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("agent_context_embeddings.content_sha256 does not match the required pattern");
    return std::nullopt;
}

// Remote coding-agent chat threads pinned to one git repository and one Kubernetes worker runtime.
inline const char* agent_remote_dev_threads_table = "agent_remote_dev_threads";
inline const std::vector<std::string> agent_remote_dev_threads_columns = { "id", "user_id", "known_git_repo_id", "title", "repo", "base_branch", "meta", "archived_at", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* agent_remote_dev_threads_select_sql = R"SQL(select
      id::text as id,
      user_id::text as user_id,
      known_git_repo_id::text as known_git_repo_id,
      title,
      repo,
      base_branch,
      meta::text as meta_json,
      to_char(archived_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as archived_at,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from agent_remote_dev_threads)SQL";

struct AgentRemoteDevThreadRow {
    std::string id;
    std::string user_id;
    std::optional<std::string> known_git_repo_id;
    std::string title;
    std::string repo;
    std::string base_branch;
    std::string meta;
    std::optional<std::string> archived_at;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline AgentRemoteDevThreadRow agent_remote_dev_threads_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    AgentRemoteDevThreadRow row;
    (void)is_null;
    row.id = get(0);
    row.user_id = get(1);
    row.known_git_repo_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.title = get(3);
    row.repo = get(4);
    row.base_branch = get(5);
    row.meta = get(6);
    row.archived_at = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.is_soft_deleted = (get(8) == "t");
    row.created_at = get(9);
    row.updated_at = get(10);
    row.created_by = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.updated_by = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    return row;
}
inline std::optional<std::string> validate_agent_remote_dev_threads_title(const std::string& value) {
    if (value.size() < 1) return std::string("agent_remote_dev_threads.title must be at least 1 characters");
    if (value.size() > 500) return std::string("agent_remote_dev_threads.title must be at most 500 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_remote_dev_threads_repo(const std::string& value) {
    if (value.size() < 1) return std::string("agent_remote_dev_threads.repo must be at least 1 characters");
    if (value.size() > 2048) return std::string("agent_remote_dev_threads.repo must be at most 2048 characters");
    if (!std::regex_match(value, std::regex(R"RX(^(git@|ssh://|https://).+)RX"))) return std::string("agent_remote_dev_threads.repo does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_remote_dev_threads_base_branch(const std::string& value) {
    if (value.size() < 1) return std::string("agent_remote_dev_threads.base_branch must be at least 1 characters");
    if (value.size() > 120) return std::string("agent_remote_dev_threads.base_branch must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._/-]{1,120}$)RX"))) return std::string("agent_remote_dev_threads.base_branch does not match the required pattern");
    return std::nullopt;
}

// Prompt/control tasks dispatched to thread-scoped remote development workers.
inline const char* agent_remote_dev_tasks_table = "agent_remote_dev_tasks";
inline const std::vector<std::string> agent_remote_dev_tasks_columns = { "id", "thread_id", "user_id", "docker_task_id", "prompt", "status", "branch", "pr_url", "pr_state", "exit_reason", "error_message", "last_event_seq", "meta", "is_soft_deleted", "started_at", "finished_at", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* agent_remote_dev_tasks_select_sql = R"SQL(select
      id::text as id,
      thread_id::text as thread_id,
      user_id::text as user_id,
      docker_task_id::text as docker_task_id,
      prompt,
      status,
      branch,
      pr_url,
      pr_state,
      exit_reason,
      error_message,
      last_event_seq,
      meta::text as meta_json,
      is_soft_deleted,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(finished_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as finished_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from agent_remote_dev_tasks)SQL";

enum class AgentRemoteDevTaskStatus { Queued, Running, Streaming, Pushed, PrOpen, PrMerged, PrClosed, Done, Failed, Cancelled };
inline std::string agent_remote_dev_tasks_status_to_string(AgentRemoteDevTaskStatus value) {
    switch (value) {
        case AgentRemoteDevTaskStatus::Queued: return "queued";
        case AgentRemoteDevTaskStatus::Running: return "running";
        case AgentRemoteDevTaskStatus::Streaming: return "streaming";
        case AgentRemoteDevTaskStatus::Pushed: return "pushed";
        case AgentRemoteDevTaskStatus::PrOpen: return "pr_open";
        case AgentRemoteDevTaskStatus::PrMerged: return "pr_merged";
        case AgentRemoteDevTaskStatus::PrClosed: return "pr_closed";
        case AgentRemoteDevTaskStatus::Done: return "done";
        case AgentRemoteDevTaskStatus::Failed: return "failed";
        case AgentRemoteDevTaskStatus::Cancelled: return "cancelled";
    }
    return "";
}
inline std::optional<AgentRemoteDevTaskStatus> parse_agent_remote_dev_tasks_status(const std::string& value) {
    if (value == "queued") return AgentRemoteDevTaskStatus::Queued;
    if (value == "running") return AgentRemoteDevTaskStatus::Running;
    if (value == "streaming") return AgentRemoteDevTaskStatus::Streaming;
    if (value == "pushed") return AgentRemoteDevTaskStatus::Pushed;
    if (value == "pr_open") return AgentRemoteDevTaskStatus::PrOpen;
    if (value == "pr_merged") return AgentRemoteDevTaskStatus::PrMerged;
    if (value == "pr_closed") return AgentRemoteDevTaskStatus::PrClosed;
    if (value == "done") return AgentRemoteDevTaskStatus::Done;
    if (value == "failed") return AgentRemoteDevTaskStatus::Failed;
    if (value == "cancelled") return AgentRemoteDevTaskStatus::Cancelled;
    return std::nullopt;
}

enum class AgentRemoteDevTaskPrState { Draft, Open, Closed, Merged };
inline std::string agent_remote_dev_tasks_pr_state_to_string(AgentRemoteDevTaskPrState value) {
    switch (value) {
        case AgentRemoteDevTaskPrState::Draft: return "draft";
        case AgentRemoteDevTaskPrState::Open: return "open";
        case AgentRemoteDevTaskPrState::Closed: return "closed";
        case AgentRemoteDevTaskPrState::Merged: return "merged";
    }
    return "";
}
inline std::optional<AgentRemoteDevTaskPrState> parse_agent_remote_dev_tasks_pr_state(const std::string& value) {
    if (value == "draft") return AgentRemoteDevTaskPrState::Draft;
    if (value == "open") return AgentRemoteDevTaskPrState::Open;
    if (value == "closed") return AgentRemoteDevTaskPrState::Closed;
    if (value == "merged") return AgentRemoteDevTaskPrState::Merged;
    return std::nullopt;
}

enum class AgentRemoteDevTaskExitReason { Completed, Cancelled, Failed };
inline std::string agent_remote_dev_tasks_exit_reason_to_string(AgentRemoteDevTaskExitReason value) {
    switch (value) {
        case AgentRemoteDevTaskExitReason::Completed: return "completed";
        case AgentRemoteDevTaskExitReason::Cancelled: return "cancelled";
        case AgentRemoteDevTaskExitReason::Failed: return "failed";
    }
    return "";
}
inline std::optional<AgentRemoteDevTaskExitReason> parse_agent_remote_dev_tasks_exit_reason(const std::string& value) {
    if (value == "completed") return AgentRemoteDevTaskExitReason::Completed;
    if (value == "cancelled") return AgentRemoteDevTaskExitReason::Cancelled;
    if (value == "failed") return AgentRemoteDevTaskExitReason::Failed;
    return std::nullopt;
}

struct AgentRemoteDevTaskRow {
    std::string id;
    std::string thread_id;
    std::string user_id;
    std::string docker_task_id;
    std::string prompt;
    std::string status;
    std::optional<std::string> branch;
    std::optional<std::string> pr_url;
    std::optional<std::string> pr_state;
    std::optional<std::string> exit_reason;
    std::optional<std::string> error_message;
    int32_t last_event_seq;
    std::string meta;
    bool is_soft_deleted;
    std::optional<std::string> started_at;
    std::optional<std::string> finished_at;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline AgentRemoteDevTaskRow agent_remote_dev_tasks_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    AgentRemoteDevTaskRow row;
    (void)is_null;
    row.id = get(0);
    row.thread_id = get(1);
    row.user_id = get(2);
    row.docker_task_id = get(3);
    row.prompt = get(4);
    row.status = get(5);
    row.branch = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.pr_url = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.pr_state = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.exit_reason = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.error_message = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.last_event_seq = std::stoi(get(11));
    row.meta = get(12);
    row.is_soft_deleted = (get(13) == "t");
    row.started_at = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.finished_at = is_null(15) ? std::nullopt : std::optional<std::string>(get(15));
    row.created_at = get(16);
    row.updated_at = get(17);
    row.created_by = is_null(18) ? std::nullopt : std::optional<std::string>(get(18));
    row.updated_by = is_null(19) ? std::nullopt : std::optional<std::string>(get(19));
    return row;
}
inline std::optional<std::string> validate_agent_remote_dev_tasks_prompt(const std::string& value) {
    if (value.size() < 1) return std::string("agent_remote_dev_tasks.prompt must be at least 1 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_remote_dev_tasks_branch(const std::string& value) {
    if (value.size() > 200) return std::string("agent_remote_dev_tasks.branch must be at most 200 characters");
    return std::nullopt;
}

// Append-only ordered event stream emitted by remote agent workers.
inline const char* agent_remote_dev_events_table = "agent_remote_dev_events";
inline const std::vector<std::string> agent_remote_dev_events_columns = { "id", "task_id", "thread_id", "seq", "event_kind", "payload", "created_at" };
inline const char* agent_remote_dev_events_select_sql = R"SQL(select
      id,
      task_id::text as task_id,
      thread_id::text as thread_id,
      seq,
      event_kind,
      payload::text as payload_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from agent_remote_dev_events)SQL";

struct AgentRemoteDevEventRow {
    int64_t id;
    std::string task_id;
    std::optional<std::string> thread_id;
    int32_t seq;
    std::string event_kind;
    std::string payload;
    std::string created_at;
};

inline AgentRemoteDevEventRow agent_remote_dev_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    AgentRemoteDevEventRow row;
    (void)is_null;
    row.id = std::stoll(get(0));
    row.task_id = get(1);
    row.thread_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.seq = std::stoi(get(3));
    row.event_kind = get(4);
    row.payload = get(5);
    row.created_at = get(6);
    return row;
}
inline std::optional<std::string> validate_agent_remote_dev_events_event_kind(const std::string& value) {
    if (value.size() < 1) return std::string("agent_remote_dev_events.event_kind must be at least 1 characters");
    if (value.size() > 80) return std::string("agent_remote_dev_events.event_kind must be at most 80 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:-]{1,80}$)RX"))) return std::string("agent_remote_dev_events.event_kind does not match the required pattern");
    return std::nullopt;
}

// Thread-scoped operational breadcrumbs (session-ready, prompt, merge-base, container-pool-result, etc.) used by dev-server to inject prior-task context and by operators to trace worker lifecycle without grepping logs. Postgres is the durable single source of truth for these; Redis (see @dd/redis-interfaces / agent-thread-breadcrumb-cache) is an optional hot-read cache.
inline const char* agent_remote_dev_breadcrumbs_table = "agent_remote_dev_breadcrumbs";
inline const std::vector<std::string> agent_remote_dev_breadcrumbs_columns = { "id", "thread_id", "task_id", "kind", "payload", "emitted_at", "pod_name", "branch", "provider" };
inline const char* agent_remote_dev_breadcrumbs_select_sql = R"SQL(select
      id,
      thread_id::text as thread_id,
      task_id::text as task_id,
      kind,
      payload::text as payload_json,
      to_char(emitted_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as emitted_at,
      pod_name,
      branch,
      provider
    from agent_remote_dev_breadcrumbs)SQL";

struct AgentRemoteDevBreadcrumbRow {
    int64_t id;
    std::string thread_id;
    std::optional<std::string> task_id;
    std::string kind;
    std::string payload;
    std::string emitted_at;
    std::optional<std::string> pod_name;
    std::optional<std::string> branch;
    std::optional<std::string> provider;
};

inline AgentRemoteDevBreadcrumbRow agent_remote_dev_breadcrumbs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    AgentRemoteDevBreadcrumbRow row;
    (void)is_null;
    row.id = std::stoll(get(0));
    row.thread_id = get(1);
    row.task_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.kind = get(3);
    row.payload = get(4);
    row.emitted_at = get(5);
    row.pod_name = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.branch = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.provider = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    return row;
}
inline std::optional<std::string> validate_agent_remote_dev_breadcrumbs_kind(const std::string& value) {
    if (value.size() < 1) return std::string("agent_remote_dev_breadcrumbs.kind must be at least 1 characters");
    if (value.size() > 80) return std::string("agent_remote_dev_breadcrumbs.kind must be at most 80 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:-]{1,80}$)RX"))) return std::string("agent_remote_dev_breadcrumbs.kind does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_remote_dev_breadcrumbs_pod_name(const std::string& value) {
    if (value.size() > 253) return std::string("agent_remote_dev_breadcrumbs.pod_name must be at most 253 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_remote_dev_breadcrumbs_branch(const std::string& value) {
    if (value.size() > 120) return std::string("agent_remote_dev_breadcrumbs.branch must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_remote_dev_breadcrumbs_provider(const std::string& value) {
    if (value.size() > 60) return std::string("agent_remote_dev_breadcrumbs.provider must be at most 60 characters");
    return std::nullopt;
}

// Artifacts published by remote agent tasks, such as logs, reports, patches, and output files.
inline const char* agent_remote_dev_artifacts_table = "agent_remote_dev_artifacts";
inline const std::vector<std::string> agent_remote_dev_artifacts_columns = { "id", "task_id", "thread_id", "filename", "content_type", "size_bytes", "storage_provider", "storage_bucket", "storage_key", "url", "signed_url_expires_at", "sha256", "meta", "created_at" };
inline const char* agent_remote_dev_artifacts_select_sql = R"SQL(select
      id::text as id,
      task_id::text as task_id,
      thread_id::text as thread_id,
      filename,
      content_type,
      size_bytes,
      storage_provider,
      storage_bucket,
      storage_key,
      url,
      to_char(signed_url_expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as signed_url_expires_at,
      sha256,
      meta::text as meta_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from agent_remote_dev_artifacts)SQL";

enum class AgentRemoteDevArtifactStorageProvider { S3, R2, Gcs, Drive, Local };
inline std::string agent_remote_dev_artifacts_storage_provider_to_string(AgentRemoteDevArtifactStorageProvider value) {
    switch (value) {
        case AgentRemoteDevArtifactStorageProvider::S3: return "s3";
        case AgentRemoteDevArtifactStorageProvider::R2: return "r2";
        case AgentRemoteDevArtifactStorageProvider::Gcs: return "gcs";
        case AgentRemoteDevArtifactStorageProvider::Drive: return "drive";
        case AgentRemoteDevArtifactStorageProvider::Local: return "local";
    }
    return "";
}
inline std::optional<AgentRemoteDevArtifactStorageProvider> parse_agent_remote_dev_artifacts_storage_provider(const std::string& value) {
    if (value == "s3") return AgentRemoteDevArtifactStorageProvider::S3;
    if (value == "r2") return AgentRemoteDevArtifactStorageProvider::R2;
    if (value == "gcs") return AgentRemoteDevArtifactStorageProvider::Gcs;
    if (value == "drive") return AgentRemoteDevArtifactStorageProvider::Drive;
    if (value == "local") return AgentRemoteDevArtifactStorageProvider::Local;
    return std::nullopt;
}

struct AgentRemoteDevArtifactRow {
    std::string id;
    std::string task_id;
    std::string thread_id;
    std::string filename;
    std::optional<std::string> content_type;
    std::optional<int64_t> size_bytes;
    std::string storage_provider;
    std::optional<std::string> storage_bucket;
    std::optional<std::string> storage_key;
    std::string url;
    std::optional<std::string> signed_url_expires_at;
    std::optional<std::string> sha256;
    std::string meta;
    std::string created_at;
};

inline AgentRemoteDevArtifactRow agent_remote_dev_artifacts_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    AgentRemoteDevArtifactRow row;
    (void)is_null;
    row.id = get(0);
    row.task_id = get(1);
    row.thread_id = get(2);
    row.filename = get(3);
    row.content_type = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.size_bytes = is_null(5) ? std::nullopt : std::optional<int64_t>(std::stoll(get(5)));
    row.storage_provider = get(6);
    row.storage_bucket = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.storage_key = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.url = get(9);
    row.signed_url_expires_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.sha256 = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.meta = get(12);
    row.created_at = get(13);
    return row;
}
inline std::optional<std::string> validate_agent_remote_dev_artifacts_filename(const std::string& value) {
    if (value.size() < 1) return std::string("agent_remote_dev_artifacts.filename must be at least 1 characters");
    if (value.size() > 1024) return std::string("agent_remote_dev_artifacts.filename must be at most 1024 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_remote_dev_artifacts_content_type(const std::string& value) {
    if (value.size() > 200) return std::string("agent_remote_dev_artifacts.content_type must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_remote_dev_artifacts_storage_bucket(const std::string& value) {
    if (value.size() > 200) return std::string("agent_remote_dev_artifacts.storage_bucket must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_remote_dev_artifacts_url(const std::string& value) {
    if (value.size() < 1) return std::string("agent_remote_dev_artifacts.url must be at least 1 characters");
    if (value.size() > 4096) return std::string("agent_remote_dev_artifacts.url must be at most 4096 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_agent_remote_dev_artifacts_sha256(const std::string& value) {
    if (value.size() > 64) return std::string("agent_remote_dev_artifacts.sha256 must be at most 64 characters");
    return std::nullopt;
}

// Short-lived leases for queue consumers and reapers that coordinate exactly-one worker ownership per thread.
inline const char* agent_remote_dev_runtime_locks_table = "agent_remote_dev_runtime_locks";
inline const std::vector<std::string> agent_remote_dev_runtime_locks_columns = { "id", "thread_id", "owner", "status", "fencing_token", "lease_expires_at", "created_at", "updated_at" };
inline const char* agent_remote_dev_runtime_locks_select_sql = R"SQL(select
      id::text as id,
      thread_id::text as thread_id,
      owner,
      status,
      fencing_token,
      to_char(lease_expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as lease_expires_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from agent_remote_dev_runtime_locks)SQL";

enum class AgentRemoteDevRuntimeLockStatus { Active, Released, Expired };
inline std::string agent_remote_dev_runtime_locks_status_to_string(AgentRemoteDevRuntimeLockStatus value) {
    switch (value) {
        case AgentRemoteDevRuntimeLockStatus::Active: return "active";
        case AgentRemoteDevRuntimeLockStatus::Released: return "released";
        case AgentRemoteDevRuntimeLockStatus::Expired: return "expired";
    }
    return "";
}
inline std::optional<AgentRemoteDevRuntimeLockStatus> parse_agent_remote_dev_runtime_locks_status(const std::string& value) {
    if (value == "active") return AgentRemoteDevRuntimeLockStatus::Active;
    if (value == "released") return AgentRemoteDevRuntimeLockStatus::Released;
    if (value == "expired") return AgentRemoteDevRuntimeLockStatus::Expired;
    return std::nullopt;
}

struct AgentRemoteDevRuntimeLockRow {
    std::string id;
    std::string thread_id;
    std::string owner;
    std::string status;
    int32_t fencing_token;
    std::string lease_expires_at;
    std::string created_at;
    std::string updated_at;
};

inline AgentRemoteDevRuntimeLockRow agent_remote_dev_runtime_locks_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    AgentRemoteDevRuntimeLockRow row;
    (void)is_null;
    row.id = get(0);
    row.thread_id = get(1);
    row.owner = get(2);
    row.status = get(3);
    row.fencing_token = std::stoi(get(4));
    row.lease_expires_at = get(5);
    row.created_at = get(6);
    row.updated_at = get(7);
    return row;
}
inline std::optional<std::string> validate_agent_remote_dev_runtime_locks_owner(const std::string& value) {
    if (value.size() < 1) return std::string("agent_remote_dev_runtime_locks.owner must be at least 1 characters");
    if (value.size() > 200) return std::string("agent_remote_dev_runtime_locks.owner must be at most 200 characters");
    return std::nullopt;
}

inline const char* mip_solver_sessions_table = "mip_solver_sessions";
inline const std::vector<std::string> mip_solver_sessions_columns = { "session_id", "revision", "problem", "created_at", "updated_at" };
inline const char* mip_solver_sessions_select_sql = R"SQL(select
      session_id,
      revision,
      problem::text as problem_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from mip_solver_sessions)SQL";

struct MipSolverSessionsRow {
    std::string session_id;
    int64_t revision;
    std::string problem;
    std::string created_at;
    std::string updated_at;
};

inline MipSolverSessionsRow mip_solver_sessions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    MipSolverSessionsRow row;
    (void)is_null;
    row.session_id = get(0);
    row.revision = std::stoll(get(1));
    row.problem = get(2);
    row.created_at = get(3);
    row.updated_at = get(4);
    return row;
}
inline std::optional<std::string> validate_mip_solver_sessions_session_id(const std::string& value) {
    if (value.size() > 200) return std::string("mip_solver_sessions.session_id must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_sessions_revision(int64_t value) {
    if (value < 0) return std::string("mip_solver_sessions.revision is below the minimum");
    return std::nullopt;
}

inline const char* mip_solver_solves_table = "mip_solver_solves";
inline const std::vector<std::string> mip_solver_solves_columns = { "solve_id", "request_id", "revision", "status", "node_id", "node_role", "problem", "options", "response", "jobs_expected", "jobs_published", "jobs_completed", "jobs_redelegated", "jobs_split", "timed_out", "distributed", "warnings", "started_at", "updated_at", "finished_at" };
inline const char* mip_solver_solves_select_sql = R"SQL(select
      solve_id,
      request_id,
      revision,
      status,
      node_id,
      node_role,
      problem::text as problem_json,
      options::text as options_json,
      response::text as response_json,
      jobs_expected,
      jobs_published,
      jobs_completed,
      jobs_redelegated,
      jobs_split,
      timed_out,
      distributed,
      warnings::text as warnings_json,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      to_char(finished_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as finished_at
    from mip_solver_solves)SQL";

enum class MipSolverSolvesNodeRole { Master, Slave };
inline std::string mip_solver_solves_node_role_to_string(MipSolverSolvesNodeRole value) {
    switch (value) {
        case MipSolverSolvesNodeRole::Master: return "master";
        case MipSolverSolvesNodeRole::Slave: return "slave";
    }
    return "";
}
inline std::optional<MipSolverSolvesNodeRole> parse_mip_solver_solves_node_role(const std::string& value) {
    if (value == "master") return MipSolverSolvesNodeRole::Master;
    if (value == "slave") return MipSolverSolvesNodeRole::Slave;
    return std::nullopt;
}

struct MipSolverSolvesRow {
    std::string solve_id;
    std::string request_id;
    int64_t revision;
    std::string status;
    std::string node_id;
    std::string node_role;
    std::string problem;
    std::string options;
    std::string response;
    int32_t jobs_expected;
    int32_t jobs_published;
    int32_t jobs_completed;
    int32_t jobs_redelegated;
    int32_t jobs_split;
    bool timed_out;
    bool distributed;
    std::string warnings;
    std::string started_at;
    std::string updated_at;
    std::optional<std::string> finished_at;
};

inline MipSolverSolvesRow mip_solver_solves_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    MipSolverSolvesRow row;
    (void)is_null;
    row.solve_id = get(0);
    row.request_id = get(1);
    row.revision = std::stoll(get(2));
    row.status = get(3);
    row.node_id = get(4);
    row.node_role = get(5);
    row.problem = get(6);
    row.options = get(7);
    row.response = get(8);
    row.jobs_expected = std::stoi(get(9));
    row.jobs_published = std::stoi(get(10));
    row.jobs_completed = std::stoi(get(11));
    row.jobs_redelegated = std::stoi(get(12));
    row.jobs_split = std::stoi(get(13));
    row.timed_out = (get(14) == "t");
    row.distributed = (get(15) == "t");
    row.warnings = get(16);
    row.started_at = get(17);
    row.updated_at = get(18);
    row.finished_at = is_null(19) ? std::nullopt : std::optional<std::string>(get(19));
    return row;
}
inline std::optional<std::string> validate_mip_solver_solves_solve_id(const std::string& value) {
    if (value.size() > 160) return std::string("mip_solver_solves.solve_id must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_solves_request_id(const std::string& value) {
    if (value.size() > 200) return std::string("mip_solver_solves.request_id must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_solves_revision(int64_t value) {
    if (value < 0) return std::string("mip_solver_solves.revision is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_solves_status(const std::string& value) {
    if (value.size() > 64) return std::string("mip_solver_solves.status must be at most 64 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_solves_node_id(const std::string& value) {
    if (value.size() > 253) return std::string("mip_solver_solves.node_id must be at most 253 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_solves_jobs_expected(int32_t value) {
    if (value < 0) return std::string("mip_solver_solves.jobs_expected is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_solves_jobs_published(int32_t value) {
    if (value < 0) return std::string("mip_solver_solves.jobs_published is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_solves_jobs_completed(int32_t value) {
    if (value < 0) return std::string("mip_solver_solves.jobs_completed is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_solves_jobs_redelegated(int32_t value) {
    if (value < 0) return std::string("mip_solver_solves.jobs_redelegated is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_solves_jobs_split(int32_t value) {
    if (value < 0) return std::string("mip_solver_solves.jobs_split is below the minimum");
    return std::nullopt;
}

inline const char* mip_solver_jobs_table = "mip_solver_jobs";
inline const std::vector<std::string> mip_solver_jobs_columns = { "job_id", "solve_id", "root_job_id", "retry_index", "depth", "status", "worker_node", "job_payload", "result_payload", "submitted_at", "finished_at", "updated_at" };
inline const char* mip_solver_jobs_select_sql = R"SQL(select
      job_id,
      solve_id,
      root_job_id,
      retry_index,
      depth,
      status,
      worker_node,
      job_payload::text as job_payload_json,
      result_payload::text as result_payload_json,
      to_char(submitted_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as submitted_at,
      to_char(finished_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as finished_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from mip_solver_jobs)SQL";

struct MipSolverJobsRow {
    std::string job_id;
    std::string solve_id;
    std::string root_job_id;
    int32_t retry_index;
    int32_t depth;
    std::string status;
    std::optional<std::string> worker_node;
    std::string job_payload;
    std::string result_payload;
    std::string submitted_at;
    std::optional<std::string> finished_at;
    std::string updated_at;
};

inline MipSolverJobsRow mip_solver_jobs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    MipSolverJobsRow row;
    (void)is_null;
    row.job_id = get(0);
    row.solve_id = get(1);
    row.root_job_id = get(2);
    row.retry_index = std::stoi(get(3));
    row.depth = std::stoi(get(4));
    row.status = get(5);
    row.worker_node = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.job_payload = get(7);
    row.result_payload = get(8);
    row.submitted_at = get(9);
    row.finished_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.updated_at = get(11);
    return row;
}
inline std::optional<std::string> validate_mip_solver_jobs_job_id(const std::string& value) {
    if (value.size() > 240) return std::string("mip_solver_jobs.job_id must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_jobs_solve_id(const std::string& value) {
    if (value.size() > 160) return std::string("mip_solver_jobs.solve_id must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_jobs_root_job_id(const std::string& value) {
    if (value.size() > 240) return std::string("mip_solver_jobs.root_job_id must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_jobs_retry_index(int32_t value) {
    if (value < 0) return std::string("mip_solver_jobs.retry_index is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_jobs_depth(int32_t value) {
    if (value < 0) return std::string("mip_solver_jobs.depth is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_jobs_status(const std::string& value) {
    if (value.size() > 64) return std::string("mip_solver_jobs.status must be at most 64 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_jobs_worker_node(const std::string& value) {
    if (value.size() > 253) return std::string("mip_solver_jobs.worker_node must be at most 253 characters");
    return std::nullopt;
}

inline const char* mip_solver_events_table = "mip_solver_events";
inline const std::vector<std::string> mip_solver_events_columns = { "id", "solve_id", "session_id", "job_id", "event_kind", "payload", "created_at" };
inline const char* mip_solver_events_select_sql = R"SQL(select
      id,
      solve_id,
      session_id,
      job_id,
      event_kind,
      payload::text as payload_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from mip_solver_events)SQL";

struct MipSolverEventsRow {
    int64_t id;
    std::optional<std::string> solve_id;
    std::optional<std::string> session_id;
    std::optional<std::string> job_id;
    std::string event_kind;
    std::string payload;
    std::string created_at;
};

inline MipSolverEventsRow mip_solver_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    MipSolverEventsRow row;
    (void)is_null;
    row.id = std::stoll(get(0));
    row.solve_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.session_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.job_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.event_kind = get(4);
    row.payload = get(5);
    row.created_at = get(6);
    return row;
}
inline std::optional<std::string> validate_mip_solver_events_solve_id(const std::string& value) {
    if (value.size() > 160) return std::string("mip_solver_events.solve_id must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_events_session_id(const std::string& value) {
    if (value.size() > 200) return std::string("mip_solver_events.session_id must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_events_job_id(const std::string& value) {
    if (value.size() > 240) return std::string("mip_solver_events.job_id must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mip_solver_events_event_kind(const std::string& value) {
    if (value.size() > 80) return std::string("mip_solver_events.event_kind must be at most 80 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:-]{1,80}$)RX"))) return std::string("mip_solver_events.event_kind does not match the required pattern");
    return std::nullopt;
}

// User-defined remote lambda functions executed by the remote runtime.
inline const char* lambda_functions_table = "lambda_functions";
inline const std::vector<std::string> lambda_functions_columns = { "id", "slug", "display_name", "description", "runtime", "entry_command", "function_body", "reuse_key", "idle_timeout_seconds", "max_run_ms", "containerized", "container_image", "container_build_status", "container_build_error", "container_built_at", "status", "env", "labels", "meta_data", "last_invoked_at", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* lambda_functions_select_sql = R"SQL(select
      id::text as id,
      slug,
      display_name,
      description,
      runtime,
      entry_command,
      function_body,
      reuse_key,
      idle_timeout_seconds,
      max_run_ms,
      containerized,
      container_image,
      container_build_status,
      container_build_error,
      to_char(container_built_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as container_built_at,
      status,
      env::text as env_json,
      labels::text as labels_json,
      meta_data::text as meta_data_json,
      to_char(last_invoked_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_invoked_at,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from lambda_functions)SQL";

enum class LambdaFunctionRuntime { Nodejs, Javascript, Typescript, Python3, Python, Ruby, Bash, Shell, Golang, Go, Dart, Erlang, Erl, Elixir, Ex, Java, Jvm, Gleam, Gleamlang, Rust, Rs, Browser };
inline std::string lambda_functions_runtime_to_string(LambdaFunctionRuntime value) {
    switch (value) {
        case LambdaFunctionRuntime::Nodejs: return "nodejs";
        case LambdaFunctionRuntime::Javascript: return "javascript";
        case LambdaFunctionRuntime::Typescript: return "typescript";
        case LambdaFunctionRuntime::Python3: return "python3";
        case LambdaFunctionRuntime::Python: return "python";
        case LambdaFunctionRuntime::Ruby: return "ruby";
        case LambdaFunctionRuntime::Bash: return "bash";
        case LambdaFunctionRuntime::Shell: return "shell";
        case LambdaFunctionRuntime::Golang: return "golang";
        case LambdaFunctionRuntime::Go: return "go";
        case LambdaFunctionRuntime::Dart: return "dart";
        case LambdaFunctionRuntime::Erlang: return "erlang";
        case LambdaFunctionRuntime::Erl: return "erl";
        case LambdaFunctionRuntime::Elixir: return "elixir";
        case LambdaFunctionRuntime::Ex: return "ex";
        case LambdaFunctionRuntime::Java: return "java";
        case LambdaFunctionRuntime::Jvm: return "jvm";
        case LambdaFunctionRuntime::Gleam: return "gleam";
        case LambdaFunctionRuntime::Gleamlang: return "gleamlang";
        case LambdaFunctionRuntime::Rust: return "rust";
        case LambdaFunctionRuntime::Rs: return "rs";
        case LambdaFunctionRuntime::Browser: return "browser";
    }
    return "";
}
inline std::optional<LambdaFunctionRuntime> parse_lambda_functions_runtime(const std::string& value) {
    if (value == "nodejs") return LambdaFunctionRuntime::Nodejs;
    if (value == "javascript") return LambdaFunctionRuntime::Javascript;
    if (value == "typescript") return LambdaFunctionRuntime::Typescript;
    if (value == "python3") return LambdaFunctionRuntime::Python3;
    if (value == "python") return LambdaFunctionRuntime::Python;
    if (value == "ruby") return LambdaFunctionRuntime::Ruby;
    if (value == "bash") return LambdaFunctionRuntime::Bash;
    if (value == "shell") return LambdaFunctionRuntime::Shell;
    if (value == "golang") return LambdaFunctionRuntime::Golang;
    if (value == "go") return LambdaFunctionRuntime::Go;
    if (value == "dart") return LambdaFunctionRuntime::Dart;
    if (value == "erlang") return LambdaFunctionRuntime::Erlang;
    if (value == "erl") return LambdaFunctionRuntime::Erl;
    if (value == "elixir") return LambdaFunctionRuntime::Elixir;
    if (value == "ex") return LambdaFunctionRuntime::Ex;
    if (value == "java") return LambdaFunctionRuntime::Java;
    if (value == "jvm") return LambdaFunctionRuntime::Jvm;
    if (value == "gleam") return LambdaFunctionRuntime::Gleam;
    if (value == "gleamlang") return LambdaFunctionRuntime::Gleamlang;
    if (value == "rust") return LambdaFunctionRuntime::Rust;
    if (value == "rs") return LambdaFunctionRuntime::Rs;
    if (value == "browser") return LambdaFunctionRuntime::Browser;
    return std::nullopt;
}

enum class LambdaFunctionContainerBuildStatus { NotRequested, Pending, Building, Built, Failed, Skipped };
inline std::string lambda_functions_container_build_status_to_string(LambdaFunctionContainerBuildStatus value) {
    switch (value) {
        case LambdaFunctionContainerBuildStatus::NotRequested: return "not_requested";
        case LambdaFunctionContainerBuildStatus::Pending: return "pending";
        case LambdaFunctionContainerBuildStatus::Building: return "building";
        case LambdaFunctionContainerBuildStatus::Built: return "built";
        case LambdaFunctionContainerBuildStatus::Failed: return "failed";
        case LambdaFunctionContainerBuildStatus::Skipped: return "skipped";
    }
    return "";
}
inline std::optional<LambdaFunctionContainerBuildStatus> parse_lambda_functions_container_build_status(const std::string& value) {
    if (value == "not_requested") return LambdaFunctionContainerBuildStatus::NotRequested;
    if (value == "pending") return LambdaFunctionContainerBuildStatus::Pending;
    if (value == "building") return LambdaFunctionContainerBuildStatus::Building;
    if (value == "built") return LambdaFunctionContainerBuildStatus::Built;
    if (value == "failed") return LambdaFunctionContainerBuildStatus::Failed;
    if (value == "skipped") return LambdaFunctionContainerBuildStatus::Skipped;
    return std::nullopt;
}

enum class LambdaFunctionStatus { Draft, Active, Paused, Archived };
inline std::string lambda_functions_status_to_string(LambdaFunctionStatus value) {
    switch (value) {
        case LambdaFunctionStatus::Draft: return "draft";
        case LambdaFunctionStatus::Active: return "active";
        case LambdaFunctionStatus::Paused: return "paused";
        case LambdaFunctionStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<LambdaFunctionStatus> parse_lambda_functions_status(const std::string& value) {
    if (value == "draft") return LambdaFunctionStatus::Draft;
    if (value == "active") return LambdaFunctionStatus::Active;
    if (value == "paused") return LambdaFunctionStatus::Paused;
    if (value == "archived") return LambdaFunctionStatus::Archived;
    return std::nullopt;
}

struct LambdaFunctionRow {
    std::string id;
    std::string slug;
    std::string display_name;
    std::string description;
    std::string runtime;
    std::string entry_command;
    std::string function_body;
    std::optional<std::string> reuse_key;
    int32_t idle_timeout_seconds;
    int32_t max_run_ms;
    bool containerized;
    std::optional<std::string> container_image;
    std::string container_build_status;
    std::optional<std::string> container_build_error;
    std::optional<std::string> container_built_at;
    std::string status;
    std::string env;
    std::string labels;
    std::string meta_data;
    std::optional<std::string> last_invoked_at;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline LambdaFunctionRow lambda_functions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    LambdaFunctionRow row;
    (void)is_null;
    row.id = get(0);
    row.slug = get(1);
    row.display_name = get(2);
    row.description = get(3);
    row.runtime = get(4);
    row.entry_command = get(5);
    row.function_body = get(6);
    row.reuse_key = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.idle_timeout_seconds = std::stoi(get(8));
    row.max_run_ms = std::stoi(get(9));
    row.containerized = (get(10) == "t");
    row.container_image = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.container_build_status = get(12);
    row.container_build_error = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    row.container_built_at = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.status = get(15);
    row.env = get(16);
    row.labels = get(17);
    row.meta_data = get(18);
    row.last_invoked_at = is_null(19) ? std::nullopt : std::optional<std::string>(get(19));
    row.is_soft_deleted = (get(20) == "t");
    row.created_at = get(21);
    row.updated_at = get(22);
    row.created_by = is_null(23) ? std::nullopt : std::optional<std::string>(get(23));
    row.updated_by = is_null(24) ? std::nullopt : std::optional<std::string>(get(24));
    return row;
}
inline std::optional<std::string> validate_lambda_functions_slug(const std::string& value) {
    if (value.size() > 120) return std::string("lambda_functions.slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9-]{1,118}[a-z0-9]$)RX"))) return std::string("lambda_functions.slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_functions_display_name(const std::string& value) {
    if (value.size() < 1) return std::string("lambda_functions.display_name must be at least 1 characters");
    if (value.size() > 200) return std::string("lambda_functions.display_name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_functions_function_body(const std::string& value) {
    if (value.size() < 1) return std::string("lambda_functions.function_body must be at least 1 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_functions_reuse_key(const std::string& value) {
    if (value.size() > 200) return std::string("lambda_functions.reuse_key must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_functions_idle_timeout_seconds(int32_t value) {
    if (value < 1) return std::string("lambda_functions.idle_timeout_seconds is below the minimum");
    if (value > 3600) return std::string("lambda_functions.idle_timeout_seconds is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_functions_max_run_ms(int32_t value) {
    if (value < 1000) return std::string("lambda_functions.max_run_ms is below the minimum");
    if (value > 300000) return std::string("lambda_functions.max_run_ms is above the maximum");
    return std::nullopt;
}

// Immutable published snapshots of lambda function code and runtime configuration.
inline const char* lambda_function_revisions_table = "lambda_function_revisions";
inline const std::vector<std::string> lambda_function_revisions_columns = { "id", "function_id", "revision_number", "definition_digest", "description", "runtime", "entry_command", "function_body", "reuse_key", "idle_timeout_seconds", "max_run_ms", "containerized", "container_image", "container_build_status", "container_build_error", "container_built_at", "env", "labels", "meta_data", "created_at", "created_by" };
inline const char* lambda_function_revisions_select_sql = R"SQL(select
      id::text as id,
      function_id::text as function_id,
      revision_number,
      definition_digest,
      description,
      runtime,
      entry_command,
      function_body,
      reuse_key,
      idle_timeout_seconds,
      max_run_ms,
      containerized,
      container_image,
      container_build_status,
      container_build_error,
      to_char(container_built_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as container_built_at,
      env::text as env_json,
      labels::text as labels_json,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      created_by::text as created_by
    from lambda_function_revisions)SQL";

enum class LambdaFunctionRevisionRuntime { Nodejs, Javascript, Typescript, Python3, Python, Ruby, Bash, Shell, Golang, Go, Dart, Erlang, Erl, Elixir, Ex, Java, Jvm, Gleam, Gleamlang, Rust, Rs, Browser };
inline std::string lambda_function_revisions_runtime_to_string(LambdaFunctionRevisionRuntime value) {
    switch (value) {
        case LambdaFunctionRevisionRuntime::Nodejs: return "nodejs";
        case LambdaFunctionRevisionRuntime::Javascript: return "javascript";
        case LambdaFunctionRevisionRuntime::Typescript: return "typescript";
        case LambdaFunctionRevisionRuntime::Python3: return "python3";
        case LambdaFunctionRevisionRuntime::Python: return "python";
        case LambdaFunctionRevisionRuntime::Ruby: return "ruby";
        case LambdaFunctionRevisionRuntime::Bash: return "bash";
        case LambdaFunctionRevisionRuntime::Shell: return "shell";
        case LambdaFunctionRevisionRuntime::Golang: return "golang";
        case LambdaFunctionRevisionRuntime::Go: return "go";
        case LambdaFunctionRevisionRuntime::Dart: return "dart";
        case LambdaFunctionRevisionRuntime::Erlang: return "erlang";
        case LambdaFunctionRevisionRuntime::Erl: return "erl";
        case LambdaFunctionRevisionRuntime::Elixir: return "elixir";
        case LambdaFunctionRevisionRuntime::Ex: return "ex";
        case LambdaFunctionRevisionRuntime::Java: return "java";
        case LambdaFunctionRevisionRuntime::Jvm: return "jvm";
        case LambdaFunctionRevisionRuntime::Gleam: return "gleam";
        case LambdaFunctionRevisionRuntime::Gleamlang: return "gleamlang";
        case LambdaFunctionRevisionRuntime::Rust: return "rust";
        case LambdaFunctionRevisionRuntime::Rs: return "rs";
        case LambdaFunctionRevisionRuntime::Browser: return "browser";
    }
    return "";
}
inline std::optional<LambdaFunctionRevisionRuntime> parse_lambda_function_revisions_runtime(const std::string& value) {
    if (value == "nodejs") return LambdaFunctionRevisionRuntime::Nodejs;
    if (value == "javascript") return LambdaFunctionRevisionRuntime::Javascript;
    if (value == "typescript") return LambdaFunctionRevisionRuntime::Typescript;
    if (value == "python3") return LambdaFunctionRevisionRuntime::Python3;
    if (value == "python") return LambdaFunctionRevisionRuntime::Python;
    if (value == "ruby") return LambdaFunctionRevisionRuntime::Ruby;
    if (value == "bash") return LambdaFunctionRevisionRuntime::Bash;
    if (value == "shell") return LambdaFunctionRevisionRuntime::Shell;
    if (value == "golang") return LambdaFunctionRevisionRuntime::Golang;
    if (value == "go") return LambdaFunctionRevisionRuntime::Go;
    if (value == "dart") return LambdaFunctionRevisionRuntime::Dart;
    if (value == "erlang") return LambdaFunctionRevisionRuntime::Erlang;
    if (value == "erl") return LambdaFunctionRevisionRuntime::Erl;
    if (value == "elixir") return LambdaFunctionRevisionRuntime::Elixir;
    if (value == "ex") return LambdaFunctionRevisionRuntime::Ex;
    if (value == "java") return LambdaFunctionRevisionRuntime::Java;
    if (value == "jvm") return LambdaFunctionRevisionRuntime::Jvm;
    if (value == "gleam") return LambdaFunctionRevisionRuntime::Gleam;
    if (value == "gleamlang") return LambdaFunctionRevisionRuntime::Gleamlang;
    if (value == "rust") return LambdaFunctionRevisionRuntime::Rust;
    if (value == "rs") return LambdaFunctionRevisionRuntime::Rs;
    if (value == "browser") return LambdaFunctionRevisionRuntime::Browser;
    return std::nullopt;
}

enum class LambdaFunctionRevisionContainerBuildStatus { NotRequested, Pending, Building, Built, Failed, Skipped };
inline std::string lambda_function_revisions_container_build_status_to_string(LambdaFunctionRevisionContainerBuildStatus value) {
    switch (value) {
        case LambdaFunctionRevisionContainerBuildStatus::NotRequested: return "not_requested";
        case LambdaFunctionRevisionContainerBuildStatus::Pending: return "pending";
        case LambdaFunctionRevisionContainerBuildStatus::Building: return "building";
        case LambdaFunctionRevisionContainerBuildStatus::Built: return "built";
        case LambdaFunctionRevisionContainerBuildStatus::Failed: return "failed";
        case LambdaFunctionRevisionContainerBuildStatus::Skipped: return "skipped";
    }
    return "";
}
inline std::optional<LambdaFunctionRevisionContainerBuildStatus> parse_lambda_function_revisions_container_build_status(const std::string& value) {
    if (value == "not_requested") return LambdaFunctionRevisionContainerBuildStatus::NotRequested;
    if (value == "pending") return LambdaFunctionRevisionContainerBuildStatus::Pending;
    if (value == "building") return LambdaFunctionRevisionContainerBuildStatus::Building;
    if (value == "built") return LambdaFunctionRevisionContainerBuildStatus::Built;
    if (value == "failed") return LambdaFunctionRevisionContainerBuildStatus::Failed;
    if (value == "skipped") return LambdaFunctionRevisionContainerBuildStatus::Skipped;
    return std::nullopt;
}

struct LambdaFunctionRevisionRow {
    std::string id;
    std::string function_id;
    int64_t revision_number;
    std::string definition_digest;
    std::string description;
    std::string runtime;
    std::string entry_command;
    std::string function_body;
    std::optional<std::string> reuse_key;
    int32_t idle_timeout_seconds;
    int32_t max_run_ms;
    bool containerized;
    std::optional<std::string> container_image;
    std::string container_build_status;
    std::optional<std::string> container_build_error;
    std::optional<std::string> container_built_at;
    std::string env;
    std::string labels;
    std::string meta_data;
    std::string created_at;
    std::optional<std::string> created_by;
};

inline LambdaFunctionRevisionRow lambda_function_revisions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    LambdaFunctionRevisionRow row;
    (void)is_null;
    row.id = get(0);
    row.function_id = get(1);
    row.revision_number = std::stoll(get(2));
    row.definition_digest = get(3);
    row.description = get(4);
    row.runtime = get(5);
    row.entry_command = get(6);
    row.function_body = get(7);
    row.reuse_key = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.idle_timeout_seconds = std::stoi(get(9));
    row.max_run_ms = std::stoi(get(10));
    row.containerized = (get(11) == "t");
    row.container_image = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.container_build_status = get(13);
    row.container_build_error = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.container_built_at = is_null(15) ? std::nullopt : std::optional<std::string>(get(15));
    row.env = get(16);
    row.labels = get(17);
    row.meta_data = get(18);
    row.created_at = get(19);
    row.created_by = is_null(20) ? std::nullopt : std::optional<std::string>(get(20));
    return row;
}
inline std::optional<std::string> validate_lambda_function_revisions_revision_number(int64_t value) {
    if (value < 1) return std::string("lambda_function_revisions.revision_number is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_function_revisions_definition_digest(const std::string& value) {
    if (value.size() < 64) return std::string("lambda_function_revisions.definition_digest must be at least 64 characters");
    if (value.size() > 64) return std::string("lambda_function_revisions.definition_digest must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("lambda_function_revisions.definition_digest does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_function_revisions_description(const std::string& value) {
    if (value.size() > 4096) return std::string("lambda_function_revisions.description must be at most 4096 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_function_revisions_function_body(const std::string& value) {
    if (value.size() < 1) return std::string("lambda_function_revisions.function_body must be at least 1 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_function_revisions_reuse_key(const std::string& value) {
    if (value.size() > 200) return std::string("lambda_function_revisions.reuse_key must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_function_revisions_idle_timeout_seconds(int32_t value) {
    if (value < 1) return std::string("lambda_function_revisions.idle_timeout_seconds is below the minimum");
    if (value > 3600) return std::string("lambda_function_revisions.idle_timeout_seconds is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_function_revisions_max_run_ms(int32_t value) {
    if (value < 1000) return std::string("lambda_function_revisions.max_run_ms is below the minimum");
    if (value > 300000) return std::string("lambda_function_revisions.max_run_ms is above the maximum");
    return std::nullopt;
}

// Named weighted routing policies over immutable lambda function revisions.
inline const char* lambda_function_aliases_table = "lambda_function_aliases";
inline const std::vector<std::string> lambda_function_aliases_columns = { "id", "function_id", "name", "description", "traffic", "routing_version", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* lambda_function_aliases_select_sql = R"SQL(select
      id::text as id,
      function_id::text as function_id,
      name,
      description,
      traffic::text as traffic_json,
      routing_version,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from lambda_function_aliases)SQL";

struct LambdaFunctionAliasRow {
    std::string id;
    std::string function_id;
    std::string name;
    std::string description;
    std::string traffic;
    int64_t routing_version;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline LambdaFunctionAliasRow lambda_function_aliases_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    LambdaFunctionAliasRow row;
    (void)is_null;
    row.id = get(0);
    row.function_id = get(1);
    row.name = get(2);
    row.description = get(3);
    row.traffic = get(4);
    row.routing_version = std::stoll(get(5));
    row.created_at = get(6);
    row.updated_at = get(7);
    row.created_by = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.updated_by = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    return row;
}
inline std::optional<std::string> validate_lambda_function_aliases_name(const std::string& value) {
    if (value.size() < 1) return std::string("lambda_function_aliases.name must be at least 1 characters");
    if (value.size() > 64) return std::string("lambda_function_aliases.name must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z][a-z0-9._-]{0,63}$)RX"))) return std::string("lambda_function_aliases.name does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_function_aliases_description(const std::string& value) {
    if (value.size() > 4096) return std::string("lambda_function_aliases.description must be at most 4096 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_function_aliases_routing_version(int64_t value) {
    if (value < 1) return std::string("lambda_function_aliases.routing_version is below the minimum");
    return std::nullopt;
}

// Durable state, alarms, and cross-replica execution leases for keyed serverless actors.
inline const char* lambda_actor_instances_table = "lambda_actor_instances";
inline const std::vector<std::string> lambda_actor_instances_columns = { "id", "function_id", "actor_key", "state", "state_version", "alarm_at", "alarm_attempt", "lease_owner", "lease_until", "last_invoked_at", "last_error", "created_at", "updated_at" };
inline const char* lambda_actor_instances_select_sql = R"SQL(select
      id::text as id,
      function_id::text as function_id,
      actor_key,
      state::text as state_json,
      state_version,
      to_char(alarm_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as alarm_at,
      alarm_attempt,
      lease_owner,
      to_char(lease_until at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as lease_until,
      to_char(last_invoked_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_invoked_at,
      last_error,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from lambda_actor_instances)SQL";

struct LambdaActorInstanceRow {
    std::string id;
    std::string function_id;
    std::string actor_key;
    std::string state;
    int64_t state_version;
    std::optional<std::string> alarm_at;
    int32_t alarm_attempt;
    std::optional<std::string> lease_owner;
    std::optional<std::string> lease_until;
    std::optional<std::string> last_invoked_at;
    std::optional<std::string> last_error;
    std::string created_at;
    std::string updated_at;
};

inline LambdaActorInstanceRow lambda_actor_instances_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    LambdaActorInstanceRow row;
    (void)is_null;
    row.id = get(0);
    row.function_id = get(1);
    row.actor_key = get(2);
    row.state = get(3);
    row.state_version = std::stoll(get(4));
    row.alarm_at = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.alarm_attempt = std::stoi(get(6));
    row.lease_owner = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.lease_until = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.last_invoked_at = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.last_error = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}
inline std::optional<std::string> validate_lambda_actor_instances_actor_key(const std::string& value) {
    if (value.size() < 1) return std::string("lambda_actor_instances.actor_key must be at least 1 characters");
    if (value.size() > 200) return std::string("lambda_actor_instances.actor_key must be at most 200 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9][A-Za-z0-9._:-]{0,199}$)RX"))) return std::string("lambda_actor_instances.actor_key does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_actor_instances_state_version(int64_t value) {
    if (value < 0) return std::string("lambda_actor_instances.state_version is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_actor_instances_alarm_attempt(int32_t value) {
    if (value < 0) return std::string("lambda_actor_instances.alarm_attempt is below the minimum");
    if (value > 6) return std::string("lambda_actor_instances.alarm_attempt is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_lambda_actor_instances_lease_owner(const std::string& value) {
    if (value.size() > 200) return std::string("lambda_actor_instances.lease_owner must be at most 200 characters");
    return std::nullopt;
}

inline const char* workflow_definitions_table = "workflow_definitions";
inline const std::vector<std::string> workflow_definitions_columns = { "id", "slug", "display_name", "description", "steps", "default_retry", "status", "labels", "meta_data", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* workflow_definitions_select_sql = R"SQL(select
      id::text as id,
      slug,
      display_name,
      description,
      steps::text as steps_json,
      default_retry::text as default_retry_json,
      status,
      labels::text as labels_json,
      meta_data::text as meta_data_json,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from workflow_definitions)SQL";

enum class WorkflowDefinitionsStatus { Draft, Active, Paused, Archived };
inline std::string workflow_definitions_status_to_string(WorkflowDefinitionsStatus value) {
    switch (value) {
        case WorkflowDefinitionsStatus::Draft: return "draft";
        case WorkflowDefinitionsStatus::Active: return "active";
        case WorkflowDefinitionsStatus::Paused: return "paused";
        case WorkflowDefinitionsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<WorkflowDefinitionsStatus> parse_workflow_definitions_status(const std::string& value) {
    if (value == "draft") return WorkflowDefinitionsStatus::Draft;
    if (value == "active") return WorkflowDefinitionsStatus::Active;
    if (value == "paused") return WorkflowDefinitionsStatus::Paused;
    if (value == "archived") return WorkflowDefinitionsStatus::Archived;
    return std::nullopt;
}

struct WorkflowDefinitionsRow {
    std::string id;
    std::string slug;
    std::string display_name;
    std::string description;
    std::string steps;
    std::string default_retry;
    std::string status;
    std::string labels;
    std::string meta_data;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline WorkflowDefinitionsRow workflow_definitions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    WorkflowDefinitionsRow row;
    (void)is_null;
    row.id = get(0);
    row.slug = get(1);
    row.display_name = get(2);
    row.description = get(3);
    row.steps = get(4);
    row.default_retry = get(5);
    row.status = get(6);
    row.labels = get(7);
    row.meta_data = get(8);
    row.is_soft_deleted = (get(9) == "t");
    row.created_at = get(10);
    row.updated_at = get(11);
    row.created_by = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.updated_by = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    return row;
}
inline std::optional<std::string> validate_workflow_definitions_slug(const std::string& value) {
    if (value.size() > 120) return std::string("workflow_definitions.slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9-]{1,118}[a-z0-9]$)RX"))) return std::string("workflow_definitions.slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_workflow_definitions_display_name(const std::string& value) {
    if (value.size() > 200) return std::string("workflow_definitions.display_name must be at most 200 characters");
    return std::nullopt;
}

inline const char* workflow_runs_table = "workflow_runs";
inline const std::vector<std::string> workflow_runs_columns = { "id", "definition_id", "definition_slug", "status", "current_step_index", "attempt", "input", "context", "output", "last_error", "wake_at", "wait_deadline", "lease_until", "signals", "idempotency_key", "started_at", "finished_at", "created_at", "updated_at", "created_by" };
inline const char* workflow_runs_select_sql = R"SQL(select
      id::text as id,
      definition_id::text as definition_id,
      definition_slug,
      status,
      current_step_index,
      attempt,
      input::text as input_json,
      context::text as context_json,
      output::text as output_json,
      last_error,
      to_char(wake_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as wake_at,
      to_char(wait_deadline at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as wait_deadline,
      to_char(lease_until at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as lease_until,
      signals::text as signals_json,
      idempotency_key,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(finished_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as finished_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by
    from workflow_runs)SQL";

enum class WorkflowRunsStatus { Pending, Running, Sleeping, Waiting, Completed, Failed, Canceled };
inline std::string workflow_runs_status_to_string(WorkflowRunsStatus value) {
    switch (value) {
        case WorkflowRunsStatus::Pending: return "pending";
        case WorkflowRunsStatus::Running: return "running";
        case WorkflowRunsStatus::Sleeping: return "sleeping";
        case WorkflowRunsStatus::Waiting: return "waiting";
        case WorkflowRunsStatus::Completed: return "completed";
        case WorkflowRunsStatus::Failed: return "failed";
        case WorkflowRunsStatus::Canceled: return "canceled";
    }
    return "";
}
inline std::optional<WorkflowRunsStatus> parse_workflow_runs_status(const std::string& value) {
    if (value == "pending") return WorkflowRunsStatus::Pending;
    if (value == "running") return WorkflowRunsStatus::Running;
    if (value == "sleeping") return WorkflowRunsStatus::Sleeping;
    if (value == "waiting") return WorkflowRunsStatus::Waiting;
    if (value == "completed") return WorkflowRunsStatus::Completed;
    if (value == "failed") return WorkflowRunsStatus::Failed;
    if (value == "canceled") return WorkflowRunsStatus::Canceled;
    return std::nullopt;
}

struct WorkflowRunsRow {
    std::string id;
    std::string definition_id;
    std::string definition_slug;
    std::string status;
    int32_t current_step_index;
    int32_t attempt;
    std::string input;
    std::string context;
    std::optional<std::string> output;
    std::optional<std::string> last_error;
    std::optional<std::string> wake_at;
    std::optional<std::string> wait_deadline;
    std::optional<std::string> lease_until;
    std::string signals;
    std::optional<std::string> idempotency_key;
    std::optional<std::string> started_at;
    std::optional<std::string> finished_at;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
};

inline WorkflowRunsRow workflow_runs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    WorkflowRunsRow row;
    (void)is_null;
    row.id = get(0);
    row.definition_id = get(1);
    row.definition_slug = get(2);
    row.status = get(3);
    row.current_step_index = std::stoi(get(4));
    row.attempt = std::stoi(get(5));
    row.input = get(6);
    row.context = get(7);
    row.output = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.last_error = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.wake_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.wait_deadline = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.lease_until = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.signals = get(13);
    row.idempotency_key = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.started_at = is_null(15) ? std::nullopt : std::optional<std::string>(get(15));
    row.finished_at = is_null(16) ? std::nullopt : std::optional<std::string>(get(16));
    row.created_at = get(17);
    row.updated_at = get(18);
    row.created_by = is_null(19) ? std::nullopt : std::optional<std::string>(get(19));
    return row;
}
inline std::optional<std::string> validate_workflow_runs_definition_slug(const std::string& value) {
    if (value.size() > 120) return std::string("workflow_runs.definition_slug must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_workflow_runs_current_step_index(int32_t value) {
    if (value < 0) return std::string("workflow_runs.current_step_index is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_workflow_runs_attempt(int32_t value) {
    if (value < 0) return std::string("workflow_runs.attempt is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_workflow_runs_idempotency_key(const std::string& value) {
    if (value.size() > 200) return std::string("workflow_runs.idempotency_key must be at most 200 characters");
    return std::nullopt;
}

inline const char* workflow_step_runs_table = "workflow_step_runs";
inline const std::vector<std::string> workflow_step_runs_columns = { "id", "run_id", "step_index", "step_name", "step_type", "function_ref", "attempt", "status", "input", "output", "error", "duration_ms", "started_at", "finished_at" };
inline const char* workflow_step_runs_select_sql = R"SQL(select
      id::text as id,
      run_id::text as run_id,
      step_index,
      step_name,
      step_type,
      function_ref,
      attempt,
      status,
      input::text as input_json,
      output::text as output_json,
      error,
      duration_ms,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(finished_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as finished_at
    from workflow_step_runs)SQL";

enum class WorkflowStepRunsStatus { Running, Succeeded, Failed };
inline std::string workflow_step_runs_status_to_string(WorkflowStepRunsStatus value) {
    switch (value) {
        case WorkflowStepRunsStatus::Running: return "running";
        case WorkflowStepRunsStatus::Succeeded: return "succeeded";
        case WorkflowStepRunsStatus::Failed: return "failed";
    }
    return "";
}
inline std::optional<WorkflowStepRunsStatus> parse_workflow_step_runs_status(const std::string& value) {
    if (value == "running") return WorkflowStepRunsStatus::Running;
    if (value == "succeeded") return WorkflowStepRunsStatus::Succeeded;
    if (value == "failed") return WorkflowStepRunsStatus::Failed;
    return std::nullopt;
}

struct WorkflowStepRunsRow {
    std::string id;
    std::string run_id;
    int32_t step_index;
    std::string step_name;
    std::string step_type;
    std::string function_ref;
    int32_t attempt;
    std::string status;
    std::optional<std::string> input;
    std::optional<std::string> output;
    std::optional<std::string> error;
    std::optional<int32_t> duration_ms;
    std::string started_at;
    std::optional<std::string> finished_at;
};

inline WorkflowStepRunsRow workflow_step_runs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    WorkflowStepRunsRow row;
    (void)is_null;
    row.id = get(0);
    row.run_id = get(1);
    row.step_index = std::stoi(get(2));
    row.step_name = get(3);
    row.step_type = get(4);
    row.function_ref = get(5);
    row.attempt = std::stoi(get(6));
    row.status = get(7);
    row.input = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.output = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.error = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.duration_ms = is_null(11) ? std::nullopt : std::optional<int32_t>(std::stoi(get(11)));
    row.started_at = get(12);
    row.finished_at = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    return row;
}
inline std::optional<std::string> validate_workflow_step_runs_step_index(int32_t value) {
    if (value < 0) return std::string("workflow_step_runs.step_index is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_workflow_step_runs_step_name(const std::string& value) {
    if (value.size() > 200) return std::string("workflow_step_runs.step_name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_workflow_step_runs_step_type(const std::string& value) {
    if (value.size() > 32) return std::string("workflow_step_runs.step_type must be at most 32 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_workflow_step_runs_function_ref(const std::string& value) {
    if (value.size() > 200) return std::string("workflow_step_runs.function_ref must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_workflow_step_runs_attempt(int32_t value) {
    if (value < 0) return std::string("workflow_step_runs.attempt is below the minimum");
    return std::nullopt;
}

inline const char* container_pool_image_revisions_table = "container_pool_image_revisions";
inline const std::vector<std::string> container_pool_image_revisions_columns = { "id", "image_slug", "image_ref", "dockerfile_path", "build_context", "dockerfile_text", "dockerfile_sha256", "source", "notes", "status", "meta_data", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* container_pool_image_revisions_select_sql = R"SQL(select
      id::text as id,
      image_slug,
      image_ref,
      dockerfile_path,
      build_context,
      dockerfile_text,
      dockerfile_sha256,
      source,
      notes,
      status,
      meta_data::text as meta_data_json,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from container_pool_image_revisions)SQL";

enum class ContainerPoolImageRevisionsSource { DiskDefault, User, System };
inline std::string container_pool_image_revisions_source_to_string(ContainerPoolImageRevisionsSource value) {
    switch (value) {
        case ContainerPoolImageRevisionsSource::DiskDefault: return "disk-default";
        case ContainerPoolImageRevisionsSource::User: return "user";
        case ContainerPoolImageRevisionsSource::System: return "system";
    }
    return "";
}
inline std::optional<ContainerPoolImageRevisionsSource> parse_container_pool_image_revisions_source(const std::string& value) {
    if (value == "disk-default") return ContainerPoolImageRevisionsSource::DiskDefault;
    if (value == "user") return ContainerPoolImageRevisionsSource::User;
    if (value == "system") return ContainerPoolImageRevisionsSource::System;
    return std::nullopt;
}

enum class ContainerPoolImageRevisionsStatus { Candidate, Active, Archived };
inline std::string container_pool_image_revisions_status_to_string(ContainerPoolImageRevisionsStatus value) {
    switch (value) {
        case ContainerPoolImageRevisionsStatus::Candidate: return "candidate";
        case ContainerPoolImageRevisionsStatus::Active: return "active";
        case ContainerPoolImageRevisionsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<ContainerPoolImageRevisionsStatus> parse_container_pool_image_revisions_status(const std::string& value) {
    if (value == "candidate") return ContainerPoolImageRevisionsStatus::Candidate;
    if (value == "active") return ContainerPoolImageRevisionsStatus::Active;
    if (value == "archived") return ContainerPoolImageRevisionsStatus::Archived;
    return std::nullopt;
}

struct ContainerPoolImageRevisionsRow {
    std::string id;
    std::string image_slug;
    std::string image_ref;
    std::string dockerfile_path;
    std::string build_context;
    std::string dockerfile_text;
    std::string dockerfile_sha256;
    std::string source;
    std::string notes;
    std::string status;
    std::string meta_data;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline ContainerPoolImageRevisionsRow container_pool_image_revisions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    ContainerPoolImageRevisionsRow row;
    (void)is_null;
    row.id = get(0);
    row.image_slug = get(1);
    row.image_ref = get(2);
    row.dockerfile_path = get(3);
    row.build_context = get(4);
    row.dockerfile_text = get(5);
    row.dockerfile_sha256 = get(6);
    row.source = get(7);
    row.notes = get(8);
    row.status = get(9);
    row.meta_data = get(10);
    row.is_soft_deleted = (get(11) == "t");
    row.created_at = get(12);
    row.updated_at = get(13);
    row.created_by = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.updated_by = is_null(15) ? std::nullopt : std::optional<std::string>(get(15));
    return row;
}
inline std::optional<std::string> validate_container_pool_image_revisions_image_slug(const std::string& value) {
    if (value.size() > 120) return std::string("container_pool_image_revisions.image_slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9-]{0,118}[a-z0-9]$)RX"))) return std::string("container_pool_image_revisions.image_slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_container_pool_image_revisions_dockerfile_sha256(const std::string& value) {
    if (value.size() > 64) return std::string("container_pool_image_revisions.dockerfile_sha256 must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9a-f]{64}$)RX"))) return std::string("container_pool_image_revisions.dockerfile_sha256 does not match the required pattern");
    return std::nullopt;
}

inline const char* container_pool_build_runs_table = "container_pool_build_runs";
inline const std::vector<std::string> container_pool_build_runs_columns = { "id", "image_slug", "revision_id", "image_ref", "candidate_tag", "build_status", "test_status", "overall_status", "test_command", "build_started_at", "build_finished_at", "test_started_at", "test_finished_at", "build_log_excerpt", "test_log_excerpt", "error_message", "triggered_by", "meta_data", "is_soft_deleted", "created_at", "updated_at" };
inline const char* container_pool_build_runs_select_sql = R"SQL(select
      id::text as id,
      image_slug,
      revision_id::text as revision_id,
      image_ref,
      candidate_tag,
      build_status,
      test_status,
      overall_status,
      test_command,
      to_char(build_started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as build_started_at,
      to_char(build_finished_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as build_finished_at,
      to_char(test_started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as test_started_at,
      to_char(test_finished_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as test_finished_at,
      build_log_excerpt,
      test_log_excerpt,
      error_message,
      triggered_by::text as triggered_by,
      meta_data::text as meta_data_json,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from container_pool_build_runs)SQL";

enum class ContainerPoolBuildRunsBuildStatus { Queued, Building, Built, Failed, Skipped, Cancelled };
inline std::string container_pool_build_runs_build_status_to_string(ContainerPoolBuildRunsBuildStatus value) {
    switch (value) {
        case ContainerPoolBuildRunsBuildStatus::Queued: return "queued";
        case ContainerPoolBuildRunsBuildStatus::Building: return "building";
        case ContainerPoolBuildRunsBuildStatus::Built: return "built";
        case ContainerPoolBuildRunsBuildStatus::Failed: return "failed";
        case ContainerPoolBuildRunsBuildStatus::Skipped: return "skipped";
        case ContainerPoolBuildRunsBuildStatus::Cancelled: return "cancelled";
    }
    return "";
}
inline std::optional<ContainerPoolBuildRunsBuildStatus> parse_container_pool_build_runs_build_status(const std::string& value) {
    if (value == "queued") return ContainerPoolBuildRunsBuildStatus::Queued;
    if (value == "building") return ContainerPoolBuildRunsBuildStatus::Building;
    if (value == "built") return ContainerPoolBuildRunsBuildStatus::Built;
    if (value == "failed") return ContainerPoolBuildRunsBuildStatus::Failed;
    if (value == "skipped") return ContainerPoolBuildRunsBuildStatus::Skipped;
    if (value == "cancelled") return ContainerPoolBuildRunsBuildStatus::Cancelled;
    return std::nullopt;
}

enum class ContainerPoolBuildRunsTestStatus { NotStarted, Pending, Testing, Passed, Failed, Skipped, Cancelled };
inline std::string container_pool_build_runs_test_status_to_string(ContainerPoolBuildRunsTestStatus value) {
    switch (value) {
        case ContainerPoolBuildRunsTestStatus::NotStarted: return "not_started";
        case ContainerPoolBuildRunsTestStatus::Pending: return "pending";
        case ContainerPoolBuildRunsTestStatus::Testing: return "testing";
        case ContainerPoolBuildRunsTestStatus::Passed: return "passed";
        case ContainerPoolBuildRunsTestStatus::Failed: return "failed";
        case ContainerPoolBuildRunsTestStatus::Skipped: return "skipped";
        case ContainerPoolBuildRunsTestStatus::Cancelled: return "cancelled";
    }
    return "";
}
inline std::optional<ContainerPoolBuildRunsTestStatus> parse_container_pool_build_runs_test_status(const std::string& value) {
    if (value == "not_started") return ContainerPoolBuildRunsTestStatus::NotStarted;
    if (value == "pending") return ContainerPoolBuildRunsTestStatus::Pending;
    if (value == "testing") return ContainerPoolBuildRunsTestStatus::Testing;
    if (value == "passed") return ContainerPoolBuildRunsTestStatus::Passed;
    if (value == "failed") return ContainerPoolBuildRunsTestStatus::Failed;
    if (value == "skipped") return ContainerPoolBuildRunsTestStatus::Skipped;
    if (value == "cancelled") return ContainerPoolBuildRunsTestStatus::Cancelled;
    return std::nullopt;
}

enum class ContainerPoolBuildRunsOverallStatus { Queued, Running, Passed, Failed, Cancelled, Errored };
inline std::string container_pool_build_runs_overall_status_to_string(ContainerPoolBuildRunsOverallStatus value) {
    switch (value) {
        case ContainerPoolBuildRunsOverallStatus::Queued: return "queued";
        case ContainerPoolBuildRunsOverallStatus::Running: return "running";
        case ContainerPoolBuildRunsOverallStatus::Passed: return "passed";
        case ContainerPoolBuildRunsOverallStatus::Failed: return "failed";
        case ContainerPoolBuildRunsOverallStatus::Cancelled: return "cancelled";
        case ContainerPoolBuildRunsOverallStatus::Errored: return "errored";
    }
    return "";
}
inline std::optional<ContainerPoolBuildRunsOverallStatus> parse_container_pool_build_runs_overall_status(const std::string& value) {
    if (value == "queued") return ContainerPoolBuildRunsOverallStatus::Queued;
    if (value == "running") return ContainerPoolBuildRunsOverallStatus::Running;
    if (value == "passed") return ContainerPoolBuildRunsOverallStatus::Passed;
    if (value == "failed") return ContainerPoolBuildRunsOverallStatus::Failed;
    if (value == "cancelled") return ContainerPoolBuildRunsOverallStatus::Cancelled;
    if (value == "errored") return ContainerPoolBuildRunsOverallStatus::Errored;
    return std::nullopt;
}

struct ContainerPoolBuildRunsRow {
    std::string id;
    std::string image_slug;
    std::string revision_id;
    std::string image_ref;
    std::string candidate_tag;
    std::string build_status;
    std::string test_status;
    std::string overall_status;
    std::string test_command;
    std::optional<std::string> build_started_at;
    std::optional<std::string> build_finished_at;
    std::optional<std::string> test_started_at;
    std::optional<std::string> test_finished_at;
    std::string build_log_excerpt;
    std::string test_log_excerpt;
    std::optional<std::string> error_message;
    std::optional<std::string> triggered_by;
    std::string meta_data;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
};

inline ContainerPoolBuildRunsRow container_pool_build_runs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    ContainerPoolBuildRunsRow row;
    (void)is_null;
    row.id = get(0);
    row.image_slug = get(1);
    row.revision_id = get(2);
    row.image_ref = get(3);
    row.candidate_tag = get(4);
    row.build_status = get(5);
    row.test_status = get(6);
    row.overall_status = get(7);
    row.test_command = get(8);
    row.build_started_at = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.build_finished_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.test_started_at = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.test_finished_at = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.build_log_excerpt = get(13);
    row.test_log_excerpt = get(14);
    row.error_message = is_null(15) ? std::nullopt : std::optional<std::string>(get(15));
    row.triggered_by = is_null(16) ? std::nullopt : std::optional<std::string>(get(16));
    row.meta_data = get(17);
    row.is_soft_deleted = (get(18) == "t");
    row.created_at = get(19);
    row.updated_at = get(20);
    return row;
}
inline std::optional<std::string> validate_container_pool_build_runs_image_slug(const std::string& value) {
    if (value.size() > 120) return std::string("container_pool_build_runs.image_slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9-]{0,118}[a-z0-9]$)RX"))) return std::string("container_pool_build_runs.image_slug does not match the required pattern");
    return std::nullopt;
}

inline const char* presence_convs_table = "presence_convs";
inline const std::vector<std::string> presence_convs_columns = { "id", "slug", "display_name", "status", "meta_data", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* presence_convs_select_sql = R"SQL(select
      id::text as id,
      slug,
      display_name,
      status,
      meta_data::text as meta_data_json,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from presence_convs)SQL";

enum class PresenceConvsStatus { Active, Paused, Archived };
inline std::string presence_convs_status_to_string(PresenceConvsStatus value) {
    switch (value) {
        case PresenceConvsStatus::Active: return "active";
        case PresenceConvsStatus::Paused: return "paused";
        case PresenceConvsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<PresenceConvsStatus> parse_presence_convs_status(const std::string& value) {
    if (value == "active") return PresenceConvsStatus::Active;
    if (value == "paused") return PresenceConvsStatus::Paused;
    if (value == "archived") return PresenceConvsStatus::Archived;
    return std::nullopt;
}

struct PresenceConvsRow {
    std::string id;
    std::string slug;
    std::string display_name;
    std::string status;
    std::string meta_data;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline PresenceConvsRow presence_convs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    PresenceConvsRow row;
    (void)is_null;
    row.id = get(0);
    row.slug = get(1);
    row.display_name = get(2);
    row.status = get(3);
    row.meta_data = get(4);
    row.is_soft_deleted = (get(5) == "t");
    row.created_at = get(6);
    row.updated_at = get(7);
    row.created_by = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.updated_by = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    return row;
}
inline std::optional<std::string> validate_presence_convs_slug(const std::string& value) {
    if (value.size() > 120) return std::string("presence_convs.slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,120}$)RX"))) return std::string("presence_convs.slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_presence_convs_display_name(const std::string& value) {
    if (value.size() > 200) return std::string("presence_convs.display_name must be at most 200 characters");
    return std::nullopt;
}

inline const char* presence_conv_members_table = "presence_conv_members";
inline const std::vector<std::string> presence_conv_members_columns = { "id", "conv_id", "user_id", "role", "status", "meta_data", "is_soft_deleted", "joined_at", "left_at", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* presence_conv_members_select_sql = R"SQL(select
      id::text as id,
      conv_id::text as conv_id,
      user_id::text as user_id,
      role,
      status,
      meta_data::text as meta_data_json,
      is_soft_deleted,
      to_char(joined_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as joined_at,
      to_char(left_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as left_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from presence_conv_members)SQL";

enum class PresenceConvMembersRole { Owner, Admin, Member, Guest, Bot };
inline std::string presence_conv_members_role_to_string(PresenceConvMembersRole value) {
    switch (value) {
        case PresenceConvMembersRole::Owner: return "owner";
        case PresenceConvMembersRole::Admin: return "admin";
        case PresenceConvMembersRole::Member: return "member";
        case PresenceConvMembersRole::Guest: return "guest";
        case PresenceConvMembersRole::Bot: return "bot";
    }
    return "";
}
inline std::optional<PresenceConvMembersRole> parse_presence_conv_members_role(const std::string& value) {
    if (value == "owner") return PresenceConvMembersRole::Owner;
    if (value == "admin") return PresenceConvMembersRole::Admin;
    if (value == "member") return PresenceConvMembersRole::Member;
    if (value == "guest") return PresenceConvMembersRole::Guest;
    if (value == "bot") return PresenceConvMembersRole::Bot;
    return std::nullopt;
}

enum class PresenceConvMembersStatus { Active, Muted, Banned, Archived };
inline std::string presence_conv_members_status_to_string(PresenceConvMembersStatus value) {
    switch (value) {
        case PresenceConvMembersStatus::Active: return "active";
        case PresenceConvMembersStatus::Muted: return "muted";
        case PresenceConvMembersStatus::Banned: return "banned";
        case PresenceConvMembersStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<PresenceConvMembersStatus> parse_presence_conv_members_status(const std::string& value) {
    if (value == "active") return PresenceConvMembersStatus::Active;
    if (value == "muted") return PresenceConvMembersStatus::Muted;
    if (value == "banned") return PresenceConvMembersStatus::Banned;
    if (value == "archived") return PresenceConvMembersStatus::Archived;
    return std::nullopt;
}

struct PresenceConvMembersRow {
    std::string id;
    std::string conv_id;
    std::string user_id;
    std::string role;
    std::string status;
    std::string meta_data;
    bool is_soft_deleted;
    std::string joined_at;
    std::optional<std::string> left_at;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline PresenceConvMembersRow presence_conv_members_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    PresenceConvMembersRow row;
    (void)is_null;
    row.id = get(0);
    row.conv_id = get(1);
    row.user_id = get(2);
    row.role = get(3);
    row.status = get(4);
    row.meta_data = get(5);
    row.is_soft_deleted = (get(6) == "t");
    row.joined_at = get(7);
    row.left_at = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.created_at = get(9);
    row.updated_at = get(10);
    row.created_by = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.updated_by = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    return row;
}

inline const char* presence_users_table = "presence_users";
inline const std::vector<std::string> presence_users_columns = { "id", "slug", "updated_at" };
inline const char* presence_users_select_sql = R"SQL(select
      id::text as id,
      slug,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from presence_users)SQL";

struct PresenceUsersRow {
    std::string id;
    std::string slug;
    std::string updated_at;
};

inline PresenceUsersRow presence_users_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    PresenceUsersRow row;
    (void)is_null;
    row.id = get(0);
    row.slug = get(1);
    row.updated_at = get(2);
    return row;
}

inline const char* presence_events_table = "presence_events";
inline const std::vector<std::string> presence_events_columns = { "seq", "event_at", "op", "conv_id", "user_id", "conv_slug", "user_slug", "conv_shard", "user_shard", "soft_deleted" };
inline const char* presence_events_select_sql = R"SQL(select
      seq,
      to_char(event_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as event_at,
      op,
      conv_id::text as conv_id,
      user_id::text as user_id,
      conv_slug,
      user_slug,
      conv_shard,
      user_shard,
      soft_deleted
    from presence_events)SQL";

enum class PresenceEventsOp { INSERT, UPDATE, DELETE };
inline std::string presence_events_op_to_string(PresenceEventsOp value) {
    switch (value) {
        case PresenceEventsOp::INSERT: return "INSERT";
        case PresenceEventsOp::UPDATE: return "UPDATE";
        case PresenceEventsOp::DELETE: return "DELETE";
    }
    return "";
}
inline std::optional<PresenceEventsOp> parse_presence_events_op(const std::string& value) {
    if (value == "INSERT") return PresenceEventsOp::INSERT;
    if (value == "UPDATE") return PresenceEventsOp::UPDATE;
    if (value == "DELETE") return PresenceEventsOp::DELETE;
    return std::nullopt;
}

struct PresenceEventsRow {
    int64_t seq;
    std::string event_at;
    std::string op;
    std::string conv_id;
    std::string user_id;
    std::string conv_slug;
    std::string user_slug;
    int32_t conv_shard;
    int32_t user_shard;
    bool soft_deleted;
};

inline PresenceEventsRow presence_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    PresenceEventsRow row;
    (void)is_null;
    row.seq = std::stoll(get(0));
    row.event_at = get(1);
    row.op = get(2);
    row.conv_id = get(3);
    row.user_id = get(4);
    row.conv_slug = get(5);
    row.user_slug = get(6);
    row.conv_shard = std::stoi(get(7));
    row.user_shard = std::stoi(get(8));
    row.soft_deleted = (get(9) == "t");
    return row;
}

inline const char* presence_consumer_checkpoints_table = "presence_consumer_checkpoints";
inline const std::vector<std::string> presence_consumer_checkpoints_columns = { "consumer_id", "last_seq", "updated_at" };
inline const char* presence_consumer_checkpoints_select_sql = R"SQL(select
      consumer_id,
      last_seq,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from presence_consumer_checkpoints)SQL";

struct PresenceConsumerCheckpointsRow {
    std::string consumer_id;
    int64_t last_seq;
    std::string updated_at;
};

inline PresenceConsumerCheckpointsRow presence_consumer_checkpoints_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    PresenceConsumerCheckpointsRow row;
    (void)is_null;
    row.consumer_id = get(0);
    row.last_seq = std::stoll(get(1));
    row.updated_at = get(2);
    return row;
}

inline const char* des_soccer_learning_experiments_table = "des_soccer_learning_experiments";
inline const std::vector<std::string> des_soccer_learning_experiments_columns = { "id", "slug", "display_name", "description", "status", "config", "labels", "meta_data", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* des_soccer_learning_experiments_select_sql = R"SQL(select
      id::text as id,
      slug,
      display_name,
      description,
      status,
      config::text as config_json,
      labels::text as labels_json,
      meta_data::text as meta_data_json,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from des_soccer_learning_experiments)SQL";

enum class DesSoccerLearningExperimentsStatus { Active, Paused, Archived };
inline std::string des_soccer_learning_experiments_status_to_string(DesSoccerLearningExperimentsStatus value) {
    switch (value) {
        case DesSoccerLearningExperimentsStatus::Active: return "active";
        case DesSoccerLearningExperimentsStatus::Paused: return "paused";
        case DesSoccerLearningExperimentsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<DesSoccerLearningExperimentsStatus> parse_des_soccer_learning_experiments_status(const std::string& value) {
    if (value == "active") return DesSoccerLearningExperimentsStatus::Active;
    if (value == "paused") return DesSoccerLearningExperimentsStatus::Paused;
    if (value == "archived") return DesSoccerLearningExperimentsStatus::Archived;
    return std::nullopt;
}

struct DesSoccerLearningExperimentsRow {
    std::string id;
    std::string slug;
    std::string display_name;
    std::string description;
    std::string status;
    std::string config;
    std::string labels;
    std::string meta_data;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline DesSoccerLearningExperimentsRow des_soccer_learning_experiments_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerLearningExperimentsRow row;
    (void)is_null;
    row.id = get(0);
    row.slug = get(1);
    row.display_name = get(2);
    row.description = get(3);
    row.status = get(4);
    row.config = get(5);
    row.labels = get(6);
    row.meta_data = get(7);
    row.is_soft_deleted = (get(8) == "t");
    row.created_at = get(9);
    row.updated_at = get(10);
    row.created_by = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.updated_by = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    return row;
}
inline std::optional<std::string> validate_des_soccer_learning_experiments_slug(const std::string& value) {
    if (value.size() > 160) return std::string("des_soccer_learning_experiments.slug must be at most 160 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9._/-]{1,158}[a-z0-9]$)RX"))) return std::string("des_soccer_learning_experiments.slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_experiments_display_name(const std::string& value) {
    if (value.size() > 240) return std::string("des_soccer_learning_experiments.display_name must be at most 240 characters");
    return std::nullopt;
}

inline const char* des_soccer_learning_policy_versions_table = "des_soccer_learning_policy_versions";
inline const std::vector<std::string> des_soccer_learning_policy_versions_columns = { "id", "experiment_id", "parent_policy_version_id", "generation", "version_label", "source_kind", "status", "options", "config", "lineage", "metrics", "entry_count", "target_entry_count", "visit_count", "fitness_micros", "branch_key", "retention_kind", "full_entries_retained", "full_entries_pruned_at", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* des_soccer_learning_policy_versions_select_sql = R"SQL(select
      id::text as id,
      experiment_id::text as experiment_id,
      parent_policy_version_id::text as parent_policy_version_id,
      generation,
      version_label,
      source_kind,
      status,
      options::text as options_json,
      config::text as config_json,
      lineage::text as lineage_json,
      metrics::text as metrics_json,
      entry_count,
      target_entry_count,
      visit_count,
      fitness_micros,
      branch_key::text as branch_key,
      retention_kind,
      full_entries_retained,
      to_char(full_entries_pruned_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as full_entries_pruned_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from des_soccer_learning_policy_versions)SQL";

enum class DesSoccerLearningPolicyVersionsSourceKind { Seed, Merge, Mutation, Crossover, Import, Replay };
inline std::string des_soccer_learning_policy_versions_source_kind_to_string(DesSoccerLearningPolicyVersionsSourceKind value) {
    switch (value) {
        case DesSoccerLearningPolicyVersionsSourceKind::Seed: return "seed";
        case DesSoccerLearningPolicyVersionsSourceKind::Merge: return "merge";
        case DesSoccerLearningPolicyVersionsSourceKind::Mutation: return "mutation";
        case DesSoccerLearningPolicyVersionsSourceKind::Crossover: return "crossover";
        case DesSoccerLearningPolicyVersionsSourceKind::Import: return "import";
        case DesSoccerLearningPolicyVersionsSourceKind::Replay: return "replay";
    }
    return "";
}
inline std::optional<DesSoccerLearningPolicyVersionsSourceKind> parse_des_soccer_learning_policy_versions_source_kind(const std::string& value) {
    if (value == "seed") return DesSoccerLearningPolicyVersionsSourceKind::Seed;
    if (value == "merge") return DesSoccerLearningPolicyVersionsSourceKind::Merge;
    if (value == "mutation") return DesSoccerLearningPolicyVersionsSourceKind::Mutation;
    if (value == "crossover") return DesSoccerLearningPolicyVersionsSourceKind::Crossover;
    if (value == "import") return DesSoccerLearningPolicyVersionsSourceKind::Import;
    if (value == "replay") return DesSoccerLearningPolicyVersionsSourceKind::Replay;
    return std::nullopt;
}

enum class DesSoccerLearningPolicyVersionsStatus { Candidate, Active, Archived, Rejected };
inline std::string des_soccer_learning_policy_versions_status_to_string(DesSoccerLearningPolicyVersionsStatus value) {
    switch (value) {
        case DesSoccerLearningPolicyVersionsStatus::Candidate: return "candidate";
        case DesSoccerLearningPolicyVersionsStatus::Active: return "active";
        case DesSoccerLearningPolicyVersionsStatus::Archived: return "archived";
        case DesSoccerLearningPolicyVersionsStatus::Rejected: return "rejected";
    }
    return "";
}
inline std::optional<DesSoccerLearningPolicyVersionsStatus> parse_des_soccer_learning_policy_versions_status(const std::string& value) {
    if (value == "candidate") return DesSoccerLearningPolicyVersionsStatus::Candidate;
    if (value == "active") return DesSoccerLearningPolicyVersionsStatus::Active;
    if (value == "archived") return DesSoccerLearningPolicyVersionsStatus::Archived;
    if (value == "rejected") return DesSoccerLearningPolicyVersionsStatus::Rejected;
    return std::nullopt;
}

enum class DesSoccerLearningPolicyVersionsRetentionKind { BranchTip, RetainAll, MetadataOnly };
inline std::string des_soccer_learning_policy_versions_retention_kind_to_string(DesSoccerLearningPolicyVersionsRetentionKind value) {
    switch (value) {
        case DesSoccerLearningPolicyVersionsRetentionKind::BranchTip: return "branch_tip";
        case DesSoccerLearningPolicyVersionsRetentionKind::RetainAll: return "retain_all";
        case DesSoccerLearningPolicyVersionsRetentionKind::MetadataOnly: return "metadata_only";
    }
    return "";
}
inline std::optional<DesSoccerLearningPolicyVersionsRetentionKind> parse_des_soccer_learning_policy_versions_retention_kind(const std::string& value) {
    if (value == "branch_tip") return DesSoccerLearningPolicyVersionsRetentionKind::BranchTip;
    if (value == "retain_all") return DesSoccerLearningPolicyVersionsRetentionKind::RetainAll;
    if (value == "metadata_only") return DesSoccerLearningPolicyVersionsRetentionKind::MetadataOnly;
    return std::nullopt;
}

struct DesSoccerLearningPolicyVersionsRow {
    std::string id;
    std::string experiment_id;
    std::optional<std::string> parent_policy_version_id;
    int32_t generation;
    std::string version_label;
    std::string source_kind;
    std::string status;
    std::string options;
    std::string config;
    std::string lineage;
    std::string metrics;
    int32_t entry_count;
    int32_t target_entry_count;
    int64_t visit_count;
    int64_t fitness_micros;
    std::string branch_key;
    std::string retention_kind;
    bool full_entries_retained;
    std::optional<std::string> full_entries_pruned_at;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline DesSoccerLearningPolicyVersionsRow des_soccer_learning_policy_versions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerLearningPolicyVersionsRow row;
    (void)is_null;
    row.id = get(0);
    row.experiment_id = get(1);
    row.parent_policy_version_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.generation = std::stoi(get(3));
    row.version_label = get(4);
    row.source_kind = get(5);
    row.status = get(6);
    row.options = get(7);
    row.config = get(8);
    row.lineage = get(9);
    row.metrics = get(10);
    row.entry_count = std::stoi(get(11));
    row.target_entry_count = std::stoi(get(12));
    row.visit_count = std::stoll(get(13));
    row.fitness_micros = std::stoll(get(14));
    row.branch_key = get(15);
    row.retention_kind = get(16);
    row.full_entries_retained = (get(17) == "t");
    row.full_entries_pruned_at = is_null(18) ? std::nullopt : std::optional<std::string>(get(18));
    row.created_at = get(19);
    row.updated_at = get(20);
    row.created_by = is_null(21) ? std::nullopt : std::optional<std::string>(get(21));
    row.updated_by = is_null(22) ? std::nullopt : std::optional<std::string>(get(22));
    return row;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_versions_generation(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_policy_versions.generation is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_versions_version_label(const std::string& value) {
    if (value.size() > 160) return std::string("des_soccer_learning_policy_versions.version_label must be at most 160 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,160}$)RX"))) return std::string("des_soccer_learning_policy_versions.version_label does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_versions_entry_count(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_policy_versions.entry_count is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_versions_target_entry_count(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_policy_versions.target_entry_count is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_versions_visit_count(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_policy_versions.visit_count is below the minimum");
    return std::nullopt;
}

inline const char* des_soccer_learning_policy_entries_table = "des_soccer_learning_policy_entries";
inline const std::vector<std::string> des_soccer_learning_policy_entries_columns = { "id", "policy_version_id", "team", "entry_kind", "state_hash", "state_key", "action", "target_fine_cell_id", "target_tactical_cell_id", "target_macro_cell_id", "target_root_cell_id", "receiver_descriptor", "value_micros", "visits", "source_run_id", "created_at" };
inline const char* des_soccer_learning_policy_entries_select_sql = R"SQL(select
      id::text as id,
      policy_version_id::text as policy_version_id,
      team,
      entry_kind,
      state_hash,
      state_key::text as state_key_json,
      action,
      target_fine_cell_id,
      target_tactical_cell_id,
      target_macro_cell_id,
      target_root_cell_id,
      receiver_descriptor,
      value_micros,
      visits,
      source_run_id::text as source_run_id,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from des_soccer_learning_policy_entries)SQL";

enum class DesSoccerLearningPolicyEntriesTeam { Home, Away };
inline std::string des_soccer_learning_policy_entries_team_to_string(DesSoccerLearningPolicyEntriesTeam value) {
    switch (value) {
        case DesSoccerLearningPolicyEntriesTeam::Home: return "home";
        case DesSoccerLearningPolicyEntriesTeam::Away: return "away";
    }
    return "";
}
inline std::optional<DesSoccerLearningPolicyEntriesTeam> parse_des_soccer_learning_policy_entries_team(const std::string& value) {
    if (value == "home") return DesSoccerLearningPolicyEntriesTeam::Home;
    if (value == "away") return DesSoccerLearningPolicyEntriesTeam::Away;
    return std::nullopt;
}

enum class DesSoccerLearningPolicyEntriesEntryKind { Action, Target };
inline std::string des_soccer_learning_policy_entries_entry_kind_to_string(DesSoccerLearningPolicyEntriesEntryKind value) {
    switch (value) {
        case DesSoccerLearningPolicyEntriesEntryKind::Action: return "action";
        case DesSoccerLearningPolicyEntriesEntryKind::Target: return "target";
    }
    return "";
}
inline std::optional<DesSoccerLearningPolicyEntriesEntryKind> parse_des_soccer_learning_policy_entries_entry_kind(const std::string& value) {
    if (value == "action") return DesSoccerLearningPolicyEntriesEntryKind::Action;
    if (value == "target") return DesSoccerLearningPolicyEntriesEntryKind::Target;
    return std::nullopt;
}

struct DesSoccerLearningPolicyEntriesRow {
    std::string id;
    std::string policy_version_id;
    std::string team;
    std::string entry_kind;
    std::string state_hash;
    std::string state_key;
    std::string action;
    int32_t target_fine_cell_id;
    int32_t target_tactical_cell_id;
    int32_t target_macro_cell_id;
    int32_t target_root_cell_id;
    int32_t receiver_descriptor;
    int64_t value_micros;
    int32_t visits;
    std::optional<std::string> source_run_id;
    std::string created_at;
};

inline DesSoccerLearningPolicyEntriesRow des_soccer_learning_policy_entries_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerLearningPolicyEntriesRow row;
    (void)is_null;
    row.id = get(0);
    row.policy_version_id = get(1);
    row.team = get(2);
    row.entry_kind = get(3);
    row.state_hash = get(4);
    row.state_key = get(5);
    row.action = get(6);
    row.target_fine_cell_id = std::stoi(get(7));
    row.target_tactical_cell_id = std::stoi(get(8));
    row.target_macro_cell_id = std::stoi(get(9));
    row.target_root_cell_id = std::stoi(get(10));
    row.receiver_descriptor = std::stoi(get(11));
    row.value_micros = std::stoll(get(12));
    row.visits = std::stoi(get(13));
    row.source_run_id = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.created_at = get(15);
    return row;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_entries_state_hash(const std::string& value) {
    if (value.size() > 32) return std::string("des_soccer_learning_policy_entries.state_hash must be at most 32 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{16,32}$)RX"))) return std::string("des_soccer_learning_policy_entries.state_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_entries_action(const std::string& value) {
    if (value.size() > 80) return std::string("des_soccer_learning_policy_entries.action must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_entries_target_fine_cell_id(int32_t value) {
    if (value < -1) return std::string("des_soccer_learning_policy_entries.target_fine_cell_id is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_entries_target_tactical_cell_id(int32_t value) {
    if (value < -1) return std::string("des_soccer_learning_policy_entries.target_tactical_cell_id is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_entries_target_macro_cell_id(int32_t value) {
    if (value < -1) return std::string("des_soccer_learning_policy_entries.target_macro_cell_id is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_entries_target_root_cell_id(int32_t value) {
    if (value < -1) return std::string("des_soccer_learning_policy_entries.target_root_cell_id is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_entries_receiver_descriptor(int32_t value) {
    if (value < -1) return std::string("des_soccer_learning_policy_entries.receiver_descriptor is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_policy_entries_visits(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_policy_entries.visits is below the minimum");
    return std::nullopt;
}

inline const char* des_soccer_learning_jobs_table = "des_soccer_learning_jobs";
inline const std::vector<std::string> des_soccer_learning_jobs_columns = { "id", "experiment_id", "base_policy_version_id", "spawn_strategy", "status", "priority", "seed", "attempt", "max_attempts", "lease_owner", "lease_expires_at", "started_at", "finished_at", "config", "runner_config", "result_run_id", "error", "created_at", "updated_at" };
inline const char* des_soccer_learning_jobs_select_sql = R"SQL(select
      id::text as id,
      experiment_id::text as experiment_id,
      base_policy_version_id::text as base_policy_version_id,
      spawn_strategy,
      status,
      priority,
      seed,
      attempt,
      max_attempts,
      lease_owner,
      to_char(lease_expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as lease_expires_at,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(finished_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as finished_at,
      config::text as config_json,
      runner_config::text as runner_config_json,
      result_run_id::text as result_run_id,
      error,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from des_soccer_learning_jobs)SQL";

enum class DesSoccerLearningJobsSpawnStrategy { Latest, Elite, Mutation, Crossover, Random, Replay };
inline std::string des_soccer_learning_jobs_spawn_strategy_to_string(DesSoccerLearningJobsSpawnStrategy value) {
    switch (value) {
        case DesSoccerLearningJobsSpawnStrategy::Latest: return "latest";
        case DesSoccerLearningJobsSpawnStrategy::Elite: return "elite";
        case DesSoccerLearningJobsSpawnStrategy::Mutation: return "mutation";
        case DesSoccerLearningJobsSpawnStrategy::Crossover: return "crossover";
        case DesSoccerLearningJobsSpawnStrategy::Random: return "random";
        case DesSoccerLearningJobsSpawnStrategy::Replay: return "replay";
    }
    return "";
}
inline std::optional<DesSoccerLearningJobsSpawnStrategy> parse_des_soccer_learning_jobs_spawn_strategy(const std::string& value) {
    if (value == "latest") return DesSoccerLearningJobsSpawnStrategy::Latest;
    if (value == "elite") return DesSoccerLearningJobsSpawnStrategy::Elite;
    if (value == "mutation") return DesSoccerLearningJobsSpawnStrategy::Mutation;
    if (value == "crossover") return DesSoccerLearningJobsSpawnStrategy::Crossover;
    if (value == "random") return DesSoccerLearningJobsSpawnStrategy::Random;
    if (value == "replay") return DesSoccerLearningJobsSpawnStrategy::Replay;
    return std::nullopt;
}

enum class DesSoccerLearningJobsStatus { Queued, Running, Completed, Failed, Canceled };
inline std::string des_soccer_learning_jobs_status_to_string(DesSoccerLearningJobsStatus value) {
    switch (value) {
        case DesSoccerLearningJobsStatus::Queued: return "queued";
        case DesSoccerLearningJobsStatus::Running: return "running";
        case DesSoccerLearningJobsStatus::Completed: return "completed";
        case DesSoccerLearningJobsStatus::Failed: return "failed";
        case DesSoccerLearningJobsStatus::Canceled: return "canceled";
    }
    return "";
}
inline std::optional<DesSoccerLearningJobsStatus> parse_des_soccer_learning_jobs_status(const std::string& value) {
    if (value == "queued") return DesSoccerLearningJobsStatus::Queued;
    if (value == "running") return DesSoccerLearningJobsStatus::Running;
    if (value == "completed") return DesSoccerLearningJobsStatus::Completed;
    if (value == "failed") return DesSoccerLearningJobsStatus::Failed;
    if (value == "canceled") return DesSoccerLearningJobsStatus::Canceled;
    return std::nullopt;
}

struct DesSoccerLearningJobsRow {
    std::string id;
    std::string experiment_id;
    std::optional<std::string> base_policy_version_id;
    std::string spawn_strategy;
    std::string status;
    int32_t priority;
    int64_t seed;
    int32_t attempt;
    int32_t max_attempts;
    std::optional<std::string> lease_owner;
    std::optional<std::string> lease_expires_at;
    std::optional<std::string> started_at;
    std::optional<std::string> finished_at;
    std::string config;
    std::string runner_config;
    std::optional<std::string> result_run_id;
    std::optional<std::string> error;
    std::string created_at;
    std::string updated_at;
};

inline DesSoccerLearningJobsRow des_soccer_learning_jobs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerLearningJobsRow row;
    (void)is_null;
    row.id = get(0);
    row.experiment_id = get(1);
    row.base_policy_version_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.spawn_strategy = get(3);
    row.status = get(4);
    row.priority = std::stoi(get(5));
    row.seed = std::stoll(get(6));
    row.attempt = std::stoi(get(7));
    row.max_attempts = std::stoi(get(8));
    row.lease_owner = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.lease_expires_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.started_at = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.finished_at = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.config = get(13);
    row.runner_config = get(14);
    row.result_run_id = is_null(15) ? std::nullopt : std::optional<std::string>(get(15));
    row.error = is_null(16) ? std::nullopt : std::optional<std::string>(get(16));
    row.created_at = get(17);
    row.updated_at = get(18);
    return row;
}
inline std::optional<std::string> validate_des_soccer_learning_jobs_seed(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_jobs.seed is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_jobs_attempt(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_jobs.attempt is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_jobs_max_attempts(int32_t value) {
    if (value < 1) return std::string("des_soccer_learning_jobs.max_attempts is below the minimum");
    if (value > 100) return std::string("des_soccer_learning_jobs.max_attempts is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_jobs_lease_owner(const std::string& value) {
    if (value.size() > 200) return std::string("des_soccer_learning_jobs.lease_owner must be at most 200 characters");
    return std::nullopt;
}

inline const char* des_soccer_learning_runs_table = "des_soccer_learning_runs";
inline const std::vector<std::string> des_soccer_learning_runs_columns = { "id", "job_id", "experiment_id", "base_policy_version_id", "output_policy_version_id", "runner_id", "seed", "episode_index", "status", "score_home", "score_away", "home_goal_diff", "away_goal_diff", "home_outcome", "away_outcome", "home_merge_weight_micros", "away_merge_weight_micros", "fitness_micros", "duration_ticks", "simulated_seconds_micros", "elapsed_millis", "transitions", "summary", "stats", "error", "created_at", "updated_at" };
inline const char* des_soccer_learning_runs_select_sql = R"SQL(select
      id::text as id,
      job_id::text as job_id,
      experiment_id::text as experiment_id,
      base_policy_version_id::text as base_policy_version_id,
      output_policy_version_id::text as output_policy_version_id,
      runner_id,
      seed,
      episode_index,
      status,
      score_home,
      score_away,
      home_goal_diff,
      away_goal_diff,
      home_outcome,
      away_outcome,
      home_merge_weight_micros,
      away_merge_weight_micros,
      fitness_micros,
      duration_ticks,
      simulated_seconds_micros,
      elapsed_millis,
      transitions,
      summary::text as summary_json,
      stats::text as stats_json,
      error,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from des_soccer_learning_runs)SQL";

enum class DesSoccerLearningRunsStatus { Completed, Failed };
inline std::string des_soccer_learning_runs_status_to_string(DesSoccerLearningRunsStatus value) {
    switch (value) {
        case DesSoccerLearningRunsStatus::Completed: return "completed";
        case DesSoccerLearningRunsStatus::Failed: return "failed";
    }
    return "";
}
inline std::optional<DesSoccerLearningRunsStatus> parse_des_soccer_learning_runs_status(const std::string& value) {
    if (value == "completed") return DesSoccerLearningRunsStatus::Completed;
    if (value == "failed") return DesSoccerLearningRunsStatus::Failed;
    return std::nullopt;
}

enum class DesSoccerLearningRunsHomeOutcome { Win, Draw, Loss };
inline std::string des_soccer_learning_runs_home_outcome_to_string(DesSoccerLearningRunsHomeOutcome value) {
    switch (value) {
        case DesSoccerLearningRunsHomeOutcome::Win: return "win";
        case DesSoccerLearningRunsHomeOutcome::Draw: return "draw";
        case DesSoccerLearningRunsHomeOutcome::Loss: return "loss";
    }
    return "";
}
inline std::optional<DesSoccerLearningRunsHomeOutcome> parse_des_soccer_learning_runs_home_outcome(const std::string& value) {
    if (value == "win") return DesSoccerLearningRunsHomeOutcome::Win;
    if (value == "draw") return DesSoccerLearningRunsHomeOutcome::Draw;
    if (value == "loss") return DesSoccerLearningRunsHomeOutcome::Loss;
    return std::nullopt;
}

enum class DesSoccerLearningRunsAwayOutcome { Win, Draw, Loss };
inline std::string des_soccer_learning_runs_away_outcome_to_string(DesSoccerLearningRunsAwayOutcome value) {
    switch (value) {
        case DesSoccerLearningRunsAwayOutcome::Win: return "win";
        case DesSoccerLearningRunsAwayOutcome::Draw: return "draw";
        case DesSoccerLearningRunsAwayOutcome::Loss: return "loss";
    }
    return "";
}
inline std::optional<DesSoccerLearningRunsAwayOutcome> parse_des_soccer_learning_runs_away_outcome(const std::string& value) {
    if (value == "win") return DesSoccerLearningRunsAwayOutcome::Win;
    if (value == "draw") return DesSoccerLearningRunsAwayOutcome::Draw;
    if (value == "loss") return DesSoccerLearningRunsAwayOutcome::Loss;
    return std::nullopt;
}

struct DesSoccerLearningRunsRow {
    std::string id;
    std::optional<std::string> job_id;
    std::string experiment_id;
    std::optional<std::string> base_policy_version_id;
    std::optional<std::string> output_policy_version_id;
    std::string runner_id;
    int64_t seed;
    int32_t episode_index;
    std::string status;
    int32_t score_home;
    int32_t score_away;
    int32_t home_goal_diff;
    int32_t away_goal_diff;
    std::string home_outcome;
    std::string away_outcome;
    int64_t home_merge_weight_micros;
    int64_t away_merge_weight_micros;
    int64_t fitness_micros;
    int64_t duration_ticks;
    int64_t simulated_seconds_micros;
    int64_t elapsed_millis;
    int32_t transitions;
    std::string summary;
    std::string stats;
    std::optional<std::string> error;
    std::string created_at;
    std::string updated_at;
};

inline DesSoccerLearningRunsRow des_soccer_learning_runs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerLearningRunsRow row;
    (void)is_null;
    row.id = get(0);
    row.job_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.experiment_id = get(2);
    row.base_policy_version_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.output_policy_version_id = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.runner_id = get(5);
    row.seed = std::stoll(get(6));
    row.episode_index = std::stoi(get(7));
    row.status = get(8);
    row.score_home = std::stoi(get(9));
    row.score_away = std::stoi(get(10));
    row.home_goal_diff = std::stoi(get(11));
    row.away_goal_diff = std::stoi(get(12));
    row.home_outcome = get(13);
    row.away_outcome = get(14);
    row.home_merge_weight_micros = std::stoll(get(15));
    row.away_merge_weight_micros = std::stoll(get(16));
    row.fitness_micros = std::stoll(get(17));
    row.duration_ticks = std::stoll(get(18));
    row.simulated_seconds_micros = std::stoll(get(19));
    row.elapsed_millis = std::stoll(get(20));
    row.transitions = std::stoi(get(21));
    row.summary = get(22);
    row.stats = get(23);
    row.error = is_null(24) ? std::nullopt : std::optional<std::string>(get(24));
    row.created_at = get(25);
    row.updated_at = get(26);
    return row;
}
inline std::optional<std::string> validate_des_soccer_learning_runs_runner_id(const std::string& value) {
    if (value.size() > 200) return std::string("des_soccer_learning_runs.runner_id must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_runs_seed(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_runs.seed is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_runs_episode_index(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_runs.episode_index is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_runs_score_home(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_runs.score_home is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_runs_score_away(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_runs.score_away is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_runs_duration_ticks(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_runs.duration_ticks is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_runs_simulated_seconds_micros(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_runs.simulated_seconds_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_runs_elapsed_millis(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_runs.elapsed_millis is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_runs_transitions(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_runs.transitions is below the minimum");
    return std::nullopt;
}

inline const char* des_soccer_learning_run_deltas_table = "des_soccer_learning_run_deltas";
inline const std::vector<std::string> des_soccer_learning_run_deltas_columns = { "id", "run_id", "team", "entry_kind", "state_hash", "state_key", "action", "target_fine_cell_id", "target_tactical_cell_id", "target_macro_cell_id", "target_root_cell_id", "receiver_descriptor", "before_value_micros", "after_value_micros", "value_delta_micros", "visit_delta", "merge_weight_micros", "effective_visit_micros", "created_at" };
inline const char* des_soccer_learning_run_deltas_select_sql = R"SQL(select
      id::text as id,
      run_id::text as run_id,
      team,
      entry_kind,
      state_hash,
      state_key::text as state_key_json,
      action,
      target_fine_cell_id,
      target_tactical_cell_id,
      target_macro_cell_id,
      target_root_cell_id,
      receiver_descriptor,
      before_value_micros,
      after_value_micros,
      value_delta_micros,
      visit_delta,
      merge_weight_micros,
      effective_visit_micros,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from des_soccer_learning_run_deltas)SQL";

enum class DesSoccerLearningRunDeltasTeam { Home, Away };
inline std::string des_soccer_learning_run_deltas_team_to_string(DesSoccerLearningRunDeltasTeam value) {
    switch (value) {
        case DesSoccerLearningRunDeltasTeam::Home: return "home";
        case DesSoccerLearningRunDeltasTeam::Away: return "away";
    }
    return "";
}
inline std::optional<DesSoccerLearningRunDeltasTeam> parse_des_soccer_learning_run_deltas_team(const std::string& value) {
    if (value == "home") return DesSoccerLearningRunDeltasTeam::Home;
    if (value == "away") return DesSoccerLearningRunDeltasTeam::Away;
    return std::nullopt;
}

enum class DesSoccerLearningRunDeltasEntryKind { Action, Target };
inline std::string des_soccer_learning_run_deltas_entry_kind_to_string(DesSoccerLearningRunDeltasEntryKind value) {
    switch (value) {
        case DesSoccerLearningRunDeltasEntryKind::Action: return "action";
        case DesSoccerLearningRunDeltasEntryKind::Target: return "target";
    }
    return "";
}
inline std::optional<DesSoccerLearningRunDeltasEntryKind> parse_des_soccer_learning_run_deltas_entry_kind(const std::string& value) {
    if (value == "action") return DesSoccerLearningRunDeltasEntryKind::Action;
    if (value == "target") return DesSoccerLearningRunDeltasEntryKind::Target;
    return std::nullopt;
}

struct DesSoccerLearningRunDeltasRow {
    std::string id;
    std::string run_id;
    std::string team;
    std::string entry_kind;
    std::string state_hash;
    std::string state_key;
    std::string action;
    int32_t target_fine_cell_id;
    int32_t target_tactical_cell_id;
    int32_t target_macro_cell_id;
    int32_t target_root_cell_id;
    int32_t receiver_descriptor;
    int64_t before_value_micros;
    int64_t after_value_micros;
    int64_t value_delta_micros;
    int32_t visit_delta;
    int64_t merge_weight_micros;
    int64_t effective_visit_micros;
    std::string created_at;
};

inline DesSoccerLearningRunDeltasRow des_soccer_learning_run_deltas_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerLearningRunDeltasRow row;
    (void)is_null;
    row.id = get(0);
    row.run_id = get(1);
    row.team = get(2);
    row.entry_kind = get(3);
    row.state_hash = get(4);
    row.state_key = get(5);
    row.action = get(6);
    row.target_fine_cell_id = std::stoi(get(7));
    row.target_tactical_cell_id = std::stoi(get(8));
    row.target_macro_cell_id = std::stoi(get(9));
    row.target_root_cell_id = std::stoi(get(10));
    row.receiver_descriptor = std::stoi(get(11));
    row.before_value_micros = std::stoll(get(12));
    row.after_value_micros = std::stoll(get(13));
    row.value_delta_micros = std::stoll(get(14));
    row.visit_delta = std::stoi(get(15));
    row.merge_weight_micros = std::stoll(get(16));
    row.effective_visit_micros = std::stoll(get(17));
    row.created_at = get(18);
    return row;
}
inline std::optional<std::string> validate_des_soccer_learning_run_deltas_state_hash(const std::string& value) {
    if (value.size() > 32) return std::string("des_soccer_learning_run_deltas.state_hash must be at most 32 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{16,32}$)RX"))) return std::string("des_soccer_learning_run_deltas.state_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_run_deltas_action(const std::string& value) {
    if (value.size() > 80) return std::string("des_soccer_learning_run_deltas.action must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_run_deltas_target_fine_cell_id(int32_t value) {
    if (value < -1) return std::string("des_soccer_learning_run_deltas.target_fine_cell_id is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_run_deltas_target_tactical_cell_id(int32_t value) {
    if (value < -1) return std::string("des_soccer_learning_run_deltas.target_tactical_cell_id is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_run_deltas_target_macro_cell_id(int32_t value) {
    if (value < -1) return std::string("des_soccer_learning_run_deltas.target_macro_cell_id is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_run_deltas_target_root_cell_id(int32_t value) {
    if (value < -1) return std::string("des_soccer_learning_run_deltas.target_root_cell_id is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_run_deltas_receiver_descriptor(int32_t value) {
    if (value < -1) return std::string("des_soccer_learning_run_deltas.receiver_descriptor is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_run_deltas_visit_delta(int32_t value) {
    if (value < 1) return std::string("des_soccer_learning_run_deltas.visit_delta is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_run_deltas_merge_weight_micros(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_run_deltas.merge_weight_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_run_deltas_effective_visit_micros(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_run_deltas.effective_visit_micros is below the minimum");
    return std::nullopt;
}

inline const char* des_soccer_learning_merge_events_table = "des_soccer_learning_merge_events";
inline const std::vector<std::string> des_soccer_learning_merge_events_columns = { "id", "experiment_id", "base_policy_version_id", "output_policy_version_id", "strategy", "input_run_count", "input_delta_count", "decay_micros", "metrics", "created_at" };
inline const char* des_soccer_learning_merge_events_select_sql = R"SQL(select
      id::text as id,
      experiment_id::text as experiment_id,
      base_policy_version_id::text as base_policy_version_id,
      output_policy_version_id::text as output_policy_version_id,
      strategy,
      input_run_count,
      input_delta_count,
      decay_micros,
      metrics::text as metrics_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from des_soccer_learning_merge_events)SQL";

enum class DesSoccerLearningMergeEventsStrategy { OutcomeWeightedAverage, Elite, Mutation, Crossover };
inline std::string des_soccer_learning_merge_events_strategy_to_string(DesSoccerLearningMergeEventsStrategy value) {
    switch (value) {
        case DesSoccerLearningMergeEventsStrategy::OutcomeWeightedAverage: return "outcome_weighted_average";
        case DesSoccerLearningMergeEventsStrategy::Elite: return "elite";
        case DesSoccerLearningMergeEventsStrategy::Mutation: return "mutation";
        case DesSoccerLearningMergeEventsStrategy::Crossover: return "crossover";
    }
    return "";
}
inline std::optional<DesSoccerLearningMergeEventsStrategy> parse_des_soccer_learning_merge_events_strategy(const std::string& value) {
    if (value == "outcome_weighted_average") return DesSoccerLearningMergeEventsStrategy::OutcomeWeightedAverage;
    if (value == "elite") return DesSoccerLearningMergeEventsStrategy::Elite;
    if (value == "mutation") return DesSoccerLearningMergeEventsStrategy::Mutation;
    if (value == "crossover") return DesSoccerLearningMergeEventsStrategy::Crossover;
    return std::nullopt;
}

struct DesSoccerLearningMergeEventsRow {
    std::string id;
    std::string experiment_id;
    std::optional<std::string> base_policy_version_id;
    std::string output_policy_version_id;
    std::string strategy;
    int32_t input_run_count;
    int32_t input_delta_count;
    int64_t decay_micros;
    std::string metrics;
    std::string created_at;
};

inline DesSoccerLearningMergeEventsRow des_soccer_learning_merge_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerLearningMergeEventsRow row;
    (void)is_null;
    row.id = get(0);
    row.experiment_id = get(1);
    row.base_policy_version_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.output_policy_version_id = get(3);
    row.strategy = get(4);
    row.input_run_count = std::stoi(get(5));
    row.input_delta_count = std::stoi(get(6));
    row.decay_micros = std::stoll(get(7));
    row.metrics = get(8);
    row.created_at = get(9);
    return row;
}
inline std::optional<std::string> validate_des_soccer_learning_merge_events_input_run_count(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_merge_events.input_run_count is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_merge_events_input_delta_count(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_merge_events.input_delta_count is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_merge_events_decay_micros(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_merge_events.decay_micros is below the minimum");
    if (value > 1000000) return std::string("des_soccer_learning_merge_events.decay_micros is above the maximum");
    return std::nullopt;
}

inline const char* des_soccer_tournaments_table = "des_soccer_tournaments";
inline const std::vector<std::string> des_soccer_tournaments_columns = { "id", "experiment_id", "tournament_date", "seed", "learning_mode", "format", "team_count", "match_count", "matches_played", "champion_team_id", "runner_up_team_id", "third_place_team_id", "wall_time_seconds", "status", "created_at", "updated_at", "finished_at" };
inline const char* des_soccer_tournaments_select_sql = R"SQL(select
      id,
      experiment_id::text as experiment_id,
      tournament_date,
      seed,
      learning_mode,
      format::text as format_json,
      team_count,
      match_count,
      matches_played,
      champion_team_id,
      runner_up_team_id,
      third_place_team_id,
      wall_time_seconds,
      status,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      to_char(finished_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as finished_at
    from des_soccer_tournaments)SQL";

enum class DesSoccerTournamentsStatus { Running, Completed, Failed, Aborted };
inline std::string des_soccer_tournaments_status_to_string(DesSoccerTournamentsStatus value) {
    switch (value) {
        case DesSoccerTournamentsStatus::Running: return "running";
        case DesSoccerTournamentsStatus::Completed: return "completed";
        case DesSoccerTournamentsStatus::Failed: return "failed";
        case DesSoccerTournamentsStatus::Aborted: return "aborted";
    }
    return "";
}
inline std::optional<DesSoccerTournamentsStatus> parse_des_soccer_tournaments_status(const std::string& value) {
    if (value == "running") return DesSoccerTournamentsStatus::Running;
    if (value == "completed") return DesSoccerTournamentsStatus::Completed;
    if (value == "failed") return DesSoccerTournamentsStatus::Failed;
    if (value == "aborted") return DesSoccerTournamentsStatus::Aborted;
    return std::nullopt;
}

struct DesSoccerTournamentsRow {
    int64_t id;
    std::string experiment_id;
    std::string tournament_date;
    int64_t seed;
    std::string learning_mode;
    std::string format;
    int32_t team_count;
    int32_t match_count;
    int32_t matches_played;
    std::optional<int32_t> champion_team_id;
    std::optional<int32_t> runner_up_team_id;
    std::optional<int32_t> third_place_team_id;
    std::optional<double> wall_time_seconds;
    std::string status;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> finished_at;
};

inline DesSoccerTournamentsRow des_soccer_tournaments_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerTournamentsRow row;
    (void)is_null;
    row.id = std::stoll(get(0));
    row.experiment_id = get(1);
    row.tournament_date = get(2);
    row.seed = std::stoll(get(3));
    row.learning_mode = get(4);
    row.format = get(5);
    row.team_count = std::stoi(get(6));
    row.match_count = std::stoi(get(7));
    row.matches_played = std::stoi(get(8));
    row.champion_team_id = is_null(9) ? std::nullopt : std::optional<int32_t>(std::stoi(get(9)));
    row.runner_up_team_id = is_null(10) ? std::nullopt : std::optional<int32_t>(std::stoi(get(10)));
    row.third_place_team_id = is_null(11) ? std::nullopt : std::optional<int32_t>(std::stoi(get(11)));
    row.wall_time_seconds = is_null(12) ? std::nullopt : std::optional<double>(std::stod(get(12)));
    row.status = get(13);
    row.created_at = get(14);
    row.updated_at = get(15);
    row.finished_at = is_null(16) ? std::nullopt : std::optional<std::string>(get(16));
    return row;
}

inline const char* des_soccer_tournament_matches_table = "des_soccer_tournament_matches";
inline const std::vector<std::string> des_soccer_tournament_matches_columns = { "id", "tournament_id", "match_index", "stage", "home_team_id", "away_team_id", "home_goals", "away_goals", "shootout_winner_team_id", "home_training_steps", "away_training_steps", "recorded_at" };
inline const char* des_soccer_tournament_matches_select_sql = R"SQL(select
      id,
      tournament_id,
      match_index,
      stage,
      home_team_id,
      away_team_id,
      home_goals,
      away_goals,
      shootout_winner_team_id,
      home_training_steps,
      away_training_steps,
      to_char(recorded_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as recorded_at
    from des_soccer_tournament_matches)SQL";

struct DesSoccerTournamentMatchesRow {
    int64_t id;
    int64_t tournament_id;
    int32_t match_index;
    std::string stage;
    int32_t home_team_id;
    int32_t away_team_id;
    int32_t home_goals;
    int32_t away_goals;
    std::optional<int32_t> shootout_winner_team_id;
    int64_t home_training_steps;
    int64_t away_training_steps;
    std::string recorded_at;
};

inline DesSoccerTournamentMatchesRow des_soccer_tournament_matches_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerTournamentMatchesRow row;
    (void)is_null;
    row.id = std::stoll(get(0));
    row.tournament_id = std::stoll(get(1));
    row.match_index = std::stoi(get(2));
    row.stage = get(3);
    row.home_team_id = std::stoi(get(4));
    row.away_team_id = std::stoi(get(5));
    row.home_goals = std::stoi(get(6));
    row.away_goals = std::stoi(get(7));
    row.shootout_winner_team_id = is_null(8) ? std::nullopt : std::optional<int32_t>(std::stoi(get(8)));
    row.home_training_steps = std::stoll(get(9));
    row.away_training_steps = std::stoll(get(10));
    row.recorded_at = get(11);
    return row;
}

inline const char* des_soccer_tournament_team_brains_table = "des_soccer_tournament_team_brains";
inline const std::vector<std::string> des_soccer_tournament_team_brains_columns = { "id", "tournament_id", "team_id", "team_name", "seed", "matches_learned", "training_steps", "played", "wins", "draws", "losses", "goals_for", "goals_against", "neural_snapshot", "genome", "updated_at" };
inline const char* des_soccer_tournament_team_brains_select_sql = R"SQL(select
      id,
      tournament_id,
      team_id,
      team_name,
      seed,
      matches_learned,
      training_steps,
      played,
      wins,
      draws,
      losses,
      goals_for,
      goals_against,
      neural_snapshot::text as neural_snapshot_json,
      genome::text as genome_json,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from des_soccer_tournament_team_brains)SQL";

struct DesSoccerTournamentTeamBrainsRow {
    int64_t id;
    int64_t tournament_id;
    int32_t team_id;
    std::string team_name;
    int64_t seed;
    int32_t matches_learned;
    int64_t training_steps;
    int32_t played;
    int32_t wins;
    int32_t draws;
    int32_t losses;
    int32_t goals_for;
    int32_t goals_against;
    std::optional<std::string> neural_snapshot;
    std::optional<std::string> genome;
    std::string updated_at;
};

inline DesSoccerTournamentTeamBrainsRow des_soccer_tournament_team_brains_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerTournamentTeamBrainsRow row;
    (void)is_null;
    row.id = std::stoll(get(0));
    row.tournament_id = std::stoll(get(1));
    row.team_id = std::stoi(get(2));
    row.team_name = get(3);
    row.seed = std::stoll(get(4));
    row.matches_learned = std::stoi(get(5));
    row.training_steps = std::stoll(get(6));
    row.played = std::stoi(get(7));
    row.wins = std::stoi(get(8));
    row.draws = std::stoi(get(9));
    row.losses = std::stoi(get(10));
    row.goals_for = std::stoi(get(11));
    row.goals_against = std::stoi(get(12));
    row.neural_snapshot = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    row.genome = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.updated_at = get(15);
    return row;
}

inline const char* des_soccer_learning_set_play_runs_table = "des_soccer_learning_set_play_runs";
inline const std::vector<std::string> des_soccer_learning_set_play_runs_columns = { "run_id", "policy_version_id", "primary_restart", "team", "spot_x_micros", "spot_y_micros", "duration_seconds_micros", "episode_count", "goals", "goal_rate_micros", "first_window_goal_rate_micros", "last_window_goal_rate_micros", "goal_rate_delta_micros", "created_at" };
inline const char* des_soccer_learning_set_play_runs_select_sql = R"SQL(select
      run_id::text as run_id,
      policy_version_id::text as policy_version_id,
      primary_restart,
      team,
      spot_x_micros,
      spot_y_micros,
      duration_seconds_micros,
      episode_count,
      goals,
      goal_rate_micros,
      first_window_goal_rate_micros,
      last_window_goal_rate_micros,
      goal_rate_delta_micros,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from des_soccer_learning_set_play_runs)SQL";

enum class DesSoccerLearningSetPlayRunsPrimaryRestart { DirectFreeKick, IndirectFreeKick };
inline std::string des_soccer_learning_set_play_runs_primary_restart_to_string(DesSoccerLearningSetPlayRunsPrimaryRestart value) {
    switch (value) {
        case DesSoccerLearningSetPlayRunsPrimaryRestart::DirectFreeKick: return "direct-free-kick";
        case DesSoccerLearningSetPlayRunsPrimaryRestart::IndirectFreeKick: return "indirect-free-kick";
    }
    return "";
}
inline std::optional<DesSoccerLearningSetPlayRunsPrimaryRestart> parse_des_soccer_learning_set_play_runs_primary_restart(const std::string& value) {
    if (value == "direct-free-kick") return DesSoccerLearningSetPlayRunsPrimaryRestart::DirectFreeKick;
    if (value == "indirect-free-kick") return DesSoccerLearningSetPlayRunsPrimaryRestart::IndirectFreeKick;
    return std::nullopt;
}

enum class DesSoccerLearningSetPlayRunsTeam { Home, Away };
inline std::string des_soccer_learning_set_play_runs_team_to_string(DesSoccerLearningSetPlayRunsTeam value) {
    switch (value) {
        case DesSoccerLearningSetPlayRunsTeam::Home: return "home";
        case DesSoccerLearningSetPlayRunsTeam::Away: return "away";
    }
    return "";
}
inline std::optional<DesSoccerLearningSetPlayRunsTeam> parse_des_soccer_learning_set_play_runs_team(const std::string& value) {
    if (value == "home") return DesSoccerLearningSetPlayRunsTeam::Home;
    if (value == "away") return DesSoccerLearningSetPlayRunsTeam::Away;
    return std::nullopt;
}

struct DesSoccerLearningSetPlayRunsRow {
    std::string run_id;
    std::string policy_version_id;
    std::string primary_restart;
    std::string team;
    int64_t spot_x_micros;
    int64_t spot_y_micros;
    int64_t duration_seconds_micros;
    int32_t episode_count;
    int32_t goals;
    int64_t goal_rate_micros;
    int64_t first_window_goal_rate_micros;
    int64_t last_window_goal_rate_micros;
    int64_t goal_rate_delta_micros;
    std::string created_at;
};

inline DesSoccerLearningSetPlayRunsRow des_soccer_learning_set_play_runs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerLearningSetPlayRunsRow row;
    (void)is_null;
    row.run_id = get(0);
    row.policy_version_id = get(1);
    row.primary_restart = get(2);
    row.team = get(3);
    row.spot_x_micros = std::stoll(get(4));
    row.spot_y_micros = std::stoll(get(5));
    row.duration_seconds_micros = std::stoll(get(6));
    row.episode_count = std::stoi(get(7));
    row.goals = std::stoi(get(8));
    row.goal_rate_micros = std::stoll(get(9));
    row.first_window_goal_rate_micros = std::stoll(get(10));
    row.last_window_goal_rate_micros = std::stoll(get(11));
    row.goal_rate_delta_micros = std::stoll(get(12));
    row.created_at = get(13);
    return row;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_runs_duration_seconds_micros(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_runs.duration_seconds_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_runs_episode_count(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_runs.episode_count is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_runs_goals(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_runs.goals is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_runs_goal_rate_micros(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_runs.goal_rate_micros is below the minimum");
    if (value > 1000000) return std::string("des_soccer_learning_set_play_runs.goal_rate_micros is above the maximum");
    return std::nullopt;
}

inline const char* des_soccer_learning_set_play_restart_mix_table = "des_soccer_learning_set_play_restart_mix";
inline const std::vector<std::string> des_soccer_learning_set_play_restart_mix_columns = { "run_id", "ordinal", "restart" };
inline const char* des_soccer_learning_set_play_restart_mix_select_sql = R"SQL(select
      run_id::text as run_id,
      ordinal,
      restart
    from des_soccer_learning_set_play_restart_mix)SQL";

enum class DesSoccerLearningSetPlayRestartMixRestart { DirectFreeKick, IndirectFreeKick };
inline std::string des_soccer_learning_set_play_restart_mix_restart_to_string(DesSoccerLearningSetPlayRestartMixRestart value) {
    switch (value) {
        case DesSoccerLearningSetPlayRestartMixRestart::DirectFreeKick: return "direct-free-kick";
        case DesSoccerLearningSetPlayRestartMixRestart::IndirectFreeKick: return "indirect-free-kick";
    }
    return "";
}
inline std::optional<DesSoccerLearningSetPlayRestartMixRestart> parse_des_soccer_learning_set_play_restart_mix_restart(const std::string& value) {
    if (value == "direct-free-kick") return DesSoccerLearningSetPlayRestartMixRestart::DirectFreeKick;
    if (value == "indirect-free-kick") return DesSoccerLearningSetPlayRestartMixRestart::IndirectFreeKick;
    return std::nullopt;
}

struct DesSoccerLearningSetPlayRestartMixRow {
    std::string run_id;
    int32_t ordinal;
    std::string restart;
};

inline DesSoccerLearningSetPlayRestartMixRow des_soccer_learning_set_play_restart_mix_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerLearningSetPlayRestartMixRow row;
    (void)is_null;
    row.run_id = get(0);
    row.ordinal = std::stoi(get(1));
    row.restart = get(2);
    return row;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_restart_mix_ordinal(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_restart_mix.ordinal is below the minimum");
    return std::nullopt;
}

inline const char* des_soccer_learning_set_play_episode_metrics_table = "des_soccer_learning_set_play_episode_metrics";
inline const std::vector<std::string> des_soccer_learning_set_play_episode_metrics_columns = { "run_id", "episode_index", "seed", "restart", "routine", "scored", "score_delta_for_team", "ticks", "simulated_seconds_micros", "policy_updates", "home_policy_entries", "home_policy_target_entries", "away_policy_entries", "away_policy_target_entries", "neural_training_steps", "neural_samples", "neural_replay_samples", "neural_last_loss_micros", "cumulative_goals", "goal_rate_so_far_micros" };
inline const char* des_soccer_learning_set_play_episode_metrics_select_sql = R"SQL(select
      run_id::text as run_id,
      episode_index,
      seed,
      restart,
      routine,
      scored,
      score_delta_for_team,
      ticks,
      simulated_seconds_micros,
      policy_updates,
      home_policy_entries,
      home_policy_target_entries,
      away_policy_entries,
      away_policy_target_entries,
      neural_training_steps,
      neural_samples,
      neural_replay_samples,
      neural_last_loss_micros,
      cumulative_goals,
      goal_rate_so_far_micros
    from des_soccer_learning_set_play_episode_metrics)SQL";

enum class DesSoccerLearningSetPlayEpisodeMetricsRestart { DirectFreeKick, IndirectFreeKick };
inline std::string des_soccer_learning_set_play_episode_metrics_restart_to_string(DesSoccerLearningSetPlayEpisodeMetricsRestart value) {
    switch (value) {
        case DesSoccerLearningSetPlayEpisodeMetricsRestart::DirectFreeKick: return "direct-free-kick";
        case DesSoccerLearningSetPlayEpisodeMetricsRestart::IndirectFreeKick: return "indirect-free-kick";
    }
    return "";
}
inline std::optional<DesSoccerLearningSetPlayEpisodeMetricsRestart> parse_des_soccer_learning_set_play_episode_metrics_restart(const std::string& value) {
    if (value == "direct-free-kick") return DesSoccerLearningSetPlayEpisodeMetricsRestart::DirectFreeKick;
    if (value == "indirect-free-kick") return DesSoccerLearningSetPlayEpisodeMetricsRestart::IndirectFreeKick;
    return std::nullopt;
}

struct DesSoccerLearningSetPlayEpisodeMetricsRow {
    std::string run_id;
    int32_t episode_index;
    int64_t seed;
    std::string restart;
    std::optional<std::string> routine;
    bool scored;
    int32_t score_delta_for_team;
    int64_t ticks;
    int64_t simulated_seconds_micros;
    int64_t policy_updates;
    int32_t home_policy_entries;
    int32_t home_policy_target_entries;
    int32_t away_policy_entries;
    int32_t away_policy_target_entries;
    int32_t neural_training_steps;
    int64_t neural_samples;
    int32_t neural_replay_samples;
    std::optional<int64_t> neural_last_loss_micros;
    int32_t cumulative_goals;
    int64_t goal_rate_so_far_micros;
};

inline DesSoccerLearningSetPlayEpisodeMetricsRow des_soccer_learning_set_play_episode_metrics_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerLearningSetPlayEpisodeMetricsRow row;
    (void)is_null;
    row.run_id = get(0);
    row.episode_index = std::stoi(get(1));
    row.seed = std::stoll(get(2));
    row.restart = get(3);
    row.routine = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.scored = (get(5) == "t");
    row.score_delta_for_team = std::stoi(get(6));
    row.ticks = std::stoll(get(7));
    row.simulated_seconds_micros = std::stoll(get(8));
    row.policy_updates = std::stoll(get(9));
    row.home_policy_entries = std::stoi(get(10));
    row.home_policy_target_entries = std::stoi(get(11));
    row.away_policy_entries = std::stoi(get(12));
    row.away_policy_target_entries = std::stoi(get(13));
    row.neural_training_steps = std::stoi(get(14));
    row.neural_samples = std::stoll(get(15));
    row.neural_replay_samples = std::stoi(get(16));
    row.neural_last_loss_micros = is_null(17) ? std::nullopt : std::optional<int64_t>(std::stoll(get(17)));
    row.cumulative_goals = std::stoi(get(18));
    row.goal_rate_so_far_micros = std::stoll(get(19));
    return row;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_episode_index(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.episode_index is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_seed(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.seed is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_routine(const std::string& value) {
    if (value.size() > 80) return std::string("des_soccer_learning_set_play_episode_metrics.routine must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_ticks(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.ticks is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_simulated_seconds_micros(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.simulated_seconds_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_policy_updates(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.policy_updates is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_home_policy_entries(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.home_policy_entries is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_home_policy_target_entries(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.home_policy_target_entries is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_away_policy_entries(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.away_policy_entries is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_away_policy_target_entries(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.away_policy_target_entries is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_neural_training_steps(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.neural_training_steps is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_neural_samples(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.neural_samples is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_neural_replay_samples(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.neural_replay_samples is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_cumulative_goals(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.cumulative_goals is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_set_play_episode_metrics_goal_rate_so_far_micros(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_set_play_episode_metrics.goal_rate_so_far_micros is below the minimum");
    if (value > 1000000) return std::string("des_soccer_learning_set_play_episode_metrics.goal_rate_so_far_micros is above the maximum");
    return std::nullopt;
}

inline const char* des_soccer_learning_neural_run_metrics_table = "des_soccer_learning_neural_run_metrics";
inline const std::vector<std::string> des_soccer_learning_neural_run_metrics_columns = { "run_id", "policy_version_id", "enabled", "backend", "training_steps", "samples", "pending_batches", "dropped_batches", "replay_samples", "replay_capacity", "parameter_count", "target_clip_micros", "last_loss_micros", "average_loss_micros", "created_at" };
inline const char* des_soccer_learning_neural_run_metrics_select_sql = R"SQL(select
      run_id::text as run_id,
      policy_version_id::text as policy_version_id,
      enabled,
      backend,
      training_steps,
      samples,
      pending_batches,
      dropped_batches,
      replay_samples,
      replay_capacity,
      parameter_count,
      target_clip_micros,
      last_loss_micros,
      average_loss_micros,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from des_soccer_learning_neural_run_metrics)SQL";

enum class DesSoccerLearningNeuralRunMetricsBackend { Inline, Threaded };
inline std::string des_soccer_learning_neural_run_metrics_backend_to_string(DesSoccerLearningNeuralRunMetricsBackend value) {
    switch (value) {
        case DesSoccerLearningNeuralRunMetricsBackend::Inline: return "inline";
        case DesSoccerLearningNeuralRunMetricsBackend::Threaded: return "threaded";
    }
    return "";
}
inline std::optional<DesSoccerLearningNeuralRunMetricsBackend> parse_des_soccer_learning_neural_run_metrics_backend(const std::string& value) {
    if (value == "inline") return DesSoccerLearningNeuralRunMetricsBackend::Inline;
    if (value == "threaded") return DesSoccerLearningNeuralRunMetricsBackend::Threaded;
    return std::nullopt;
}

struct DesSoccerLearningNeuralRunMetricsRow {
    std::string run_id;
    std::string policy_version_id;
    bool enabled;
    std::string backend;
    int32_t training_steps;
    int64_t samples;
    int32_t pending_batches;
    int32_t dropped_batches;
    int32_t replay_samples;
    int32_t replay_capacity;
    int32_t parameter_count;
    int64_t target_clip_micros;
    std::optional<int64_t> last_loss_micros;
    std::optional<int64_t> average_loss_micros;
    std::string created_at;
};

inline DesSoccerLearningNeuralRunMetricsRow des_soccer_learning_neural_run_metrics_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerLearningNeuralRunMetricsRow row;
    (void)is_null;
    row.run_id = get(0);
    row.policy_version_id = get(1);
    row.enabled = (get(2) == "t");
    row.backend = get(3);
    row.training_steps = std::stoi(get(4));
    row.samples = std::stoll(get(5));
    row.pending_batches = std::stoi(get(6));
    row.dropped_batches = std::stoi(get(7));
    row.replay_samples = std::stoi(get(8));
    row.replay_capacity = std::stoi(get(9));
    row.parameter_count = std::stoi(get(10));
    row.target_clip_micros = std::stoll(get(11));
    row.last_loss_micros = is_null(12) ? std::nullopt : std::optional<int64_t>(std::stoll(get(12)));
    row.average_loss_micros = is_null(13) ? std::nullopt : std::optional<int64_t>(std::stoll(get(13)));
    row.created_at = get(14);
    return row;
}
inline std::optional<std::string> validate_des_soccer_learning_neural_run_metrics_training_steps(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_neural_run_metrics.training_steps is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_neural_run_metrics_samples(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_neural_run_metrics.samples is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_neural_run_metrics_pending_batches(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_neural_run_metrics.pending_batches is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_neural_run_metrics_dropped_batches(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_neural_run_metrics.dropped_batches is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_neural_run_metrics_replay_samples(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_neural_run_metrics.replay_samples is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_neural_run_metrics_replay_capacity(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_neural_run_metrics.replay_capacity is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_neural_run_metrics_parameter_count(int32_t value) {
    if (value < 0) return std::string("des_soccer_learning_neural_run_metrics.parameter_count is below the minimum");
    return std::nullopt;
}

inline const char* des_soccer_learning_pass_metrics_table = "des_soccer_learning_pass_metrics";
inline const std::vector<std::string> des_soccer_learning_pass_metrics_columns = { "git_commit", "runs", "passes_attempted", "passes_completed", "completed_pass_gain_yards_micros", "pass_chains", "pass_chain_gain_yards_micros", "pass_chains_net_loss", "shots_on_target", "shots_after_pass", "first_seen_at", "updated_at" };
inline const char* des_soccer_learning_pass_metrics_select_sql = R"SQL(select
      git_commit,
      runs,
      passes_attempted,
      passes_completed,
      completed_pass_gain_yards_micros,
      pass_chains,
      pass_chain_gain_yards_micros,
      pass_chains_net_loss,
      shots_on_target,
      shots_after_pass,
      to_char(first_seen_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as first_seen_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from des_soccer_learning_pass_metrics)SQL";

struct DesSoccerLearningPassMetricsRow {
    std::string git_commit;
    int64_t runs;
    int64_t passes_attempted;
    int64_t passes_completed;
    int64_t completed_pass_gain_yards_micros;
    int64_t pass_chains;
    int64_t pass_chain_gain_yards_micros;
    int64_t pass_chains_net_loss;
    int64_t shots_on_target;
    int64_t shots_after_pass;
    std::string first_seen_at;
    std::string updated_at;
};

inline DesSoccerLearningPassMetricsRow des_soccer_learning_pass_metrics_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesSoccerLearningPassMetricsRow row;
    (void)is_null;
    row.git_commit = get(0);
    row.runs = std::stoll(get(1));
    row.passes_attempted = std::stoll(get(2));
    row.passes_completed = std::stoll(get(3));
    row.completed_pass_gain_yards_micros = std::stoll(get(4));
    row.pass_chains = std::stoll(get(5));
    row.pass_chain_gain_yards_micros = std::stoll(get(6));
    row.pass_chains_net_loss = std::stoll(get(7));
    row.shots_on_target = std::stoll(get(8));
    row.shots_after_pass = std::stoll(get(9));
    row.first_seen_at = get(10);
    row.updated_at = get(11);
    return row;
}
inline std::optional<std::string> validate_des_soccer_learning_pass_metrics_git_commit(const std::string& value) {
    if (value.size() > 64) return std::string("des_soccer_learning_pass_metrics.git_commit must be at most 64 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_pass_metrics_runs(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_pass_metrics.runs is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_pass_metrics_passes_attempted(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_pass_metrics.passes_attempted is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_pass_metrics_passes_completed(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_pass_metrics.passes_completed is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_pass_metrics_pass_chains(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_pass_metrics.pass_chains is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_pass_metrics_pass_chains_net_loss(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_pass_metrics.pass_chains_net_loss is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_pass_metrics_shots_on_target(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_pass_metrics.shots_on_target is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_soccer_learning_pass_metrics_shots_after_pass(int64_t value) {
    if (value < 0) return std::string("des_soccer_learning_pass_metrics.shots_after_pass is below the minimum");
    return std::nullopt;
}

inline const char* des_fel_elevator_learning_runs_table = "des_fel_elevator_learning_runs";
inline const std::vector<std::string> des_fel_elevator_learning_runs_columns = { "id", "run_label", "scenario_slug", "status", "dispatch_policy", "seed", "floors", "shafts", "capacity", "travel_seconds_micros", "dwell_seconds_micros", "arrival_rate_micros", "horizon_seconds_micros", "events", "arrivals", "boarded", "served", "mean_wait_micros", "dispatch_decisions", "pomdp_belief_updates", "online_learning_updates", "online_learning_loss_last_micros", "config", "metrics", "artifact", "created_at", "updated_at" };
inline const char* des_fel_elevator_learning_runs_select_sql = R"SQL(select
      id::text as id,
      run_label,
      scenario_slug,
      status,
      dispatch_policy,
      seed,
      floors,
      shafts,
      capacity,
      travel_seconds_micros,
      dwell_seconds_micros,
      arrival_rate_micros,
      horizon_seconds_micros,
      events,
      arrivals,
      boarded,
      served,
      mean_wait_micros,
      dispatch_decisions,
      pomdp_belief_updates,
      online_learning_updates,
      online_learning_loss_last_micros,
      config::text as config_json,
      metrics::text as metrics_json,
      artifact::text as artifact_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from des_fel_elevator_learning_runs)SQL";

enum class DesFelElevatorLearningRunsStatus { Completed, Failed, Imported };
inline std::string des_fel_elevator_learning_runs_status_to_string(DesFelElevatorLearningRunsStatus value) {
    switch (value) {
        case DesFelElevatorLearningRunsStatus::Completed: return "completed";
        case DesFelElevatorLearningRunsStatus::Failed: return "failed";
        case DesFelElevatorLearningRunsStatus::Imported: return "imported";
    }
    return "";
}
inline std::optional<DesFelElevatorLearningRunsStatus> parse_des_fel_elevator_learning_runs_status(const std::string& value) {
    if (value == "completed") return DesFelElevatorLearningRunsStatus::Completed;
    if (value == "failed") return DesFelElevatorLearningRunsStatus::Failed;
    if (value == "imported") return DesFelElevatorLearningRunsStatus::Imported;
    return std::nullopt;
}

enum class DesFelElevatorLearningRunsDispatchPolicy { Look, MdpTable, NeuralScorer, PomdpBelief, NeuralTd };
inline std::string des_fel_elevator_learning_runs_dispatch_policy_to_string(DesFelElevatorLearningRunsDispatchPolicy value) {
    switch (value) {
        case DesFelElevatorLearningRunsDispatchPolicy::Look: return "look";
        case DesFelElevatorLearningRunsDispatchPolicy::MdpTable: return "mdp-table";
        case DesFelElevatorLearningRunsDispatchPolicy::NeuralScorer: return "neural-scorer";
        case DesFelElevatorLearningRunsDispatchPolicy::PomdpBelief: return "pomdp-belief";
        case DesFelElevatorLearningRunsDispatchPolicy::NeuralTd: return "neural-td";
    }
    return "";
}
inline std::optional<DesFelElevatorLearningRunsDispatchPolicy> parse_des_fel_elevator_learning_runs_dispatch_policy(const std::string& value) {
    if (value == "look") return DesFelElevatorLearningRunsDispatchPolicy::Look;
    if (value == "mdp-table") return DesFelElevatorLearningRunsDispatchPolicy::MdpTable;
    if (value == "neural-scorer") return DesFelElevatorLearningRunsDispatchPolicy::NeuralScorer;
    if (value == "pomdp-belief") return DesFelElevatorLearningRunsDispatchPolicy::PomdpBelief;
    if (value == "neural-td") return DesFelElevatorLearningRunsDispatchPolicy::NeuralTd;
    return std::nullopt;
}

struct DesFelElevatorLearningRunsRow {
    std::string id;
    std::string run_label;
    std::string scenario_slug;
    std::string status;
    std::string dispatch_policy;
    int64_t seed;
    int32_t floors;
    int32_t shafts;
    int32_t capacity;
    int64_t travel_seconds_micros;
    int64_t dwell_seconds_micros;
    int64_t arrival_rate_micros;
    int64_t horizon_seconds_micros;
    int64_t events;
    int64_t arrivals;
    int64_t boarded;
    int64_t served;
    int64_t mean_wait_micros;
    int32_t dispatch_decisions;
    int32_t pomdp_belief_updates;
    int64_t online_learning_updates;
    std::optional<int64_t> online_learning_loss_last_micros;
    std::string config;
    std::string metrics;
    std::string artifact;
    std::string created_at;
    std::string updated_at;
};

inline DesFelElevatorLearningRunsRow des_fel_elevator_learning_runs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesFelElevatorLearningRunsRow row;
    (void)is_null;
    row.id = get(0);
    row.run_label = get(1);
    row.scenario_slug = get(2);
    row.status = get(3);
    row.dispatch_policy = get(4);
    row.seed = std::stoll(get(5));
    row.floors = std::stoi(get(6));
    row.shafts = std::stoi(get(7));
    row.capacity = std::stoi(get(8));
    row.travel_seconds_micros = std::stoll(get(9));
    row.dwell_seconds_micros = std::stoll(get(10));
    row.arrival_rate_micros = std::stoll(get(11));
    row.horizon_seconds_micros = std::stoll(get(12));
    row.events = std::stoll(get(13));
    row.arrivals = std::stoll(get(14));
    row.boarded = std::stoll(get(15));
    row.served = std::stoll(get(16));
    row.mean_wait_micros = std::stoll(get(17));
    row.dispatch_decisions = std::stoi(get(18));
    row.pomdp_belief_updates = std::stoi(get(19));
    row.online_learning_updates = std::stoll(get(20));
    row.online_learning_loss_last_micros = is_null(21) ? std::nullopt : std::optional<int64_t>(std::stoll(get(21)));
    row.config = get(22);
    row.metrics = get(23);
    row.artifact = get(24);
    row.created_at = get(25);
    row.updated_at = get(26);
    return row;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_run_label(const std::string& value) {
    if (value.size() > 200) return std::string("des_fel_elevator_learning_runs.run_label must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_scenario_slug(const std::string& value) {
    if (value.size() > 160) return std::string("des_fel_elevator_learning_runs.scenario_slug must be at most 160 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9._/-]{1,158}[a-z0-9]$)RX"))) return std::string("des_fel_elevator_learning_runs.scenario_slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_seed(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.seed is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_floors(int32_t value) {
    if (value < 2) return std::string("des_fel_elevator_learning_runs.floors is below the minimum");
    if (value > 256) return std::string("des_fel_elevator_learning_runs.floors is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_shafts(int32_t value) {
    if (value < 1) return std::string("des_fel_elevator_learning_runs.shafts is below the minimum");
    if (value > 128) return std::string("des_fel_elevator_learning_runs.shafts is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_capacity(int32_t value) {
    if (value < 1) return std::string("des_fel_elevator_learning_runs.capacity is below the minimum");
    if (value > 10000) return std::string("des_fel_elevator_learning_runs.capacity is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_travel_seconds_micros(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.travel_seconds_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_dwell_seconds_micros(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.dwell_seconds_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_arrival_rate_micros(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.arrival_rate_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_horizon_seconds_micros(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.horizon_seconds_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_events(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.events is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_arrivals(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.arrivals is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_boarded(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.boarded is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_served(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.served is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_mean_wait_micros(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.mean_wait_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_dispatch_decisions(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.dispatch_decisions is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_pomdp_belief_updates(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.pomdp_belief_updates is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_online_learning_updates(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.online_learning_updates is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_learning_runs_online_learning_loss_last_micros(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_learning_runs.online_learning_loss_last_micros is below the minimum");
    return std::nullopt;
}

inline const char* des_fel_elevator_policy_states_table = "des_fel_elevator_policy_states";
inline const std::vector<std::string> des_fel_elevator_policy_states_columns = { "id", "run_id", "policy_kind", "source_kind", "feature_dim", "output_dim", "parameter_count", "online_learning_updates", "loss_history", "state", "created_at" };
inline const char* des_fel_elevator_policy_states_select_sql = R"SQL(select
      id::text as id,
      run_id::text as run_id,
      policy_kind,
      source_kind,
      feature_dim,
      output_dim,
      parameter_count,
      online_learning_updates,
      loss_history::text as loss_history_json,
      state::text as state_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from des_fel_elevator_policy_states)SQL";

enum class DesFelElevatorPolicyStatesPolicyKind { Look, MdpTable, NeuralScorer, PomdpBelief, NeuralTd };
inline std::string des_fel_elevator_policy_states_policy_kind_to_string(DesFelElevatorPolicyStatesPolicyKind value) {
    switch (value) {
        case DesFelElevatorPolicyStatesPolicyKind::Look: return "look";
        case DesFelElevatorPolicyStatesPolicyKind::MdpTable: return "mdp-table";
        case DesFelElevatorPolicyStatesPolicyKind::NeuralScorer: return "neural-scorer";
        case DesFelElevatorPolicyStatesPolicyKind::PomdpBelief: return "pomdp-belief";
        case DesFelElevatorPolicyStatesPolicyKind::NeuralTd: return "neural-td";
    }
    return "";
}
inline std::optional<DesFelElevatorPolicyStatesPolicyKind> parse_des_fel_elevator_policy_states_policy_kind(const std::string& value) {
    if (value == "look") return DesFelElevatorPolicyStatesPolicyKind::Look;
    if (value == "mdp-table") return DesFelElevatorPolicyStatesPolicyKind::MdpTable;
    if (value == "neural-scorer") return DesFelElevatorPolicyStatesPolicyKind::NeuralScorer;
    if (value == "pomdp-belief") return DesFelElevatorPolicyStatesPolicyKind::PomdpBelief;
    if (value == "neural-td") return DesFelElevatorPolicyStatesPolicyKind::NeuralTd;
    return std::nullopt;
}

enum class DesFelElevatorPolicyStatesSourceKind { RunFinal, OfflineTraining, Import, Checkpoint };
inline std::string des_fel_elevator_policy_states_source_kind_to_string(DesFelElevatorPolicyStatesSourceKind value) {
    switch (value) {
        case DesFelElevatorPolicyStatesSourceKind::RunFinal: return "run-final";
        case DesFelElevatorPolicyStatesSourceKind::OfflineTraining: return "offline-training";
        case DesFelElevatorPolicyStatesSourceKind::Import: return "import";
        case DesFelElevatorPolicyStatesSourceKind::Checkpoint: return "checkpoint";
    }
    return "";
}
inline std::optional<DesFelElevatorPolicyStatesSourceKind> parse_des_fel_elevator_policy_states_source_kind(const std::string& value) {
    if (value == "run-final") return DesFelElevatorPolicyStatesSourceKind::RunFinal;
    if (value == "offline-training") return DesFelElevatorPolicyStatesSourceKind::OfflineTraining;
    if (value == "import") return DesFelElevatorPolicyStatesSourceKind::Import;
    if (value == "checkpoint") return DesFelElevatorPolicyStatesSourceKind::Checkpoint;
    return std::nullopt;
}

struct DesFelElevatorPolicyStatesRow {
    std::string id;
    std::string run_id;
    std::string policy_kind;
    std::string source_kind;
    int32_t feature_dim;
    int32_t output_dim;
    int32_t parameter_count;
    int64_t online_learning_updates;
    std::string loss_history;
    std::string state;
    std::string created_at;
};

inline DesFelElevatorPolicyStatesRow des_fel_elevator_policy_states_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesFelElevatorPolicyStatesRow row;
    (void)is_null;
    row.id = get(0);
    row.run_id = get(1);
    row.policy_kind = get(2);
    row.source_kind = get(3);
    row.feature_dim = std::stoi(get(4));
    row.output_dim = std::stoi(get(5));
    row.parameter_count = std::stoi(get(6));
    row.online_learning_updates = std::stoll(get(7));
    row.loss_history = get(8);
    row.state = get(9);
    row.created_at = get(10);
    return row;
}
inline std::optional<std::string> validate_des_fel_elevator_policy_states_feature_dim(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_policy_states.feature_dim is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_policy_states_output_dim(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_policy_states.output_dim is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_policy_states_parameter_count(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_policy_states.parameter_count is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_policy_states_online_learning_updates(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_policy_states.online_learning_updates is below the minimum");
    return std::nullopt;
}

inline const char* des_fel_elevator_dispatch_decisions_table = "des_fel_elevator_dispatch_decisions";
inline const std::vector<std::string> des_fel_elevator_dispatch_decisions_columns = { "id", "run_id", "decision_index", "sim_time_micros", "call_floor", "car_index", "policy_kind", "meta_data", "created_at" };
inline const char* des_fel_elevator_dispatch_decisions_select_sql = R"SQL(select
      id::text as id,
      run_id::text as run_id,
      decision_index,
      sim_time_micros,
      call_floor,
      car_index,
      policy_kind,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from des_fel_elevator_dispatch_decisions)SQL";

enum class DesFelElevatorDispatchDecisionsPolicyKind { Look, MdpTable, NeuralScorer, PomdpBelief, NeuralTd };
inline std::string des_fel_elevator_dispatch_decisions_policy_kind_to_string(DesFelElevatorDispatchDecisionsPolicyKind value) {
    switch (value) {
        case DesFelElevatorDispatchDecisionsPolicyKind::Look: return "look";
        case DesFelElevatorDispatchDecisionsPolicyKind::MdpTable: return "mdp-table";
        case DesFelElevatorDispatchDecisionsPolicyKind::NeuralScorer: return "neural-scorer";
        case DesFelElevatorDispatchDecisionsPolicyKind::PomdpBelief: return "pomdp-belief";
        case DesFelElevatorDispatchDecisionsPolicyKind::NeuralTd: return "neural-td";
    }
    return "";
}
inline std::optional<DesFelElevatorDispatchDecisionsPolicyKind> parse_des_fel_elevator_dispatch_decisions_policy_kind(const std::string& value) {
    if (value == "look") return DesFelElevatorDispatchDecisionsPolicyKind::Look;
    if (value == "mdp-table") return DesFelElevatorDispatchDecisionsPolicyKind::MdpTable;
    if (value == "neural-scorer") return DesFelElevatorDispatchDecisionsPolicyKind::NeuralScorer;
    if (value == "pomdp-belief") return DesFelElevatorDispatchDecisionsPolicyKind::PomdpBelief;
    if (value == "neural-td") return DesFelElevatorDispatchDecisionsPolicyKind::NeuralTd;
    return std::nullopt;
}

struct DesFelElevatorDispatchDecisionsRow {
    std::string id;
    std::string run_id;
    int32_t decision_index;
    int64_t sim_time_micros;
    int32_t call_floor;
    int32_t car_index;
    std::string policy_kind;
    std::string meta_data;
    std::string created_at;
};

inline DesFelElevatorDispatchDecisionsRow des_fel_elevator_dispatch_decisions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesFelElevatorDispatchDecisionsRow row;
    (void)is_null;
    row.id = get(0);
    row.run_id = get(1);
    row.decision_index = std::stoi(get(2));
    row.sim_time_micros = std::stoll(get(3));
    row.call_floor = std::stoi(get(4));
    row.car_index = std::stoi(get(5));
    row.policy_kind = get(6);
    row.meta_data = get(7);
    row.created_at = get(8);
    return row;
}
inline std::optional<std::string> validate_des_fel_elevator_dispatch_decisions_decision_index(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_dispatch_decisions.decision_index is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_dispatch_decisions_sim_time_micros(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_dispatch_decisions.sim_time_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_dispatch_decisions_call_floor(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_dispatch_decisions.call_floor is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_dispatch_decisions_car_index(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_dispatch_decisions.car_index is below the minimum");
    return std::nullopt;
}

inline const char* des_fel_elevator_pomdp_beliefs_table = "des_fel_elevator_pomdp_beliefs";
inline const std::vector<std::string> des_fel_elevator_pomdp_beliefs_columns = { "id", "run_id", "belief_index", "sim_time_micros", "floor", "action", "observation", "empty_prob_micros", "waiting_prob_micros", "crowded_prob_micros", "belief", "created_at" };
inline const char* des_fel_elevator_pomdp_beliefs_select_sql = R"SQL(select
      id::text as id,
      run_id::text as run_id,
      belief_index,
      sim_time_micros,
      floor,
      action,
      observation,
      empty_prob_micros,
      waiting_prob_micros,
      crowded_prob_micros,
      belief::text as belief_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from des_fel_elevator_pomdp_beliefs)SQL";

enum class DesFelElevatorPomdpBeliefsAction { Hold, Dispatch };
inline std::string des_fel_elevator_pomdp_beliefs_action_to_string(DesFelElevatorPomdpBeliefsAction value) {
    switch (value) {
        case DesFelElevatorPomdpBeliefsAction::Hold: return "hold";
        case DesFelElevatorPomdpBeliefsAction::Dispatch: return "dispatch";
    }
    return "";
}
inline std::optional<DesFelElevatorPomdpBeliefsAction> parse_des_fel_elevator_pomdp_beliefs_action(const std::string& value) {
    if (value == "hold") return DesFelElevatorPomdpBeliefsAction::Hold;
    if (value == "dispatch") return DesFelElevatorPomdpBeliefsAction::Dispatch;
    return std::nullopt;
}

enum class DesFelElevatorPomdpBeliefsObservation { Quiet, Call };
inline std::string des_fel_elevator_pomdp_beliefs_observation_to_string(DesFelElevatorPomdpBeliefsObservation value) {
    switch (value) {
        case DesFelElevatorPomdpBeliefsObservation::Quiet: return "quiet";
        case DesFelElevatorPomdpBeliefsObservation::Call: return "call";
    }
    return "";
}
inline std::optional<DesFelElevatorPomdpBeliefsObservation> parse_des_fel_elevator_pomdp_beliefs_observation(const std::string& value) {
    if (value == "quiet") return DesFelElevatorPomdpBeliefsObservation::Quiet;
    if (value == "call") return DesFelElevatorPomdpBeliefsObservation::Call;
    return std::nullopt;
}

struct DesFelElevatorPomdpBeliefsRow {
    std::string id;
    std::string run_id;
    int32_t belief_index;
    int64_t sim_time_micros;
    int32_t floor;
    std::string action;
    std::string observation;
    int32_t empty_prob_micros;
    int32_t waiting_prob_micros;
    int32_t crowded_prob_micros;
    std::string belief;
    std::string created_at;
};

inline DesFelElevatorPomdpBeliefsRow des_fel_elevator_pomdp_beliefs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    DesFelElevatorPomdpBeliefsRow row;
    (void)is_null;
    row.id = get(0);
    row.run_id = get(1);
    row.belief_index = std::stoi(get(2));
    row.sim_time_micros = std::stoll(get(3));
    row.floor = std::stoi(get(4));
    row.action = get(5);
    row.observation = get(6);
    row.empty_prob_micros = std::stoi(get(7));
    row.waiting_prob_micros = std::stoi(get(8));
    row.crowded_prob_micros = std::stoi(get(9));
    row.belief = get(10);
    row.created_at = get(11);
    return row;
}
inline std::optional<std::string> validate_des_fel_elevator_pomdp_beliefs_belief_index(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_pomdp_beliefs.belief_index is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_pomdp_beliefs_sim_time_micros(int64_t value) {
    if (value < 0) return std::string("des_fel_elevator_pomdp_beliefs.sim_time_micros is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_pomdp_beliefs_floor(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_pomdp_beliefs.floor is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_pomdp_beliefs_empty_prob_micros(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_pomdp_beliefs.empty_prob_micros is below the minimum");
    if (value > 1000000) return std::string("des_fel_elevator_pomdp_beliefs.empty_prob_micros is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_pomdp_beliefs_waiting_prob_micros(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_pomdp_beliefs.waiting_prob_micros is below the minimum");
    if (value > 1000000) return std::string("des_fel_elevator_pomdp_beliefs.waiting_prob_micros is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_des_fel_elevator_pomdp_beliefs_crowded_prob_micros(int32_t value) {
    if (value < 0) return std::string("des_fel_elevator_pomdp_beliefs.crowded_prob_micros is below the minimum");
    if (value > 1000000) return std::string("des_fel_elevator_pomdp_beliefs.crowded_prob_micros is above the maximum");
    return std::nullopt;
}

inline const char* benefactor_marketing_clients_table = "benefactor_marketing_clients";
inline const std::vector<std::string> benefactor_marketing_clients_columns = { "id", "status", "name", "slug", "industry", "website_url", "billing_email", "owner_user_id", "service_package", "onboarding_stage", "portal_enabled", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_clients_select_sql = R"SQL(select
      id::text as id,
      status,
      name,
      slug,
      industry,
      website_url,
      billing_email,
      owner_user_id::text as owner_user_id,
      service_package,
      onboarding_stage,
      portal_enabled,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_clients)SQL";

enum class BenefactorMarketingClientsStatus { Onboarding, Active, Paused, Archived };
inline std::string benefactor_marketing_clients_status_to_string(BenefactorMarketingClientsStatus value) {
    switch (value) {
        case BenefactorMarketingClientsStatus::Onboarding: return "onboarding";
        case BenefactorMarketingClientsStatus::Active: return "active";
        case BenefactorMarketingClientsStatus::Paused: return "paused";
        case BenefactorMarketingClientsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<BenefactorMarketingClientsStatus> parse_benefactor_marketing_clients_status(const std::string& value) {
    if (value == "onboarding") return BenefactorMarketingClientsStatus::Onboarding;
    if (value == "active") return BenefactorMarketingClientsStatus::Active;
    if (value == "paused") return BenefactorMarketingClientsStatus::Paused;
    if (value == "archived") return BenefactorMarketingClientsStatus::Archived;
    return std::nullopt;
}

struct BenefactorMarketingClientsRow {
    std::string id;
    std::string status;
    std::string name;
    std::string slug;
    std::optional<std::string> industry;
    std::optional<std::string> website_url;
    std::optional<std::string> billing_email;
    std::optional<std::string> owner_user_id;
    std::optional<std::string> service_package;
    std::string onboarding_stage;
    bool portal_enabled;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingClientsRow benefactor_marketing_clients_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingClientsRow row;
    (void)is_null;
    row.id = get(0);
    row.status = get(1);
    row.name = get(2);
    row.slug = get(3);
    row.industry = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.website_url = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.billing_email = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.owner_user_id = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.service_package = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.onboarding_stage = get(9);
    row.portal_enabled = (get(10) == "t");
    row.meta_data = get(11);
    row.created_at = get(12);
    row.updated_at = get(13);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_clients_name(const std::string& value) {
    if (value.size() > 200) return std::string("benefactor_marketing_clients.name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_clients_slug(const std::string& value) {
    if (value.size() > 220) return std::string("benefactor_marketing_clients.slug must be at most 220 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9-]{1,218}[a-z0-9]$)RX"))) return std::string("benefactor_marketing_clients.slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_clients_industry(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_marketing_clients.industry must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_clients_billing_email(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_clients.billing_email must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_clients_service_package(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_marketing_clients.service_package must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_clients_onboarding_stage(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_marketing_clients.onboarding_stage must be at most 80 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,80}$)RX"))) return std::string("benefactor_marketing_clients.onboarding_stage does not match the required pattern");
    return std::nullopt;
}

inline const char* benefactor_marketing_contacts_table = "benefactor_marketing_contacts";
inline const std::vector<std::string> benefactor_marketing_contacts_columns = { "id", "client_id", "status", "first_name", "last_name", "email", "phone", "job_title", "lifecycle_role", "consent_status", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_contacts_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      status,
      first_name,
      last_name,
      email,
      phone,
      job_title,
      lifecycle_role,
      consent_status,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_contacts)SQL";

enum class BenefactorMarketingContactsStatus { Active, Inactive, Bounced, Unsubscribed };
inline std::string benefactor_marketing_contacts_status_to_string(BenefactorMarketingContactsStatus value) {
    switch (value) {
        case BenefactorMarketingContactsStatus::Active: return "active";
        case BenefactorMarketingContactsStatus::Inactive: return "inactive";
        case BenefactorMarketingContactsStatus::Bounced: return "bounced";
        case BenefactorMarketingContactsStatus::Unsubscribed: return "unsubscribed";
    }
    return "";
}
inline std::optional<BenefactorMarketingContactsStatus> parse_benefactor_marketing_contacts_status(const std::string& value) {
    if (value == "active") return BenefactorMarketingContactsStatus::Active;
    if (value == "inactive") return BenefactorMarketingContactsStatus::Inactive;
    if (value == "bounced") return BenefactorMarketingContactsStatus::Bounced;
    if (value == "unsubscribed") return BenefactorMarketingContactsStatus::Unsubscribed;
    return std::nullopt;
}

enum class BenefactorMarketingContactsLifecycleRole { Primary, DecisionMaker, Billing, Technical, Marketing, Other };
inline std::string benefactor_marketing_contacts_lifecycle_role_to_string(BenefactorMarketingContactsLifecycleRole value) {
    switch (value) {
        case BenefactorMarketingContactsLifecycleRole::Primary: return "primary";
        case BenefactorMarketingContactsLifecycleRole::DecisionMaker: return "decision_maker";
        case BenefactorMarketingContactsLifecycleRole::Billing: return "billing";
        case BenefactorMarketingContactsLifecycleRole::Technical: return "technical";
        case BenefactorMarketingContactsLifecycleRole::Marketing: return "marketing";
        case BenefactorMarketingContactsLifecycleRole::Other: return "other";
    }
    return "";
}
inline std::optional<BenefactorMarketingContactsLifecycleRole> parse_benefactor_marketing_contacts_lifecycle_role(const std::string& value) {
    if (value == "primary") return BenefactorMarketingContactsLifecycleRole::Primary;
    if (value == "decision_maker") return BenefactorMarketingContactsLifecycleRole::DecisionMaker;
    if (value == "billing") return BenefactorMarketingContactsLifecycleRole::Billing;
    if (value == "technical") return BenefactorMarketingContactsLifecycleRole::Technical;
    if (value == "marketing") return BenefactorMarketingContactsLifecycleRole::Marketing;
    if (value == "other") return BenefactorMarketingContactsLifecycleRole::Other;
    return std::nullopt;
}

enum class BenefactorMarketingContactsConsentStatus { Unknown, OptedIn, OptedOut };
inline std::string benefactor_marketing_contacts_consent_status_to_string(BenefactorMarketingContactsConsentStatus value) {
    switch (value) {
        case BenefactorMarketingContactsConsentStatus::Unknown: return "unknown";
        case BenefactorMarketingContactsConsentStatus::OptedIn: return "opted_in";
        case BenefactorMarketingContactsConsentStatus::OptedOut: return "opted_out";
    }
    return "";
}
inline std::optional<BenefactorMarketingContactsConsentStatus> parse_benefactor_marketing_contacts_consent_status(const std::string& value) {
    if (value == "unknown") return BenefactorMarketingContactsConsentStatus::Unknown;
    if (value == "opted_in") return BenefactorMarketingContactsConsentStatus::OptedIn;
    if (value == "opted_out") return BenefactorMarketingContactsConsentStatus::OptedOut;
    return std::nullopt;
}

struct BenefactorMarketingContactsRow {
    std::string id;
    std::string client_id;
    std::string status;
    std::optional<std::string> first_name;
    std::optional<std::string> last_name;
    std::optional<std::string> email;
    std::optional<std::string> phone;
    std::optional<std::string> job_title;
    std::string lifecycle_role;
    std::string consent_status;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingContactsRow benefactor_marketing_contacts_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingContactsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.status = get(2);
    row.first_name = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.last_name = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.email = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.phone = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.job_title = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.lifecycle_role = get(8);
    row.consent_status = get(9);
    row.meta_data = get(10);
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_contacts_first_name(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_marketing_contacts.first_name must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_contacts_last_name(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_marketing_contacts.last_name must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_contacts_email(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_contacts.email must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_contacts_phone(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_marketing_contacts.phone must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_contacts_job_title(const std::string& value) {
    if (value.size() > 160) return std::string("benefactor_marketing_contacts.job_title must be at most 160 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_service_packages_table = "benefactor_marketing_service_packages";
inline const std::vector<std::string> benefactor_marketing_service_packages_columns = { "id", "status", "code", "name", "channel_mix", "deliverables", "monthly_budget_cents", "retainer_cents", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_service_packages_select_sql = R"SQL(select
      id::text as id,
      status,
      code,
      name,
      channel_mix::text as channel_mix_json,
      deliverables::text as deliverables_json,
      monthly_budget_cents,
      retainer_cents,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_service_packages)SQL";

enum class BenefactorMarketingServicePackagesStatus { Active, Retired };
inline std::string benefactor_marketing_service_packages_status_to_string(BenefactorMarketingServicePackagesStatus value) {
    switch (value) {
        case BenefactorMarketingServicePackagesStatus::Active: return "active";
        case BenefactorMarketingServicePackagesStatus::Retired: return "retired";
    }
    return "";
}
inline std::optional<BenefactorMarketingServicePackagesStatus> parse_benefactor_marketing_service_packages_status(const std::string& value) {
    if (value == "active") return BenefactorMarketingServicePackagesStatus::Active;
    if (value == "retired") return BenefactorMarketingServicePackagesStatus::Retired;
    return std::nullopt;
}

struct BenefactorMarketingServicePackagesRow {
    std::string id;
    std::string status;
    std::string code;
    std::string name;
    std::string channel_mix;
    std::string deliverables;
    int32_t monthly_budget_cents;
    int32_t retainer_cents;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingServicePackagesRow benefactor_marketing_service_packages_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingServicePackagesRow row;
    (void)is_null;
    row.id = get(0);
    row.status = get(1);
    row.code = get(2);
    row.name = get(3);
    row.channel_mix = get(4);
    row.deliverables = get(5);
    row.monthly_budget_cents = std::stoi(get(6));
    row.retainer_cents = std::stoi(get(7));
    row.meta_data = get(8);
    row.created_at = get(9);
    row.updated_at = get(10);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_service_packages_code(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_marketing_service_packages.code must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,120}$)RX"))) return std::string("benefactor_marketing_service_packages.code does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_service_packages_name(const std::string& value) {
    if (value.size() > 200) return std::string("benefactor_marketing_service_packages.name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_service_packages_monthly_budget_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_service_packages.monthly_budget_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_service_packages_retainer_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_service_packages.retainer_cents is below the minimum");
    return std::nullopt;
}

inline const char* benefactor_marketing_contracts_table = "benefactor_marketing_contracts";
inline const std::vector<std::string> benefactor_marketing_contracts_columns = { "id", "client_id", "package_id", "status", "contract_number", "starts_on", "ends_on", "billing_terms", "total_value_cents", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_contracts_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      package_id::text as package_id,
      status,
      contract_number,
      starts_on,
      ends_on,
      billing_terms::text as billing_terms_json,
      total_value_cents,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_contracts)SQL";

enum class BenefactorMarketingContractsStatus { Draft, Active, Renewal, Expired, Terminated };
inline std::string benefactor_marketing_contracts_status_to_string(BenefactorMarketingContractsStatus value) {
    switch (value) {
        case BenefactorMarketingContractsStatus::Draft: return "draft";
        case BenefactorMarketingContractsStatus::Active: return "active";
        case BenefactorMarketingContractsStatus::Renewal: return "renewal";
        case BenefactorMarketingContractsStatus::Expired: return "expired";
        case BenefactorMarketingContractsStatus::Terminated: return "terminated";
    }
    return "";
}
inline std::optional<BenefactorMarketingContractsStatus> parse_benefactor_marketing_contracts_status(const std::string& value) {
    if (value == "draft") return BenefactorMarketingContractsStatus::Draft;
    if (value == "active") return BenefactorMarketingContractsStatus::Active;
    if (value == "renewal") return BenefactorMarketingContractsStatus::Renewal;
    if (value == "expired") return BenefactorMarketingContractsStatus::Expired;
    if (value == "terminated") return BenefactorMarketingContractsStatus::Terminated;
    return std::nullopt;
}

struct BenefactorMarketingContractsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> package_id;
    std::string status;
    std::optional<std::string> contract_number;
    std::optional<std::string> starts_on;
    std::optional<std::string> ends_on;
    std::string billing_terms;
    int32_t total_value_cents;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingContractsRow benefactor_marketing_contracts_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingContractsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.package_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.status = get(3);
    row.contract_number = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.starts_on = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.ends_on = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.billing_terms = get(7);
    row.total_value_cents = std::stoi(get(8));
    row.meta_data = get(9);
    row.created_at = get(10);
    row.updated_at = get(11);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_contracts_contract_number(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_marketing_contracts.contract_number must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_contracts_starts_on(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_contracts.starts_on must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_contracts.starts_on does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_contracts_ends_on(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_contracts.ends_on must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_contracts.ends_on does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_contracts_total_value_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_contracts.total_value_cents is below the minimum");
    return std::nullopt;
}

inline const char* benefactor_marketing_invoices_table = "benefactor_marketing_invoices";
inline const std::vector<std::string> benefactor_marketing_invoices_columns = { "id", "client_id", "contract_id", "status", "invoice_number", "due_on", "amount_cents", "paid_at", "line_items", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_invoices_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      contract_id::text as contract_id,
      status,
      invoice_number,
      due_on,
      amount_cents,
      to_char(paid_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as paid_at,
      line_items::text as line_items_json,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_invoices)SQL";

enum class BenefactorMarketingInvoicesStatus { Draft, Sent, Paid, Overdue, Void };
inline std::string benefactor_marketing_invoices_status_to_string(BenefactorMarketingInvoicesStatus value) {
    switch (value) {
        case BenefactorMarketingInvoicesStatus::Draft: return "draft";
        case BenefactorMarketingInvoicesStatus::Sent: return "sent";
        case BenefactorMarketingInvoicesStatus::Paid: return "paid";
        case BenefactorMarketingInvoicesStatus::Overdue: return "overdue";
        case BenefactorMarketingInvoicesStatus::Void: return "void";
    }
    return "";
}
inline std::optional<BenefactorMarketingInvoicesStatus> parse_benefactor_marketing_invoices_status(const std::string& value) {
    if (value == "draft") return BenefactorMarketingInvoicesStatus::Draft;
    if (value == "sent") return BenefactorMarketingInvoicesStatus::Sent;
    if (value == "paid") return BenefactorMarketingInvoicesStatus::Paid;
    if (value == "overdue") return BenefactorMarketingInvoicesStatus::Overdue;
    if (value == "void") return BenefactorMarketingInvoicesStatus::Void;
    return std::nullopt;
}

struct BenefactorMarketingInvoicesRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> contract_id;
    std::string status;
    std::optional<std::string> invoice_number;
    std::optional<std::string> due_on;
    int32_t amount_cents;
    std::optional<std::string> paid_at;
    std::string line_items;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingInvoicesRow benefactor_marketing_invoices_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingInvoicesRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.contract_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.status = get(3);
    row.invoice_number = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.due_on = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.amount_cents = std::stoi(get(6));
    row.paid_at = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.line_items = get(8);
    row.meta_data = get(9);
    row.created_at = get(10);
    row.updated_at = get(11);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_invoices_invoice_number(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_marketing_invoices.invoice_number must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_invoices_due_on(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_invoices.due_on must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_invoices.due_on does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_invoices_amount_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_invoices.amount_cents is below the minimum");
    return std::nullopt;
}

inline const char* benefactor_marketing_integrations_table = "benefactor_marketing_integrations";
inline const std::vector<std::string> benefactor_marketing_integrations_columns = { "id", "client_id", "platform", "status", "auth_kind", "external_account_id", "sync_cursor", "config", "last_sync_at", "created_at", "updated_at" };
inline const char* benefactor_marketing_integrations_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      platform,
      status,
      auth_kind,
      external_account_id,
      sync_cursor,
      config::text as config_json,
      to_char(last_sync_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_sync_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_integrations)SQL";

enum class BenefactorMarketingIntegrationsPlatform { Salesforce, Hubspot, Apollo, Zoominfo, GoogleAnalytics, GoogleAds, LinkedinAds, MetaAds, Mailchimp, Sendgrid, Scraper, Custom };
inline std::string benefactor_marketing_integrations_platform_to_string(BenefactorMarketingIntegrationsPlatform value) {
    switch (value) {
        case BenefactorMarketingIntegrationsPlatform::Salesforce: return "salesforce";
        case BenefactorMarketingIntegrationsPlatform::Hubspot: return "hubspot";
        case BenefactorMarketingIntegrationsPlatform::Apollo: return "apollo";
        case BenefactorMarketingIntegrationsPlatform::Zoominfo: return "zoominfo";
        case BenefactorMarketingIntegrationsPlatform::GoogleAnalytics: return "google_analytics";
        case BenefactorMarketingIntegrationsPlatform::GoogleAds: return "google_ads";
        case BenefactorMarketingIntegrationsPlatform::LinkedinAds: return "linkedin_ads";
        case BenefactorMarketingIntegrationsPlatform::MetaAds: return "meta_ads";
        case BenefactorMarketingIntegrationsPlatform::Mailchimp: return "mailchimp";
        case BenefactorMarketingIntegrationsPlatform::Sendgrid: return "sendgrid";
        case BenefactorMarketingIntegrationsPlatform::Scraper: return "scraper";
        case BenefactorMarketingIntegrationsPlatform::Custom: return "custom";
    }
    return "";
}
inline std::optional<BenefactorMarketingIntegrationsPlatform> parse_benefactor_marketing_integrations_platform(const std::string& value) {
    if (value == "salesforce") return BenefactorMarketingIntegrationsPlatform::Salesforce;
    if (value == "hubspot") return BenefactorMarketingIntegrationsPlatform::Hubspot;
    if (value == "apollo") return BenefactorMarketingIntegrationsPlatform::Apollo;
    if (value == "zoominfo") return BenefactorMarketingIntegrationsPlatform::Zoominfo;
    if (value == "google_analytics") return BenefactorMarketingIntegrationsPlatform::GoogleAnalytics;
    if (value == "google_ads") return BenefactorMarketingIntegrationsPlatform::GoogleAds;
    if (value == "linkedin_ads") return BenefactorMarketingIntegrationsPlatform::LinkedinAds;
    if (value == "meta_ads") return BenefactorMarketingIntegrationsPlatform::MetaAds;
    if (value == "mailchimp") return BenefactorMarketingIntegrationsPlatform::Mailchimp;
    if (value == "sendgrid") return BenefactorMarketingIntegrationsPlatform::Sendgrid;
    if (value == "scraper") return BenefactorMarketingIntegrationsPlatform::Scraper;
    if (value == "custom") return BenefactorMarketingIntegrationsPlatform::Custom;
    return std::nullopt;
}

enum class BenefactorMarketingIntegrationsStatus { Connected, Disabled, Error };
inline std::string benefactor_marketing_integrations_status_to_string(BenefactorMarketingIntegrationsStatus value) {
    switch (value) {
        case BenefactorMarketingIntegrationsStatus::Connected: return "connected";
        case BenefactorMarketingIntegrationsStatus::Disabled: return "disabled";
        case BenefactorMarketingIntegrationsStatus::Error: return "error";
    }
    return "";
}
inline std::optional<BenefactorMarketingIntegrationsStatus> parse_benefactor_marketing_integrations_status(const std::string& value) {
    if (value == "connected") return BenefactorMarketingIntegrationsStatus::Connected;
    if (value == "disabled") return BenefactorMarketingIntegrationsStatus::Disabled;
    if (value == "error") return BenefactorMarketingIntegrationsStatus::Error;
    return std::nullopt;
}

enum class BenefactorMarketingIntegrationsAuthKind { Oauth2, ApiKey, Webhook, Manual };
inline std::string benefactor_marketing_integrations_auth_kind_to_string(BenefactorMarketingIntegrationsAuthKind value) {
    switch (value) {
        case BenefactorMarketingIntegrationsAuthKind::Oauth2: return "oauth2";
        case BenefactorMarketingIntegrationsAuthKind::ApiKey: return "api_key";
        case BenefactorMarketingIntegrationsAuthKind::Webhook: return "webhook";
        case BenefactorMarketingIntegrationsAuthKind::Manual: return "manual";
    }
    return "";
}
inline std::optional<BenefactorMarketingIntegrationsAuthKind> parse_benefactor_marketing_integrations_auth_kind(const std::string& value) {
    if (value == "oauth2") return BenefactorMarketingIntegrationsAuthKind::Oauth2;
    if (value == "api_key") return BenefactorMarketingIntegrationsAuthKind::ApiKey;
    if (value == "webhook") return BenefactorMarketingIntegrationsAuthKind::Webhook;
    if (value == "manual") return BenefactorMarketingIntegrationsAuthKind::Manual;
    return std::nullopt;
}

struct BenefactorMarketingIntegrationsRow {
    std::string id;
    std::optional<std::string> client_id;
    std::string platform;
    std::string status;
    std::string auth_kind;
    std::optional<std::string> external_account_id;
    std::optional<std::string> sync_cursor;
    std::string config;
    std::optional<std::string> last_sync_at;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingIntegrationsRow benefactor_marketing_integrations_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingIntegrationsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.platform = get(2);
    row.status = get(3);
    row.auth_kind = get(4);
    row.external_account_id = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.sync_cursor = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.config = get(7);
    row.last_sync_at = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.created_at = get(9);
    row.updated_at = get(10);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_integrations_external_account_id(const std::string& value) {
    if (value.size() > 200) return std::string("benefactor_marketing_integrations.external_account_id must be at most 200 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_leads_table = "benefactor_marketing_leads";
inline const std::vector<std::string> benefactor_marketing_leads_columns = { "id", "client_id", "source_integration_id", "status", "company_name", "domain", "contact_name", "contact_email", "contact_title", "country_code", "lead_score", "icp_fit_score", "verification_status", "enrichment_status", "company_profile", "signals", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_leads_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      source_integration_id::text as source_integration_id,
      status,
      company_name,
      domain,
      contact_name,
      contact_email,
      contact_title,
      country_code,
      lead_score,
      icp_fit_score,
      verification_status,
      enrichment_status,
      company_profile::text as company_profile_json,
      signals::text as signals_json,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_leads)SQL";

enum class BenefactorMarketingLeadsStatus { New, Researching, Qualified, Disqualified, Contacted, Converted };
inline std::string benefactor_marketing_leads_status_to_string(BenefactorMarketingLeadsStatus value) {
    switch (value) {
        case BenefactorMarketingLeadsStatus::New: return "new";
        case BenefactorMarketingLeadsStatus::Researching: return "researching";
        case BenefactorMarketingLeadsStatus::Qualified: return "qualified";
        case BenefactorMarketingLeadsStatus::Disqualified: return "disqualified";
        case BenefactorMarketingLeadsStatus::Contacted: return "contacted";
        case BenefactorMarketingLeadsStatus::Converted: return "converted";
    }
    return "";
}
inline std::optional<BenefactorMarketingLeadsStatus> parse_benefactor_marketing_leads_status(const std::string& value) {
    if (value == "new") return BenefactorMarketingLeadsStatus::New;
    if (value == "researching") return BenefactorMarketingLeadsStatus::Researching;
    if (value == "qualified") return BenefactorMarketingLeadsStatus::Qualified;
    if (value == "disqualified") return BenefactorMarketingLeadsStatus::Disqualified;
    if (value == "contacted") return BenefactorMarketingLeadsStatus::Contacted;
    if (value == "converted") return BenefactorMarketingLeadsStatus::Converted;
    return std::nullopt;
}

enum class BenefactorMarketingLeadsVerificationStatus { Unknown, Verified, Invalid, Risky };
inline std::string benefactor_marketing_leads_verification_status_to_string(BenefactorMarketingLeadsVerificationStatus value) {
    switch (value) {
        case BenefactorMarketingLeadsVerificationStatus::Unknown: return "unknown";
        case BenefactorMarketingLeadsVerificationStatus::Verified: return "verified";
        case BenefactorMarketingLeadsVerificationStatus::Invalid: return "invalid";
        case BenefactorMarketingLeadsVerificationStatus::Risky: return "risky";
    }
    return "";
}
inline std::optional<BenefactorMarketingLeadsVerificationStatus> parse_benefactor_marketing_leads_verification_status(const std::string& value) {
    if (value == "unknown") return BenefactorMarketingLeadsVerificationStatus::Unknown;
    if (value == "verified") return BenefactorMarketingLeadsVerificationStatus::Verified;
    if (value == "invalid") return BenefactorMarketingLeadsVerificationStatus::Invalid;
    if (value == "risky") return BenefactorMarketingLeadsVerificationStatus::Risky;
    return std::nullopt;
}

enum class BenefactorMarketingLeadsEnrichmentStatus { Pending, Running, Completed, Failed };
inline std::string benefactor_marketing_leads_enrichment_status_to_string(BenefactorMarketingLeadsEnrichmentStatus value) {
    switch (value) {
        case BenefactorMarketingLeadsEnrichmentStatus::Pending: return "pending";
        case BenefactorMarketingLeadsEnrichmentStatus::Running: return "running";
        case BenefactorMarketingLeadsEnrichmentStatus::Completed: return "completed";
        case BenefactorMarketingLeadsEnrichmentStatus::Failed: return "failed";
    }
    return "";
}
inline std::optional<BenefactorMarketingLeadsEnrichmentStatus> parse_benefactor_marketing_leads_enrichment_status(const std::string& value) {
    if (value == "pending") return BenefactorMarketingLeadsEnrichmentStatus::Pending;
    if (value == "running") return BenefactorMarketingLeadsEnrichmentStatus::Running;
    if (value == "completed") return BenefactorMarketingLeadsEnrichmentStatus::Completed;
    if (value == "failed") return BenefactorMarketingLeadsEnrichmentStatus::Failed;
    return std::nullopt;
}

struct BenefactorMarketingLeadsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> source_integration_id;
    std::string status;
    std::string company_name;
    std::optional<std::string> domain;
    std::optional<std::string> contact_name;
    std::optional<std::string> contact_email;
    std::optional<std::string> contact_title;
    std::optional<std::string> country_code;
    int32_t lead_score;
    int32_t icp_fit_score;
    std::string verification_status;
    std::string enrichment_status;
    std::string company_profile;
    std::string signals;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingLeadsRow benefactor_marketing_leads_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingLeadsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.source_integration_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.status = get(3);
    row.company_name = get(4);
    row.domain = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.contact_name = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.contact_email = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.contact_title = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.country_code = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.lead_score = std::stoi(get(10));
    row.icp_fit_score = std::stoi(get(11));
    row.verification_status = get(12);
    row.enrichment_status = get(13);
    row.company_profile = get(14);
    row.signals = get(15);
    row.meta_data = get(16);
    row.created_at = get(17);
    row.updated_at = get(18);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_leads_company_name(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_leads.company_name must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_leads_domain(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_leads.domain must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_leads_contact_name(const std::string& value) {
    if (value.size() > 200) return std::string("benefactor_marketing_leads.contact_name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_leads_contact_email(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_leads.contact_email must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_leads_contact_title(const std::string& value) {
    if (value.size() > 160) return std::string("benefactor_marketing_leads.contact_title must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_leads_country_code(const std::string& value) {
    if (value.size() > 8) return std::string("benefactor_marketing_leads.country_code must be at most 8 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_leads_lead_score(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_leads.lead_score is below the minimum");
    if (value > 100) return std::string("benefactor_marketing_leads.lead_score is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_leads_icp_fit_score(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_leads.icp_fit_score is below the minimum");
    if (value > 100) return std::string("benefactor_marketing_leads.icp_fit_score is above the maximum");
    return std::nullopt;
}

inline const char* benefactor_marketing_enrichment_jobs_table = "benefactor_marketing_enrichment_jobs";
inline const std::vector<std::string> benefactor_marketing_enrichment_jobs_columns = { "id", "client_id", "lead_id", "job_kind", "status", "external_job_id", "scraper_handoff_url", "input", "result", "error_summary", "queued_at", "started_at", "completed_at", "created_at", "updated_at" };
inline const char* benefactor_marketing_enrichment_jobs_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      lead_id::text as lead_id,
      job_kind,
      status,
      external_job_id,
      scraper_handoff_url,
      input::text as input_json,
      result::text as result_json,
      error_summary,
      to_char(queued_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as queued_at,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(completed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as completed_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_enrichment_jobs)SQL";

enum class BenefactorMarketingEnrichmentJobsJobKind { LeadEnrichment, CompanyResearch, ContactVerification, ProspectScrape, CompetitiveIntel };
inline std::string benefactor_marketing_enrichment_jobs_job_kind_to_string(BenefactorMarketingEnrichmentJobsJobKind value) {
    switch (value) {
        case BenefactorMarketingEnrichmentJobsJobKind::LeadEnrichment: return "lead_enrichment";
        case BenefactorMarketingEnrichmentJobsJobKind::CompanyResearch: return "company_research";
        case BenefactorMarketingEnrichmentJobsJobKind::ContactVerification: return "contact_verification";
        case BenefactorMarketingEnrichmentJobsJobKind::ProspectScrape: return "prospect_scrape";
        case BenefactorMarketingEnrichmentJobsJobKind::CompetitiveIntel: return "competitive_intel";
    }
    return "";
}
inline std::optional<BenefactorMarketingEnrichmentJobsJobKind> parse_benefactor_marketing_enrichment_jobs_job_kind(const std::string& value) {
    if (value == "lead_enrichment") return BenefactorMarketingEnrichmentJobsJobKind::LeadEnrichment;
    if (value == "company_research") return BenefactorMarketingEnrichmentJobsJobKind::CompanyResearch;
    if (value == "contact_verification") return BenefactorMarketingEnrichmentJobsJobKind::ContactVerification;
    if (value == "prospect_scrape") return BenefactorMarketingEnrichmentJobsJobKind::ProspectScrape;
    if (value == "competitive_intel") return BenefactorMarketingEnrichmentJobsJobKind::CompetitiveIntel;
    return std::nullopt;
}

enum class BenefactorMarketingEnrichmentJobsStatus { Queued, Running, Completed, Failed, Canceled };
inline std::string benefactor_marketing_enrichment_jobs_status_to_string(BenefactorMarketingEnrichmentJobsStatus value) {
    switch (value) {
        case BenefactorMarketingEnrichmentJobsStatus::Queued: return "queued";
        case BenefactorMarketingEnrichmentJobsStatus::Running: return "running";
        case BenefactorMarketingEnrichmentJobsStatus::Completed: return "completed";
        case BenefactorMarketingEnrichmentJobsStatus::Failed: return "failed";
        case BenefactorMarketingEnrichmentJobsStatus::Canceled: return "canceled";
    }
    return "";
}
inline std::optional<BenefactorMarketingEnrichmentJobsStatus> parse_benefactor_marketing_enrichment_jobs_status(const std::string& value) {
    if (value == "queued") return BenefactorMarketingEnrichmentJobsStatus::Queued;
    if (value == "running") return BenefactorMarketingEnrichmentJobsStatus::Running;
    if (value == "completed") return BenefactorMarketingEnrichmentJobsStatus::Completed;
    if (value == "failed") return BenefactorMarketingEnrichmentJobsStatus::Failed;
    if (value == "canceled") return BenefactorMarketingEnrichmentJobsStatus::Canceled;
    return std::nullopt;
}

struct BenefactorMarketingEnrichmentJobsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> lead_id;
    std::string job_kind;
    std::string status;
    std::optional<std::string> external_job_id;
    std::optional<std::string> scraper_handoff_url;
    std::string input;
    std::string result;
    std::optional<std::string> error_summary;
    std::string queued_at;
    std::optional<std::string> started_at;
    std::optional<std::string> completed_at;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingEnrichmentJobsRow benefactor_marketing_enrichment_jobs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingEnrichmentJobsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.lead_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.job_kind = get(3);
    row.status = get(4);
    row.external_job_id = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.scraper_handoff_url = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.input = get(7);
    row.result = get(8);
    row.error_summary = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.queued_at = get(10);
    row.started_at = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.completed_at = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.created_at = get(13);
    row.updated_at = get(14);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_enrichment_jobs_external_job_id(const std::string& value) {
    if (value.size() > 200) return std::string("benefactor_marketing_enrichment_jobs.external_job_id must be at most 200 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_campaigns_table = "benefactor_marketing_campaigns";
inline const std::vector<std::string> benefactor_marketing_campaigns_columns = { "id", "client_id", "status", "campaign_kind", "name", "objective", "budget_cents", "starts_on", "ends_on", "target_segments", "kpis", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_campaigns_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      status,
      campaign_kind,
      name,
      objective,
      budget_cents,
      starts_on,
      ends_on,
      target_segments::text as target_segments_json,
      kpis::text as kpis_json,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_campaigns)SQL";

enum class BenefactorMarketingCampaignsStatus { Draft, Active, Paused, Completed, Archived };
inline std::string benefactor_marketing_campaigns_status_to_string(BenefactorMarketingCampaignsStatus value) {
    switch (value) {
        case BenefactorMarketingCampaignsStatus::Draft: return "draft";
        case BenefactorMarketingCampaignsStatus::Active: return "active";
        case BenefactorMarketingCampaignsStatus::Paused: return "paused";
        case BenefactorMarketingCampaignsStatus::Completed: return "completed";
        case BenefactorMarketingCampaignsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<BenefactorMarketingCampaignsStatus> parse_benefactor_marketing_campaigns_status(const std::string& value) {
    if (value == "draft") return BenefactorMarketingCampaignsStatus::Draft;
    if (value == "active") return BenefactorMarketingCampaignsStatus::Active;
    if (value == "paused") return BenefactorMarketingCampaignsStatus::Paused;
    if (value == "completed") return BenefactorMarketingCampaignsStatus::Completed;
    if (value == "archived") return BenefactorMarketingCampaignsStatus::Archived;
    return std::nullopt;
}

enum class BenefactorMarketingCampaignsCampaignKind { SocialMedia, SeoAeo, Email, Outreach, PaidAds, Content, MultiChannel };
inline std::string benefactor_marketing_campaigns_campaign_kind_to_string(BenefactorMarketingCampaignsCampaignKind value) {
    switch (value) {
        case BenefactorMarketingCampaignsCampaignKind::SocialMedia: return "social_media";
        case BenefactorMarketingCampaignsCampaignKind::SeoAeo: return "seo_aeo";
        case BenefactorMarketingCampaignsCampaignKind::Email: return "email";
        case BenefactorMarketingCampaignsCampaignKind::Outreach: return "outreach";
        case BenefactorMarketingCampaignsCampaignKind::PaidAds: return "paid_ads";
        case BenefactorMarketingCampaignsCampaignKind::Content: return "content";
        case BenefactorMarketingCampaignsCampaignKind::MultiChannel: return "multi_channel";
    }
    return "";
}
inline std::optional<BenefactorMarketingCampaignsCampaignKind> parse_benefactor_marketing_campaigns_campaign_kind(const std::string& value) {
    if (value == "social_media") return BenefactorMarketingCampaignsCampaignKind::SocialMedia;
    if (value == "seo_aeo") return BenefactorMarketingCampaignsCampaignKind::SeoAeo;
    if (value == "email") return BenefactorMarketingCampaignsCampaignKind::Email;
    if (value == "outreach") return BenefactorMarketingCampaignsCampaignKind::Outreach;
    if (value == "paid_ads") return BenefactorMarketingCampaignsCampaignKind::PaidAds;
    if (value == "content") return BenefactorMarketingCampaignsCampaignKind::Content;
    if (value == "multi_channel") return BenefactorMarketingCampaignsCampaignKind::MultiChannel;
    return std::nullopt;
}

struct BenefactorMarketingCampaignsRow {
    std::string id;
    std::string client_id;
    std::string status;
    std::string campaign_kind;
    std::string name;
    std::optional<std::string> objective;
    int32_t budget_cents;
    std::optional<std::string> starts_on;
    std::optional<std::string> ends_on;
    std::string target_segments;
    std::string kpis;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingCampaignsRow benefactor_marketing_campaigns_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingCampaignsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.status = get(2);
    row.campaign_kind = get(3);
    row.name = get(4);
    row.objective = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.budget_cents = std::stoi(get(6));
    row.starts_on = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.ends_on = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.target_segments = get(9);
    row.kpis = get(10);
    row.meta_data = get(11);
    row.created_at = get(12);
    row.updated_at = get(13);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_campaigns_name(const std::string& value) {
    if (value.size() > 220) return std::string("benefactor_marketing_campaigns.name must be at most 220 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_campaigns_budget_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_campaigns.budget_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_campaigns_starts_on(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_campaigns.starts_on must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_campaigns.starts_on does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_campaigns_ends_on(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_campaigns.ends_on must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_campaigns.ends_on does not match the required pattern");
    return std::nullopt;
}

inline const char* benefactor_marketing_campaign_channels_table = "benefactor_marketing_campaign_channels";
inline const std::vector<std::string> benefactor_marketing_campaign_channels_columns = { "id", "campaign_id", "channel", "status", "external_campaign_id", "strategy", "schedule", "metrics_snapshot", "created_at", "updated_at" };
inline const char* benefactor_marketing_campaign_channels_select_sql = R"SQL(select
      id::text as id,
      campaign_id::text as campaign_id,
      channel,
      status,
      external_campaign_id,
      strategy::text as strategy_json,
      schedule::text as schedule_json,
      metrics_snapshot::text as metrics_snapshot_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_campaign_channels)SQL";

enum class BenefactorMarketingCampaignChannelsChannel { Social, Linkedin, Email, Sms, Seo, Aeo, GoogleAds, MetaAds, LandingPage, Content };
inline std::string benefactor_marketing_campaign_channels_channel_to_string(BenefactorMarketingCampaignChannelsChannel value) {
    switch (value) {
        case BenefactorMarketingCampaignChannelsChannel::Social: return "social";
        case BenefactorMarketingCampaignChannelsChannel::Linkedin: return "linkedin";
        case BenefactorMarketingCampaignChannelsChannel::Email: return "email";
        case BenefactorMarketingCampaignChannelsChannel::Sms: return "sms";
        case BenefactorMarketingCampaignChannelsChannel::Seo: return "seo";
        case BenefactorMarketingCampaignChannelsChannel::Aeo: return "aeo";
        case BenefactorMarketingCampaignChannelsChannel::GoogleAds: return "google_ads";
        case BenefactorMarketingCampaignChannelsChannel::MetaAds: return "meta_ads";
        case BenefactorMarketingCampaignChannelsChannel::LandingPage: return "landing_page";
        case BenefactorMarketingCampaignChannelsChannel::Content: return "content";
    }
    return "";
}
inline std::optional<BenefactorMarketingCampaignChannelsChannel> parse_benefactor_marketing_campaign_channels_channel(const std::string& value) {
    if (value == "social") return BenefactorMarketingCampaignChannelsChannel::Social;
    if (value == "linkedin") return BenefactorMarketingCampaignChannelsChannel::Linkedin;
    if (value == "email") return BenefactorMarketingCampaignChannelsChannel::Email;
    if (value == "sms") return BenefactorMarketingCampaignChannelsChannel::Sms;
    if (value == "seo") return BenefactorMarketingCampaignChannelsChannel::Seo;
    if (value == "aeo") return BenefactorMarketingCampaignChannelsChannel::Aeo;
    if (value == "google_ads") return BenefactorMarketingCampaignChannelsChannel::GoogleAds;
    if (value == "meta_ads") return BenefactorMarketingCampaignChannelsChannel::MetaAds;
    if (value == "landing_page") return BenefactorMarketingCampaignChannelsChannel::LandingPage;
    if (value == "content") return BenefactorMarketingCampaignChannelsChannel::Content;
    return std::nullopt;
}

enum class BenefactorMarketingCampaignChannelsStatus { Draft, Scheduled, Live, Paused, Completed };
inline std::string benefactor_marketing_campaign_channels_status_to_string(BenefactorMarketingCampaignChannelsStatus value) {
    switch (value) {
        case BenefactorMarketingCampaignChannelsStatus::Draft: return "draft";
        case BenefactorMarketingCampaignChannelsStatus::Scheduled: return "scheduled";
        case BenefactorMarketingCampaignChannelsStatus::Live: return "live";
        case BenefactorMarketingCampaignChannelsStatus::Paused: return "paused";
        case BenefactorMarketingCampaignChannelsStatus::Completed: return "completed";
    }
    return "";
}
inline std::optional<BenefactorMarketingCampaignChannelsStatus> parse_benefactor_marketing_campaign_channels_status(const std::string& value) {
    if (value == "draft") return BenefactorMarketingCampaignChannelsStatus::Draft;
    if (value == "scheduled") return BenefactorMarketingCampaignChannelsStatus::Scheduled;
    if (value == "live") return BenefactorMarketingCampaignChannelsStatus::Live;
    if (value == "paused") return BenefactorMarketingCampaignChannelsStatus::Paused;
    if (value == "completed") return BenefactorMarketingCampaignChannelsStatus::Completed;
    return std::nullopt;
}

struct BenefactorMarketingCampaignChannelsRow {
    std::string id;
    std::string campaign_id;
    std::string channel;
    std::string status;
    std::optional<std::string> external_campaign_id;
    std::string strategy;
    std::string schedule;
    std::string metrics_snapshot;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingCampaignChannelsRow benefactor_marketing_campaign_channels_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingCampaignChannelsRow row;
    (void)is_null;
    row.id = get(0);
    row.campaign_id = get(1);
    row.channel = get(2);
    row.status = get(3);
    row.external_campaign_id = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.strategy = get(5);
    row.schedule = get(6);
    row.metrics_snapshot = get(7);
    row.created_at = get(8);
    row.updated_at = get(9);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_campaign_channels_external_campaign_id(const std::string& value) {
    if (value.size() > 200) return std::string("benefactor_marketing_campaign_channels.external_campaign_id must be at most 200 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_campaign_experiments_table = "benefactor_marketing_campaign_experiments";
inline const std::vector<std::string> benefactor_marketing_campaign_experiments_columns = { "id", "campaign_id", "status", "experiment_kind", "hypothesis", "variants", "winning_variant", "result_summary", "started_at", "ended_at", "created_at", "updated_at" };
inline const char* benefactor_marketing_campaign_experiments_select_sql = R"SQL(select
      id::text as id,
      campaign_id::text as campaign_id,
      status,
      experiment_kind,
      hypothesis,
      variants::text as variants_json,
      winning_variant,
      result_summary::text as result_summary_json,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(ended_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as ended_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_campaign_experiments)SQL";

enum class BenefactorMarketingCampaignExperimentsStatus { Draft, Running, WinnerSelected, Stopped };
inline std::string benefactor_marketing_campaign_experiments_status_to_string(BenefactorMarketingCampaignExperimentsStatus value) {
    switch (value) {
        case BenefactorMarketingCampaignExperimentsStatus::Draft: return "draft";
        case BenefactorMarketingCampaignExperimentsStatus::Running: return "running";
        case BenefactorMarketingCampaignExperimentsStatus::WinnerSelected: return "winner_selected";
        case BenefactorMarketingCampaignExperimentsStatus::Stopped: return "stopped";
    }
    return "";
}
inline std::optional<BenefactorMarketingCampaignExperimentsStatus> parse_benefactor_marketing_campaign_experiments_status(const std::string& value) {
    if (value == "draft") return BenefactorMarketingCampaignExperimentsStatus::Draft;
    if (value == "running") return BenefactorMarketingCampaignExperimentsStatus::Running;
    if (value == "winner_selected") return BenefactorMarketingCampaignExperimentsStatus::WinnerSelected;
    if (value == "stopped") return BenefactorMarketingCampaignExperimentsStatus::Stopped;
    return std::nullopt;
}

enum class BenefactorMarketingCampaignExperimentsExperimentKind { SubjectLine, Creative, Copy, LandingPage, Audience, Budget };
inline std::string benefactor_marketing_campaign_experiments_experiment_kind_to_string(BenefactorMarketingCampaignExperimentsExperimentKind value) {
    switch (value) {
        case BenefactorMarketingCampaignExperimentsExperimentKind::SubjectLine: return "subject_line";
        case BenefactorMarketingCampaignExperimentsExperimentKind::Creative: return "creative";
        case BenefactorMarketingCampaignExperimentsExperimentKind::Copy: return "copy";
        case BenefactorMarketingCampaignExperimentsExperimentKind::LandingPage: return "landing_page";
        case BenefactorMarketingCampaignExperimentsExperimentKind::Audience: return "audience";
        case BenefactorMarketingCampaignExperimentsExperimentKind::Budget: return "budget";
    }
    return "";
}
inline std::optional<BenefactorMarketingCampaignExperimentsExperimentKind> parse_benefactor_marketing_campaign_experiments_experiment_kind(const std::string& value) {
    if (value == "subject_line") return BenefactorMarketingCampaignExperimentsExperimentKind::SubjectLine;
    if (value == "creative") return BenefactorMarketingCampaignExperimentsExperimentKind::Creative;
    if (value == "copy") return BenefactorMarketingCampaignExperimentsExperimentKind::Copy;
    if (value == "landing_page") return BenefactorMarketingCampaignExperimentsExperimentKind::LandingPage;
    if (value == "audience") return BenefactorMarketingCampaignExperimentsExperimentKind::Audience;
    if (value == "budget") return BenefactorMarketingCampaignExperimentsExperimentKind::Budget;
    return std::nullopt;
}

struct BenefactorMarketingCampaignExperimentsRow {
    std::string id;
    std::string campaign_id;
    std::string status;
    std::string experiment_kind;
    std::optional<std::string> hypothesis;
    std::string variants;
    std::optional<std::string> winning_variant;
    std::string result_summary;
    std::optional<std::string> started_at;
    std::optional<std::string> ended_at;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingCampaignExperimentsRow benefactor_marketing_campaign_experiments_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingCampaignExperimentsRow row;
    (void)is_null;
    row.id = get(0);
    row.campaign_id = get(1);
    row.status = get(2);
    row.experiment_kind = get(3);
    row.hypothesis = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.variants = get(5);
    row.winning_variant = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.result_summary = get(7);
    row.started_at = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.ended_at = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.created_at = get(10);
    row.updated_at = get(11);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_campaign_experiments_winning_variant(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_marketing_campaign_experiments.winning_variant must be at most 120 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_automation_workflows_table = "benefactor_marketing_automation_workflows";
inline const std::vector<std::string> benefactor_marketing_automation_workflows_columns = { "id", "client_id", "status", "name", "trigger_kind", "trigger_config", "action_graph", "last_run_at", "created_at", "updated_at" };
inline const char* benefactor_marketing_automation_workflows_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      status,
      name,
      trigger_kind,
      trigger_config::text as trigger_config_json,
      action_graph::text as action_graph_json,
      to_char(last_run_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_run_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_automation_workflows)SQL";

enum class BenefactorMarketingAutomationWorkflowsStatus { Draft, Active, Paused, Archived };
inline std::string benefactor_marketing_automation_workflows_status_to_string(BenefactorMarketingAutomationWorkflowsStatus value) {
    switch (value) {
        case BenefactorMarketingAutomationWorkflowsStatus::Draft: return "draft";
        case BenefactorMarketingAutomationWorkflowsStatus::Active: return "active";
        case BenefactorMarketingAutomationWorkflowsStatus::Paused: return "paused";
        case BenefactorMarketingAutomationWorkflowsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<BenefactorMarketingAutomationWorkflowsStatus> parse_benefactor_marketing_automation_workflows_status(const std::string& value) {
    if (value == "draft") return BenefactorMarketingAutomationWorkflowsStatus::Draft;
    if (value == "active") return BenefactorMarketingAutomationWorkflowsStatus::Active;
    if (value == "paused") return BenefactorMarketingAutomationWorkflowsStatus::Paused;
    if (value == "archived") return BenefactorMarketingAutomationWorkflowsStatus::Archived;
    return std::nullopt;
}

enum class BenefactorMarketingAutomationWorkflowsTriggerKind { LeadCreated, ScoreChanged, FormSubmit, EmailEvent, CampaignEvent, Manual, Schedule, Webhook };
inline std::string benefactor_marketing_automation_workflows_trigger_kind_to_string(BenefactorMarketingAutomationWorkflowsTriggerKind value) {
    switch (value) {
        case BenefactorMarketingAutomationWorkflowsTriggerKind::LeadCreated: return "lead_created";
        case BenefactorMarketingAutomationWorkflowsTriggerKind::ScoreChanged: return "score_changed";
        case BenefactorMarketingAutomationWorkflowsTriggerKind::FormSubmit: return "form_submit";
        case BenefactorMarketingAutomationWorkflowsTriggerKind::EmailEvent: return "email_event";
        case BenefactorMarketingAutomationWorkflowsTriggerKind::CampaignEvent: return "campaign_event";
        case BenefactorMarketingAutomationWorkflowsTriggerKind::Manual: return "manual";
        case BenefactorMarketingAutomationWorkflowsTriggerKind::Schedule: return "schedule";
        case BenefactorMarketingAutomationWorkflowsTriggerKind::Webhook: return "webhook";
    }
    return "";
}
inline std::optional<BenefactorMarketingAutomationWorkflowsTriggerKind> parse_benefactor_marketing_automation_workflows_trigger_kind(const std::string& value) {
    if (value == "lead_created") return BenefactorMarketingAutomationWorkflowsTriggerKind::LeadCreated;
    if (value == "score_changed") return BenefactorMarketingAutomationWorkflowsTriggerKind::ScoreChanged;
    if (value == "form_submit") return BenefactorMarketingAutomationWorkflowsTriggerKind::FormSubmit;
    if (value == "email_event") return BenefactorMarketingAutomationWorkflowsTriggerKind::EmailEvent;
    if (value == "campaign_event") return BenefactorMarketingAutomationWorkflowsTriggerKind::CampaignEvent;
    if (value == "manual") return BenefactorMarketingAutomationWorkflowsTriggerKind::Manual;
    if (value == "schedule") return BenefactorMarketingAutomationWorkflowsTriggerKind::Schedule;
    if (value == "webhook") return BenefactorMarketingAutomationWorkflowsTriggerKind::Webhook;
    return std::nullopt;
}

struct BenefactorMarketingAutomationWorkflowsRow {
    std::string id;
    std::string client_id;
    std::string status;
    std::string name;
    std::string trigger_kind;
    std::string trigger_config;
    std::string action_graph;
    std::optional<std::string> last_run_at;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingAutomationWorkflowsRow benefactor_marketing_automation_workflows_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingAutomationWorkflowsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.status = get(2);
    row.name = get(3);
    row.trigger_kind = get(4);
    row.trigger_config = get(5);
    row.action_graph = get(6);
    row.last_run_at = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.created_at = get(8);
    row.updated_at = get(9);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_automation_workflows_name(const std::string& value) {
    if (value.size() > 220) return std::string("benefactor_marketing_automation_workflows.name must be at most 220 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_automation_events_table = "benefactor_marketing_automation_events";
inline const std::vector<std::string> benefactor_marketing_automation_events_columns = { "id", "client_id", "workflow_id", "lead_id", "event_kind", "status", "payload", "error_summary", "created_at" };
inline const char* benefactor_marketing_automation_events_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      workflow_id::text as workflow_id,
      lead_id::text as lead_id,
      event_kind,
      status,
      payload::text as payload_json,
      error_summary,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from benefactor_marketing_automation_events)SQL";

enum class BenefactorMarketingAutomationEventsStatus { Received, Processed, Failed, Skipped };
inline std::string benefactor_marketing_automation_events_status_to_string(BenefactorMarketingAutomationEventsStatus value) {
    switch (value) {
        case BenefactorMarketingAutomationEventsStatus::Received: return "received";
        case BenefactorMarketingAutomationEventsStatus::Processed: return "processed";
        case BenefactorMarketingAutomationEventsStatus::Failed: return "failed";
        case BenefactorMarketingAutomationEventsStatus::Skipped: return "skipped";
    }
    return "";
}
inline std::optional<BenefactorMarketingAutomationEventsStatus> parse_benefactor_marketing_automation_events_status(const std::string& value) {
    if (value == "received") return BenefactorMarketingAutomationEventsStatus::Received;
    if (value == "processed") return BenefactorMarketingAutomationEventsStatus::Processed;
    if (value == "failed") return BenefactorMarketingAutomationEventsStatus::Failed;
    if (value == "skipped") return BenefactorMarketingAutomationEventsStatus::Skipped;
    return std::nullopt;
}

struct BenefactorMarketingAutomationEventsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> workflow_id;
    std::optional<std::string> lead_id;
    std::string event_kind;
    std::string status;
    std::string payload;
    std::optional<std::string> error_summary;
    std::string created_at;
};

inline BenefactorMarketingAutomationEventsRow benefactor_marketing_automation_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingAutomationEventsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.workflow_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.lead_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.event_kind = get(4);
    row.status = get(5);
    row.payload = get(6);
    row.error_summary = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.created_at = get(8);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_automation_events_event_kind(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_marketing_automation_events.event_kind must be at most 80 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,80}$)RX"))) return std::string("benefactor_marketing_automation_events.event_kind does not match the required pattern");
    return std::nullopt;
}

inline const char* benefactor_marketing_reports_table = "benefactor_marketing_reports";
inline const std::vector<std::string> benefactor_marketing_reports_columns = { "id", "client_id", "campaign_id", "report_kind", "status", "period_start", "period_end", "metrics", "narrative", "delivery_targets", "generated_at", "created_at", "updated_at" };
inline const char* benefactor_marketing_reports_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      campaign_id::text as campaign_id,
      report_kind,
      status,
      period_start,
      period_end,
      metrics::text as metrics_json,
      narrative,
      delivery_targets::text as delivery_targets_json,
      to_char(generated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as generated_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_reports)SQL";

enum class BenefactorMarketingReportsReportKind { Dashboard, ExecutiveSummary, Attribution, Funnel, Roi, SeoAeo, ClientPortal };
inline std::string benefactor_marketing_reports_report_kind_to_string(BenefactorMarketingReportsReportKind value) {
    switch (value) {
        case BenefactorMarketingReportsReportKind::Dashboard: return "dashboard";
        case BenefactorMarketingReportsReportKind::ExecutiveSummary: return "executive_summary";
        case BenefactorMarketingReportsReportKind::Attribution: return "attribution";
        case BenefactorMarketingReportsReportKind::Funnel: return "funnel";
        case BenefactorMarketingReportsReportKind::Roi: return "roi";
        case BenefactorMarketingReportsReportKind::SeoAeo: return "seo_aeo";
        case BenefactorMarketingReportsReportKind::ClientPortal: return "client_portal";
    }
    return "";
}
inline std::optional<BenefactorMarketingReportsReportKind> parse_benefactor_marketing_reports_report_kind(const std::string& value) {
    if (value == "dashboard") return BenefactorMarketingReportsReportKind::Dashboard;
    if (value == "executive_summary") return BenefactorMarketingReportsReportKind::ExecutiveSummary;
    if (value == "attribution") return BenefactorMarketingReportsReportKind::Attribution;
    if (value == "funnel") return BenefactorMarketingReportsReportKind::Funnel;
    if (value == "roi") return BenefactorMarketingReportsReportKind::Roi;
    if (value == "seo_aeo") return BenefactorMarketingReportsReportKind::SeoAeo;
    if (value == "client_portal") return BenefactorMarketingReportsReportKind::ClientPortal;
    return std::nullopt;
}

enum class BenefactorMarketingReportsStatus { Draft, Ready, Sent, Archived };
inline std::string benefactor_marketing_reports_status_to_string(BenefactorMarketingReportsStatus value) {
    switch (value) {
        case BenefactorMarketingReportsStatus::Draft: return "draft";
        case BenefactorMarketingReportsStatus::Ready: return "ready";
        case BenefactorMarketingReportsStatus::Sent: return "sent";
        case BenefactorMarketingReportsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<BenefactorMarketingReportsStatus> parse_benefactor_marketing_reports_status(const std::string& value) {
    if (value == "draft") return BenefactorMarketingReportsStatus::Draft;
    if (value == "ready") return BenefactorMarketingReportsStatus::Ready;
    if (value == "sent") return BenefactorMarketingReportsStatus::Sent;
    if (value == "archived") return BenefactorMarketingReportsStatus::Archived;
    return std::nullopt;
}

struct BenefactorMarketingReportsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> campaign_id;
    std::string report_kind;
    std::string status;
    std::optional<std::string> period_start;
    std::optional<std::string> period_end;
    std::string metrics;
    std::optional<std::string> narrative;
    std::string delivery_targets;
    std::optional<std::string> generated_at;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingReportsRow benefactor_marketing_reports_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingReportsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.campaign_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.report_kind = get(3);
    row.status = get(4);
    row.period_start = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.period_end = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.metrics = get(7);
    row.narrative = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.delivery_targets = get(9);
    row.generated_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_reports_period_start(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_reports.period_start must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_reports.period_start does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_reports_period_end(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_reports.period_end must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_reports.period_end does not match the required pattern");
    return std::nullopt;
}

inline const char* benefactor_marketing_attribution_events_table = "benefactor_marketing_attribution_events";
inline const std::vector<std::string> benefactor_marketing_attribution_events_columns = { "id", "client_id", "campaign_id", "lead_id", "event_type", "source_platform", "source_event_id", "occurred_at", "value_cents", "payload", "created_at" };
inline const char* benefactor_marketing_attribution_events_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      campaign_id::text as campaign_id,
      lead_id::text as lead_id,
      event_type,
      source_platform,
      source_event_id,
      to_char(occurred_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as occurred_at,
      value_cents,
      payload::text as payload_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from benefactor_marketing_attribution_events)SQL";

enum class BenefactorMarketingAttributionEventsEventType { Impression, Click, FormSubmit, EmailOpen, EmailClick, MeetingBooked, OpportunityCreated, DealWon, Revenue };
inline std::string benefactor_marketing_attribution_events_event_type_to_string(BenefactorMarketingAttributionEventsEventType value) {
    switch (value) {
        case BenefactorMarketingAttributionEventsEventType::Impression: return "impression";
        case BenefactorMarketingAttributionEventsEventType::Click: return "click";
        case BenefactorMarketingAttributionEventsEventType::FormSubmit: return "form_submit";
        case BenefactorMarketingAttributionEventsEventType::EmailOpen: return "email_open";
        case BenefactorMarketingAttributionEventsEventType::EmailClick: return "email_click";
        case BenefactorMarketingAttributionEventsEventType::MeetingBooked: return "meeting_booked";
        case BenefactorMarketingAttributionEventsEventType::OpportunityCreated: return "opportunity_created";
        case BenefactorMarketingAttributionEventsEventType::DealWon: return "deal_won";
        case BenefactorMarketingAttributionEventsEventType::Revenue: return "revenue";
    }
    return "";
}
inline std::optional<BenefactorMarketingAttributionEventsEventType> parse_benefactor_marketing_attribution_events_event_type(const std::string& value) {
    if (value == "impression") return BenefactorMarketingAttributionEventsEventType::Impression;
    if (value == "click") return BenefactorMarketingAttributionEventsEventType::Click;
    if (value == "form_submit") return BenefactorMarketingAttributionEventsEventType::FormSubmit;
    if (value == "email_open") return BenefactorMarketingAttributionEventsEventType::EmailOpen;
    if (value == "email_click") return BenefactorMarketingAttributionEventsEventType::EmailClick;
    if (value == "meeting_booked") return BenefactorMarketingAttributionEventsEventType::MeetingBooked;
    if (value == "opportunity_created") return BenefactorMarketingAttributionEventsEventType::OpportunityCreated;
    if (value == "deal_won") return BenefactorMarketingAttributionEventsEventType::DealWon;
    if (value == "revenue") return BenefactorMarketingAttributionEventsEventType::Revenue;
    return std::nullopt;
}

struct BenefactorMarketingAttributionEventsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> campaign_id;
    std::optional<std::string> lead_id;
    std::string event_type;
    std::optional<std::string> source_platform;
    std::optional<std::string> source_event_id;
    std::string occurred_at;
    int32_t value_cents;
    std::string payload;
    std::string created_at;
};

inline BenefactorMarketingAttributionEventsRow benefactor_marketing_attribution_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingAttributionEventsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.campaign_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.lead_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.event_type = get(4);
    row.source_platform = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.source_event_id = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.occurred_at = get(7);
    row.value_cents = std::stoi(get(8));
    row.payload = get(9);
    row.created_at = get(10);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_attribution_events_source_platform(const std::string& value) {
    if (value.size() > 64) return std::string("benefactor_marketing_attribution_events.source_platform must be at most 64 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_attribution_events_source_event_id(const std::string& value) {
    if (value.size() > 200) return std::string("benefactor_marketing_attribution_events.source_event_id must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_attribution_events_value_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_attribution_events.value_cents is below the minimum");
    return std::nullopt;
}

inline const char* benefactor_marketing_opportunities_table = "benefactor_marketing_opportunities";
inline const std::vector<std::string> benefactor_marketing_opportunities_columns = { "id", "client_id", "lead_id", "status", "stage", "name", "amount_cents", "probability_micros", "expected_close_on", "owner_user_id", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_opportunities_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      lead_id::text as lead_id,
      status,
      stage,
      name,
      amount_cents,
      probability_micros,
      expected_close_on,
      owner_user_id::text as owner_user_id,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_opportunities)SQL";

enum class BenefactorMarketingOpportunitiesStatus { Open, Won, Lost, Paused };
inline std::string benefactor_marketing_opportunities_status_to_string(BenefactorMarketingOpportunitiesStatus value) {
    switch (value) {
        case BenefactorMarketingOpportunitiesStatus::Open: return "open";
        case BenefactorMarketingOpportunitiesStatus::Won: return "won";
        case BenefactorMarketingOpportunitiesStatus::Lost: return "lost";
        case BenefactorMarketingOpportunitiesStatus::Paused: return "paused";
    }
    return "";
}
inline std::optional<BenefactorMarketingOpportunitiesStatus> parse_benefactor_marketing_opportunities_status(const std::string& value) {
    if (value == "open") return BenefactorMarketingOpportunitiesStatus::Open;
    if (value == "won") return BenefactorMarketingOpportunitiesStatus::Won;
    if (value == "lost") return BenefactorMarketingOpportunitiesStatus::Lost;
    if (value == "paused") return BenefactorMarketingOpportunitiesStatus::Paused;
    return std::nullopt;
}

enum class BenefactorMarketingOpportunitiesStage { Prospecting, Qualified, Meeting, Proposal, Negotiation, Closed };
inline std::string benefactor_marketing_opportunities_stage_to_string(BenefactorMarketingOpportunitiesStage value) {
    switch (value) {
        case BenefactorMarketingOpportunitiesStage::Prospecting: return "prospecting";
        case BenefactorMarketingOpportunitiesStage::Qualified: return "qualified";
        case BenefactorMarketingOpportunitiesStage::Meeting: return "meeting";
        case BenefactorMarketingOpportunitiesStage::Proposal: return "proposal";
        case BenefactorMarketingOpportunitiesStage::Negotiation: return "negotiation";
        case BenefactorMarketingOpportunitiesStage::Closed: return "closed";
    }
    return "";
}
inline std::optional<BenefactorMarketingOpportunitiesStage> parse_benefactor_marketing_opportunities_stage(const std::string& value) {
    if (value == "prospecting") return BenefactorMarketingOpportunitiesStage::Prospecting;
    if (value == "qualified") return BenefactorMarketingOpportunitiesStage::Qualified;
    if (value == "meeting") return BenefactorMarketingOpportunitiesStage::Meeting;
    if (value == "proposal") return BenefactorMarketingOpportunitiesStage::Proposal;
    if (value == "negotiation") return BenefactorMarketingOpportunitiesStage::Negotiation;
    if (value == "closed") return BenefactorMarketingOpportunitiesStage::Closed;
    return std::nullopt;
}

struct BenefactorMarketingOpportunitiesRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> lead_id;
    std::string status;
    std::string stage;
    std::string name;
    int32_t amount_cents;
    int32_t probability_micros;
    std::optional<std::string> expected_close_on;
    std::optional<std::string> owner_user_id;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingOpportunitiesRow benefactor_marketing_opportunities_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingOpportunitiesRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.lead_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.status = get(3);
    row.stage = get(4);
    row.name = get(5);
    row.amount_cents = std::stoi(get(6));
    row.probability_micros = std::stoi(get(7));
    row.expected_close_on = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.owner_user_id = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.meta_data = get(10);
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_opportunities_name(const std::string& value) {
    if (value.size() > 220) return std::string("benefactor_marketing_opportunities.name must be at most 220 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_opportunities_amount_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_opportunities.amount_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_opportunities_probability_micros(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_opportunities.probability_micros is below the minimum");
    if (value > 1000000) return std::string("benefactor_marketing_opportunities.probability_micros is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_opportunities_expected_close_on(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_opportunities.expected_close_on must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_opportunities.expected_close_on does not match the required pattern");
    return std::nullopt;
}

inline const char* benefactor_marketing_content_assets_table = "benefactor_marketing_content_assets";
inline const std::vector<std::string> benefactor_marketing_content_assets_columns = { "id", "client_id", "campaign_id", "status", "asset_kind", "title", "channel", "body", "asset_uri", "seo_keywords", "approval_status", "publish_at", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_content_assets_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      campaign_id::text as campaign_id,
      status,
      asset_kind,
      title,
      channel,
      body,
      asset_uri,
      seo_keywords::text as seo_keywords_json,
      approval_status,
      to_char(publish_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as publish_at,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_content_assets)SQL";

enum class BenefactorMarketingContentAssetsStatus { Draft, InReview, Approved, Scheduled, Published, Archived };
inline std::string benefactor_marketing_content_assets_status_to_string(BenefactorMarketingContentAssetsStatus value) {
    switch (value) {
        case BenefactorMarketingContentAssetsStatus::Draft: return "draft";
        case BenefactorMarketingContentAssetsStatus::InReview: return "in_review";
        case BenefactorMarketingContentAssetsStatus::Approved: return "approved";
        case BenefactorMarketingContentAssetsStatus::Scheduled: return "scheduled";
        case BenefactorMarketingContentAssetsStatus::Published: return "published";
        case BenefactorMarketingContentAssetsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<BenefactorMarketingContentAssetsStatus> parse_benefactor_marketing_content_assets_status(const std::string& value) {
    if (value == "draft") return BenefactorMarketingContentAssetsStatus::Draft;
    if (value == "in_review") return BenefactorMarketingContentAssetsStatus::InReview;
    if (value == "approved") return BenefactorMarketingContentAssetsStatus::Approved;
    if (value == "scheduled") return BenefactorMarketingContentAssetsStatus::Scheduled;
    if (value == "published") return BenefactorMarketingContentAssetsStatus::Published;
    if (value == "archived") return BenefactorMarketingContentAssetsStatus::Archived;
    return std::nullopt;
}

enum class BenefactorMarketingContentAssetsAssetKind { Blog, SocialPost, Email, LandingPage, AdCreative, Video, Script, Proposal, Report };
inline std::string benefactor_marketing_content_assets_asset_kind_to_string(BenefactorMarketingContentAssetsAssetKind value) {
    switch (value) {
        case BenefactorMarketingContentAssetsAssetKind::Blog: return "blog";
        case BenefactorMarketingContentAssetsAssetKind::SocialPost: return "social_post";
        case BenefactorMarketingContentAssetsAssetKind::Email: return "email";
        case BenefactorMarketingContentAssetsAssetKind::LandingPage: return "landing_page";
        case BenefactorMarketingContentAssetsAssetKind::AdCreative: return "ad_creative";
        case BenefactorMarketingContentAssetsAssetKind::Video: return "video";
        case BenefactorMarketingContentAssetsAssetKind::Script: return "script";
        case BenefactorMarketingContentAssetsAssetKind::Proposal: return "proposal";
        case BenefactorMarketingContentAssetsAssetKind::Report: return "report";
    }
    return "";
}
inline std::optional<BenefactorMarketingContentAssetsAssetKind> parse_benefactor_marketing_content_assets_asset_kind(const std::string& value) {
    if (value == "blog") return BenefactorMarketingContentAssetsAssetKind::Blog;
    if (value == "social_post") return BenefactorMarketingContentAssetsAssetKind::SocialPost;
    if (value == "email") return BenefactorMarketingContentAssetsAssetKind::Email;
    if (value == "landing_page") return BenefactorMarketingContentAssetsAssetKind::LandingPage;
    if (value == "ad_creative") return BenefactorMarketingContentAssetsAssetKind::AdCreative;
    if (value == "video") return BenefactorMarketingContentAssetsAssetKind::Video;
    if (value == "script") return BenefactorMarketingContentAssetsAssetKind::Script;
    if (value == "proposal") return BenefactorMarketingContentAssetsAssetKind::Proposal;
    if (value == "report") return BenefactorMarketingContentAssetsAssetKind::Report;
    return std::nullopt;
}

enum class BenefactorMarketingContentAssetsApprovalStatus { Pending, Approved, Rejected, ChangesRequested };
inline std::string benefactor_marketing_content_assets_approval_status_to_string(BenefactorMarketingContentAssetsApprovalStatus value) {
    switch (value) {
        case BenefactorMarketingContentAssetsApprovalStatus::Pending: return "pending";
        case BenefactorMarketingContentAssetsApprovalStatus::Approved: return "approved";
        case BenefactorMarketingContentAssetsApprovalStatus::Rejected: return "rejected";
        case BenefactorMarketingContentAssetsApprovalStatus::ChangesRequested: return "changes_requested";
    }
    return "";
}
inline std::optional<BenefactorMarketingContentAssetsApprovalStatus> parse_benefactor_marketing_content_assets_approval_status(const std::string& value) {
    if (value == "pending") return BenefactorMarketingContentAssetsApprovalStatus::Pending;
    if (value == "approved") return BenefactorMarketingContentAssetsApprovalStatus::Approved;
    if (value == "rejected") return BenefactorMarketingContentAssetsApprovalStatus::Rejected;
    if (value == "changes_requested") return BenefactorMarketingContentAssetsApprovalStatus::ChangesRequested;
    return std::nullopt;
}

struct BenefactorMarketingContentAssetsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> campaign_id;
    std::string status;
    std::string asset_kind;
    std::string title;
    std::optional<std::string> channel;
    std::optional<std::string> body;
    std::optional<std::string> asset_uri;
    std::string seo_keywords;
    std::string approval_status;
    std::optional<std::string> publish_at;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingContentAssetsRow benefactor_marketing_content_assets_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingContentAssetsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.campaign_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.status = get(3);
    row.asset_kind = get(4);
    row.title = get(5);
    row.channel = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.body = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.asset_uri = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.seo_keywords = get(9);
    row.approval_status = get(10);
    row.publish_at = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.meta_data = get(12);
    row.created_at = get(13);
    row.updated_at = get(14);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_content_assets_title(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_content_assets.title must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_content_assets_channel(const std::string& value) {
    if (value.size() > 64) return std::string("benefactor_marketing_content_assets.channel must be at most 64 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_project_tasks_table = "benefactor_marketing_project_tasks";
inline const std::vector<std::string> benefactor_marketing_project_tasks_columns = { "id", "client_id", "campaign_id", "content_asset_id", "status", "priority", "title", "description", "assigned_to", "due_on", "sla_due_at", "time_spent_minutes", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_project_tasks_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      campaign_id::text as campaign_id,
      content_asset_id::text as content_asset_id,
      status,
      priority,
      title,
      description,
      assigned_to::text as assigned_to,
      due_on,
      to_char(sla_due_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as sla_due_at,
      time_spent_minutes,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_project_tasks)SQL";

enum class BenefactorMarketingProjectTasksStatus { Todo, InProgress, Blocked, Done, Canceled };
inline std::string benefactor_marketing_project_tasks_status_to_string(BenefactorMarketingProjectTasksStatus value) {
    switch (value) {
        case BenefactorMarketingProjectTasksStatus::Todo: return "todo";
        case BenefactorMarketingProjectTasksStatus::InProgress: return "in_progress";
        case BenefactorMarketingProjectTasksStatus::Blocked: return "blocked";
        case BenefactorMarketingProjectTasksStatus::Done: return "done";
        case BenefactorMarketingProjectTasksStatus::Canceled: return "canceled";
    }
    return "";
}
inline std::optional<BenefactorMarketingProjectTasksStatus> parse_benefactor_marketing_project_tasks_status(const std::string& value) {
    if (value == "todo") return BenefactorMarketingProjectTasksStatus::Todo;
    if (value == "in_progress") return BenefactorMarketingProjectTasksStatus::InProgress;
    if (value == "blocked") return BenefactorMarketingProjectTasksStatus::Blocked;
    if (value == "done") return BenefactorMarketingProjectTasksStatus::Done;
    if (value == "canceled") return BenefactorMarketingProjectTasksStatus::Canceled;
    return std::nullopt;
}

enum class BenefactorMarketingProjectTasksPriority { Low, Normal, High, Urgent };
inline std::string benefactor_marketing_project_tasks_priority_to_string(BenefactorMarketingProjectTasksPriority value) {
    switch (value) {
        case BenefactorMarketingProjectTasksPriority::Low: return "low";
        case BenefactorMarketingProjectTasksPriority::Normal: return "normal";
        case BenefactorMarketingProjectTasksPriority::High: return "high";
        case BenefactorMarketingProjectTasksPriority::Urgent: return "urgent";
    }
    return "";
}
inline std::optional<BenefactorMarketingProjectTasksPriority> parse_benefactor_marketing_project_tasks_priority(const std::string& value) {
    if (value == "low") return BenefactorMarketingProjectTasksPriority::Low;
    if (value == "normal") return BenefactorMarketingProjectTasksPriority::Normal;
    if (value == "high") return BenefactorMarketingProjectTasksPriority::High;
    if (value == "urgent") return BenefactorMarketingProjectTasksPriority::Urgent;
    return std::nullopt;
}

struct BenefactorMarketingProjectTasksRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> campaign_id;
    std::optional<std::string> content_asset_id;
    std::string status;
    std::string priority;
    std::string title;
    std::optional<std::string> description;
    std::optional<std::string> assigned_to;
    std::optional<std::string> due_on;
    std::optional<std::string> sla_due_at;
    int32_t time_spent_minutes;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingProjectTasksRow benefactor_marketing_project_tasks_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingProjectTasksRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.campaign_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.content_asset_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.status = get(4);
    row.priority = get(5);
    row.title = get(6);
    row.description = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.assigned_to = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.due_on = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.sla_due_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.time_spent_minutes = std::stoi(get(11));
    row.meta_data = get(12);
    row.created_at = get(13);
    row.updated_at = get(14);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_project_tasks_title(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_project_tasks.title must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_project_tasks_due_on(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_project_tasks.due_on must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_project_tasks.due_on does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_project_tasks_time_spent_minutes(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_project_tasks.time_spent_minutes is below the minimum");
    return std::nullopt;
}

inline const char* benefactor_marketing_client_approvals_table = "benefactor_marketing_client_approvals";
inline const std::vector<std::string> benefactor_marketing_client_approvals_columns = { "id", "client_id", "campaign_id", "content_asset_id", "requested_by", "status", "approval_kind", "title", "request_payload", "response_note", "due_at", "decided_at", "created_at", "updated_at" };
inline const char* benefactor_marketing_client_approvals_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      campaign_id::text as campaign_id,
      content_asset_id::text as content_asset_id,
      requested_by::text as requested_by,
      status,
      approval_kind,
      title,
      request_payload::text as request_payload_json,
      response_note,
      to_char(due_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as due_at,
      to_char(decided_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as decided_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_client_approvals)SQL";

enum class BenefactorMarketingClientApprovalsStatus { Pending, Approved, Rejected, Expired, Canceled };
inline std::string benefactor_marketing_client_approvals_status_to_string(BenefactorMarketingClientApprovalsStatus value) {
    switch (value) {
        case BenefactorMarketingClientApprovalsStatus::Pending: return "pending";
        case BenefactorMarketingClientApprovalsStatus::Approved: return "approved";
        case BenefactorMarketingClientApprovalsStatus::Rejected: return "rejected";
        case BenefactorMarketingClientApprovalsStatus::Expired: return "expired";
        case BenefactorMarketingClientApprovalsStatus::Canceled: return "canceled";
    }
    return "";
}
inline std::optional<BenefactorMarketingClientApprovalsStatus> parse_benefactor_marketing_client_approvals_status(const std::string& value) {
    if (value == "pending") return BenefactorMarketingClientApprovalsStatus::Pending;
    if (value == "approved") return BenefactorMarketingClientApprovalsStatus::Approved;
    if (value == "rejected") return BenefactorMarketingClientApprovalsStatus::Rejected;
    if (value == "expired") return BenefactorMarketingClientApprovalsStatus::Expired;
    if (value == "canceled") return BenefactorMarketingClientApprovalsStatus::Canceled;
    return std::nullopt;
}

enum class BenefactorMarketingClientApprovalsApprovalKind { CampaignLaunch, ContentPublish, BudgetChange, ReportSend, LeadList };
inline std::string benefactor_marketing_client_approvals_approval_kind_to_string(BenefactorMarketingClientApprovalsApprovalKind value) {
    switch (value) {
        case BenefactorMarketingClientApprovalsApprovalKind::CampaignLaunch: return "campaign_launch";
        case BenefactorMarketingClientApprovalsApprovalKind::ContentPublish: return "content_publish";
        case BenefactorMarketingClientApprovalsApprovalKind::BudgetChange: return "budget_change";
        case BenefactorMarketingClientApprovalsApprovalKind::ReportSend: return "report_send";
        case BenefactorMarketingClientApprovalsApprovalKind::LeadList: return "lead_list";
    }
    return "";
}
inline std::optional<BenefactorMarketingClientApprovalsApprovalKind> parse_benefactor_marketing_client_approvals_approval_kind(const std::string& value) {
    if (value == "campaign_launch") return BenefactorMarketingClientApprovalsApprovalKind::CampaignLaunch;
    if (value == "content_publish") return BenefactorMarketingClientApprovalsApprovalKind::ContentPublish;
    if (value == "budget_change") return BenefactorMarketingClientApprovalsApprovalKind::BudgetChange;
    if (value == "report_send") return BenefactorMarketingClientApprovalsApprovalKind::ReportSend;
    if (value == "lead_list") return BenefactorMarketingClientApprovalsApprovalKind::LeadList;
    return std::nullopt;
}

struct BenefactorMarketingClientApprovalsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> campaign_id;
    std::optional<std::string> content_asset_id;
    std::optional<std::string> requested_by;
    std::string status;
    std::string approval_kind;
    std::string title;
    std::string request_payload;
    std::optional<std::string> response_note;
    std::optional<std::string> due_at;
    std::optional<std::string> decided_at;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingClientApprovalsRow benefactor_marketing_client_approvals_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingClientApprovalsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.campaign_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.content_asset_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.requested_by = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.status = get(5);
    row.approval_kind = get(6);
    row.title = get(7);
    row.request_payload = get(8);
    row.response_note = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.due_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.decided_at = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.created_at = get(12);
    row.updated_at = get(13);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_client_approvals_title(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_client_approvals.title must be at most 240 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_tickets_table = "benefactor_marketing_tickets";
inline const std::vector<std::string> benefactor_marketing_tickets_columns = { "id", "client_id", "status", "priority", "subject", "description", "source", "assigned_to", "last_activity_at", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_tickets_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      status,
      priority,
      subject,
      description,
      source,
      assigned_to::text as assigned_to,
      to_char(last_activity_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_activity_at,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_tickets)SQL";

enum class BenefactorMarketingTicketsStatus { Open, PendingClient, PendingAgency, Resolved, Closed };
inline std::string benefactor_marketing_tickets_status_to_string(BenefactorMarketingTicketsStatus value) {
    switch (value) {
        case BenefactorMarketingTicketsStatus::Open: return "open";
        case BenefactorMarketingTicketsStatus::PendingClient: return "pending_client";
        case BenefactorMarketingTicketsStatus::PendingAgency: return "pending_agency";
        case BenefactorMarketingTicketsStatus::Resolved: return "resolved";
        case BenefactorMarketingTicketsStatus::Closed: return "closed";
    }
    return "";
}
inline std::optional<BenefactorMarketingTicketsStatus> parse_benefactor_marketing_tickets_status(const std::string& value) {
    if (value == "open") return BenefactorMarketingTicketsStatus::Open;
    if (value == "pending_client") return BenefactorMarketingTicketsStatus::PendingClient;
    if (value == "pending_agency") return BenefactorMarketingTicketsStatus::PendingAgency;
    if (value == "resolved") return BenefactorMarketingTicketsStatus::Resolved;
    if (value == "closed") return BenefactorMarketingTicketsStatus::Closed;
    return std::nullopt;
}

enum class BenefactorMarketingTicketsPriority { Low, Normal, High, Urgent };
inline std::string benefactor_marketing_tickets_priority_to_string(BenefactorMarketingTicketsPriority value) {
    switch (value) {
        case BenefactorMarketingTicketsPriority::Low: return "low";
        case BenefactorMarketingTicketsPriority::Normal: return "normal";
        case BenefactorMarketingTicketsPriority::High: return "high";
        case BenefactorMarketingTicketsPriority::Urgent: return "urgent";
    }
    return "";
}
inline std::optional<BenefactorMarketingTicketsPriority> parse_benefactor_marketing_tickets_priority(const std::string& value) {
    if (value == "low") return BenefactorMarketingTicketsPriority::Low;
    if (value == "normal") return BenefactorMarketingTicketsPriority::Normal;
    if (value == "high") return BenefactorMarketingTicketsPriority::High;
    if (value == "urgent") return BenefactorMarketingTicketsPriority::Urgent;
    return std::nullopt;
}

enum class BenefactorMarketingTicketsSource { Portal, Email, Internal };
inline std::string benefactor_marketing_tickets_source_to_string(BenefactorMarketingTicketsSource value) {
    switch (value) {
        case BenefactorMarketingTicketsSource::Portal: return "portal";
        case BenefactorMarketingTicketsSource::Email: return "email";
        case BenefactorMarketingTicketsSource::Internal: return "internal";
    }
    return "";
}
inline std::optional<BenefactorMarketingTicketsSource> parse_benefactor_marketing_tickets_source(const std::string& value) {
    if (value == "portal") return BenefactorMarketingTicketsSource::Portal;
    if (value == "email") return BenefactorMarketingTicketsSource::Email;
    if (value == "internal") return BenefactorMarketingTicketsSource::Internal;
    return std::nullopt;
}

struct BenefactorMarketingTicketsRow {
    std::string id;
    std::string client_id;
    std::string status;
    std::string priority;
    std::string subject;
    std::optional<std::string> description;
    std::string source;
    std::optional<std::string> assigned_to;
    std::string last_activity_at;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingTicketsRow benefactor_marketing_tickets_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingTicketsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.status = get(2);
    row.priority = get(3);
    row.subject = get(4);
    row.description = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.source = get(6);
    row.assigned_to = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.last_activity_at = get(8);
    row.meta_data = get(9);
    row.created_at = get(10);
    row.updated_at = get(11);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_tickets_subject(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_tickets.subject must be at most 240 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_meetings_table = "benefactor_marketing_meetings";
inline const std::vector<std::string> benefactor_marketing_meetings_columns = { "id", "client_id", "lead_id", "opportunity_id", "status", "meeting_kind", "title", "scheduled_at", "duration_minutes", "notes", "recording_uri", "transcript_summary", "created_at", "updated_at" };
inline const char* benefactor_marketing_meetings_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      lead_id::text as lead_id,
      opportunity_id::text as opportunity_id,
      status,
      meeting_kind,
      title,
      to_char(scheduled_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as scheduled_at,
      duration_minutes,
      notes,
      recording_uri,
      transcript_summary::text as transcript_summary_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_meetings)SQL";

enum class BenefactorMarketingMeetingsStatus { Scheduled, Completed, Canceled, NoShow };
inline std::string benefactor_marketing_meetings_status_to_string(BenefactorMarketingMeetingsStatus value) {
    switch (value) {
        case BenefactorMarketingMeetingsStatus::Scheduled: return "scheduled";
        case BenefactorMarketingMeetingsStatus::Completed: return "completed";
        case BenefactorMarketingMeetingsStatus::Canceled: return "canceled";
        case BenefactorMarketingMeetingsStatus::NoShow: return "no_show";
    }
    return "";
}
inline std::optional<BenefactorMarketingMeetingsStatus> parse_benefactor_marketing_meetings_status(const std::string& value) {
    if (value == "scheduled") return BenefactorMarketingMeetingsStatus::Scheduled;
    if (value == "completed") return BenefactorMarketingMeetingsStatus::Completed;
    if (value == "canceled") return BenefactorMarketingMeetingsStatus::Canceled;
    if (value == "no_show") return BenefactorMarketingMeetingsStatus::NoShow;
    return std::nullopt;
}

enum class BenefactorMarketingMeetingsMeetingKind { Onboarding, ReportReview, SalesDiscovery, Strategy, ContentReview, Support };
inline std::string benefactor_marketing_meetings_meeting_kind_to_string(BenefactorMarketingMeetingsMeetingKind value) {
    switch (value) {
        case BenefactorMarketingMeetingsMeetingKind::Onboarding: return "onboarding";
        case BenefactorMarketingMeetingsMeetingKind::ReportReview: return "report_review";
        case BenefactorMarketingMeetingsMeetingKind::SalesDiscovery: return "sales_discovery";
        case BenefactorMarketingMeetingsMeetingKind::Strategy: return "strategy";
        case BenefactorMarketingMeetingsMeetingKind::ContentReview: return "content_review";
        case BenefactorMarketingMeetingsMeetingKind::Support: return "support";
    }
    return "";
}
inline std::optional<BenefactorMarketingMeetingsMeetingKind> parse_benefactor_marketing_meetings_meeting_kind(const std::string& value) {
    if (value == "onboarding") return BenefactorMarketingMeetingsMeetingKind::Onboarding;
    if (value == "report_review") return BenefactorMarketingMeetingsMeetingKind::ReportReview;
    if (value == "sales_discovery") return BenefactorMarketingMeetingsMeetingKind::SalesDiscovery;
    if (value == "strategy") return BenefactorMarketingMeetingsMeetingKind::Strategy;
    if (value == "content_review") return BenefactorMarketingMeetingsMeetingKind::ContentReview;
    if (value == "support") return BenefactorMarketingMeetingsMeetingKind::Support;
    return std::nullopt;
}

struct BenefactorMarketingMeetingsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> lead_id;
    std::optional<std::string> opportunity_id;
    std::string status;
    std::string meeting_kind;
    std::string title;
    std::string scheduled_at;
    int32_t duration_minutes;
    std::optional<std::string> notes;
    std::optional<std::string> recording_uri;
    std::string transcript_summary;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingMeetingsRow benefactor_marketing_meetings_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingMeetingsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.lead_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.opportunity_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.status = get(4);
    row.meeting_kind = get(5);
    row.title = get(6);
    row.scheduled_at = get(7);
    row.duration_minutes = std::stoi(get(8));
    row.notes = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.recording_uri = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.transcript_summary = get(11);
    row.created_at = get(12);
    row.updated_at = get(13);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_meetings_title(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_meetings.title must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_meetings_duration_minutes(int32_t value) {
    if (value < 1) return std::string("benefactor_marketing_meetings.duration_minutes is below the minimum");
    if (value > 1440) return std::string("benefactor_marketing_meetings.duration_minutes is above the maximum");
    return std::nullopt;
}

inline const char* benefactor_marketing_team_allocations_table = "benefactor_marketing_team_allocations";
inline const std::vector<std::string> benefactor_marketing_team_allocations_columns = { "id", "client_id", "campaign_id", "user_id", "role", "allocation_percent", "starts_on", "ends_on", "billable", "created_at", "updated_at" };
inline const char* benefactor_marketing_team_allocations_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      campaign_id::text as campaign_id,
      user_id::text as user_id,
      role,
      allocation_percent,
      starts_on,
      ends_on,
      billable,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_team_allocations)SQL";

enum class BenefactorMarketingTeamAllocationsRole { Strategist, Designer, Copywriter, Analyst, Sdr, AccountManager, SeoSpecialist };
inline std::string benefactor_marketing_team_allocations_role_to_string(BenefactorMarketingTeamAllocationsRole value) {
    switch (value) {
        case BenefactorMarketingTeamAllocationsRole::Strategist: return "strategist";
        case BenefactorMarketingTeamAllocationsRole::Designer: return "designer";
        case BenefactorMarketingTeamAllocationsRole::Copywriter: return "copywriter";
        case BenefactorMarketingTeamAllocationsRole::Analyst: return "analyst";
        case BenefactorMarketingTeamAllocationsRole::Sdr: return "sdr";
        case BenefactorMarketingTeamAllocationsRole::AccountManager: return "account_manager";
        case BenefactorMarketingTeamAllocationsRole::SeoSpecialist: return "seo_specialist";
    }
    return "";
}
inline std::optional<BenefactorMarketingTeamAllocationsRole> parse_benefactor_marketing_team_allocations_role(const std::string& value) {
    if (value == "strategist") return BenefactorMarketingTeamAllocationsRole::Strategist;
    if (value == "designer") return BenefactorMarketingTeamAllocationsRole::Designer;
    if (value == "copywriter") return BenefactorMarketingTeamAllocationsRole::Copywriter;
    if (value == "analyst") return BenefactorMarketingTeamAllocationsRole::Analyst;
    if (value == "sdr") return BenefactorMarketingTeamAllocationsRole::Sdr;
    if (value == "account_manager") return BenefactorMarketingTeamAllocationsRole::AccountManager;
    if (value == "seo_specialist") return BenefactorMarketingTeamAllocationsRole::SeoSpecialist;
    return std::nullopt;
}

struct BenefactorMarketingTeamAllocationsRow {
    std::string id;
    std::optional<std::string> client_id;
    std::optional<std::string> campaign_id;
    std::string user_id;
    std::string role;
    int32_t allocation_percent;
    std::optional<std::string> starts_on;
    std::optional<std::string> ends_on;
    bool billable;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingTeamAllocationsRow benefactor_marketing_team_allocations_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingTeamAllocationsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.campaign_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.user_id = get(3);
    row.role = get(4);
    row.allocation_percent = std::stoi(get(5));
    row.starts_on = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.ends_on = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.billable = (get(8) == "t");
    row.created_at = get(9);
    row.updated_at = get(10);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_team_allocations_allocation_percent(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_team_allocations.allocation_percent is below the minimum");
    if (value > 100) return std::string("benefactor_marketing_team_allocations.allocation_percent is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_team_allocations_starts_on(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_team_allocations.starts_on must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_team_allocations.starts_on does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_team_allocations_ends_on(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_team_allocations.ends_on must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_team_allocations.ends_on does not match the required pattern");
    return std::nullopt;
}

inline const char* benefactor_marketing_integration_sync_runs_table = "benefactor_marketing_integration_sync_runs";
inline const std::vector<std::string> benefactor_marketing_integration_sync_runs_columns = { "id", "integration_id", "client_id", "sync_kind", "direction", "status", "records_seen", "records_changed", "cursor_before", "cursor_after", "payload", "error_summary", "started_at", "completed_at", "created_at", "updated_at" };
inline const char* benefactor_marketing_integration_sync_runs_select_sql = R"SQL(select
      id::text as id,
      integration_id::text as integration_id,
      client_id::text as client_id,
      sync_kind,
      direction,
      status,
      records_seen,
      records_changed,
      cursor_before,
      cursor_after,
      payload::text as payload_json,
      error_summary,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(completed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as completed_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_integration_sync_runs)SQL";

enum class BenefactorMarketingIntegrationSyncRunsSyncKind { Incremental, Full, Webhook, Backfill, Export };
inline std::string benefactor_marketing_integration_sync_runs_sync_kind_to_string(BenefactorMarketingIntegrationSyncRunsSyncKind value) {
    switch (value) {
        case BenefactorMarketingIntegrationSyncRunsSyncKind::Incremental: return "incremental";
        case BenefactorMarketingIntegrationSyncRunsSyncKind::Full: return "full";
        case BenefactorMarketingIntegrationSyncRunsSyncKind::Webhook: return "webhook";
        case BenefactorMarketingIntegrationSyncRunsSyncKind::Backfill: return "backfill";
        case BenefactorMarketingIntegrationSyncRunsSyncKind::Export: return "export";
    }
    return "";
}
inline std::optional<BenefactorMarketingIntegrationSyncRunsSyncKind> parse_benefactor_marketing_integration_sync_runs_sync_kind(const std::string& value) {
    if (value == "incremental") return BenefactorMarketingIntegrationSyncRunsSyncKind::Incremental;
    if (value == "full") return BenefactorMarketingIntegrationSyncRunsSyncKind::Full;
    if (value == "webhook") return BenefactorMarketingIntegrationSyncRunsSyncKind::Webhook;
    if (value == "backfill") return BenefactorMarketingIntegrationSyncRunsSyncKind::Backfill;
    if (value == "export") return BenefactorMarketingIntegrationSyncRunsSyncKind::Export;
    return std::nullopt;
}

enum class BenefactorMarketingIntegrationSyncRunsDirection { Import, Export, Bidirectional };
inline std::string benefactor_marketing_integration_sync_runs_direction_to_string(BenefactorMarketingIntegrationSyncRunsDirection value) {
    switch (value) {
        case BenefactorMarketingIntegrationSyncRunsDirection::Import: return "import";
        case BenefactorMarketingIntegrationSyncRunsDirection::Export: return "export";
        case BenefactorMarketingIntegrationSyncRunsDirection::Bidirectional: return "bidirectional";
    }
    return "";
}
inline std::optional<BenefactorMarketingIntegrationSyncRunsDirection> parse_benefactor_marketing_integration_sync_runs_direction(const std::string& value) {
    if (value == "import") return BenefactorMarketingIntegrationSyncRunsDirection::Import;
    if (value == "export") return BenefactorMarketingIntegrationSyncRunsDirection::Export;
    if (value == "bidirectional") return BenefactorMarketingIntegrationSyncRunsDirection::Bidirectional;
    return std::nullopt;
}

enum class BenefactorMarketingIntegrationSyncRunsStatus { Queued, Running, Succeeded, Failed, Canceled };
inline std::string benefactor_marketing_integration_sync_runs_status_to_string(BenefactorMarketingIntegrationSyncRunsStatus value) {
    switch (value) {
        case BenefactorMarketingIntegrationSyncRunsStatus::Queued: return "queued";
        case BenefactorMarketingIntegrationSyncRunsStatus::Running: return "running";
        case BenefactorMarketingIntegrationSyncRunsStatus::Succeeded: return "succeeded";
        case BenefactorMarketingIntegrationSyncRunsStatus::Failed: return "failed";
        case BenefactorMarketingIntegrationSyncRunsStatus::Canceled: return "canceled";
    }
    return "";
}
inline std::optional<BenefactorMarketingIntegrationSyncRunsStatus> parse_benefactor_marketing_integration_sync_runs_status(const std::string& value) {
    if (value == "queued") return BenefactorMarketingIntegrationSyncRunsStatus::Queued;
    if (value == "running") return BenefactorMarketingIntegrationSyncRunsStatus::Running;
    if (value == "succeeded") return BenefactorMarketingIntegrationSyncRunsStatus::Succeeded;
    if (value == "failed") return BenefactorMarketingIntegrationSyncRunsStatus::Failed;
    if (value == "canceled") return BenefactorMarketingIntegrationSyncRunsStatus::Canceled;
    return std::nullopt;
}

struct BenefactorMarketingIntegrationSyncRunsRow {
    std::string id;
    std::string integration_id;
    std::optional<std::string> client_id;
    std::string sync_kind;
    std::string direction;
    std::string status;
    int32_t records_seen;
    int32_t records_changed;
    std::optional<std::string> cursor_before;
    std::optional<std::string> cursor_after;
    std::string payload;
    std::optional<std::string> error_summary;
    std::optional<std::string> started_at;
    std::optional<std::string> completed_at;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingIntegrationSyncRunsRow benefactor_marketing_integration_sync_runs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingIntegrationSyncRunsRow row;
    (void)is_null;
    row.id = get(0);
    row.integration_id = get(1);
    row.client_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.sync_kind = get(3);
    row.direction = get(4);
    row.status = get(5);
    row.records_seen = std::stoi(get(6));
    row.records_changed = std::stoi(get(7));
    row.cursor_before = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.cursor_after = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.payload = get(10);
    row.error_summary = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.started_at = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.completed_at = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    row.created_at = get(14);
    row.updated_at = get(15);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_integration_sync_runs_records_seen(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_integration_sync_runs.records_seen is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_integration_sync_runs_records_changed(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_integration_sync_runs.records_changed is below the minimum");
    return std::nullopt;
}

inline const char* benefactor_marketing_outreach_sequences_table = "benefactor_marketing_outreach_sequences";
inline const std::vector<std::string> benefactor_marketing_outreach_sequences_columns = { "id", "client_id", "campaign_id", "status", "channel", "name", "audience_filter", "cadence", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_outreach_sequences_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      campaign_id::text as campaign_id,
      status,
      channel,
      name,
      audience_filter::text as audience_filter_json,
      cadence::text as cadence_json,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_outreach_sequences)SQL";

enum class BenefactorMarketingOutreachSequencesStatus { Draft, Active, Paused, Completed, Archived };
inline std::string benefactor_marketing_outreach_sequences_status_to_string(BenefactorMarketingOutreachSequencesStatus value) {
    switch (value) {
        case BenefactorMarketingOutreachSequencesStatus::Draft: return "draft";
        case BenefactorMarketingOutreachSequencesStatus::Active: return "active";
        case BenefactorMarketingOutreachSequencesStatus::Paused: return "paused";
        case BenefactorMarketingOutreachSequencesStatus::Completed: return "completed";
        case BenefactorMarketingOutreachSequencesStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<BenefactorMarketingOutreachSequencesStatus> parse_benefactor_marketing_outreach_sequences_status(const std::string& value) {
    if (value == "draft") return BenefactorMarketingOutreachSequencesStatus::Draft;
    if (value == "active") return BenefactorMarketingOutreachSequencesStatus::Active;
    if (value == "paused") return BenefactorMarketingOutreachSequencesStatus::Paused;
    if (value == "completed") return BenefactorMarketingOutreachSequencesStatus::Completed;
    if (value == "archived") return BenefactorMarketingOutreachSequencesStatus::Archived;
    return std::nullopt;
}

enum class BenefactorMarketingOutreachSequencesChannel { Email, Linkedin, Sms, Phone, MultiChannel };
inline std::string benefactor_marketing_outreach_sequences_channel_to_string(BenefactorMarketingOutreachSequencesChannel value) {
    switch (value) {
        case BenefactorMarketingOutreachSequencesChannel::Email: return "email";
        case BenefactorMarketingOutreachSequencesChannel::Linkedin: return "linkedin";
        case BenefactorMarketingOutreachSequencesChannel::Sms: return "sms";
        case BenefactorMarketingOutreachSequencesChannel::Phone: return "phone";
        case BenefactorMarketingOutreachSequencesChannel::MultiChannel: return "multi_channel";
    }
    return "";
}
inline std::optional<BenefactorMarketingOutreachSequencesChannel> parse_benefactor_marketing_outreach_sequences_channel(const std::string& value) {
    if (value == "email") return BenefactorMarketingOutreachSequencesChannel::Email;
    if (value == "linkedin") return BenefactorMarketingOutreachSequencesChannel::Linkedin;
    if (value == "sms") return BenefactorMarketingOutreachSequencesChannel::Sms;
    if (value == "phone") return BenefactorMarketingOutreachSequencesChannel::Phone;
    if (value == "multi_channel") return BenefactorMarketingOutreachSequencesChannel::MultiChannel;
    return std::nullopt;
}

struct BenefactorMarketingOutreachSequencesRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> campaign_id;
    std::string status;
    std::string channel;
    std::string name;
    std::string audience_filter;
    std::string cadence;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingOutreachSequencesRow benefactor_marketing_outreach_sequences_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingOutreachSequencesRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.campaign_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.status = get(3);
    row.channel = get(4);
    row.name = get(5);
    row.audience_filter = get(6);
    row.cadence = get(7);
    row.meta_data = get(8);
    row.created_at = get(9);
    row.updated_at = get(10);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_outreach_sequences_name(const std::string& value) {
    if (value.size() > 220) return std::string("benefactor_marketing_outreach_sequences.name must be at most 220 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_outreach_steps_table = "benefactor_marketing_outreach_steps";
inline const std::vector<std::string> benefactor_marketing_outreach_steps_columns = { "id", "sequence_id", "status", "step_order", "channel", "delay_minutes", "subject", "body_template", "personalization_hints", "experiment_key", "created_at", "updated_at" };
inline const char* benefactor_marketing_outreach_steps_select_sql = R"SQL(select
      id::text as id,
      sequence_id::text as sequence_id,
      status,
      step_order,
      channel,
      delay_minutes,
      subject,
      body_template,
      personalization_hints::text as personalization_hints_json,
      experiment_key,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_outreach_steps)SQL";

enum class BenefactorMarketingOutreachStepsStatus { Active, Disabled, Archived };
inline std::string benefactor_marketing_outreach_steps_status_to_string(BenefactorMarketingOutreachStepsStatus value) {
    switch (value) {
        case BenefactorMarketingOutreachStepsStatus::Active: return "active";
        case BenefactorMarketingOutreachStepsStatus::Disabled: return "disabled";
        case BenefactorMarketingOutreachStepsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<BenefactorMarketingOutreachStepsStatus> parse_benefactor_marketing_outreach_steps_status(const std::string& value) {
    if (value == "active") return BenefactorMarketingOutreachStepsStatus::Active;
    if (value == "disabled") return BenefactorMarketingOutreachStepsStatus::Disabled;
    if (value == "archived") return BenefactorMarketingOutreachStepsStatus::Archived;
    return std::nullopt;
}

enum class BenefactorMarketingOutreachStepsChannel { Email, Linkedin, Sms, Phone, Task };
inline std::string benefactor_marketing_outreach_steps_channel_to_string(BenefactorMarketingOutreachStepsChannel value) {
    switch (value) {
        case BenefactorMarketingOutreachStepsChannel::Email: return "email";
        case BenefactorMarketingOutreachStepsChannel::Linkedin: return "linkedin";
        case BenefactorMarketingOutreachStepsChannel::Sms: return "sms";
        case BenefactorMarketingOutreachStepsChannel::Phone: return "phone";
        case BenefactorMarketingOutreachStepsChannel::Task: return "task";
    }
    return "";
}
inline std::optional<BenefactorMarketingOutreachStepsChannel> parse_benefactor_marketing_outreach_steps_channel(const std::string& value) {
    if (value == "email") return BenefactorMarketingOutreachStepsChannel::Email;
    if (value == "linkedin") return BenefactorMarketingOutreachStepsChannel::Linkedin;
    if (value == "sms") return BenefactorMarketingOutreachStepsChannel::Sms;
    if (value == "phone") return BenefactorMarketingOutreachStepsChannel::Phone;
    if (value == "task") return BenefactorMarketingOutreachStepsChannel::Task;
    return std::nullopt;
}

struct BenefactorMarketingOutreachStepsRow {
    std::string id;
    std::string sequence_id;
    std::string status;
    int32_t step_order;
    std::string channel;
    int32_t delay_minutes;
    std::optional<std::string> subject;
    std::optional<std::string> body_template;
    std::string personalization_hints;
    std::optional<std::string> experiment_key;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingOutreachStepsRow benefactor_marketing_outreach_steps_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingOutreachStepsRow row;
    (void)is_null;
    row.id = get(0);
    row.sequence_id = get(1);
    row.status = get(2);
    row.step_order = std::stoi(get(3));
    row.channel = get(4);
    row.delay_minutes = std::stoi(get(5));
    row.subject = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.body_template = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.personalization_hints = get(8);
    row.experiment_key = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.created_at = get(10);
    row.updated_at = get(11);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_outreach_steps_step_order(int32_t value) {
    if (value < 1) return std::string("benefactor_marketing_outreach_steps.step_order is below the minimum");
    if (value > 100) return std::string("benefactor_marketing_outreach_steps.step_order is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_outreach_steps_delay_minutes(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_outreach_steps.delay_minutes is below the minimum");
    if (value > 525600) return std::string("benefactor_marketing_outreach_steps.delay_minutes is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_outreach_steps_subject(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_outreach_steps.subject must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_outreach_steps_experiment_key(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_marketing_outreach_steps.experiment_key must be at most 120 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_outreach_enrollments_table = "benefactor_marketing_outreach_enrollments";
inline const std::vector<std::string> benefactor_marketing_outreach_enrollments_columns = { "id", "client_id", "sequence_id", "lead_id", "contact_id", "status", "current_step_order", "enrollment_context", "last_touch_at", "next_touch_at", "outcome", "created_at", "updated_at" };
inline const char* benefactor_marketing_outreach_enrollments_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      sequence_id::text as sequence_id,
      lead_id::text as lead_id,
      contact_id::text as contact_id,
      status,
      current_step_order,
      enrollment_context::text as enrollment_context_json,
      to_char(last_touch_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_touch_at,
      to_char(next_touch_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as next_touch_at,
      outcome,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_outreach_enrollments)SQL";

enum class BenefactorMarketingOutreachEnrollmentsStatus { Active, Paused, Completed, Bounced, Unsubscribed, Failed };
inline std::string benefactor_marketing_outreach_enrollments_status_to_string(BenefactorMarketingOutreachEnrollmentsStatus value) {
    switch (value) {
        case BenefactorMarketingOutreachEnrollmentsStatus::Active: return "active";
        case BenefactorMarketingOutreachEnrollmentsStatus::Paused: return "paused";
        case BenefactorMarketingOutreachEnrollmentsStatus::Completed: return "completed";
        case BenefactorMarketingOutreachEnrollmentsStatus::Bounced: return "bounced";
        case BenefactorMarketingOutreachEnrollmentsStatus::Unsubscribed: return "unsubscribed";
        case BenefactorMarketingOutreachEnrollmentsStatus::Failed: return "failed";
    }
    return "";
}
inline std::optional<BenefactorMarketingOutreachEnrollmentsStatus> parse_benefactor_marketing_outreach_enrollments_status(const std::string& value) {
    if (value == "active") return BenefactorMarketingOutreachEnrollmentsStatus::Active;
    if (value == "paused") return BenefactorMarketingOutreachEnrollmentsStatus::Paused;
    if (value == "completed") return BenefactorMarketingOutreachEnrollmentsStatus::Completed;
    if (value == "bounced") return BenefactorMarketingOutreachEnrollmentsStatus::Bounced;
    if (value == "unsubscribed") return BenefactorMarketingOutreachEnrollmentsStatus::Unsubscribed;
    if (value == "failed") return BenefactorMarketingOutreachEnrollmentsStatus::Failed;
    return std::nullopt;
}

struct BenefactorMarketingOutreachEnrollmentsRow {
    std::string id;
    std::string client_id;
    std::string sequence_id;
    std::optional<std::string> lead_id;
    std::optional<std::string> contact_id;
    std::string status;
    int32_t current_step_order;
    std::string enrollment_context;
    std::optional<std::string> last_touch_at;
    std::optional<std::string> next_touch_at;
    std::optional<std::string> outcome;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingOutreachEnrollmentsRow benefactor_marketing_outreach_enrollments_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingOutreachEnrollmentsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.sequence_id = get(2);
    row.lead_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.contact_id = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.status = get(5);
    row.current_step_order = std::stoi(get(6));
    row.enrollment_context = get(7);
    row.last_touch_at = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.next_touch_at = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.outcome = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_outreach_enrollments_current_step_order(int32_t value) {
    if (value < 1) return std::string("benefactor_marketing_outreach_enrollments.current_step_order is below the minimum");
    if (value > 100) return std::string("benefactor_marketing_outreach_enrollments.current_step_order is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_outreach_enrollments_outcome(const std::string& value) {
    if (value.size() > 64) return std::string("benefactor_marketing_outreach_enrollments.outcome must be at most 64 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_outreach_touchpoints_table = "benefactor_marketing_outreach_touchpoints";
inline const std::vector<std::string> benefactor_marketing_outreach_touchpoints_columns = { "id", "client_id", "sequence_id", "enrollment_id", "campaign_id", "lead_id", "contact_id", "channel", "direction", "status", "subject", "body_excerpt", "external_message_id", "occurred_at", "payload", "created_at" };
inline const char* benefactor_marketing_outreach_touchpoints_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      sequence_id::text as sequence_id,
      enrollment_id::text as enrollment_id,
      campaign_id::text as campaign_id,
      lead_id::text as lead_id,
      contact_id::text as contact_id,
      channel,
      direction,
      status,
      subject,
      body_excerpt,
      external_message_id,
      to_char(occurred_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as occurred_at,
      payload::text as payload_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from benefactor_marketing_outreach_touchpoints)SQL";

enum class BenefactorMarketingOutreachTouchpointsChannel { Email, Linkedin, Sms, Phone, Task, Meeting };
inline std::string benefactor_marketing_outreach_touchpoints_channel_to_string(BenefactorMarketingOutreachTouchpointsChannel value) {
    switch (value) {
        case BenefactorMarketingOutreachTouchpointsChannel::Email: return "email";
        case BenefactorMarketingOutreachTouchpointsChannel::Linkedin: return "linkedin";
        case BenefactorMarketingOutreachTouchpointsChannel::Sms: return "sms";
        case BenefactorMarketingOutreachTouchpointsChannel::Phone: return "phone";
        case BenefactorMarketingOutreachTouchpointsChannel::Task: return "task";
        case BenefactorMarketingOutreachTouchpointsChannel::Meeting: return "meeting";
    }
    return "";
}
inline std::optional<BenefactorMarketingOutreachTouchpointsChannel> parse_benefactor_marketing_outreach_touchpoints_channel(const std::string& value) {
    if (value == "email") return BenefactorMarketingOutreachTouchpointsChannel::Email;
    if (value == "linkedin") return BenefactorMarketingOutreachTouchpointsChannel::Linkedin;
    if (value == "sms") return BenefactorMarketingOutreachTouchpointsChannel::Sms;
    if (value == "phone") return BenefactorMarketingOutreachTouchpointsChannel::Phone;
    if (value == "task") return BenefactorMarketingOutreachTouchpointsChannel::Task;
    if (value == "meeting") return BenefactorMarketingOutreachTouchpointsChannel::Meeting;
    return std::nullopt;
}

enum class BenefactorMarketingOutreachTouchpointsDirection { Outbound, Inbound, Internal };
inline std::string benefactor_marketing_outreach_touchpoints_direction_to_string(BenefactorMarketingOutreachTouchpointsDirection value) {
    switch (value) {
        case BenefactorMarketingOutreachTouchpointsDirection::Outbound: return "outbound";
        case BenefactorMarketingOutreachTouchpointsDirection::Inbound: return "inbound";
        case BenefactorMarketingOutreachTouchpointsDirection::Internal: return "internal";
    }
    return "";
}
inline std::optional<BenefactorMarketingOutreachTouchpointsDirection> parse_benefactor_marketing_outreach_touchpoints_direction(const std::string& value) {
    if (value == "outbound") return BenefactorMarketingOutreachTouchpointsDirection::Outbound;
    if (value == "inbound") return BenefactorMarketingOutreachTouchpointsDirection::Inbound;
    if (value == "internal") return BenefactorMarketingOutreachTouchpointsDirection::Internal;
    return std::nullopt;
}

enum class BenefactorMarketingOutreachTouchpointsStatus { Planned, Sent, Delivered, Opened, Clicked, Replied, Failed, Bounced };
inline std::string benefactor_marketing_outreach_touchpoints_status_to_string(BenefactorMarketingOutreachTouchpointsStatus value) {
    switch (value) {
        case BenefactorMarketingOutreachTouchpointsStatus::Planned: return "planned";
        case BenefactorMarketingOutreachTouchpointsStatus::Sent: return "sent";
        case BenefactorMarketingOutreachTouchpointsStatus::Delivered: return "delivered";
        case BenefactorMarketingOutreachTouchpointsStatus::Opened: return "opened";
        case BenefactorMarketingOutreachTouchpointsStatus::Clicked: return "clicked";
        case BenefactorMarketingOutreachTouchpointsStatus::Replied: return "replied";
        case BenefactorMarketingOutreachTouchpointsStatus::Failed: return "failed";
        case BenefactorMarketingOutreachTouchpointsStatus::Bounced: return "bounced";
    }
    return "";
}
inline std::optional<BenefactorMarketingOutreachTouchpointsStatus> parse_benefactor_marketing_outreach_touchpoints_status(const std::string& value) {
    if (value == "planned") return BenefactorMarketingOutreachTouchpointsStatus::Planned;
    if (value == "sent") return BenefactorMarketingOutreachTouchpointsStatus::Sent;
    if (value == "delivered") return BenefactorMarketingOutreachTouchpointsStatus::Delivered;
    if (value == "opened") return BenefactorMarketingOutreachTouchpointsStatus::Opened;
    if (value == "clicked") return BenefactorMarketingOutreachTouchpointsStatus::Clicked;
    if (value == "replied") return BenefactorMarketingOutreachTouchpointsStatus::Replied;
    if (value == "failed") return BenefactorMarketingOutreachTouchpointsStatus::Failed;
    if (value == "bounced") return BenefactorMarketingOutreachTouchpointsStatus::Bounced;
    return std::nullopt;
}

struct BenefactorMarketingOutreachTouchpointsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> sequence_id;
    std::optional<std::string> enrollment_id;
    std::optional<std::string> campaign_id;
    std::optional<std::string> lead_id;
    std::optional<std::string> contact_id;
    std::string channel;
    std::string direction;
    std::string status;
    std::optional<std::string> subject;
    std::optional<std::string> body_excerpt;
    std::optional<std::string> external_message_id;
    std::string occurred_at;
    std::string payload;
    std::string created_at;
};

inline BenefactorMarketingOutreachTouchpointsRow benefactor_marketing_outreach_touchpoints_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingOutreachTouchpointsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.sequence_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.enrollment_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.campaign_id = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.lead_id = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.contact_id = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.channel = get(7);
    row.direction = get(8);
    row.status = get(9);
    row.subject = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.body_excerpt = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.external_message_id = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.occurred_at = get(13);
    row.payload = get(14);
    row.created_at = get(15);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_outreach_touchpoints_subject(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_outreach_touchpoints.subject must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_outreach_touchpoints_external_message_id(const std::string& value) {
    if (value.size() > 200) return std::string("benefactor_marketing_outreach_touchpoints.external_message_id must be at most 200 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_prospect_research_briefs_table = "benefactor_marketing_prospect_research_briefs";
inline const std::vector<std::string> benefactor_marketing_prospect_research_briefs_columns = { "id", "client_id", "lead_id", "status", "research_kind", "source", "summary", "findings", "recommended_actions", "confidence_micros", "model_name", "generated_at", "created_at", "updated_at" };
inline const char* benefactor_marketing_prospect_research_briefs_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      lead_id::text as lead_id,
      status,
      research_kind,
      source,
      summary,
      findings::text as findings_json,
      recommended_actions::text as recommended_actions_json,
      confidence_micros,
      model_name,
      to_char(generated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as generated_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_prospect_research_briefs)SQL";

enum class BenefactorMarketingProspectResearchBriefsStatus { Draft, Ready, Stale, Failed };
inline std::string benefactor_marketing_prospect_research_briefs_status_to_string(BenefactorMarketingProspectResearchBriefsStatus value) {
    switch (value) {
        case BenefactorMarketingProspectResearchBriefsStatus::Draft: return "draft";
        case BenefactorMarketingProspectResearchBriefsStatus::Ready: return "ready";
        case BenefactorMarketingProspectResearchBriefsStatus::Stale: return "stale";
        case BenefactorMarketingProspectResearchBriefsStatus::Failed: return "failed";
    }
    return "";
}
inline std::optional<BenefactorMarketingProspectResearchBriefsStatus> parse_benefactor_marketing_prospect_research_briefs_status(const std::string& value) {
    if (value == "draft") return BenefactorMarketingProspectResearchBriefsStatus::Draft;
    if (value == "ready") return BenefactorMarketingProspectResearchBriefsStatus::Ready;
    if (value == "stale") return BenefactorMarketingProspectResearchBriefsStatus::Stale;
    if (value == "failed") return BenefactorMarketingProspectResearchBriefsStatus::Failed;
    return std::nullopt;
}

enum class BenefactorMarketingProspectResearchBriefsResearchKind { AccountResearch, ContactResearch, CompetitiveIntel, ProposalBrief, OutreachPersonalization };
inline std::string benefactor_marketing_prospect_research_briefs_research_kind_to_string(BenefactorMarketingProspectResearchBriefsResearchKind value) {
    switch (value) {
        case BenefactorMarketingProspectResearchBriefsResearchKind::AccountResearch: return "account_research";
        case BenefactorMarketingProspectResearchBriefsResearchKind::ContactResearch: return "contact_research";
        case BenefactorMarketingProspectResearchBriefsResearchKind::CompetitiveIntel: return "competitive_intel";
        case BenefactorMarketingProspectResearchBriefsResearchKind::ProposalBrief: return "proposal_brief";
        case BenefactorMarketingProspectResearchBriefsResearchKind::OutreachPersonalization: return "outreach_personalization";
    }
    return "";
}
inline std::optional<BenefactorMarketingProspectResearchBriefsResearchKind> parse_benefactor_marketing_prospect_research_briefs_research_kind(const std::string& value) {
    if (value == "account_research") return BenefactorMarketingProspectResearchBriefsResearchKind::AccountResearch;
    if (value == "contact_research") return BenefactorMarketingProspectResearchBriefsResearchKind::ContactResearch;
    if (value == "competitive_intel") return BenefactorMarketingProspectResearchBriefsResearchKind::CompetitiveIntel;
    if (value == "proposal_brief") return BenefactorMarketingProspectResearchBriefsResearchKind::ProposalBrief;
    if (value == "outreach_personalization") return BenefactorMarketingProspectResearchBriefsResearchKind::OutreachPersonalization;
    return std::nullopt;
}

enum class BenefactorMarketingProspectResearchBriefsSource { AiAssisted, Analyst, Scraper, Integration };
inline std::string benefactor_marketing_prospect_research_briefs_source_to_string(BenefactorMarketingProspectResearchBriefsSource value) {
    switch (value) {
        case BenefactorMarketingProspectResearchBriefsSource::AiAssisted: return "ai_assisted";
        case BenefactorMarketingProspectResearchBriefsSource::Analyst: return "analyst";
        case BenefactorMarketingProspectResearchBriefsSource::Scraper: return "scraper";
        case BenefactorMarketingProspectResearchBriefsSource::Integration: return "integration";
    }
    return "";
}
inline std::optional<BenefactorMarketingProspectResearchBriefsSource> parse_benefactor_marketing_prospect_research_briefs_source(const std::string& value) {
    if (value == "ai_assisted") return BenefactorMarketingProspectResearchBriefsSource::AiAssisted;
    if (value == "analyst") return BenefactorMarketingProspectResearchBriefsSource::Analyst;
    if (value == "scraper") return BenefactorMarketingProspectResearchBriefsSource::Scraper;
    if (value == "integration") return BenefactorMarketingProspectResearchBriefsSource::Integration;
    return std::nullopt;
}

struct BenefactorMarketingProspectResearchBriefsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> lead_id;
    std::string status;
    std::string research_kind;
    std::string source;
    std::optional<std::string> summary;
    std::string findings;
    std::string recommended_actions;
    int32_t confidence_micros;
    std::optional<std::string> model_name;
    std::optional<std::string> generated_at;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingProspectResearchBriefsRow benefactor_marketing_prospect_research_briefs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingProspectResearchBriefsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.lead_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.status = get(3);
    row.research_kind = get(4);
    row.source = get(5);
    row.summary = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.findings = get(7);
    row.recommended_actions = get(8);
    row.confidence_micros = std::stoi(get(9));
    row.model_name = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.generated_at = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.created_at = get(12);
    row.updated_at = get(13);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_prospect_research_briefs_confidence_micros(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_prospect_research_briefs.confidence_micros is below the minimum");
    if (value > 1000000) return std::string("benefactor_marketing_prospect_research_briefs.confidence_micros is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_prospect_research_briefs_model_name(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_marketing_prospect_research_briefs.model_name must be at most 120 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_conversion_events_table = "benefactor_marketing_conversion_events";
inline const std::vector<std::string> benefactor_marketing_conversion_events_columns = { "id", "client_id", "campaign_id", "lead_id", "content_asset_id", "event_type", "source_platform", "source_event_id", "session_id", "visitor_key", "occurred_at", "value_cents", "utm", "payload", "created_at" };
inline const char* benefactor_marketing_conversion_events_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      campaign_id::text as campaign_id,
      lead_id::text as lead_id,
      content_asset_id::text as content_asset_id,
      event_type,
      source_platform,
      source_event_id,
      session_id,
      visitor_key,
      to_char(occurred_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as occurred_at,
      value_cents,
      utm::text as utm_json,
      payload::text as payload_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from benefactor_marketing_conversion_events)SQL";

enum class BenefactorMarketingConversionEventsEventType { LandingPageView, FormSubmit, ChatStarted, CalendarBooked, AssetDownload, TrialSignup, Purchase, Custom };
inline std::string benefactor_marketing_conversion_events_event_type_to_string(BenefactorMarketingConversionEventsEventType value) {
    switch (value) {
        case BenefactorMarketingConversionEventsEventType::LandingPageView: return "landing_page_view";
        case BenefactorMarketingConversionEventsEventType::FormSubmit: return "form_submit";
        case BenefactorMarketingConversionEventsEventType::ChatStarted: return "chat_started";
        case BenefactorMarketingConversionEventsEventType::CalendarBooked: return "calendar_booked";
        case BenefactorMarketingConversionEventsEventType::AssetDownload: return "asset_download";
        case BenefactorMarketingConversionEventsEventType::TrialSignup: return "trial_signup";
        case BenefactorMarketingConversionEventsEventType::Purchase: return "purchase";
        case BenefactorMarketingConversionEventsEventType::Custom: return "custom";
    }
    return "";
}
inline std::optional<BenefactorMarketingConversionEventsEventType> parse_benefactor_marketing_conversion_events_event_type(const std::string& value) {
    if (value == "landing_page_view") return BenefactorMarketingConversionEventsEventType::LandingPageView;
    if (value == "form_submit") return BenefactorMarketingConversionEventsEventType::FormSubmit;
    if (value == "chat_started") return BenefactorMarketingConversionEventsEventType::ChatStarted;
    if (value == "calendar_booked") return BenefactorMarketingConversionEventsEventType::CalendarBooked;
    if (value == "asset_download") return BenefactorMarketingConversionEventsEventType::AssetDownload;
    if (value == "trial_signup") return BenefactorMarketingConversionEventsEventType::TrialSignup;
    if (value == "purchase") return BenefactorMarketingConversionEventsEventType::Purchase;
    if (value == "custom") return BenefactorMarketingConversionEventsEventType::Custom;
    return std::nullopt;
}

struct BenefactorMarketingConversionEventsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> campaign_id;
    std::optional<std::string> lead_id;
    std::optional<std::string> content_asset_id;
    std::string event_type;
    std::optional<std::string> source_platform;
    std::optional<std::string> source_event_id;
    std::optional<std::string> session_id;
    std::optional<std::string> visitor_key;
    std::string occurred_at;
    int32_t value_cents;
    std::string utm;
    std::string payload;
    std::string created_at;
};

inline BenefactorMarketingConversionEventsRow benefactor_marketing_conversion_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingConversionEventsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.campaign_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.lead_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.content_asset_id = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.event_type = get(5);
    row.source_platform = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.source_event_id = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.session_id = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.visitor_key = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.occurred_at = get(10);
    row.value_cents = std::stoi(get(11));
    row.utm = get(12);
    row.payload = get(13);
    row.created_at = get(14);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_conversion_events_source_platform(const std::string& value) {
    if (value.size() > 64) return std::string("benefactor_marketing_conversion_events.source_platform must be at most 64 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_conversion_events_source_event_id(const std::string& value) {
    if (value.size() > 200) return std::string("benefactor_marketing_conversion_events.source_event_id must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_conversion_events_session_id(const std::string& value) {
    if (value.size() > 200) return std::string("benefactor_marketing_conversion_events.session_id must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_conversion_events_visitor_key(const std::string& value) {
    if (value.size() > 200) return std::string("benefactor_marketing_conversion_events.visitor_key must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_conversion_events_value_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_conversion_events.value_cents is below the minimum");
    return std::nullopt;
}

inline const char* benefactor_marketing_portal_members_table = "benefactor_marketing_portal_members";
inline const std::vector<std::string> benefactor_marketing_portal_members_columns = { "id", "client_id", "contact_id", "user_id", "email", "status", "role", "access_scope", "last_seen_at", "invited_at", "accepted_at", "created_at", "updated_at" };
inline const char* benefactor_marketing_portal_members_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      contact_id::text as contact_id,
      user_id::text as user_id,
      email,
      status,
      role,
      access_scope::text as access_scope_json,
      to_char(last_seen_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_seen_at,
      to_char(invited_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as invited_at,
      to_char(accepted_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as accepted_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_portal_members)SQL";

enum class BenefactorMarketingPortalMembersStatus { Invited, Active, Disabled, Revoked };
inline std::string benefactor_marketing_portal_members_status_to_string(BenefactorMarketingPortalMembersStatus value) {
    switch (value) {
        case BenefactorMarketingPortalMembersStatus::Invited: return "invited";
        case BenefactorMarketingPortalMembersStatus::Active: return "active";
        case BenefactorMarketingPortalMembersStatus::Disabled: return "disabled";
        case BenefactorMarketingPortalMembersStatus::Revoked: return "revoked";
    }
    return "";
}
inline std::optional<BenefactorMarketingPortalMembersStatus> parse_benefactor_marketing_portal_members_status(const std::string& value) {
    if (value == "invited") return BenefactorMarketingPortalMembersStatus::Invited;
    if (value == "active") return BenefactorMarketingPortalMembersStatus::Active;
    if (value == "disabled") return BenefactorMarketingPortalMembersStatus::Disabled;
    if (value == "revoked") return BenefactorMarketingPortalMembersStatus::Revoked;
    return std::nullopt;
}

enum class BenefactorMarketingPortalMembersRole { Owner, Approver, Viewer, Billing, Collaborator };
inline std::string benefactor_marketing_portal_members_role_to_string(BenefactorMarketingPortalMembersRole value) {
    switch (value) {
        case BenefactorMarketingPortalMembersRole::Owner: return "owner";
        case BenefactorMarketingPortalMembersRole::Approver: return "approver";
        case BenefactorMarketingPortalMembersRole::Viewer: return "viewer";
        case BenefactorMarketingPortalMembersRole::Billing: return "billing";
        case BenefactorMarketingPortalMembersRole::Collaborator: return "collaborator";
    }
    return "";
}
inline std::optional<BenefactorMarketingPortalMembersRole> parse_benefactor_marketing_portal_members_role(const std::string& value) {
    if (value == "owner") return BenefactorMarketingPortalMembersRole::Owner;
    if (value == "approver") return BenefactorMarketingPortalMembersRole::Approver;
    if (value == "viewer") return BenefactorMarketingPortalMembersRole::Viewer;
    if (value == "billing") return BenefactorMarketingPortalMembersRole::Billing;
    if (value == "collaborator") return BenefactorMarketingPortalMembersRole::Collaborator;
    return std::nullopt;
}

struct BenefactorMarketingPortalMembersRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> contact_id;
    std::optional<std::string> user_id;
    std::string email;
    std::string status;
    std::string role;
    std::string access_scope;
    std::optional<std::string> last_seen_at;
    std::string invited_at;
    std::optional<std::string> accepted_at;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingPortalMembersRow benefactor_marketing_portal_members_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingPortalMembersRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.contact_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.user_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.email = get(4);
    row.status = get(5);
    row.role = get(6);
    row.access_scope = get(7);
    row.last_seen_at = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.invited_at = get(9);
    row.accepted_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_portal_members_email(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_portal_members.email must be at most 240 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_shared_documents_table = "benefactor_marketing_shared_documents";
inline const std::vector<std::string> benefactor_marketing_shared_documents_columns = { "id", "client_id", "campaign_id", "content_asset_id", "status", "document_kind", "title", "storage_uri", "mime_type", "visibility", "uploaded_by", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_shared_documents_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      campaign_id::text as campaign_id,
      content_asset_id::text as content_asset_id,
      status,
      document_kind,
      title,
      storage_uri,
      mime_type,
      visibility,
      uploaded_by::text as uploaded_by,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_shared_documents)SQL";

enum class BenefactorMarketingSharedDocumentsStatus { Active, Archived, Deleted };
inline std::string benefactor_marketing_shared_documents_status_to_string(BenefactorMarketingSharedDocumentsStatus value) {
    switch (value) {
        case BenefactorMarketingSharedDocumentsStatus::Active: return "active";
        case BenefactorMarketingSharedDocumentsStatus::Archived: return "archived";
        case BenefactorMarketingSharedDocumentsStatus::Deleted: return "deleted";
    }
    return "";
}
inline std::optional<BenefactorMarketingSharedDocumentsStatus> parse_benefactor_marketing_shared_documents_status(const std::string& value) {
    if (value == "active") return BenefactorMarketingSharedDocumentsStatus::Active;
    if (value == "archived") return BenefactorMarketingSharedDocumentsStatus::Archived;
    if (value == "deleted") return BenefactorMarketingSharedDocumentsStatus::Deleted;
    return std::nullopt;
}

enum class BenefactorMarketingSharedDocumentsDocumentKind { Contract, Invoice, Report, Creative, BrandAsset, Proposal, MeetingNotes, Other };
inline std::string benefactor_marketing_shared_documents_document_kind_to_string(BenefactorMarketingSharedDocumentsDocumentKind value) {
    switch (value) {
        case BenefactorMarketingSharedDocumentsDocumentKind::Contract: return "contract";
        case BenefactorMarketingSharedDocumentsDocumentKind::Invoice: return "invoice";
        case BenefactorMarketingSharedDocumentsDocumentKind::Report: return "report";
        case BenefactorMarketingSharedDocumentsDocumentKind::Creative: return "creative";
        case BenefactorMarketingSharedDocumentsDocumentKind::BrandAsset: return "brand_asset";
        case BenefactorMarketingSharedDocumentsDocumentKind::Proposal: return "proposal";
        case BenefactorMarketingSharedDocumentsDocumentKind::MeetingNotes: return "meeting_notes";
        case BenefactorMarketingSharedDocumentsDocumentKind::Other: return "other";
    }
    return "";
}
inline std::optional<BenefactorMarketingSharedDocumentsDocumentKind> parse_benefactor_marketing_shared_documents_document_kind(const std::string& value) {
    if (value == "contract") return BenefactorMarketingSharedDocumentsDocumentKind::Contract;
    if (value == "invoice") return BenefactorMarketingSharedDocumentsDocumentKind::Invoice;
    if (value == "report") return BenefactorMarketingSharedDocumentsDocumentKind::Report;
    if (value == "creative") return BenefactorMarketingSharedDocumentsDocumentKind::Creative;
    if (value == "brand_asset") return BenefactorMarketingSharedDocumentsDocumentKind::BrandAsset;
    if (value == "proposal") return BenefactorMarketingSharedDocumentsDocumentKind::Proposal;
    if (value == "meeting_notes") return BenefactorMarketingSharedDocumentsDocumentKind::MeetingNotes;
    if (value == "other") return BenefactorMarketingSharedDocumentsDocumentKind::Other;
    return std::nullopt;
}

enum class BenefactorMarketingSharedDocumentsVisibility { Internal, ClientPortal, PublicLink };
inline std::string benefactor_marketing_shared_documents_visibility_to_string(BenefactorMarketingSharedDocumentsVisibility value) {
    switch (value) {
        case BenefactorMarketingSharedDocumentsVisibility::Internal: return "internal";
        case BenefactorMarketingSharedDocumentsVisibility::ClientPortal: return "client_portal";
        case BenefactorMarketingSharedDocumentsVisibility::PublicLink: return "public_link";
    }
    return "";
}
inline std::optional<BenefactorMarketingSharedDocumentsVisibility> parse_benefactor_marketing_shared_documents_visibility(const std::string& value) {
    if (value == "internal") return BenefactorMarketingSharedDocumentsVisibility::Internal;
    if (value == "client_portal") return BenefactorMarketingSharedDocumentsVisibility::ClientPortal;
    if (value == "public_link") return BenefactorMarketingSharedDocumentsVisibility::PublicLink;
    return std::nullopt;
}

struct BenefactorMarketingSharedDocumentsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> campaign_id;
    std::optional<std::string> content_asset_id;
    std::string status;
    std::string document_kind;
    std::string title;
    std::string storage_uri;
    std::optional<std::string> mime_type;
    std::string visibility;
    std::optional<std::string> uploaded_by;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingSharedDocumentsRow benefactor_marketing_shared_documents_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingSharedDocumentsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.campaign_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.content_asset_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.status = get(4);
    row.document_kind = get(5);
    row.title = get(6);
    row.storage_uri = get(7);
    row.mime_type = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.visibility = get(9);
    row.uploaded_by = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.meta_data = get(11);
    row.created_at = get(12);
    row.updated_at = get(13);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_shared_documents_title(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_shared_documents.title must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_shared_documents_mime_type(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_marketing_shared_documents.mime_type must be at most 120 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_collaboration_comments_table = "benefactor_marketing_collaboration_comments";
inline const std::vector<std::string> benefactor_marketing_collaboration_comments_columns = { "id", "client_id", "parent_comment_id", "resource_type", "resource_id", "author_user_id", "author_contact_id", "body", "status", "visibility", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_collaboration_comments_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      parent_comment_id::text as parent_comment_id,
      resource_type,
      resource_id::text as resource_id,
      author_user_id::text as author_user_id,
      author_contact_id::text as author_contact_id,
      body,
      status,
      visibility,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_collaboration_comments)SQL";

enum class BenefactorMarketingCollaborationCommentsResourceType { Client, Campaign, ContentAsset, Approval, Ticket, Document, Report, Meeting };
inline std::string benefactor_marketing_collaboration_comments_resource_type_to_string(BenefactorMarketingCollaborationCommentsResourceType value) {
    switch (value) {
        case BenefactorMarketingCollaborationCommentsResourceType::Client: return "client";
        case BenefactorMarketingCollaborationCommentsResourceType::Campaign: return "campaign";
        case BenefactorMarketingCollaborationCommentsResourceType::ContentAsset: return "content_asset";
        case BenefactorMarketingCollaborationCommentsResourceType::Approval: return "approval";
        case BenefactorMarketingCollaborationCommentsResourceType::Ticket: return "ticket";
        case BenefactorMarketingCollaborationCommentsResourceType::Document: return "document";
        case BenefactorMarketingCollaborationCommentsResourceType::Report: return "report";
        case BenefactorMarketingCollaborationCommentsResourceType::Meeting: return "meeting";
    }
    return "";
}
inline std::optional<BenefactorMarketingCollaborationCommentsResourceType> parse_benefactor_marketing_collaboration_comments_resource_type(const std::string& value) {
    if (value == "client") return BenefactorMarketingCollaborationCommentsResourceType::Client;
    if (value == "campaign") return BenefactorMarketingCollaborationCommentsResourceType::Campaign;
    if (value == "content_asset") return BenefactorMarketingCollaborationCommentsResourceType::ContentAsset;
    if (value == "approval") return BenefactorMarketingCollaborationCommentsResourceType::Approval;
    if (value == "ticket") return BenefactorMarketingCollaborationCommentsResourceType::Ticket;
    if (value == "document") return BenefactorMarketingCollaborationCommentsResourceType::Document;
    if (value == "report") return BenefactorMarketingCollaborationCommentsResourceType::Report;
    if (value == "meeting") return BenefactorMarketingCollaborationCommentsResourceType::Meeting;
    return std::nullopt;
}

enum class BenefactorMarketingCollaborationCommentsStatus { Open, Resolved, Archived };
inline std::string benefactor_marketing_collaboration_comments_status_to_string(BenefactorMarketingCollaborationCommentsStatus value) {
    switch (value) {
        case BenefactorMarketingCollaborationCommentsStatus::Open: return "open";
        case BenefactorMarketingCollaborationCommentsStatus::Resolved: return "resolved";
        case BenefactorMarketingCollaborationCommentsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<BenefactorMarketingCollaborationCommentsStatus> parse_benefactor_marketing_collaboration_comments_status(const std::string& value) {
    if (value == "open") return BenefactorMarketingCollaborationCommentsStatus::Open;
    if (value == "resolved") return BenefactorMarketingCollaborationCommentsStatus::Resolved;
    if (value == "archived") return BenefactorMarketingCollaborationCommentsStatus::Archived;
    return std::nullopt;
}

enum class BenefactorMarketingCollaborationCommentsVisibility { Internal, ClientPortal };
inline std::string benefactor_marketing_collaboration_comments_visibility_to_string(BenefactorMarketingCollaborationCommentsVisibility value) {
    switch (value) {
        case BenefactorMarketingCollaborationCommentsVisibility::Internal: return "internal";
        case BenefactorMarketingCollaborationCommentsVisibility::ClientPortal: return "client_portal";
    }
    return "";
}
inline std::optional<BenefactorMarketingCollaborationCommentsVisibility> parse_benefactor_marketing_collaboration_comments_visibility(const std::string& value) {
    if (value == "internal") return BenefactorMarketingCollaborationCommentsVisibility::Internal;
    if (value == "client_portal") return BenefactorMarketingCollaborationCommentsVisibility::ClientPortal;
    return std::nullopt;
}

struct BenefactorMarketingCollaborationCommentsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> parent_comment_id;
    std::string resource_type;
    std::optional<std::string> resource_id;
    std::optional<std::string> author_user_id;
    std::optional<std::string> author_contact_id;
    std::string body;
    std::string status;
    std::string visibility;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingCollaborationCommentsRow benefactor_marketing_collaboration_comments_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingCollaborationCommentsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.parent_comment_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.resource_type = get(3);
    row.resource_id = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.author_user_id = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.author_contact_id = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.body = get(7);
    row.status = get(8);
    row.visibility = get(9);
    row.meta_data = get(10);
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}

inline const char* benefactor_marketing_notifications_table = "benefactor_marketing_notifications";
inline const std::vector<std::string> benefactor_marketing_notifications_columns = { "id", "client_id", "recipient_user_id", "recipient_contact_id", "channel", "status", "notification_kind", "title", "body", "payload", "scheduled_at", "sent_at", "created_at", "updated_at" };
inline const char* benefactor_marketing_notifications_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      recipient_user_id::text as recipient_user_id,
      recipient_contact_id::text as recipient_contact_id,
      channel,
      status,
      notification_kind,
      title,
      body,
      payload::text as payload_json,
      to_char(scheduled_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as scheduled_at,
      to_char(sent_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as sent_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_notifications)SQL";

enum class BenefactorMarketingNotificationsChannel { Email, Sms, Portal, Slack, Webhook };
inline std::string benefactor_marketing_notifications_channel_to_string(BenefactorMarketingNotificationsChannel value) {
    switch (value) {
        case BenefactorMarketingNotificationsChannel::Email: return "email";
        case BenefactorMarketingNotificationsChannel::Sms: return "sms";
        case BenefactorMarketingNotificationsChannel::Portal: return "portal";
        case BenefactorMarketingNotificationsChannel::Slack: return "slack";
        case BenefactorMarketingNotificationsChannel::Webhook: return "webhook";
    }
    return "";
}
inline std::optional<BenefactorMarketingNotificationsChannel> parse_benefactor_marketing_notifications_channel(const std::string& value) {
    if (value == "email") return BenefactorMarketingNotificationsChannel::Email;
    if (value == "sms") return BenefactorMarketingNotificationsChannel::Sms;
    if (value == "portal") return BenefactorMarketingNotificationsChannel::Portal;
    if (value == "slack") return BenefactorMarketingNotificationsChannel::Slack;
    if (value == "webhook") return BenefactorMarketingNotificationsChannel::Webhook;
    return std::nullopt;
}

enum class BenefactorMarketingNotificationsStatus { Queued, Scheduled, Sent, Failed, Canceled };
inline std::string benefactor_marketing_notifications_status_to_string(BenefactorMarketingNotificationsStatus value) {
    switch (value) {
        case BenefactorMarketingNotificationsStatus::Queued: return "queued";
        case BenefactorMarketingNotificationsStatus::Scheduled: return "scheduled";
        case BenefactorMarketingNotificationsStatus::Sent: return "sent";
        case BenefactorMarketingNotificationsStatus::Failed: return "failed";
        case BenefactorMarketingNotificationsStatus::Canceled: return "canceled";
    }
    return "";
}
inline std::optional<BenefactorMarketingNotificationsStatus> parse_benefactor_marketing_notifications_status(const std::string& value) {
    if (value == "queued") return BenefactorMarketingNotificationsStatus::Queued;
    if (value == "scheduled") return BenefactorMarketingNotificationsStatus::Scheduled;
    if (value == "sent") return BenefactorMarketingNotificationsStatus::Sent;
    if (value == "failed") return BenefactorMarketingNotificationsStatus::Failed;
    if (value == "canceled") return BenefactorMarketingNotificationsStatus::Canceled;
    return std::nullopt;
}

enum class BenefactorMarketingNotificationsNotificationKind { ApprovalRequest, Comment, ReportReady, TicketUpdate, MeetingReminder, BudgetAlert, Custom };
inline std::string benefactor_marketing_notifications_notification_kind_to_string(BenefactorMarketingNotificationsNotificationKind value) {
    switch (value) {
        case BenefactorMarketingNotificationsNotificationKind::ApprovalRequest: return "approval_request";
        case BenefactorMarketingNotificationsNotificationKind::Comment: return "comment";
        case BenefactorMarketingNotificationsNotificationKind::ReportReady: return "report_ready";
        case BenefactorMarketingNotificationsNotificationKind::TicketUpdate: return "ticket_update";
        case BenefactorMarketingNotificationsNotificationKind::MeetingReminder: return "meeting_reminder";
        case BenefactorMarketingNotificationsNotificationKind::BudgetAlert: return "budget_alert";
        case BenefactorMarketingNotificationsNotificationKind::Custom: return "custom";
    }
    return "";
}
inline std::optional<BenefactorMarketingNotificationsNotificationKind> parse_benefactor_marketing_notifications_notification_kind(const std::string& value) {
    if (value == "approval_request") return BenefactorMarketingNotificationsNotificationKind::ApprovalRequest;
    if (value == "comment") return BenefactorMarketingNotificationsNotificationKind::Comment;
    if (value == "report_ready") return BenefactorMarketingNotificationsNotificationKind::ReportReady;
    if (value == "ticket_update") return BenefactorMarketingNotificationsNotificationKind::TicketUpdate;
    if (value == "meeting_reminder") return BenefactorMarketingNotificationsNotificationKind::MeetingReminder;
    if (value == "budget_alert") return BenefactorMarketingNotificationsNotificationKind::BudgetAlert;
    if (value == "custom") return BenefactorMarketingNotificationsNotificationKind::Custom;
    return std::nullopt;
}

struct BenefactorMarketingNotificationsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> recipient_user_id;
    std::optional<std::string> recipient_contact_id;
    std::string channel;
    std::string status;
    std::string notification_kind;
    std::string title;
    std::optional<std::string> body;
    std::string payload;
    std::optional<std::string> scheduled_at;
    std::optional<std::string> sent_at;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingNotificationsRow benefactor_marketing_notifications_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingNotificationsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.recipient_user_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.recipient_contact_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.channel = get(4);
    row.status = get(5);
    row.notification_kind = get(6);
    row.title = get(7);
    row.body = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.payload = get(9);
    row.scheduled_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.sent_at = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.created_at = get(12);
    row.updated_at = get(13);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_notifications_title(const std::string& value) {
    if (value.size() > 240) return std::string("benefactor_marketing_notifications.title must be at most 240 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_time_entries_table = "benefactor_marketing_time_entries";
inline const std::vector<std::string> benefactor_marketing_time_entries_columns = { "id", "client_id", "campaign_id", "project_task_id", "user_id", "entry_date", "minutes", "billable", "rate_cents", "cost_cents", "notes", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_time_entries_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      campaign_id::text as campaign_id,
      project_task_id::text as project_task_id,
      user_id::text as user_id,
      entry_date,
      minutes,
      billable,
      rate_cents,
      cost_cents,
      notes,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_time_entries)SQL";

struct BenefactorMarketingTimeEntriesRow {
    std::string id;
    std::optional<std::string> client_id;
    std::optional<std::string> campaign_id;
    std::optional<std::string> project_task_id;
    std::string user_id;
    std::string entry_date;
    int32_t minutes;
    bool billable;
    int32_t rate_cents;
    int32_t cost_cents;
    std::optional<std::string> notes;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingTimeEntriesRow benefactor_marketing_time_entries_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingTimeEntriesRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.campaign_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.project_task_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.user_id = get(4);
    row.entry_date = get(5);
    row.minutes = std::stoi(get(6));
    row.billable = (get(7) == "t");
    row.rate_cents = std::stoi(get(8));
    row.cost_cents = std::stoi(get(9));
    row.notes = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.meta_data = get(11);
    row.created_at = get(12);
    row.updated_at = get(13);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_time_entries_entry_date(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_time_entries.entry_date must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_time_entries.entry_date does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_time_entries_minutes(int32_t value) {
    if (value < 1) return std::string("benefactor_marketing_time_entries.minutes is below the minimum");
    if (value > 1440) return std::string("benefactor_marketing_time_entries.minutes is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_time_entries_rate_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_time_entries.rate_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_time_entries_cost_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_time_entries.cost_cents is below the minimum");
    return std::nullopt;
}

inline const char* benefactor_marketing_vendor_costs_table = "benefactor_marketing_vendor_costs";
inline const std::vector<std::string> benefactor_marketing_vendor_costs_columns = { "id", "client_id", "campaign_id", "vendor_name", "category", "status", "amount_cents", "incurred_on", "invoice_ref", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_vendor_costs_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      campaign_id::text as campaign_id,
      vendor_name,
      category,
      status,
      amount_cents,
      incurred_on,
      invoice_ref,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_vendor_costs)SQL";

enum class BenefactorMarketingVendorCostsCategory { Ads, Creative, Data, Software, Contractor, Events, Other };
inline std::string benefactor_marketing_vendor_costs_category_to_string(BenefactorMarketingVendorCostsCategory value) {
    switch (value) {
        case BenefactorMarketingVendorCostsCategory::Ads: return "ads";
        case BenefactorMarketingVendorCostsCategory::Creative: return "creative";
        case BenefactorMarketingVendorCostsCategory::Data: return "data";
        case BenefactorMarketingVendorCostsCategory::Software: return "software";
        case BenefactorMarketingVendorCostsCategory::Contractor: return "contractor";
        case BenefactorMarketingVendorCostsCategory::Events: return "events";
        case BenefactorMarketingVendorCostsCategory::Other: return "other";
    }
    return "";
}
inline std::optional<BenefactorMarketingVendorCostsCategory> parse_benefactor_marketing_vendor_costs_category(const std::string& value) {
    if (value == "ads") return BenefactorMarketingVendorCostsCategory::Ads;
    if (value == "creative") return BenefactorMarketingVendorCostsCategory::Creative;
    if (value == "data") return BenefactorMarketingVendorCostsCategory::Data;
    if (value == "software") return BenefactorMarketingVendorCostsCategory::Software;
    if (value == "contractor") return BenefactorMarketingVendorCostsCategory::Contractor;
    if (value == "events") return BenefactorMarketingVendorCostsCategory::Events;
    if (value == "other") return BenefactorMarketingVendorCostsCategory::Other;
    return std::nullopt;
}

enum class BenefactorMarketingVendorCostsStatus { Planned, Approved, Incurred, Invoiced, Paid, Canceled };
inline std::string benefactor_marketing_vendor_costs_status_to_string(BenefactorMarketingVendorCostsStatus value) {
    switch (value) {
        case BenefactorMarketingVendorCostsStatus::Planned: return "planned";
        case BenefactorMarketingVendorCostsStatus::Approved: return "approved";
        case BenefactorMarketingVendorCostsStatus::Incurred: return "incurred";
        case BenefactorMarketingVendorCostsStatus::Invoiced: return "invoiced";
        case BenefactorMarketingVendorCostsStatus::Paid: return "paid";
        case BenefactorMarketingVendorCostsStatus::Canceled: return "canceled";
    }
    return "";
}
inline std::optional<BenefactorMarketingVendorCostsStatus> parse_benefactor_marketing_vendor_costs_status(const std::string& value) {
    if (value == "planned") return BenefactorMarketingVendorCostsStatus::Planned;
    if (value == "approved") return BenefactorMarketingVendorCostsStatus::Approved;
    if (value == "incurred") return BenefactorMarketingVendorCostsStatus::Incurred;
    if (value == "invoiced") return BenefactorMarketingVendorCostsStatus::Invoiced;
    if (value == "paid") return BenefactorMarketingVendorCostsStatus::Paid;
    if (value == "canceled") return BenefactorMarketingVendorCostsStatus::Canceled;
    return std::nullopt;
}

struct BenefactorMarketingVendorCostsRow {
    std::string id;
    std::optional<std::string> client_id;
    std::optional<std::string> campaign_id;
    std::string vendor_name;
    std::string category;
    std::string status;
    int32_t amount_cents;
    std::optional<std::string> incurred_on;
    std::optional<std::string> invoice_ref;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingVendorCostsRow benefactor_marketing_vendor_costs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingVendorCostsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.campaign_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.vendor_name = get(3);
    row.category = get(4);
    row.status = get(5);
    row.amount_cents = std::stoi(get(6));
    row.incurred_on = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.invoice_ref = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.meta_data = get(9);
    row.created_at = get(10);
    row.updated_at = get(11);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_vendor_costs_vendor_name(const std::string& value) {
    if (value.size() > 200) return std::string("benefactor_marketing_vendor_costs.vendor_name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_vendor_costs_amount_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_vendor_costs.amount_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_vendor_costs_incurred_on(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_vendor_costs.incurred_on must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_vendor_costs.incurred_on does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_vendor_costs_invoice_ref(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_marketing_vendor_costs.invoice_ref must be at most 120 characters");
    return std::nullopt;
}

inline const char* benefactor_marketing_commission_entries_table = "benefactor_marketing_commission_entries";
inline const std::vector<std::string> benefactor_marketing_commission_entries_columns = { "id", "client_id", "opportunity_id", "user_id", "status", "commission_kind", "basis_cents", "rate_micros", "amount_cents", "earned_on", "paid_at", "meta_data", "created_at", "updated_at" };
inline const char* benefactor_marketing_commission_entries_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      opportunity_id::text as opportunity_id,
      user_id::text as user_id,
      status,
      commission_kind,
      basis_cents,
      rate_micros,
      amount_cents,
      earned_on,
      to_char(paid_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as paid_at,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_commission_entries)SQL";

enum class BenefactorMarketingCommissionEntriesStatus { Pending, Approved, Paid, Void };
inline std::string benefactor_marketing_commission_entries_status_to_string(BenefactorMarketingCommissionEntriesStatus value) {
    switch (value) {
        case BenefactorMarketingCommissionEntriesStatus::Pending: return "pending";
        case BenefactorMarketingCommissionEntriesStatus::Approved: return "approved";
        case BenefactorMarketingCommissionEntriesStatus::Paid: return "paid";
        case BenefactorMarketingCommissionEntriesStatus::Void: return "void";
    }
    return "";
}
inline std::optional<BenefactorMarketingCommissionEntriesStatus> parse_benefactor_marketing_commission_entries_status(const std::string& value) {
    if (value == "pending") return BenefactorMarketingCommissionEntriesStatus::Pending;
    if (value == "approved") return BenefactorMarketingCommissionEntriesStatus::Approved;
    if (value == "paid") return BenefactorMarketingCommissionEntriesStatus::Paid;
    if (value == "void") return BenefactorMarketingCommissionEntriesStatus::Void;
    return std::nullopt;
}

enum class BenefactorMarketingCommissionEntriesCommissionKind { Deal, Retainer, Renewal, Upsell, Appointment };
inline std::string benefactor_marketing_commission_entries_commission_kind_to_string(BenefactorMarketingCommissionEntriesCommissionKind value) {
    switch (value) {
        case BenefactorMarketingCommissionEntriesCommissionKind::Deal: return "deal";
        case BenefactorMarketingCommissionEntriesCommissionKind::Retainer: return "retainer";
        case BenefactorMarketingCommissionEntriesCommissionKind::Renewal: return "renewal";
        case BenefactorMarketingCommissionEntriesCommissionKind::Upsell: return "upsell";
        case BenefactorMarketingCommissionEntriesCommissionKind::Appointment: return "appointment";
    }
    return "";
}
inline std::optional<BenefactorMarketingCommissionEntriesCommissionKind> parse_benefactor_marketing_commission_entries_commission_kind(const std::string& value) {
    if (value == "deal") return BenefactorMarketingCommissionEntriesCommissionKind::Deal;
    if (value == "retainer") return BenefactorMarketingCommissionEntriesCommissionKind::Retainer;
    if (value == "renewal") return BenefactorMarketingCommissionEntriesCommissionKind::Renewal;
    if (value == "upsell") return BenefactorMarketingCommissionEntriesCommissionKind::Upsell;
    if (value == "appointment") return BenefactorMarketingCommissionEntriesCommissionKind::Appointment;
    return std::nullopt;
}

struct BenefactorMarketingCommissionEntriesRow {
    std::string id;
    std::optional<std::string> client_id;
    std::optional<std::string> opportunity_id;
    std::string user_id;
    std::string status;
    std::string commission_kind;
    int32_t basis_cents;
    int32_t rate_micros;
    int32_t amount_cents;
    std::optional<std::string> earned_on;
    std::optional<std::string> paid_at;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingCommissionEntriesRow benefactor_marketing_commission_entries_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingCommissionEntriesRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.opportunity_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.user_id = get(3);
    row.status = get(4);
    row.commission_kind = get(5);
    row.basis_cents = std::stoi(get(6));
    row.rate_micros = std::stoi(get(7));
    row.amount_cents = std::stoi(get(8));
    row.earned_on = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.paid_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.meta_data = get(11);
    row.created_at = get(12);
    row.updated_at = get(13);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_commission_entries_basis_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_commission_entries.basis_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_commission_entries_rate_micros(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_commission_entries.rate_micros is below the minimum");
    if (value > 1000000) return std::string("benefactor_marketing_commission_entries.rate_micros is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_commission_entries_amount_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_commission_entries.amount_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_commission_entries_earned_on(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_commission_entries.earned_on must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_commission_entries.earned_on does not match the required pattern");
    return std::nullopt;
}

inline const char* benefactor_marketing_budget_forecasts_table = "benefactor_marketing_budget_forecasts";
inline const std::vector<std::string> benefactor_marketing_budget_forecasts_columns = { "id", "client_id", "campaign_id", "forecast_kind", "period_start", "period_end", "status", "revenue_cents", "media_spend_cents", "labor_cost_cents", "vendor_cost_cents", "gross_margin_cents", "assumptions", "created_at", "updated_at" };
inline const char* benefactor_marketing_budget_forecasts_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      campaign_id::text as campaign_id,
      forecast_kind,
      period_start,
      period_end,
      status,
      revenue_cents,
      media_spend_cents,
      labor_cost_cents,
      vendor_cost_cents,
      gross_margin_cents,
      assumptions::text as assumptions_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_budget_forecasts)SQL";

enum class BenefactorMarketingBudgetForecastsForecastKind { Monthly, Quarterly, Campaign, Annual };
inline std::string benefactor_marketing_budget_forecasts_forecast_kind_to_string(BenefactorMarketingBudgetForecastsForecastKind value) {
    switch (value) {
        case BenefactorMarketingBudgetForecastsForecastKind::Monthly: return "monthly";
        case BenefactorMarketingBudgetForecastsForecastKind::Quarterly: return "quarterly";
        case BenefactorMarketingBudgetForecastsForecastKind::Campaign: return "campaign";
        case BenefactorMarketingBudgetForecastsForecastKind::Annual: return "annual";
    }
    return "";
}
inline std::optional<BenefactorMarketingBudgetForecastsForecastKind> parse_benefactor_marketing_budget_forecasts_forecast_kind(const std::string& value) {
    if (value == "monthly") return BenefactorMarketingBudgetForecastsForecastKind::Monthly;
    if (value == "quarterly") return BenefactorMarketingBudgetForecastsForecastKind::Quarterly;
    if (value == "campaign") return BenefactorMarketingBudgetForecastsForecastKind::Campaign;
    if (value == "annual") return BenefactorMarketingBudgetForecastsForecastKind::Annual;
    return std::nullopt;
}

enum class BenefactorMarketingBudgetForecastsStatus { Draft, Approved, Locked, Archived };
inline std::string benefactor_marketing_budget_forecasts_status_to_string(BenefactorMarketingBudgetForecastsStatus value) {
    switch (value) {
        case BenefactorMarketingBudgetForecastsStatus::Draft: return "draft";
        case BenefactorMarketingBudgetForecastsStatus::Approved: return "approved";
        case BenefactorMarketingBudgetForecastsStatus::Locked: return "locked";
        case BenefactorMarketingBudgetForecastsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<BenefactorMarketingBudgetForecastsStatus> parse_benefactor_marketing_budget_forecasts_status(const std::string& value) {
    if (value == "draft") return BenefactorMarketingBudgetForecastsStatus::Draft;
    if (value == "approved") return BenefactorMarketingBudgetForecastsStatus::Approved;
    if (value == "locked") return BenefactorMarketingBudgetForecastsStatus::Locked;
    if (value == "archived") return BenefactorMarketingBudgetForecastsStatus::Archived;
    return std::nullopt;
}

struct BenefactorMarketingBudgetForecastsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> campaign_id;
    std::string forecast_kind;
    std::string period_start;
    std::string period_end;
    std::string status;
    int32_t revenue_cents;
    int32_t media_spend_cents;
    int32_t labor_cost_cents;
    int32_t vendor_cost_cents;
    int32_t gross_margin_cents;
    std::string assumptions;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingBudgetForecastsRow benefactor_marketing_budget_forecasts_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingBudgetForecastsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.campaign_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.forecast_kind = get(3);
    row.period_start = get(4);
    row.period_end = get(5);
    row.status = get(6);
    row.revenue_cents = std::stoi(get(7));
    row.media_spend_cents = std::stoi(get(8));
    row.labor_cost_cents = std::stoi(get(9));
    row.vendor_cost_cents = std::stoi(get(10));
    row.gross_margin_cents = std::stoi(get(11));
    row.assumptions = get(12);
    row.created_at = get(13);
    row.updated_at = get(14);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_budget_forecasts_period_start(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_budget_forecasts.period_start must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_budget_forecasts.period_start does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_budget_forecasts_period_end(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_marketing_budget_forecasts.period_end must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("benefactor_marketing_budget_forecasts.period_end does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_budget_forecasts_revenue_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_budget_forecasts.revenue_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_budget_forecasts_media_spend_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_budget_forecasts.media_spend_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_budget_forecasts_labor_cost_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_budget_forecasts.labor_cost_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_budget_forecasts_vendor_cost_cents(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_budget_forecasts.vendor_cost_cents is below the minimum");
    return std::nullopt;
}

inline const char* benefactor_marketing_call_insights_table = "benefactor_marketing_call_insights";
inline const std::vector<std::string> benefactor_marketing_call_insights_columns = { "id", "client_id", "meeting_id", "lead_id", "opportunity_id", "status", "provider", "transcript_uri", "summary", "sentiment", "action_items", "objections", "next_steps", "confidence_micros", "analyzed_at", "created_at", "updated_at" };
inline const char* benefactor_marketing_call_insights_select_sql = R"SQL(select
      id::text as id,
      client_id::text as client_id,
      meeting_id::text as meeting_id,
      lead_id::text as lead_id,
      opportunity_id::text as opportunity_id,
      status,
      provider,
      transcript_uri,
      summary,
      sentiment,
      action_items::text as action_items_json,
      objections::text as objections_json,
      next_steps::text as next_steps_json,
      confidence_micros,
      to_char(analyzed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as analyzed_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from benefactor_marketing_call_insights)SQL";

enum class BenefactorMarketingCallInsightsStatus { Processing, Ready, Failed, Archived };
inline std::string benefactor_marketing_call_insights_status_to_string(BenefactorMarketingCallInsightsStatus value) {
    switch (value) {
        case BenefactorMarketingCallInsightsStatus::Processing: return "processing";
        case BenefactorMarketingCallInsightsStatus::Ready: return "ready";
        case BenefactorMarketingCallInsightsStatus::Failed: return "failed";
        case BenefactorMarketingCallInsightsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<BenefactorMarketingCallInsightsStatus> parse_benefactor_marketing_call_insights_status(const std::string& value) {
    if (value == "processing") return BenefactorMarketingCallInsightsStatus::Processing;
    if (value == "ready") return BenefactorMarketingCallInsightsStatus::Ready;
    if (value == "failed") return BenefactorMarketingCallInsightsStatus::Failed;
    if (value == "archived") return BenefactorMarketingCallInsightsStatus::Archived;
    return std::nullopt;
}

enum class BenefactorMarketingCallInsightsSentiment { Positive, Neutral, Negative, Mixed };
inline std::string benefactor_marketing_call_insights_sentiment_to_string(BenefactorMarketingCallInsightsSentiment value) {
    switch (value) {
        case BenefactorMarketingCallInsightsSentiment::Positive: return "positive";
        case BenefactorMarketingCallInsightsSentiment::Neutral: return "neutral";
        case BenefactorMarketingCallInsightsSentiment::Negative: return "negative";
        case BenefactorMarketingCallInsightsSentiment::Mixed: return "mixed";
    }
    return "";
}
inline std::optional<BenefactorMarketingCallInsightsSentiment> parse_benefactor_marketing_call_insights_sentiment(const std::string& value) {
    if (value == "positive") return BenefactorMarketingCallInsightsSentiment::Positive;
    if (value == "neutral") return BenefactorMarketingCallInsightsSentiment::Neutral;
    if (value == "negative") return BenefactorMarketingCallInsightsSentiment::Negative;
    if (value == "mixed") return BenefactorMarketingCallInsightsSentiment::Mixed;
    return std::nullopt;
}

struct BenefactorMarketingCallInsightsRow {
    std::string id;
    std::string client_id;
    std::optional<std::string> meeting_id;
    std::optional<std::string> lead_id;
    std::optional<std::string> opportunity_id;
    std::string status;
    std::optional<std::string> provider;
    std::optional<std::string> transcript_uri;
    std::optional<std::string> summary;
    std::optional<std::string> sentiment;
    std::string action_items;
    std::string objections;
    std::string next_steps;
    int32_t confidence_micros;
    std::string analyzed_at;
    std::string created_at;
    std::string updated_at;
};

inline BenefactorMarketingCallInsightsRow benefactor_marketing_call_insights_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorMarketingCallInsightsRow row;
    (void)is_null;
    row.id = get(0);
    row.client_id = get(1);
    row.meeting_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.lead_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.opportunity_id = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.status = get(5);
    row.provider = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.transcript_uri = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.summary = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.sentiment = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.action_items = get(10);
    row.objections = get(11);
    row.next_steps = get(12);
    row.confidence_micros = std::stoi(get(13));
    row.analyzed_at = get(14);
    row.created_at = get(15);
    row.updated_at = get(16);
    return row;
}
inline std::optional<std::string> validate_benefactor_marketing_call_insights_provider(const std::string& value) {
    if (value.size() > 64) return std::string("benefactor_marketing_call_insights.provider must be at most 64 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_marketing_call_insights_confidence_micros(int32_t value) {
    if (value < 0) return std::string("benefactor_marketing_call_insights.confidence_micros is below the minimum");
    if (value > 1000000) return std::string("benefactor_marketing_call_insights.confidence_micros is above the maximum");
    return std::nullopt;
}

inline const char* usacc_users_table = "usacc_users";
inline const std::vector<std::string> usacc_users_columns = { "id", "external_subject", "email_hash", "display_name", "user_kind", "status", "kyc_level", "roles", "is_legal_entity", "legal_region", "meta_data", "created_at", "updated_at" };
inline const char* usacc_users_select_sql = R"SQL(select
      id::text as id,
      external_subject,
      email_hash,
      display_name,
      user_kind,
      status,
      kyc_level,
      roles::text as roles_json,
      is_legal_entity,
      legal_region,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from usacc_users)SQL";

enum class UsaccUsersUserKind { NaturalPerson, LegalEntity, ServiceAccount, SimAgent };
inline std::string usacc_users_user_kind_to_string(UsaccUsersUserKind value) {
    switch (value) {
        case UsaccUsersUserKind::NaturalPerson: return "natural_person";
        case UsaccUsersUserKind::LegalEntity: return "legal_entity";
        case UsaccUsersUserKind::ServiceAccount: return "service_account";
        case UsaccUsersUserKind::SimAgent: return "sim_agent";
    }
    return "";
}
inline std::optional<UsaccUsersUserKind> parse_usacc_users_user_kind(const std::string& value) {
    if (value == "natural_person") return UsaccUsersUserKind::NaturalPerson;
    if (value == "legal_entity") return UsaccUsersUserKind::LegalEntity;
    if (value == "service_account") return UsaccUsersUserKind::ServiceAccount;
    if (value == "sim_agent") return UsaccUsersUserKind::SimAgent;
    return std::nullopt;
}

enum class UsaccUsersStatus { Active, Pending, Suspended, Banned, Alumni, Archived };
inline std::string usacc_users_status_to_string(UsaccUsersStatus value) {
    switch (value) {
        case UsaccUsersStatus::Active: return "active";
        case UsaccUsersStatus::Pending: return "pending";
        case UsaccUsersStatus::Suspended: return "suspended";
        case UsaccUsersStatus::Banned: return "banned";
        case UsaccUsersStatus::Alumni: return "alumni";
        case UsaccUsersStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<UsaccUsersStatus> parse_usacc_users_status(const std::string& value) {
    if (value == "active") return UsaccUsersStatus::Active;
    if (value == "pending") return UsaccUsersStatus::Pending;
    if (value == "suspended") return UsaccUsersStatus::Suspended;
    if (value == "banned") return UsaccUsersStatus::Banned;
    if (value == "alumni") return UsaccUsersStatus::Alumni;
    if (value == "archived") return UsaccUsersStatus::Archived;
    return std::nullopt;
}

enum class UsaccUsersKycLevel { None, Light, Medium, High };
inline std::string usacc_users_kyc_level_to_string(UsaccUsersKycLevel value) {
    switch (value) {
        case UsaccUsersKycLevel::None: return "none";
        case UsaccUsersKycLevel::Light: return "light";
        case UsaccUsersKycLevel::Medium: return "medium";
        case UsaccUsersKycLevel::High: return "high";
    }
    return "";
}
inline std::optional<UsaccUsersKycLevel> parse_usacc_users_kyc_level(const std::string& value) {
    if (value == "none") return UsaccUsersKycLevel::None;
    if (value == "light") return UsaccUsersKycLevel::Light;
    if (value == "medium") return UsaccUsersKycLevel::Medium;
    if (value == "high") return UsaccUsersKycLevel::High;
    return std::nullopt;
}

struct UsaccUsersRow {
    std::string id;
    std::optional<std::string> external_subject;
    std::optional<std::string> email_hash;
    std::string display_name;
    std::string user_kind;
    std::string status;
    std::string kyc_level;
    std::string roles;
    bool is_legal_entity;
    std::optional<std::string> legal_region;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline UsaccUsersRow usacc_users_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    UsaccUsersRow row;
    (void)is_null;
    row.id = get(0);
    row.external_subject = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.email_hash = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.display_name = get(3);
    row.user_kind = get(4);
    row.status = get(5);
    row.kyc_level = get(6);
    row.roles = get(7);
    row.is_legal_entity = (get(8) == "t");
    row.legal_region = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.meta_data = get(10);
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}
inline std::optional<std::string> validate_usacc_users_external_subject(const std::string& value) {
    if (value.size() > 240) return std::string("usacc_users.external_subject must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_users_email_hash(const std::string& value) {
    if (value.size() > 64) return std::string("usacc_users.email_hash must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-f0-9]{64}$)RX"))) return std::string("usacc_users.email_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_users_display_name(const std::string& value) {
    if (value.size() > 200) return std::string("usacc_users.display_name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_users_legal_region(const std::string& value) {
    if (value.size() > 64) return std::string("usacc_users.legal_region must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,64}$)RX"))) return std::string("usacc_users.legal_region does not match the required pattern");
    return std::nullopt;
}

inline const char* usacc_cases_table = "usacc_cases";
inline const std::vector<std::string> usacc_cases_columns = { "id", "case_number", "title", "status", "filing_tier", "plaintiff_user_id", "defendant_summary", "conduct_summary", "conduct_fingerprint", "conduct_window_start", "conduct_window_end", "priority_score_micros", "meta_data", "opened_at", "closed_at", "created_at", "updated_at" };
inline const char* usacc_cases_select_sql = R"SQL(select
      id::text as id,
      case_number,
      title,
      status,
      filing_tier,
      plaintiff_user_id::text as plaintiff_user_id,
      defendant_summary,
      conduct_summary,
      conduct_fingerprint,
      conduct_window_start,
      conduct_window_end,
      priority_score_micros,
      meta_data::text as meta_data_json,
      to_char(opened_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as opened_at,
      to_char(closed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as closed_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from usacc_cases)SQL";

enum class UsaccCasesStatus { Draft, SignatureCollection, Screening, Inquiry, AdmissionReview, Trial, Appeal, Resolved, Canceled, Archived };
inline std::string usacc_cases_status_to_string(UsaccCasesStatus value) {
    switch (value) {
        case UsaccCasesStatus::Draft: return "draft";
        case UsaccCasesStatus::SignatureCollection: return "signature_collection";
        case UsaccCasesStatus::Screening: return "screening";
        case UsaccCasesStatus::Inquiry: return "inquiry";
        case UsaccCasesStatus::AdmissionReview: return "admission_review";
        case UsaccCasesStatus::Trial: return "trial";
        case UsaccCasesStatus::Appeal: return "appeal";
        case UsaccCasesStatus::Resolved: return "resolved";
        case UsaccCasesStatus::Canceled: return "canceled";
        case UsaccCasesStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<UsaccCasesStatus> parse_usacc_cases_status(const std::string& value) {
    if (value == "draft") return UsaccCasesStatus::Draft;
    if (value == "signature_collection") return UsaccCasesStatus::SignatureCollection;
    if (value == "screening") return UsaccCasesStatus::Screening;
    if (value == "inquiry") return UsaccCasesStatus::Inquiry;
    if (value == "admission_review") return UsaccCasesStatus::AdmissionReview;
    if (value == "trial") return UsaccCasesStatus::Trial;
    if (value == "appeal") return UsaccCasesStatus::Appeal;
    if (value == "resolved") return UsaccCasesStatus::Resolved;
    if (value == "canceled") return UsaccCasesStatus::Canceled;
    if (value == "archived") return UsaccCasesStatus::Archived;
    return std::nullopt;
}

enum class UsaccCasesFilingTier { Screen, Inquiry, Trial1, Trial2, Trial3, Trial5, Trial10 };
inline std::string usacc_cases_filing_tier_to_string(UsaccCasesFilingTier value) {
    switch (value) {
        case UsaccCasesFilingTier::Screen: return "screen";
        case UsaccCasesFilingTier::Inquiry: return "inquiry";
        case UsaccCasesFilingTier::Trial1: return "trial_1";
        case UsaccCasesFilingTier::Trial2: return "trial_2";
        case UsaccCasesFilingTier::Trial3: return "trial_3";
        case UsaccCasesFilingTier::Trial5: return "trial_5";
        case UsaccCasesFilingTier::Trial10: return "trial_10";
    }
    return "";
}
inline std::optional<UsaccCasesFilingTier> parse_usacc_cases_filing_tier(const std::string& value) {
    if (value == "screen") return UsaccCasesFilingTier::Screen;
    if (value == "inquiry") return UsaccCasesFilingTier::Inquiry;
    if (value == "trial_1") return UsaccCasesFilingTier::Trial1;
    if (value == "trial_2") return UsaccCasesFilingTier::Trial2;
    if (value == "trial_3") return UsaccCasesFilingTier::Trial3;
    if (value == "trial_5") return UsaccCasesFilingTier::Trial5;
    if (value == "trial_10") return UsaccCasesFilingTier::Trial10;
    return std::nullopt;
}

struct UsaccCasesRow {
    std::string id;
    std::string case_number;
    std::string title;
    std::string status;
    std::string filing_tier;
    std::optional<std::string> plaintiff_user_id;
    std::string defendant_summary;
    std::string conduct_summary;
    std::optional<std::string> conduct_fingerprint;
    std::optional<std::string> conduct_window_start;
    std::optional<std::string> conduct_window_end;
    int32_t priority_score_micros;
    std::string meta_data;
    std::optional<std::string> opened_at;
    std::optional<std::string> closed_at;
    std::string created_at;
    std::string updated_at;
};

inline UsaccCasesRow usacc_cases_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    UsaccCasesRow row;
    (void)is_null;
    row.id = get(0);
    row.case_number = get(1);
    row.title = get(2);
    row.status = get(3);
    row.filing_tier = get(4);
    row.plaintiff_user_id = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.defendant_summary = get(6);
    row.conduct_summary = get(7);
    row.conduct_fingerprint = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.conduct_window_start = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.conduct_window_end = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.priority_score_micros = std::stoi(get(11));
    row.meta_data = get(12);
    row.opened_at = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    row.closed_at = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.created_at = get(15);
    row.updated_at = get(16);
    return row;
}
inline std::optional<std::string> validate_usacc_cases_case_number(const std::string& value) {
    if (value.size() > 80) return std::string("usacc_cases.case_number must be at most 80 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,80}$)RX"))) return std::string("usacc_cases.case_number does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_cases_title(const std::string& value) {
    if (value.size() > 240) return std::string("usacc_cases.title must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_cases_conduct_fingerprint(const std::string& value) {
    if (value.size() > 128) return std::string("usacc_cases.conduct_fingerprint must be at most 128 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,128}$)RX"))) return std::string("usacc_cases.conduct_fingerprint does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_cases_conduct_window_start(const std::string& value) {
    if (value.size() > 10) return std::string("usacc_cases.conduct_window_start must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("usacc_cases.conduct_window_start does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_cases_conduct_window_end(const std::string& value) {
    if (value.size() > 10) return std::string("usacc_cases.conduct_window_end must be at most 10 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}-[0-9]{2}-[0-9]{2}$)RX"))) return std::string("usacc_cases.conduct_window_end does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_cases_priority_score_micros(int32_t value) {
    if (value < 0) return std::string("usacc_cases.priority_score_micros is below the minimum");
    if (value > 1000000) return std::string("usacc_cases.priority_score_micros is above the maximum");
    return std::nullopt;
}

inline const char* usacc_case_participants_table = "usacc_case_participants";
inline const std::vector<std::string> usacc_case_participants_columns = { "id", "case_id", "user_id", "role", "status", "granted_by", "granted_by_policy_version", "ended_at", "ended_reason", "meta_data", "created_at", "updated_at" };
inline const char* usacc_case_participants_select_sql = R"SQL(select
      id::text as id,
      case_id::text as case_id,
      user_id::text as user_id,
      role,
      status,
      granted_by::text as granted_by,
      granted_by_policy_version,
      to_char(ended_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as ended_at,
      ended_reason,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from usacc_case_participants)SQL";

enum class UsaccCaseParticipantsRole { Plaintiff, Defendant, Sponsor, Witness, Judge, PanelJuror, AppealJudge, PresidingJuror, Paralegal, Investigator, IntakeReviewer, ClerkOfCourt, ComplianceMonitor, Counsel, OversightBoard, Auditor, Ombuds };
inline std::string usacc_case_participants_role_to_string(UsaccCaseParticipantsRole value) {
    switch (value) {
        case UsaccCaseParticipantsRole::Plaintiff: return "plaintiff";
        case UsaccCaseParticipantsRole::Defendant: return "defendant";
        case UsaccCaseParticipantsRole::Sponsor: return "sponsor";
        case UsaccCaseParticipantsRole::Witness: return "witness";
        case UsaccCaseParticipantsRole::Judge: return "judge";
        case UsaccCaseParticipantsRole::PanelJuror: return "panel_juror";
        case UsaccCaseParticipantsRole::AppealJudge: return "appeal_judge";
        case UsaccCaseParticipantsRole::PresidingJuror: return "presiding_juror";
        case UsaccCaseParticipantsRole::Paralegal: return "paralegal";
        case UsaccCaseParticipantsRole::Investigator: return "investigator";
        case UsaccCaseParticipantsRole::IntakeReviewer: return "intake_reviewer";
        case UsaccCaseParticipantsRole::ClerkOfCourt: return "clerk_of_court";
        case UsaccCaseParticipantsRole::ComplianceMonitor: return "compliance_monitor";
        case UsaccCaseParticipantsRole::Counsel: return "counsel";
        case UsaccCaseParticipantsRole::OversightBoard: return "oversight_board";
        case UsaccCaseParticipantsRole::Auditor: return "auditor";
        case UsaccCaseParticipantsRole::Ombuds: return "ombuds";
    }
    return "";
}
inline std::optional<UsaccCaseParticipantsRole> parse_usacc_case_participants_role(const std::string& value) {
    if (value == "plaintiff") return UsaccCaseParticipantsRole::Plaintiff;
    if (value == "defendant") return UsaccCaseParticipantsRole::Defendant;
    if (value == "sponsor") return UsaccCaseParticipantsRole::Sponsor;
    if (value == "witness") return UsaccCaseParticipantsRole::Witness;
    if (value == "judge") return UsaccCaseParticipantsRole::Judge;
    if (value == "panel_juror") return UsaccCaseParticipantsRole::PanelJuror;
    if (value == "appeal_judge") return UsaccCaseParticipantsRole::AppealJudge;
    if (value == "presiding_juror") return UsaccCaseParticipantsRole::PresidingJuror;
    if (value == "paralegal") return UsaccCaseParticipantsRole::Paralegal;
    if (value == "investigator") return UsaccCaseParticipantsRole::Investigator;
    if (value == "intake_reviewer") return UsaccCaseParticipantsRole::IntakeReviewer;
    if (value == "clerk_of_court") return UsaccCaseParticipantsRole::ClerkOfCourt;
    if (value == "compliance_monitor") return UsaccCaseParticipantsRole::ComplianceMonitor;
    if (value == "counsel") return UsaccCaseParticipantsRole::Counsel;
    if (value == "oversight_board") return UsaccCaseParticipantsRole::OversightBoard;
    if (value == "auditor") return UsaccCaseParticipantsRole::Auditor;
    if (value == "ombuds") return UsaccCaseParticipantsRole::Ombuds;
    return std::nullopt;
}

enum class UsaccCaseParticipantsStatus { Active, Pending, Declined, Suspended, Ended, Banned };
inline std::string usacc_case_participants_status_to_string(UsaccCaseParticipantsStatus value) {
    switch (value) {
        case UsaccCaseParticipantsStatus::Active: return "active";
        case UsaccCaseParticipantsStatus::Pending: return "pending";
        case UsaccCaseParticipantsStatus::Declined: return "declined";
        case UsaccCaseParticipantsStatus::Suspended: return "suspended";
        case UsaccCaseParticipantsStatus::Ended: return "ended";
        case UsaccCaseParticipantsStatus::Banned: return "banned";
    }
    return "";
}
inline std::optional<UsaccCaseParticipantsStatus> parse_usacc_case_participants_status(const std::string& value) {
    if (value == "active") return UsaccCaseParticipantsStatus::Active;
    if (value == "pending") return UsaccCaseParticipantsStatus::Pending;
    if (value == "declined") return UsaccCaseParticipantsStatus::Declined;
    if (value == "suspended") return UsaccCaseParticipantsStatus::Suspended;
    if (value == "ended") return UsaccCaseParticipantsStatus::Ended;
    if (value == "banned") return UsaccCaseParticipantsStatus::Banned;
    return std::nullopt;
}

struct UsaccCaseParticipantsRow {
    std::string id;
    std::string case_id;
    std::string user_id;
    std::string role;
    std::string status;
    std::optional<std::string> granted_by;
    std::optional<std::string> granted_by_policy_version;
    std::optional<std::string> ended_at;
    std::optional<std::string> ended_reason;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline UsaccCaseParticipantsRow usacc_case_participants_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    UsaccCaseParticipantsRow row;
    (void)is_null;
    row.id = get(0);
    row.case_id = get(1);
    row.user_id = get(2);
    row.role = get(3);
    row.status = get(4);
    row.granted_by = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.granted_by_policy_version = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.ended_at = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.ended_reason = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.meta_data = get(9);
    row.created_at = get(10);
    row.updated_at = get(11);
    return row;
}
inline std::optional<std::string> validate_usacc_case_participants_granted_by_policy_version(const std::string& value) {
    if (value.size() > 120) return std::string("usacc_case_participants.granted_by_policy_version must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,120}$)RX"))) return std::string("usacc_case_participants.granted_by_policy_version does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_case_participants_ended_reason(const std::string& value) {
    if (value.size() > 240) return std::string("usacc_case_participants.ended_reason must be at most 240 characters");
    return std::nullopt;
}

inline const char* usacc_case_stages_table = "usacc_case_stages";
inline const std::vector<std::string> usacc_case_stages_columns = { "id", "case_id", "stage_key", "stage_order", "title", "status", "assigned_user_id", "opened_at", "due_at", "closed_at", "decision_summary", "meta_data", "created_at", "updated_at" };
inline const char* usacc_case_stages_select_sql = R"SQL(select
      id::text as id,
      case_id::text as case_id,
      stage_key,
      stage_order,
      title,
      status,
      assigned_user_id::text as assigned_user_id,
      to_char(opened_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as opened_at,
      to_char(due_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as due_at,
      to_char(closed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as closed_at,
      decision_summary,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from usacc_case_stages)SQL";

enum class UsaccCaseStagesStatus { Pending, Open, Blocked, Complete, Skipped, Canceled };
inline std::string usacc_case_stages_status_to_string(UsaccCaseStagesStatus value) {
    switch (value) {
        case UsaccCaseStagesStatus::Pending: return "pending";
        case UsaccCaseStagesStatus::Open: return "open";
        case UsaccCaseStagesStatus::Blocked: return "blocked";
        case UsaccCaseStagesStatus::Complete: return "complete";
        case UsaccCaseStagesStatus::Skipped: return "skipped";
        case UsaccCaseStagesStatus::Canceled: return "canceled";
    }
    return "";
}
inline std::optional<UsaccCaseStagesStatus> parse_usacc_case_stages_status(const std::string& value) {
    if (value == "pending") return UsaccCaseStagesStatus::Pending;
    if (value == "open") return UsaccCaseStagesStatus::Open;
    if (value == "blocked") return UsaccCaseStagesStatus::Blocked;
    if (value == "complete") return UsaccCaseStagesStatus::Complete;
    if (value == "skipped") return UsaccCaseStagesStatus::Skipped;
    if (value == "canceled") return UsaccCaseStagesStatus::Canceled;
    return std::nullopt;
}

struct UsaccCaseStagesRow {
    std::string id;
    std::string case_id;
    std::string stage_key;
    int32_t stage_order;
    std::string title;
    std::string status;
    std::optional<std::string> assigned_user_id;
    std::optional<std::string> opened_at;
    std::optional<std::string> due_at;
    std::optional<std::string> closed_at;
    std::optional<std::string> decision_summary;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline UsaccCaseStagesRow usacc_case_stages_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    UsaccCaseStagesRow row;
    (void)is_null;
    row.id = get(0);
    row.case_id = get(1);
    row.stage_key = get(2);
    row.stage_order = std::stoi(get(3));
    row.title = get(4);
    row.status = get(5);
    row.assigned_user_id = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.opened_at = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.due_at = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.closed_at = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.decision_summary = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.meta_data = get(11);
    row.created_at = get(12);
    row.updated_at = get(13);
    return row;
}
inline std::optional<std::string> validate_usacc_case_stages_stage_key(const std::string& value) {
    if (value.size() > 64) return std::string("usacc_case_stages.stage_key must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,64}$)RX"))) return std::string("usacc_case_stages.stage_key does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_case_stages_stage_order(int32_t value) {
    if (value < 0) return std::string("usacc_case_stages.stage_order is below the minimum");
    if (value > 1000) return std::string("usacc_case_stages.stage_order is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_case_stages_title(const std::string& value) {
    if (value.size() > 200) return std::string("usacc_case_stages.title must be at most 200 characters");
    return std::nullopt;
}

inline const char* usacc_elections_table = "usacc_elections";
inline const std::vector<std::string> usacc_elections_columns = { "id", "case_id", "stage_id", "election_kind", "title", "status", "quorum_count", "threshold_micros", "opens_at", "closes_at", "sealed_until", "tally", "meta_data", "created_at", "updated_at" };
inline const char* usacc_elections_select_sql = R"SQL(select
      id::text as id,
      case_id::text as case_id,
      stage_id::text as stage_id,
      election_kind,
      title,
      status,
      quorum_count,
      threshold_micros,
      to_char(opens_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as opens_at,
      to_char(closes_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as closes_at,
      to_char(sealed_until at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as sealed_until,
      tally::text as tally_json,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from usacc_elections)SQL";

enum class UsaccElectionsElectionKind { Priority, Admission, PanelVerdict, Appeal, Oversight, Policy, AssignmentAcceptance };
inline std::string usacc_elections_election_kind_to_string(UsaccElectionsElectionKind value) {
    switch (value) {
        case UsaccElectionsElectionKind::Priority: return "priority";
        case UsaccElectionsElectionKind::Admission: return "admission";
        case UsaccElectionsElectionKind::PanelVerdict: return "panel_verdict";
        case UsaccElectionsElectionKind::Appeal: return "appeal";
        case UsaccElectionsElectionKind::Oversight: return "oversight";
        case UsaccElectionsElectionKind::Policy: return "policy";
        case UsaccElectionsElectionKind::AssignmentAcceptance: return "assignment_acceptance";
    }
    return "";
}
inline std::optional<UsaccElectionsElectionKind> parse_usacc_elections_election_kind(const std::string& value) {
    if (value == "priority") return UsaccElectionsElectionKind::Priority;
    if (value == "admission") return UsaccElectionsElectionKind::Admission;
    if (value == "panel_verdict") return UsaccElectionsElectionKind::PanelVerdict;
    if (value == "appeal") return UsaccElectionsElectionKind::Appeal;
    if (value == "oversight") return UsaccElectionsElectionKind::Oversight;
    if (value == "policy") return UsaccElectionsElectionKind::Policy;
    if (value == "assignment_acceptance") return UsaccElectionsElectionKind::AssignmentAcceptance;
    return std::nullopt;
}

enum class UsaccElectionsStatus { Draft, Open, Sealed, Tallying, Certified, Void, Archived };
inline std::string usacc_elections_status_to_string(UsaccElectionsStatus value) {
    switch (value) {
        case UsaccElectionsStatus::Draft: return "draft";
        case UsaccElectionsStatus::Open: return "open";
        case UsaccElectionsStatus::Sealed: return "sealed";
        case UsaccElectionsStatus::Tallying: return "tallying";
        case UsaccElectionsStatus::Certified: return "certified";
        case UsaccElectionsStatus::Void: return "void";
        case UsaccElectionsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<UsaccElectionsStatus> parse_usacc_elections_status(const std::string& value) {
    if (value == "draft") return UsaccElectionsStatus::Draft;
    if (value == "open") return UsaccElectionsStatus::Open;
    if (value == "sealed") return UsaccElectionsStatus::Sealed;
    if (value == "tallying") return UsaccElectionsStatus::Tallying;
    if (value == "certified") return UsaccElectionsStatus::Certified;
    if (value == "void") return UsaccElectionsStatus::Void;
    if (value == "archived") return UsaccElectionsStatus::Archived;
    return std::nullopt;
}

struct UsaccElectionsRow {
    std::string id;
    std::optional<std::string> case_id;
    std::optional<std::string> stage_id;
    std::string election_kind;
    std::string title;
    std::string status;
    int32_t quorum_count;
    int32_t threshold_micros;
    std::optional<std::string> opens_at;
    std::optional<std::string> closes_at;
    std::optional<std::string> sealed_until;
    std::string tally;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline UsaccElectionsRow usacc_elections_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    UsaccElectionsRow row;
    (void)is_null;
    row.id = get(0);
    row.case_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.stage_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.election_kind = get(3);
    row.title = get(4);
    row.status = get(5);
    row.quorum_count = std::stoi(get(6));
    row.threshold_micros = std::stoi(get(7));
    row.opens_at = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.closes_at = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.sealed_until = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.tally = get(11);
    row.meta_data = get(12);
    row.created_at = get(13);
    row.updated_at = get(14);
    return row;
}
inline std::optional<std::string> validate_usacc_elections_title(const std::string& value) {
    if (value.size() > 220) return std::string("usacc_elections.title must be at most 220 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_elections_quorum_count(int32_t value) {
    if (value < 1) return std::string("usacc_elections.quorum_count is below the minimum");
    if (value > 1000000) return std::string("usacc_elections.quorum_count is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_elections_threshold_micros(int32_t value) {
    if (value < 1) return std::string("usacc_elections.threshold_micros is below the minimum");
    if (value > 1000000) return std::string("usacc_elections.threshold_micros is above the maximum");
    return std::nullopt;
}

inline const char* usacc_votes_table = "usacc_votes";
inline const std::vector<std::string> usacc_votes_columns = { "id", "election_id", "case_id", "voter_user_id", "vote_kind", "vote_value", "weight_micros", "commitment_hash", "sealed_payload", "revealed_at", "contract_digest", "meta_data", "created_at", "updated_at" };
inline const char* usacc_votes_select_sql = R"SQL(select
      id::text as id,
      election_id::text as election_id,
      case_id::text as case_id,
      voter_user_id::text as voter_user_id,
      vote_kind,
      vote_value,
      weight_micros,
      commitment_hash,
      sealed_payload::text as sealed_payload_json,
      to_char(revealed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as revealed_at,
      contract_digest,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from usacc_votes)SQL";

enum class UsaccVotesVoteKind { Choice, PriorityDollarWeighted, Verdict, Approval, AssignmentResponse };
inline std::string usacc_votes_vote_kind_to_string(UsaccVotesVoteKind value) {
    switch (value) {
        case UsaccVotesVoteKind::Choice: return "choice";
        case UsaccVotesVoteKind::PriorityDollarWeighted: return "priority_dollar_weighted";
        case UsaccVotesVoteKind::Verdict: return "verdict";
        case UsaccVotesVoteKind::Approval: return "approval";
        case UsaccVotesVoteKind::AssignmentResponse: return "assignment_response";
    }
    return "";
}
inline std::optional<UsaccVotesVoteKind> parse_usacc_votes_vote_kind(const std::string& value) {
    if (value == "choice") return UsaccVotesVoteKind::Choice;
    if (value == "priority_dollar_weighted") return UsaccVotesVoteKind::PriorityDollarWeighted;
    if (value == "verdict") return UsaccVotesVoteKind::Verdict;
    if (value == "approval") return UsaccVotesVoteKind::Approval;
    if (value == "assignment_response") return UsaccVotesVoteKind::AssignmentResponse;
    return std::nullopt;
}

struct UsaccVotesRow {
    std::string id;
    std::string election_id;
    std::optional<std::string> case_id;
    std::string voter_user_id;
    std::string vote_kind;
    std::string vote_value;
    int32_t weight_micros;
    std::optional<std::string> commitment_hash;
    std::optional<std::string> sealed_payload;
    std::optional<std::string> revealed_at;
    std::optional<std::string> contract_digest;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline UsaccVotesRow usacc_votes_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    UsaccVotesRow row;
    (void)is_null;
    row.id = get(0);
    row.election_id = get(1);
    row.case_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.voter_user_id = get(3);
    row.vote_kind = get(4);
    row.vote_value = get(5);
    row.weight_micros = std::stoi(get(6));
    row.commitment_hash = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.sealed_payload = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.revealed_at = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.contract_digest = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.meta_data = get(11);
    row.created_at = get(12);
    row.updated_at = get(13);
    return row;
}
inline std::optional<std::string> validate_usacc_votes_vote_value(const std::string& value) {
    if (value.size() > 80) return std::string("usacc_votes.vote_value must be at most 80 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,80}$)RX"))) return std::string("usacc_votes.vote_value does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_votes_weight_micros(int32_t value) {
    if (value < 0) return std::string("usacc_votes.weight_micros is below the minimum");
    if (value > 1000000000) return std::string("usacc_votes.weight_micros is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_votes_commitment_hash(const std::string& value) {
    if (value.size() > 128) return std::string("usacc_votes.commitment_hash must be at most 128 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,128}$)RX"))) return std::string("usacc_votes.commitment_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_votes_contract_digest(const std::string& value) {
    if (value.size() > 160) return std::string("usacc_votes.contract_digest must be at most 160 characters");
    return std::nullopt;
}

inline const char* usacc_escrow_accounts_table = "usacc_escrow_accounts";
inline const std::vector<std::string> usacc_escrow_accounts_columns = { "id", "case_id", "status", "provider", "provider_account_ref", "currency", "target_amount_cents", "committed_amount_cents", "captured_amount_cents", "disbursed_amount_cents", "meta_data", "created_at", "updated_at" };
inline const char* usacc_escrow_accounts_select_sql = R"SQL(select
      id::text as id,
      case_id::text as case_id,
      status,
      provider,
      provider_account_ref,
      currency,
      target_amount_cents,
      committed_amount_cents,
      captured_amount_cents,
      disbursed_amount_cents,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from usacc_escrow_accounts)SQL";

enum class UsaccEscrowAccountsStatus { Pending, Open, Funding, Locked, Disbursing, Closed, Canceled };
inline std::string usacc_escrow_accounts_status_to_string(UsaccEscrowAccountsStatus value) {
    switch (value) {
        case UsaccEscrowAccountsStatus::Pending: return "pending";
        case UsaccEscrowAccountsStatus::Open: return "open";
        case UsaccEscrowAccountsStatus::Funding: return "funding";
        case UsaccEscrowAccountsStatus::Locked: return "locked";
        case UsaccEscrowAccountsStatus::Disbursing: return "disbursing";
        case UsaccEscrowAccountsStatus::Closed: return "closed";
        case UsaccEscrowAccountsStatus::Canceled: return "canceled";
    }
    return "";
}
inline std::optional<UsaccEscrowAccountsStatus> parse_usacc_escrow_accounts_status(const std::string& value) {
    if (value == "pending") return UsaccEscrowAccountsStatus::Pending;
    if (value == "open") return UsaccEscrowAccountsStatus::Open;
    if (value == "funding") return UsaccEscrowAccountsStatus::Funding;
    if (value == "locked") return UsaccEscrowAccountsStatus::Locked;
    if (value == "disbursing") return UsaccEscrowAccountsStatus::Disbursing;
    if (value == "closed") return UsaccEscrowAccountsStatus::Closed;
    if (value == "canceled") return UsaccEscrowAccountsStatus::Canceled;
    return std::nullopt;
}

enum class UsaccEscrowAccountsProvider { StripeTreasury, StripeConnect, Column, Evolve, Mercury, TrustCompany, Manual };
inline std::string usacc_escrow_accounts_provider_to_string(UsaccEscrowAccountsProvider value) {
    switch (value) {
        case UsaccEscrowAccountsProvider::StripeTreasury: return "stripe_treasury";
        case UsaccEscrowAccountsProvider::StripeConnect: return "stripe_connect";
        case UsaccEscrowAccountsProvider::Column: return "column";
        case UsaccEscrowAccountsProvider::Evolve: return "evolve";
        case UsaccEscrowAccountsProvider::Mercury: return "mercury";
        case UsaccEscrowAccountsProvider::TrustCompany: return "trust_company";
        case UsaccEscrowAccountsProvider::Manual: return "manual";
    }
    return "";
}
inline std::optional<UsaccEscrowAccountsProvider> parse_usacc_escrow_accounts_provider(const std::string& value) {
    if (value == "stripe_treasury") return UsaccEscrowAccountsProvider::StripeTreasury;
    if (value == "stripe_connect") return UsaccEscrowAccountsProvider::StripeConnect;
    if (value == "column") return UsaccEscrowAccountsProvider::Column;
    if (value == "evolve") return UsaccEscrowAccountsProvider::Evolve;
    if (value == "mercury") return UsaccEscrowAccountsProvider::Mercury;
    if (value == "trust_company") return UsaccEscrowAccountsProvider::TrustCompany;
    if (value == "manual") return UsaccEscrowAccountsProvider::Manual;
    return std::nullopt;
}

struct UsaccEscrowAccountsRow {
    std::string id;
    std::string case_id;
    std::string status;
    std::string provider;
    std::optional<std::string> provider_account_ref;
    std::string currency;
    int64_t target_amount_cents;
    int64_t committed_amount_cents;
    int64_t captured_amount_cents;
    int64_t disbursed_amount_cents;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline UsaccEscrowAccountsRow usacc_escrow_accounts_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    UsaccEscrowAccountsRow row;
    (void)is_null;
    row.id = get(0);
    row.case_id = get(1);
    row.status = get(2);
    row.provider = get(3);
    row.provider_account_ref = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.currency = get(5);
    row.target_amount_cents = std::stoll(get(6));
    row.committed_amount_cents = std::stoll(get(7));
    row.captured_amount_cents = std::stoll(get(8));
    row.disbursed_amount_cents = std::stoll(get(9));
    row.meta_data = get(10);
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}
inline std::optional<std::string> validate_usacc_escrow_accounts_provider_account_ref(const std::string& value) {
    if (value.size() > 240) return std::string("usacc_escrow_accounts.provider_account_ref must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_escrow_accounts_currency(const std::string& value) {
    if (value.size() > 12) return std::string("usacc_escrow_accounts.currency must be at most 12 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Z]{3,12}$)RX"))) return std::string("usacc_escrow_accounts.currency does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_escrow_accounts_target_amount_cents(int64_t value) {
    if (value < 0) return std::string("usacc_escrow_accounts.target_amount_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_escrow_accounts_committed_amount_cents(int64_t value) {
    if (value < 0) return std::string("usacc_escrow_accounts.committed_amount_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_escrow_accounts_captured_amount_cents(int64_t value) {
    if (value < 0) return std::string("usacc_escrow_accounts.captured_amount_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_escrow_accounts_disbursed_amount_cents(int64_t value) {
    if (value < 0) return std::string("usacc_escrow_accounts.disbursed_amount_cents is below the minimum");
    return std::nullopt;
}

inline const char* usacc_ledger_entries_table = "usacc_ledger_entries";
inline const std::vector<std::string> usacc_ledger_entries_columns = { "id", "case_id", "escrow_account_id", "user_id", "entry_kind", "direction", "amount_cents", "currency", "provider_ref", "contract_digest", "meta_data", "created_at" };
inline const char* usacc_ledger_entries_select_sql = R"SQL(select
      id::text as id,
      case_id::text as case_id,
      escrow_account_id::text as escrow_account_id,
      user_id::text as user_id,
      entry_kind,
      direction,
      amount_cents,
      currency,
      provider_ref,
      contract_digest,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from usacc_ledger_entries)SQL";

enum class UsaccLedgerEntriesEntryKind { Pledge, Authorization, Capture, Refund, Disbursement, Fee, Adjustment };
inline std::string usacc_ledger_entries_entry_kind_to_string(UsaccLedgerEntriesEntryKind value) {
    switch (value) {
        case UsaccLedgerEntriesEntryKind::Pledge: return "pledge";
        case UsaccLedgerEntriesEntryKind::Authorization: return "authorization";
        case UsaccLedgerEntriesEntryKind::Capture: return "capture";
        case UsaccLedgerEntriesEntryKind::Refund: return "refund";
        case UsaccLedgerEntriesEntryKind::Disbursement: return "disbursement";
        case UsaccLedgerEntriesEntryKind::Fee: return "fee";
        case UsaccLedgerEntriesEntryKind::Adjustment: return "adjustment";
    }
    return "";
}
inline std::optional<UsaccLedgerEntriesEntryKind> parse_usacc_ledger_entries_entry_kind(const std::string& value) {
    if (value == "pledge") return UsaccLedgerEntriesEntryKind::Pledge;
    if (value == "authorization") return UsaccLedgerEntriesEntryKind::Authorization;
    if (value == "capture") return UsaccLedgerEntriesEntryKind::Capture;
    if (value == "refund") return UsaccLedgerEntriesEntryKind::Refund;
    if (value == "disbursement") return UsaccLedgerEntriesEntryKind::Disbursement;
    if (value == "fee") return UsaccLedgerEntriesEntryKind::Fee;
    if (value == "adjustment") return UsaccLedgerEntriesEntryKind::Adjustment;
    return std::nullopt;
}

enum class UsaccLedgerEntriesDirection { Debit, Credit };
inline std::string usacc_ledger_entries_direction_to_string(UsaccLedgerEntriesDirection value) {
    switch (value) {
        case UsaccLedgerEntriesDirection::Debit: return "debit";
        case UsaccLedgerEntriesDirection::Credit: return "credit";
    }
    return "";
}
inline std::optional<UsaccLedgerEntriesDirection> parse_usacc_ledger_entries_direction(const std::string& value) {
    if (value == "debit") return UsaccLedgerEntriesDirection::Debit;
    if (value == "credit") return UsaccLedgerEntriesDirection::Credit;
    return std::nullopt;
}

struct UsaccLedgerEntriesRow {
    std::string id;
    std::optional<std::string> case_id;
    std::optional<std::string> escrow_account_id;
    std::optional<std::string> user_id;
    std::string entry_kind;
    std::string direction;
    int64_t amount_cents;
    std::string currency;
    std::optional<std::string> provider_ref;
    std::optional<std::string> contract_digest;
    std::string meta_data;
    std::string created_at;
};

inline UsaccLedgerEntriesRow usacc_ledger_entries_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    UsaccLedgerEntriesRow row;
    (void)is_null;
    row.id = get(0);
    row.case_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.escrow_account_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.user_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.entry_kind = get(4);
    row.direction = get(5);
    row.amount_cents = std::stoll(get(6));
    row.currency = get(7);
    row.provider_ref = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.contract_digest = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.meta_data = get(10);
    row.created_at = get(11);
    return row;
}
inline std::optional<std::string> validate_usacc_ledger_entries_amount_cents(int64_t value) {
    if (value < 0) return std::string("usacc_ledger_entries.amount_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_ledger_entries_currency(const std::string& value) {
    if (value.size() > 12) return std::string("usacc_ledger_entries.currency must be at most 12 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Z]{3,12}$)RX"))) return std::string("usacc_ledger_entries.currency does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_ledger_entries_provider_ref(const std::string& value) {
    if (value.size() > 240) return std::string("usacc_ledger_entries.provider_ref must be at most 240 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_ledger_entries_contract_digest(const std::string& value) {
    if (value.size() > 160) return std::string("usacc_ledger_entries.contract_digest must be at most 160 characters");
    return std::nullopt;
}

inline const char* usacc_contract_operations_table = "usacc_contract_operations";
inline const std::vector<std::string> usacc_contract_operations_columns = { "id", "case_id", "election_id", "vote_id", "request_id", "operation_kind", "status", "program_id", "digest", "envelope", "response", "created_at", "updated_at" };
inline const char* usacc_contract_operations_select_sql = R"SQL(select
      id::text as id,
      case_id::text as case_id,
      election_id::text as election_id,
      vote_id::text as vote_id,
      request_id,
      operation_kind,
      status,
      program_id,
      digest,
      envelope::text as envelope_json,
      response::text as response_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from usacc_contract_operations)SQL";

enum class UsaccContractOperationsOperationKind { ValidateEnvelope, SimulateTransaction, SendTransaction, VoteCommitment, EscrowNotary };
inline std::string usacc_contract_operations_operation_kind_to_string(UsaccContractOperationsOperationKind value) {
    switch (value) {
        case UsaccContractOperationsOperationKind::ValidateEnvelope: return "validate_envelope";
        case UsaccContractOperationsOperationKind::SimulateTransaction: return "simulate_transaction";
        case UsaccContractOperationsOperationKind::SendTransaction: return "send_transaction";
        case UsaccContractOperationsOperationKind::VoteCommitment: return "vote_commitment";
        case UsaccContractOperationsOperationKind::EscrowNotary: return "escrow_notary";
    }
    return "";
}
inline std::optional<UsaccContractOperationsOperationKind> parse_usacc_contract_operations_operation_kind(const std::string& value) {
    if (value == "validate_envelope") return UsaccContractOperationsOperationKind::ValidateEnvelope;
    if (value == "simulate_transaction") return UsaccContractOperationsOperationKind::SimulateTransaction;
    if (value == "send_transaction") return UsaccContractOperationsOperationKind::SendTransaction;
    if (value == "vote_commitment") return UsaccContractOperationsOperationKind::VoteCommitment;
    if (value == "escrow_notary") return UsaccContractOperationsOperationKind::EscrowNotary;
    return std::nullopt;
}

enum class UsaccContractOperationsStatus { Pending, Validated, Simulated, Sent, Failed, Canceled };
inline std::string usacc_contract_operations_status_to_string(UsaccContractOperationsStatus value) {
    switch (value) {
        case UsaccContractOperationsStatus::Pending: return "pending";
        case UsaccContractOperationsStatus::Validated: return "validated";
        case UsaccContractOperationsStatus::Simulated: return "simulated";
        case UsaccContractOperationsStatus::Sent: return "sent";
        case UsaccContractOperationsStatus::Failed: return "failed";
        case UsaccContractOperationsStatus::Canceled: return "canceled";
    }
    return "";
}
inline std::optional<UsaccContractOperationsStatus> parse_usacc_contract_operations_status(const std::string& value) {
    if (value == "pending") return UsaccContractOperationsStatus::Pending;
    if (value == "validated") return UsaccContractOperationsStatus::Validated;
    if (value == "simulated") return UsaccContractOperationsStatus::Simulated;
    if (value == "sent") return UsaccContractOperationsStatus::Sent;
    if (value == "failed") return UsaccContractOperationsStatus::Failed;
    if (value == "canceled") return UsaccContractOperationsStatus::Canceled;
    return std::nullopt;
}

struct UsaccContractOperationsRow {
    std::string id;
    std::optional<std::string> case_id;
    std::optional<std::string> election_id;
    std::optional<std::string> vote_id;
    std::string request_id;
    std::string operation_kind;
    std::string status;
    std::optional<std::string> program_id;
    std::optional<std::string> digest;
    std::string envelope;
    std::string response;
    std::string created_at;
    std::string updated_at;
};

inline UsaccContractOperationsRow usacc_contract_operations_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    UsaccContractOperationsRow row;
    (void)is_null;
    row.id = get(0);
    row.case_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.election_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.vote_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.request_id = get(4);
    row.operation_kind = get(5);
    row.status = get(6);
    row.program_id = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.digest = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.envelope = get(9);
    row.response = get(10);
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}
inline std::optional<std::string> validate_usacc_contract_operations_request_id(const std::string& value) {
    if (value.size() > 160) return std::string("usacc_contract_operations.request_id must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_contract_operations_program_id(const std::string& value) {
    if (value.size() > 128) return std::string("usacc_contract_operations.program_id must be at most 128 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_contract_operations_digest(const std::string& value) {
    if (value.size() > 160) return std::string("usacc_contract_operations.digest must be at most 160 characters");
    return std::nullopt;
}

inline const char* usacc_simulation_runs_table = "usacc_simulation_runs";
inline const std::vector<std::string> usacc_simulation_runs_columns = { "id", "case_id", "status", "mode", "seed", "horizon_days", "actor_count", "event_count", "metrics", "trace", "input", "started_at", "finished_at", "created_at", "updated_at" };
inline const char* usacc_simulation_runs_select_sql = R"SQL(select
      id::text as id,
      case_id::text as case_id,
      status,
      mode,
      seed,
      horizon_days,
      actor_count,
      event_count,
      metrics::text as metrics_json,
      trace::text as trace_json,
      input::text as input_json,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(finished_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as finished_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from usacc_simulation_runs)SQL";

enum class UsaccSimulationRunsStatus { Queued, Running, Succeeded, Failed, Canceled };
inline std::string usacc_simulation_runs_status_to_string(UsaccSimulationRunsStatus value) {
    switch (value) {
        case UsaccSimulationRunsStatus::Queued: return "queued";
        case UsaccSimulationRunsStatus::Running: return "running";
        case UsaccSimulationRunsStatus::Succeeded: return "succeeded";
        case UsaccSimulationRunsStatus::Failed: return "failed";
        case UsaccSimulationRunsStatus::Canceled: return "canceled";
    }
    return "";
}
inline std::optional<UsaccSimulationRunsStatus> parse_usacc_simulation_runs_status(const std::string& value) {
    if (value == "queued") return UsaccSimulationRunsStatus::Queued;
    if (value == "running") return UsaccSimulationRunsStatus::Running;
    if (value == "succeeded") return UsaccSimulationRunsStatus::Succeeded;
    if (value == "failed") return UsaccSimulationRunsStatus::Failed;
    if (value == "canceled") return UsaccSimulationRunsStatus::Canceled;
    return std::nullopt;
}

enum class UsaccSimulationRunsMode { Sim, LiveShadow, Replay };
inline std::string usacc_simulation_runs_mode_to_string(UsaccSimulationRunsMode value) {
    switch (value) {
        case UsaccSimulationRunsMode::Sim: return "sim";
        case UsaccSimulationRunsMode::LiveShadow: return "live_shadow";
        case UsaccSimulationRunsMode::Replay: return "replay";
    }
    return "";
}
inline std::optional<UsaccSimulationRunsMode> parse_usacc_simulation_runs_mode(const std::string& value) {
    if (value == "sim") return UsaccSimulationRunsMode::Sim;
    if (value == "live_shadow") return UsaccSimulationRunsMode::LiveShadow;
    if (value == "replay") return UsaccSimulationRunsMode::Replay;
    return std::nullopt;
}

struct UsaccSimulationRunsRow {
    std::string id;
    std::optional<std::string> case_id;
    std::string status;
    std::string mode;
    int64_t seed;
    int32_t horizon_days;
    int32_t actor_count;
    int32_t event_count;
    std::string metrics;
    std::string trace;
    std::string input;
    std::optional<std::string> started_at;
    std::optional<std::string> finished_at;
    std::string created_at;
    std::string updated_at;
};

inline UsaccSimulationRunsRow usacc_simulation_runs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    UsaccSimulationRunsRow row;
    (void)is_null;
    row.id = get(0);
    row.case_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.status = get(2);
    row.mode = get(3);
    row.seed = std::stoll(get(4));
    row.horizon_days = std::stoi(get(5));
    row.actor_count = std::stoi(get(6));
    row.event_count = std::stoi(get(7));
    row.metrics = get(8);
    row.trace = get(9);
    row.input = get(10);
    row.started_at = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.finished_at = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.created_at = get(13);
    row.updated_at = get(14);
    return row;
}
inline std::optional<std::string> validate_usacc_simulation_runs_horizon_days(int32_t value) {
    if (value < 1) return std::string("usacc_simulation_runs.horizon_days is below the minimum");
    if (value > 3650) return std::string("usacc_simulation_runs.horizon_days is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_simulation_runs_actor_count(int32_t value) {
    if (value < 0) return std::string("usacc_simulation_runs.actor_count is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_simulation_runs_event_count(int32_t value) {
    if (value < 0) return std::string("usacc_simulation_runs.event_count is below the minimum");
    return std::nullopt;
}

inline const char* usacc_audit_events_table = "usacc_audit_events";
inline const std::vector<std::string> usacc_audit_events_columns = { "id", "case_id", "actor_user_id", "event_type", "event_hash", "source", "payload", "created_at" };
inline const char* usacc_audit_events_select_sql = R"SQL(select
      id::text as id,
      case_id::text as case_id,
      actor_user_id::text as actor_user_id,
      event_type,
      event_hash,
      source,
      payload::text as payload_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from usacc_audit_events)SQL";

struct UsaccAuditEventsRow {
    std::string id;
    std::optional<std::string> case_id;
    std::optional<std::string> actor_user_id;
    std::string event_type;
    std::string event_hash;
    std::string source;
    std::string payload;
    std::string created_at;
};

inline UsaccAuditEventsRow usacc_audit_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    UsaccAuditEventsRow row;
    (void)is_null;
    row.id = get(0);
    row.case_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.actor_user_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.event_type = get(3);
    row.event_hash = get(4);
    row.source = get(5);
    row.payload = get(6);
    row.created_at = get(7);
    return row;
}
inline std::optional<std::string> validate_usacc_audit_events_event_type(const std::string& value) {
    if (value.size() > 96) return std::string("usacc_audit_events.event_type must be at most 96 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,96}$)RX"))) return std::string("usacc_audit_events.event_type does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_audit_events_event_hash(const std::string& value) {
    if (value.size() > 128) return std::string("usacc_audit_events.event_hash must be at most 128 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,128}$)RX"))) return std::string("usacc_audit_events.event_hash does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_usacc_audit_events_source(const std::string& value) {
    if (value.size() > 80) return std::string("usacc_audit_events.source must be at most 80 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,80}$)RX"))) return std::string("usacc_audit_events.source does not match the required pattern");
    return std::nullopt;
}

inline const char* benefactor_leads_table = "benefactor.benefactor_leads";
inline const std::vector<std::string> benefactor_leads_columns = { "id", "business_name", "owner_first_name", "owner_last_name", "primary_email", "secondary_email", "primary_phone", "website_url", "service_category", "service_subcategories", "city", "state", "zip_code", "country", "service_area", "lead_status", "outreach_status", "total_outreach_attempts", "last_outreach_at", "contact_attempts", "source_url", "source_query", "source_tool", "source_engine", "is_verified", "tags", "meta_data", "notes", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* benefactor_leads_select_sql = R"SQL(select
      id::text as id,
      business_name,
      owner_first_name,
      owner_last_name,
      primary_email,
      secondary_email,
      primary_phone,
      website_url,
      service_category,
      service_subcategories::text as service_subcategories_json,
      city,
      state,
      zip_code,
      country,
      service_area,
      lead_status,
      outreach_status,
      total_outreach_attempts,
      to_char(last_outreach_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_outreach_at,
      contact_attempts::text as contact_attempts_json,
      source_url,
      source_query,
      source_tool,
      source_engine,
      is_verified,
      tags::text as tags_json,
      meta_data::text as meta_data_json,
      notes,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from benefactor.benefactor_leads)SQL";

enum class BenefactorLeadsLeadStatus { New, Contacted, Replied, Booked, Rejected, Unsubscribed, Unqualified, DoNotContact };
inline std::string benefactor_leads_lead_status_to_string(BenefactorLeadsLeadStatus value) {
    switch (value) {
        case BenefactorLeadsLeadStatus::New: return "new";
        case BenefactorLeadsLeadStatus::Contacted: return "contacted";
        case BenefactorLeadsLeadStatus::Replied: return "replied";
        case BenefactorLeadsLeadStatus::Booked: return "booked";
        case BenefactorLeadsLeadStatus::Rejected: return "rejected";
        case BenefactorLeadsLeadStatus::Unsubscribed: return "unsubscribed";
        case BenefactorLeadsLeadStatus::Unqualified: return "unqualified";
        case BenefactorLeadsLeadStatus::DoNotContact: return "do_not_contact";
    }
    return "";
}
inline std::optional<BenefactorLeadsLeadStatus> parse_benefactor_leads_lead_status(const std::string& value) {
    if (value == "new") return BenefactorLeadsLeadStatus::New;
    if (value == "contacted") return BenefactorLeadsLeadStatus::Contacted;
    if (value == "replied") return BenefactorLeadsLeadStatus::Replied;
    if (value == "booked") return BenefactorLeadsLeadStatus::Booked;
    if (value == "rejected") return BenefactorLeadsLeadStatus::Rejected;
    if (value == "unsubscribed") return BenefactorLeadsLeadStatus::Unsubscribed;
    if (value == "unqualified") return BenefactorLeadsLeadStatus::Unqualified;
    if (value == "do_not_contact") return BenefactorLeadsLeadStatus::DoNotContact;
    return std::nullopt;
}

enum class BenefactorLeadsOutreachStatus { Pending, New, Contacted, Failed };
inline std::string benefactor_leads_outreach_status_to_string(BenefactorLeadsOutreachStatus value) {
    switch (value) {
        case BenefactorLeadsOutreachStatus::Pending: return "pending";
        case BenefactorLeadsOutreachStatus::New: return "new";
        case BenefactorLeadsOutreachStatus::Contacted: return "contacted";
        case BenefactorLeadsOutreachStatus::Failed: return "failed";
    }
    return "";
}
inline std::optional<BenefactorLeadsOutreachStatus> parse_benefactor_leads_outreach_status(const std::string& value) {
    if (value == "pending") return BenefactorLeadsOutreachStatus::Pending;
    if (value == "new") return BenefactorLeadsOutreachStatus::New;
    if (value == "contacted") return BenefactorLeadsOutreachStatus::Contacted;
    if (value == "failed") return BenefactorLeadsOutreachStatus::Failed;
    return std::nullopt;
}

struct BenefactorLeadsRow {
    std::string id;
    std::string business_name;
    std::string owner_first_name;
    std::string owner_last_name;
    std::string primary_email;
    std::optional<std::string> secondary_email;
    std::optional<std::string> primary_phone;
    std::optional<std::string> website_url;
    std::string service_category;
    std::string service_subcategories;
    std::optional<std::string> city;
    std::optional<std::string> state;
    std::optional<std::string> zip_code;
    std::string country;
    std::optional<std::string> service_area;
    std::string lead_status;
    std::string outreach_status;
    int32_t total_outreach_attempts;
    std::optional<std::string> last_outreach_at;
    std::string contact_attempts;
    std::optional<std::string> source_url;
    std::optional<std::string> source_query;
    std::optional<std::string> source_tool;
    std::optional<std::string> source_engine;
    bool is_verified;
    std::string tags;
    std::string meta_data;
    std::optional<std::string> notes;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline BenefactorLeadsRow benefactor_leads_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorLeadsRow row;
    (void)is_null;
    row.id = get(0);
    row.business_name = get(1);
    row.owner_first_name = get(2);
    row.owner_last_name = get(3);
    row.primary_email = get(4);
    row.secondary_email = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.primary_phone = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.website_url = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.service_category = get(8);
    row.service_subcategories = get(9);
    row.city = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.state = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.zip_code = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.country = get(13);
    row.service_area = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.lead_status = get(15);
    row.outreach_status = get(16);
    row.total_outreach_attempts = std::stoi(get(17));
    row.last_outreach_at = is_null(18) ? std::nullopt : std::optional<std::string>(get(18));
    row.contact_attempts = get(19);
    row.source_url = is_null(20) ? std::nullopt : std::optional<std::string>(get(20));
    row.source_query = is_null(21) ? std::nullopt : std::optional<std::string>(get(21));
    row.source_tool = is_null(22) ? std::nullopt : std::optional<std::string>(get(22));
    row.source_engine = is_null(23) ? std::nullopt : std::optional<std::string>(get(23));
    row.is_verified = (get(24) == "t");
    row.tags = get(25);
    row.meta_data = get(26);
    row.notes = is_null(27) ? std::nullopt : std::optional<std::string>(get(27));
    row.is_soft_deleted = (get(28) == "t");
    row.created_at = get(29);
    row.updated_at = get(30);
    row.created_by = is_null(31) ? std::nullopt : std::optional<std::string>(get(31));
    row.updated_by = is_null(32) ? std::nullopt : std::optional<std::string>(get(32));
    return row;
}
inline std::optional<std::string> validate_benefactor_leads_business_name(const std::string& value) {
    if (value.size() > 300) return std::string("benefactor_leads.business_name must be at most 300 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_owner_first_name(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_leads.owner_first_name must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_owner_last_name(const std::string& value) {
    if (value.size() > 130) return std::string("benefactor_leads.owner_last_name must be at most 130 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_primary_email(const std::string& value) {
    if (value.size() > 255) return std::string("benefactor_leads.primary_email must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_secondary_email(const std::string& value) {
    if (value.size() > 255) return std::string("benefactor_leads.secondary_email must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_primary_phone(const std::string& value) {
    if (value.size() > 100) return std::string("benefactor_leads.primary_phone must be at most 100 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_website_url(const std::string& value) {
    if (value.size() > 500) return std::string("benefactor_leads.website_url must be at most 500 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_service_category(const std::string& value) {
    if (value.size() > 60) return std::string("benefactor_leads.service_category must be at most 60 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_city(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_leads.city must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_state(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_leads.state must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_zip_code(const std::string& value) {
    if (value.size() > 20) return std::string("benefactor_leads.zip_code must be at most 20 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_country(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_leads.country must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_service_area(const std::string& value) {
    if (value.size() > 500) return std::string("benefactor_leads.service_area must be at most 500 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_source_url(const std::string& value) {
    if (value.size() > 1000) return std::string("benefactor_leads.source_url must be at most 1000 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_source_query(const std::string& value) {
    if (value.size() > 500) return std::string("benefactor_leads.source_query must be at most 500 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_source_tool(const std::string& value) {
    if (value.size() > 60) return std::string("benefactor_leads.source_tool must be at most 60 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_source_engine(const std::string& value) {
    if (value.size() > 30) return std::string("benefactor_leads.source_engine must be at most 30 characters");
    return std::nullopt;
}

inline const char* benefactor_leads_domains_table = "benefactor.benefactor_leads_domains";
inline const std::vector<std::string> benefactor_leads_domains_columns = { "id", "domain", "domain_kind", "status", "reason", "source", "is_blacklisted", "is_blocked", "is_permanently_blocked", "blocked_reason", "blocked_until", "skip_until", "scrape_count", "skip_count", "skipped_count", "email_found_count", "lead_inserted_count", "last_seen_at", "last_scraped_at", "last_skipped_at", "last_email_found_at", "last_lead_inserted_at", "last_seen_url", "meta_data", "is_active", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* benefactor_leads_domains_select_sql = R"SQL(select
      id::text as id,
      domain,
      domain_kind,
      status,
      reason,
      source,
      is_blacklisted,
      is_blocked,
      is_permanently_blocked,
      blocked_reason,
      to_char(blocked_until at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as blocked_until,
      to_char(skip_until at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as skip_until,
      scrape_count,
      skip_count,
      skipped_count,
      email_found_count,
      lead_inserted_count,
      to_char(last_seen_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_seen_at,
      to_char(last_scraped_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_scraped_at,
      to_char(last_skipped_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_skipped_at,
      to_char(last_email_found_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_email_found_at,
      to_char(last_lead_inserted_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_lead_inserted_at,
      last_seen_url,
      meta_data::text as meta_data_json,
      is_active,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from benefactor.benefactor_leads_domains)SQL";

enum class BenefactorLeadsDomainsDomainKind { Email, Website };
inline std::string benefactor_leads_domains_domain_kind_to_string(BenefactorLeadsDomainsDomainKind value) {
    switch (value) {
        case BenefactorLeadsDomainsDomainKind::Email: return "email";
        case BenefactorLeadsDomainsDomainKind::Website: return "website";
    }
    return "";
}
inline std::optional<BenefactorLeadsDomainsDomainKind> parse_benefactor_leads_domains_domain_kind(const std::string& value) {
    if (value == "email") return BenefactorLeadsDomainsDomainKind::Email;
    if (value == "website") return BenefactorLeadsDomainsDomainKind::Website;
    return std::nullopt;
}

enum class BenefactorLeadsDomainsStatus { Allowed, Blocked, Skipped, ScrapedRecently, RecentlyScraped };
inline std::string benefactor_leads_domains_status_to_string(BenefactorLeadsDomainsStatus value) {
    switch (value) {
        case BenefactorLeadsDomainsStatus::Allowed: return "allowed";
        case BenefactorLeadsDomainsStatus::Blocked: return "blocked";
        case BenefactorLeadsDomainsStatus::Skipped: return "skipped";
        case BenefactorLeadsDomainsStatus::ScrapedRecently: return "scraped_recently";
        case BenefactorLeadsDomainsStatus::RecentlyScraped: return "recently_scraped";
    }
    return "";
}
inline std::optional<BenefactorLeadsDomainsStatus> parse_benefactor_leads_domains_status(const std::string& value) {
    if (value == "allowed") return BenefactorLeadsDomainsStatus::Allowed;
    if (value == "blocked") return BenefactorLeadsDomainsStatus::Blocked;
    if (value == "skipped") return BenefactorLeadsDomainsStatus::Skipped;
    if (value == "scraped_recently") return BenefactorLeadsDomainsStatus::ScrapedRecently;
    if (value == "recently_scraped") return BenefactorLeadsDomainsStatus::RecentlyScraped;
    return std::nullopt;
}

struct BenefactorLeadsDomainsRow {
    std::string id;
    std::string domain;
    std::string domain_kind;
    std::string status;
    std::optional<std::string> reason;
    std::string source;
    bool is_blacklisted;
    bool is_blocked;
    bool is_permanently_blocked;
    std::optional<std::string> blocked_reason;
    std::optional<std::string> blocked_until;
    std::optional<std::string> skip_until;
    int32_t scrape_count;
    int32_t skip_count;
    int32_t skipped_count;
    int32_t email_found_count;
    int32_t lead_inserted_count;
    std::optional<std::string> last_seen_at;
    std::optional<std::string> last_scraped_at;
    std::optional<std::string> last_skipped_at;
    std::optional<std::string> last_email_found_at;
    std::optional<std::string> last_lead_inserted_at;
    std::optional<std::string> last_seen_url;
    std::string meta_data;
    bool is_active;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline BenefactorLeadsDomainsRow benefactor_leads_domains_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorLeadsDomainsRow row;
    (void)is_null;
    row.id = get(0);
    row.domain = get(1);
    row.domain_kind = get(2);
    row.status = get(3);
    row.reason = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.source = get(5);
    row.is_blacklisted = (get(6) == "t");
    row.is_blocked = (get(7) == "t");
    row.is_permanently_blocked = (get(8) == "t");
    row.blocked_reason = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.blocked_until = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.skip_until = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.scrape_count = std::stoi(get(12));
    row.skip_count = std::stoi(get(13));
    row.skipped_count = std::stoi(get(14));
    row.email_found_count = std::stoi(get(15));
    row.lead_inserted_count = std::stoi(get(16));
    row.last_seen_at = is_null(17) ? std::nullopt : std::optional<std::string>(get(17));
    row.last_scraped_at = is_null(18) ? std::nullopt : std::optional<std::string>(get(18));
    row.last_skipped_at = is_null(19) ? std::nullopt : std::optional<std::string>(get(19));
    row.last_email_found_at = is_null(20) ? std::nullopt : std::optional<std::string>(get(20));
    row.last_lead_inserted_at = is_null(21) ? std::nullopt : std::optional<std::string>(get(21));
    row.last_seen_url = is_null(22) ? std::nullopt : std::optional<std::string>(get(22));
    row.meta_data = get(23);
    row.is_active = (get(24) == "t");
    row.is_soft_deleted = (get(25) == "t");
    row.created_at = get(26);
    row.updated_at = get(27);
    row.created_by = is_null(28) ? std::nullopt : std::optional<std::string>(get(28));
    row.updated_by = is_null(29) ? std::nullopt : std::optional<std::string>(get(29));
    return row;
}
inline std::optional<std::string> validate_benefactor_leads_domains_domain(const std::string& value) {
    if (value.size() > 255) return std::string("benefactor_leads_domains.domain must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_domains_source(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_leads_domains.source must be at most 80 characters");
    return std::nullopt;
}

inline const char* benefactor_search_locations_table = "benefactor.benefactor_search_locations";
inline const std::vector<std::string> benefactor_search_locations_columns = { "id", "slug", "city", "state", "state_code", "country", "metro_area", "military_area", "primary_installation", "installation_aliases", "location_type", "priority", "search_weight", "total_query_runs", "total_emails_inserted", "success_count", "failure_count", "last_run_at", "last_success_at", "last_failure_at", "cooldown_until", "meta_data", "is_active", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* benefactor_search_locations_select_sql = R"SQL(select
      id::text as id,
      slug,
      city,
      state,
      state_code,
      country,
      metro_area,
      military_area,
      primary_installation,
      installation_aliases::text as installation_aliases_json,
      location_type,
      priority,
      search_weight,
      total_query_runs,
      total_emails_inserted,
      success_count,
      failure_count,
      to_char(last_run_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_run_at,
      to_char(last_success_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_success_at,
      to_char(last_failure_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_failure_at,
      to_char(cooldown_until at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as cooldown_until,
      meta_data::text as meta_data_json,
      is_active,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from benefactor.benefactor_search_locations)SQL";

struct BenefactorSearchLocationsRow {
    std::string id;
    std::string slug;
    std::string city;
    std::string state;
    std::optional<std::string> state_code;
    std::string country;
    std::optional<std::string> metro_area;
    std::optional<std::string> military_area;
    std::optional<std::string> primary_installation;
    std::string installation_aliases;
    std::string location_type;
    int32_t priority;
    int32_t search_weight;
    int32_t total_query_runs;
    int32_t total_emails_inserted;
    int32_t success_count;
    int32_t failure_count;
    std::optional<std::string> last_run_at;
    std::optional<std::string> last_success_at;
    std::optional<std::string> last_failure_at;
    std::optional<std::string> cooldown_until;
    std::string meta_data;
    bool is_active;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline BenefactorSearchLocationsRow benefactor_search_locations_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorSearchLocationsRow row;
    (void)is_null;
    row.id = get(0);
    row.slug = get(1);
    row.city = get(2);
    row.state = get(3);
    row.state_code = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.country = get(5);
    row.metro_area = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.military_area = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.primary_installation = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.installation_aliases = get(9);
    row.location_type = get(10);
    row.priority = std::stoi(get(11));
    row.search_weight = std::stoi(get(12));
    row.total_query_runs = std::stoi(get(13));
    row.total_emails_inserted = std::stoi(get(14));
    row.success_count = std::stoi(get(15));
    row.failure_count = std::stoi(get(16));
    row.last_run_at = is_null(17) ? std::nullopt : std::optional<std::string>(get(17));
    row.last_success_at = is_null(18) ? std::nullopt : std::optional<std::string>(get(18));
    row.last_failure_at = is_null(19) ? std::nullopt : std::optional<std::string>(get(19));
    row.cooldown_until = is_null(20) ? std::nullopt : std::optional<std::string>(get(20));
    row.meta_data = get(21);
    row.is_active = (get(22) == "t");
    row.is_soft_deleted = (get(23) == "t");
    row.created_at = get(24);
    row.updated_at = get(25);
    row.created_by = is_null(26) ? std::nullopt : std::optional<std::string>(get(26));
    row.updated_by = is_null(27) ? std::nullopt : std::optional<std::string>(get(27));
    return row;
}
inline std::optional<std::string> validate_benefactor_search_locations_slug(const std::string& value) {
    if (value.size() > 160) return std::string("benefactor_search_locations.slug must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_search_locations_city(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_search_locations.city must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_search_locations_state(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_search_locations.state must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_search_locations_state_code(const std::string& value) {
    if (value.size() > 10) return std::string("benefactor_search_locations.state_code must be at most 10 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_search_locations_country(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_search_locations.country must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_search_locations_metro_area(const std::string& value) {
    if (value.size() > 220) return std::string("benefactor_search_locations.metro_area must be at most 220 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_search_locations_military_area(const std::string& value) {
    if (value.size() > 220) return std::string("benefactor_search_locations.military_area must be at most 220 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_search_locations_primary_installation(const std::string& value) {
    if (value.size() > 220) return std::string("benefactor_search_locations.primary_installation must be at most 220 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_search_locations_location_type(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_search_locations.location_type must be at most 80 characters");
    return std::nullopt;
}

inline const char* benefactor_scrape_queries_table = "benefactor.benefactor_scrape_queries";
inline const std::vector<std::string> benefactor_scrape_queries_columns = { "id", "query_text", "query_hash", "benefactor_icp_slug", "benefactor_icp_name", "benefactor_search_location_id", "service_category", "target_city", "target_state", "target_country", "target_military_area", "target_installation", "query_variant", "search_page_depth", "priority", "total_runs", "total_urls_visited", "total_emails_found", "total_emails_inserted", "total_emails_duplicate", "total_errors", "success_count", "failure_count", "last_run_at", "last_success_at", "last_failure_at", "last_run_emails_found", "last_run_emails_inserted", "last_run_success", "last_run_duration_ms", "last_run_error", "cooldown_until", "consecutive_zero_new_runs", "last_zero_new_run_at", "meta_data", "is_active", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* benefactor_scrape_queries_select_sql = R"SQL(select
      id::text as id,
      query_text,
      query_hash,
      benefactor_icp_slug,
      benefactor_icp_name,
      benefactor_search_location_id::text as benefactor_search_location_id,
      service_category,
      target_city,
      target_state,
      target_country,
      target_military_area,
      target_installation,
      query_variant,
      search_page_depth,
      priority,
      total_runs,
      total_urls_visited,
      total_emails_found,
      total_emails_inserted,
      total_emails_duplicate,
      total_errors,
      success_count,
      failure_count,
      to_char(last_run_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_run_at,
      to_char(last_success_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_success_at,
      to_char(last_failure_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_failure_at,
      last_run_emails_found,
      last_run_emails_inserted,
      last_run_success,
      last_run_duration_ms,
      last_run_error,
      to_char(cooldown_until at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as cooldown_until,
      consecutive_zero_new_runs,
      to_char(last_zero_new_run_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_zero_new_run_at,
      meta_data::text as meta_data_json,
      is_active,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from benefactor.benefactor_scrape_queries)SQL";

enum class BenefactorScrapeQueriesQueryVariant { EmailContact, ContactUs, WebsiteDomain, FuzzyEmail, FuzzyCity };
inline std::string benefactor_scrape_queries_query_variant_to_string(BenefactorScrapeQueriesQueryVariant value) {
    switch (value) {
        case BenefactorScrapeQueriesQueryVariant::EmailContact: return "email_contact";
        case BenefactorScrapeQueriesQueryVariant::ContactUs: return "contact_us";
        case BenefactorScrapeQueriesQueryVariant::WebsiteDomain: return "website_domain";
        case BenefactorScrapeQueriesQueryVariant::FuzzyEmail: return "fuzzy_email";
        case BenefactorScrapeQueriesQueryVariant::FuzzyCity: return "fuzzy_city";
    }
    return "";
}
inline std::optional<BenefactorScrapeQueriesQueryVariant> parse_benefactor_scrape_queries_query_variant(const std::string& value) {
    if (value == "email_contact") return BenefactorScrapeQueriesQueryVariant::EmailContact;
    if (value == "contact_us") return BenefactorScrapeQueriesQueryVariant::ContactUs;
    if (value == "website_domain") return BenefactorScrapeQueriesQueryVariant::WebsiteDomain;
    if (value == "fuzzy_email") return BenefactorScrapeQueriesQueryVariant::FuzzyEmail;
    if (value == "fuzzy_city") return BenefactorScrapeQueriesQueryVariant::FuzzyCity;
    return std::nullopt;
}

struct BenefactorScrapeQueriesRow {
    std::string id;
    std::string query_text;
    std::string query_hash;
    std::optional<std::string> benefactor_icp_slug;
    std::optional<std::string> benefactor_icp_name;
    std::optional<std::string> benefactor_search_location_id;
    std::string service_category;
    std::optional<std::string> target_city;
    std::optional<std::string> target_state;
    std::string target_country;
    std::optional<std::string> target_military_area;
    std::optional<std::string> target_installation;
    std::string query_variant;
    int32_t search_page_depth;
    int32_t priority;
    int32_t total_runs;
    int32_t total_urls_visited;
    int32_t total_emails_found;
    int32_t total_emails_inserted;
    int32_t total_emails_duplicate;
    int32_t total_errors;
    int32_t success_count;
    int32_t failure_count;
    std::optional<std::string> last_run_at;
    std::optional<std::string> last_success_at;
    std::optional<std::string> last_failure_at;
    int32_t last_run_emails_found;
    int32_t last_run_emails_inserted;
    bool last_run_success;
    int32_t last_run_duration_ms;
    std::optional<std::string> last_run_error;
    std::optional<std::string> cooldown_until;
    int32_t consecutive_zero_new_runs;
    std::optional<std::string> last_zero_new_run_at;
    std::string meta_data;
    bool is_active;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline BenefactorScrapeQueriesRow benefactor_scrape_queries_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorScrapeQueriesRow row;
    (void)is_null;
    row.id = get(0);
    row.query_text = get(1);
    row.query_hash = get(2);
    row.benefactor_icp_slug = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.benefactor_icp_name = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.benefactor_search_location_id = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.service_category = get(6);
    row.target_city = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.target_state = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.target_country = get(9);
    row.target_military_area = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.target_installation = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.query_variant = get(12);
    row.search_page_depth = std::stoi(get(13));
    row.priority = std::stoi(get(14));
    row.total_runs = std::stoi(get(15));
    row.total_urls_visited = std::stoi(get(16));
    row.total_emails_found = std::stoi(get(17));
    row.total_emails_inserted = std::stoi(get(18));
    row.total_emails_duplicate = std::stoi(get(19));
    row.total_errors = std::stoi(get(20));
    row.success_count = std::stoi(get(21));
    row.failure_count = std::stoi(get(22));
    row.last_run_at = is_null(23) ? std::nullopt : std::optional<std::string>(get(23));
    row.last_success_at = is_null(24) ? std::nullopt : std::optional<std::string>(get(24));
    row.last_failure_at = is_null(25) ? std::nullopt : std::optional<std::string>(get(25));
    row.last_run_emails_found = std::stoi(get(26));
    row.last_run_emails_inserted = std::stoi(get(27));
    row.last_run_success = (get(28) == "t");
    row.last_run_duration_ms = std::stoi(get(29));
    row.last_run_error = is_null(30) ? std::nullopt : std::optional<std::string>(get(30));
    row.cooldown_until = is_null(31) ? std::nullopt : std::optional<std::string>(get(31));
    row.consecutive_zero_new_runs = std::stoi(get(32));
    row.last_zero_new_run_at = is_null(33) ? std::nullopt : std::optional<std::string>(get(33));
    row.meta_data = get(34);
    row.is_active = (get(35) == "t");
    row.is_soft_deleted = (get(36) == "t");
    row.created_at = get(37);
    row.updated_at = get(38);
    row.created_by = is_null(39) ? std::nullopt : std::optional<std::string>(get(39));
    row.updated_by = is_null(40) ? std::nullopt : std::optional<std::string>(get(40));
    return row;
}
inline std::optional<std::string> validate_benefactor_scrape_queries_query_hash(const std::string& value) {
    if (value.size() > 64) return std::string("benefactor_scrape_queries.query_hash must be at most 64 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_scrape_queries_benefactor_icp_slug(const std::string& value) {
    if (value.size() > 160) return std::string("benefactor_scrape_queries.benefactor_icp_slug must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_scrape_queries_benefactor_icp_name(const std::string& value) {
    if (value.size() > 220) return std::string("benefactor_scrape_queries.benefactor_icp_name must be at most 220 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_scrape_queries_service_category(const std::string& value) {
    if (value.size() > 60) return std::string("benefactor_scrape_queries.service_category must be at most 60 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_scrape_queries_target_city(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_scrape_queries.target_city must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_scrape_queries_target_state(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_scrape_queries.target_state must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_scrape_queries_target_country(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_scrape_queries.target_country must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_scrape_queries_target_military_area(const std::string& value) {
    if (value.size() > 220) return std::string("benefactor_scrape_queries.target_military_area must be at most 220 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_scrape_queries_target_installation(const std::string& value) {
    if (value.size() > 220) return std::string("benefactor_scrape_queries.target_installation must be at most 220 characters");
    return std::nullopt;
}

inline const char* benefactor_domain_search_tracking_table = "benefactor.benefactor_domain_search_tracking";
inline const std::vector<std::string> benefactor_domain_search_tracking_columns = { "id", "domain", "for_what", "search_result_appearances", "queued_visit_count", "visit_count", "good_result_count", "bad_result_count", "email_found_count", "lead_inserted_count", "last_queued_at", "last_visited_at", "last_good_result_at", "last_bad_result_at", "last_email_found_at", "last_lead_inserted_at", "last_good_url", "last_bad_url", "blocked_until", "is_permanently_blocked", "blocked_reason", "meta_data", "is_active", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* benefactor_domain_search_tracking_select_sql = R"SQL(select
      id::text as id,
      domain,
      for_what,
      search_result_appearances,
      queued_visit_count,
      visit_count,
      good_result_count,
      bad_result_count,
      email_found_count,
      lead_inserted_count,
      to_char(last_queued_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_queued_at,
      to_char(last_visited_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_visited_at,
      to_char(last_good_result_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_good_result_at,
      to_char(last_bad_result_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_bad_result_at,
      to_char(last_email_found_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_email_found_at,
      to_char(last_lead_inserted_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_lead_inserted_at,
      last_good_url,
      last_bad_url,
      to_char(blocked_until at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as blocked_until,
      is_permanently_blocked,
      blocked_reason,
      meta_data::text as meta_data_json,
      is_active,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from benefactor.benefactor_domain_search_tracking)SQL";

struct BenefactorDomainSearchTrackingRow {
    std::string id;
    std::string domain;
    std::string for_what;
    int32_t search_result_appearances;
    int32_t queued_visit_count;
    int32_t visit_count;
    int32_t good_result_count;
    int32_t bad_result_count;
    int32_t email_found_count;
    int32_t lead_inserted_count;
    std::optional<std::string> last_queued_at;
    std::optional<std::string> last_visited_at;
    std::optional<std::string> last_good_result_at;
    std::optional<std::string> last_bad_result_at;
    std::optional<std::string> last_email_found_at;
    std::optional<std::string> last_lead_inserted_at;
    std::optional<std::string> last_good_url;
    std::optional<std::string> last_bad_url;
    std::optional<std::string> blocked_until;
    bool is_permanently_blocked;
    std::optional<std::string> blocked_reason;
    std::string meta_data;
    bool is_active;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline BenefactorDomainSearchTrackingRow benefactor_domain_search_tracking_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorDomainSearchTrackingRow row;
    (void)is_null;
    row.id = get(0);
    row.domain = get(1);
    row.for_what = get(2);
    row.search_result_appearances = std::stoi(get(3));
    row.queued_visit_count = std::stoi(get(4));
    row.visit_count = std::stoi(get(5));
    row.good_result_count = std::stoi(get(6));
    row.bad_result_count = std::stoi(get(7));
    row.email_found_count = std::stoi(get(8));
    row.lead_inserted_count = std::stoi(get(9));
    row.last_queued_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.last_visited_at = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.last_good_result_at = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.last_bad_result_at = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    row.last_email_found_at = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.last_lead_inserted_at = is_null(15) ? std::nullopt : std::optional<std::string>(get(15));
    row.last_good_url = is_null(16) ? std::nullopt : std::optional<std::string>(get(16));
    row.last_bad_url = is_null(17) ? std::nullopt : std::optional<std::string>(get(17));
    row.blocked_until = is_null(18) ? std::nullopt : std::optional<std::string>(get(18));
    row.is_permanently_blocked = (get(19) == "t");
    row.blocked_reason = is_null(20) ? std::nullopt : std::optional<std::string>(get(20));
    row.meta_data = get(21);
    row.is_active = (get(22) == "t");
    row.is_soft_deleted = (get(23) == "t");
    row.created_at = get(24);
    row.updated_at = get(25);
    row.created_by = is_null(26) ? std::nullopt : std::optional<std::string>(get(26));
    row.updated_by = is_null(27) ? std::nullopt : std::optional<std::string>(get(27));
    return row;
}
inline std::optional<std::string> validate_benefactor_domain_search_tracking_domain(const std::string& value) {
    if (value.size() > 255) return std::string("benefactor_domain_search_tracking.domain must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_domain_search_tracking_for_what(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_domain_search_tracking.for_what must be at most 80 characters");
    return std::nullopt;
}

inline const char* benefactor_icps_table = "benefactor.benefactor_icps";
inline const std::vector<std::string> benefactor_icps_columns = { "id", "slug", "name", "category", "service_category", "description", "outcall_fit_score", "priority", "search_terms", "search_signals", "target_home_services", "target_medical", "target_legal", "target_events", "target_corporate", "target_industrial", "meta_data", "is_active", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* benefactor_icps_select_sql = R"SQL(select
      id::text as id,
      slug,
      name,
      category,
      service_category,
      description,
      outcall_fit_score,
      priority,
      search_terms::text as search_terms_json,
      search_signals::text as search_signals_json,
      target_home_services,
      target_medical,
      target_legal,
      target_events,
      target_corporate,
      target_industrial,
      meta_data::text as meta_data_json,
      is_active,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from benefactor.benefactor_icps)SQL";

struct BenefactorIcpsRow {
    std::string id;
    std::string slug;
    std::string name;
    std::string category;
    std::string service_category;
    std::string description;
    int32_t outcall_fit_score;
    int32_t priority;
    std::string search_terms;
    std::string search_signals;
    bool target_home_services;
    bool target_medical;
    bool target_legal;
    bool target_events;
    bool target_corporate;
    bool target_industrial;
    std::string meta_data;
    bool is_active;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline BenefactorIcpsRow benefactor_icps_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorIcpsRow row;
    (void)is_null;
    row.id = get(0);
    row.slug = get(1);
    row.name = get(2);
    row.category = get(3);
    row.service_category = get(4);
    row.description = get(5);
    row.outcall_fit_score = std::stoi(get(6));
    row.priority = std::stoi(get(7));
    row.search_terms = get(8);
    row.search_signals = get(9);
    row.target_home_services = (get(10) == "t");
    row.target_medical = (get(11) == "t");
    row.target_legal = (get(12) == "t");
    row.target_events = (get(13) == "t");
    row.target_corporate = (get(14) == "t");
    row.target_industrial = (get(15) == "t");
    row.meta_data = get(16);
    row.is_active = (get(17) == "t");
    row.is_soft_deleted = (get(18) == "t");
    row.created_at = get(19);
    row.updated_at = get(20);
    row.created_by = is_null(21) ? std::nullopt : std::optional<std::string>(get(21));
    row.updated_by = is_null(22) ? std::nullopt : std::optional<std::string>(get(22));
    return row;
}
inline std::optional<std::string> validate_benefactor_icps_slug(const std::string& value) {
    if (value.size() > 160) return std::string("benefactor_icps.slug must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_icps_name(const std::string& value) {
    if (value.size() > 220) return std::string("benefactor_icps.name must be at most 220 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_icps_category(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_icps.category must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_icps_service_category(const std::string& value) {
    if (value.size() > 120) return std::string("benefactor_icps.service_category must be at most 120 characters");
    return std::nullopt;
}

inline const char* benefactor_leads_throttling_table = "benefactor.benefactor_leads_throttling";
inline const std::vector<std::string> benefactor_leads_throttling_columns = { "id", "benefactor_lead_id", "email", "request_type", "last_request_at", "next_allowed_at", "request_count", "throttle_window_days", "last_request_source", "meta_data", "is_active", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* benefactor_leads_throttling_select_sql = R"SQL(select
      id::text as id,
      benefactor_lead_id::text as benefactor_lead_id,
      email,
      request_type,
      to_char(last_request_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_request_at,
      to_char(next_allowed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as next_allowed_at,
      request_count,
      throttle_window_days,
      last_request_source,
      meta_data::text as meta_data_json,
      is_active,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from benefactor.benefactor_leads_throttling)SQL";

struct BenefactorLeadsThrottlingRow {
    std::string id;
    std::optional<std::string> benefactor_lead_id;
    std::string email;
    std::string request_type;
    std::string last_request_at;
    std::optional<std::string> next_allowed_at;
    int32_t request_count;
    int32_t throttle_window_days;
    std::optional<std::string> last_request_source;
    std::string meta_data;
    bool is_active;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline BenefactorLeadsThrottlingRow benefactor_leads_throttling_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorLeadsThrottlingRow row;
    (void)is_null;
    row.id = get(0);
    row.benefactor_lead_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.email = get(2);
    row.request_type = get(3);
    row.last_request_at = get(4);
    row.next_allowed_at = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.request_count = std::stoi(get(6));
    row.throttle_window_days = std::stoi(get(7));
    row.last_request_source = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.meta_data = get(9);
    row.is_active = (get(10) == "t");
    row.is_soft_deleted = (get(11) == "t");
    row.created_at = get(12);
    row.updated_at = get(13);
    row.created_by = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.updated_by = is_null(15) ? std::nullopt : std::optional<std::string>(get(15));
    return row;
}
inline std::optional<std::string> validate_benefactor_leads_throttling_email(const std::string& value) {
    if (value.size() > 255) return std::string("benefactor_leads_throttling.email must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_throttling_request_type(const std::string& value) {
    if (value.size() > 100) return std::string("benefactor_leads_throttling.request_type must be at most 100 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_throttling_last_request_source(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_leads_throttling.last_request_source must be at most 80 characters");
    return std::nullopt;
}

inline const char* benefactor_leads_reminders_table = "benefactor.benefactor_leads_reminders";
inline const std::vector<std::string> benefactor_leads_reminders_columns = { "id", "benefactor_lead_id", "reminder_type", "channel", "email", "first_name", "last_name", "subject", "original_request_sent_at", "original_request_message_id", "sent_at", "last_reminder_sent_at", "reminder_count", "last_reminder_message_id", "message_id", "tags", "meta_data", "is_active", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* benefactor_leads_reminders_select_sql = R"SQL(select
      id::text as id,
      benefactor_lead_id::text as benefactor_lead_id,
      reminder_type,
      channel,
      email,
      first_name,
      last_name,
      subject,
      to_char(original_request_sent_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as original_request_sent_at,
      original_request_message_id,
      to_char(sent_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as sent_at,
      to_char(last_reminder_sent_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_reminder_sent_at,
      reminder_count,
      last_reminder_message_id,
      message_id,
      tags::text as tags_json,
      meta_data::text as meta_data_json,
      is_active,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from benefactor.benefactor_leads_reminders)SQL";

struct BenefactorLeadsRemindersRow {
    std::string id;
    std::optional<std::string> benefactor_lead_id;
    std::string reminder_type;
    std::string channel;
    std::string email;
    std::optional<std::string> first_name;
    std::optional<std::string> last_name;
    std::optional<std::string> subject;
    std::string original_request_sent_at;
    std::optional<std::string> original_request_message_id;
    std::string sent_at;
    std::optional<std::string> last_reminder_sent_at;
    int32_t reminder_count;
    std::optional<std::string> last_reminder_message_id;
    std::optional<std::string> message_id;
    std::string tags;
    std::string meta_data;
    bool is_active;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline BenefactorLeadsRemindersRow benefactor_leads_reminders_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BenefactorLeadsRemindersRow row;
    (void)is_null;
    row.id = get(0);
    row.benefactor_lead_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.reminder_type = get(2);
    row.channel = get(3);
    row.email = get(4);
    row.first_name = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.last_name = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.subject = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.original_request_sent_at = get(8);
    row.original_request_message_id = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.sent_at = get(10);
    row.last_reminder_sent_at = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.reminder_count = std::stoi(get(12));
    row.last_reminder_message_id = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    row.message_id = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.tags = get(15);
    row.meta_data = get(16);
    row.is_active = (get(17) == "t");
    row.is_soft_deleted = (get(18) == "t");
    row.created_at = get(19);
    row.updated_at = get(20);
    row.created_by = is_null(21) ? std::nullopt : std::optional<std::string>(get(21));
    row.updated_by = is_null(22) ? std::nullopt : std::optional<std::string>(get(22));
    return row;
}
inline std::optional<std::string> validate_benefactor_leads_reminders_reminder_type(const std::string& value) {
    if (value.size() > 80) return std::string("benefactor_leads_reminders.reminder_type must be at most 80 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_reminders_channel(const std::string& value) {
    if (value.size() > 50) return std::string("benefactor_leads_reminders.channel must be at most 50 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_reminders_email(const std::string& value) {
    if (value.size() > 255) return std::string("benefactor_leads_reminders.email must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_reminders_first_name(const std::string& value) {
    if (value.size() > 160) return std::string("benefactor_leads_reminders.first_name must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_reminders_last_name(const std::string& value) {
    if (value.size() > 160) return std::string("benefactor_leads_reminders.last_name must be at most 160 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_benefactor_leads_reminders_message_id(const std::string& value) {
    if (value.size() > 255) return std::string("benefactor_leads_reminders.message_id must be at most 255 characters");
    return std::nullopt;
}

inline const char* vcs_repositories_table = "vcs_repositories";
inline const std::vector<std::string> vcs_repositories_columns = { "id", "slug", "display_name", "vcs_kind", "remote_url", "default_branch", "mirror_path", "mirror_status", "visibility", "last_synced_at", "last_error", "size_bytes", "ref_count", "meta_data", "is_soft_deleted", "created_at", "updated_at", "created_by", "updated_by" };
inline const char* vcs_repositories_select_sql = R"SQL(select
      id::text as id,
      slug,
      display_name,
      vcs_kind,
      remote_url,
      default_branch,
      mirror_path,
      mirror_status,
      visibility,
      to_char(last_synced_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_synced_at,
      last_error,
      size_bytes,
      ref_count,
      meta_data::text as meta_data_json,
      is_soft_deleted,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      created_by::text as created_by,
      updated_by::text as updated_by
    from vcs_repositories)SQL";

enum class VcsRepositoriesVcsKind { Git, Hg, Svn, Fossil };
inline std::string vcs_repositories_vcs_kind_to_string(VcsRepositoriesVcsKind value) {
    switch (value) {
        case VcsRepositoriesVcsKind::Git: return "git";
        case VcsRepositoriesVcsKind::Hg: return "hg";
        case VcsRepositoriesVcsKind::Svn: return "svn";
        case VcsRepositoriesVcsKind::Fossil: return "fossil";
    }
    return "";
}
inline std::optional<VcsRepositoriesVcsKind> parse_vcs_repositories_vcs_kind(const std::string& value) {
    if (value == "git") return VcsRepositoriesVcsKind::Git;
    if (value == "hg") return VcsRepositoriesVcsKind::Hg;
    if (value == "svn") return VcsRepositoriesVcsKind::Svn;
    if (value == "fossil") return VcsRepositoriesVcsKind::Fossil;
    return std::nullopt;
}

enum class VcsRepositoriesMirrorStatus { Pending, Mirroring, Ready, Error, Disabled };
inline std::string vcs_repositories_mirror_status_to_string(VcsRepositoriesMirrorStatus value) {
    switch (value) {
        case VcsRepositoriesMirrorStatus::Pending: return "pending";
        case VcsRepositoriesMirrorStatus::Mirroring: return "mirroring";
        case VcsRepositoriesMirrorStatus::Ready: return "ready";
        case VcsRepositoriesMirrorStatus::Error: return "error";
        case VcsRepositoriesMirrorStatus::Disabled: return "disabled";
    }
    return "";
}
inline std::optional<VcsRepositoriesMirrorStatus> parse_vcs_repositories_mirror_status(const std::string& value) {
    if (value == "pending") return VcsRepositoriesMirrorStatus::Pending;
    if (value == "mirroring") return VcsRepositoriesMirrorStatus::Mirroring;
    if (value == "ready") return VcsRepositoriesMirrorStatus::Ready;
    if (value == "error") return VcsRepositoriesMirrorStatus::Error;
    if (value == "disabled") return VcsRepositoriesMirrorStatus::Disabled;
    return std::nullopt;
}

enum class VcsRepositoriesVisibility { Private, Internal, Public };
inline std::string vcs_repositories_visibility_to_string(VcsRepositoriesVisibility value) {
    switch (value) {
        case VcsRepositoriesVisibility::Private: return "private";
        case VcsRepositoriesVisibility::Internal: return "internal";
        case VcsRepositoriesVisibility::Public: return "public";
    }
    return "";
}
inline std::optional<VcsRepositoriesVisibility> parse_vcs_repositories_visibility(const std::string& value) {
    if (value == "private") return VcsRepositoriesVisibility::Private;
    if (value == "internal") return VcsRepositoriesVisibility::Internal;
    if (value == "public") return VcsRepositoriesVisibility::Public;
    return std::nullopt;
}

struct VcsRepositoriesRow {
    std::string id;
    std::string slug;
    std::string display_name;
    std::string vcs_kind;
    std::string remote_url;
    std::string default_branch;
    std::optional<std::string> mirror_path;
    std::string mirror_status;
    std::string visibility;
    std::optional<std::string> last_synced_at;
    std::optional<std::string> last_error;
    int64_t size_bytes;
    int32_t ref_count;
    std::string meta_data;
    bool is_soft_deleted;
    std::string created_at;
    std::string updated_at;
    std::optional<std::string> created_by;
    std::optional<std::string> updated_by;
};

inline VcsRepositoriesRow vcs_repositories_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    VcsRepositoriesRow row;
    (void)is_null;
    row.id = get(0);
    row.slug = get(1);
    row.display_name = get(2);
    row.vcs_kind = get(3);
    row.remote_url = get(4);
    row.default_branch = get(5);
    row.mirror_path = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.mirror_status = get(7);
    row.visibility = get(8);
    row.last_synced_at = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.last_error = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.size_bytes = std::stoll(get(11));
    row.ref_count = std::stoi(get(12));
    row.meta_data = get(13);
    row.is_soft_deleted = (get(14) == "t");
    row.created_at = get(15);
    row.updated_at = get(16);
    row.created_by = is_null(17) ? std::nullopt : std::optional<std::string>(get(17));
    row.updated_by = is_null(18) ? std::nullopt : std::optional<std::string>(get(18));
    return row;
}
inline std::optional<std::string> validate_vcs_repositories_slug(const std::string& value) {
    if (value.size() > 120) return std::string("vcs_repositories.slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9._-]{0,119}$)RX"))) return std::string("vcs_repositories.slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_vcs_repositories_display_name(const std::string& value) {
    if (value.size() > 200) return std::string("vcs_repositories.display_name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_vcs_repositories_default_branch(const std::string& value) {
    if (value.size() > 160) return std::string("vcs_repositories.default_branch must be at most 160 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._/-]{1,160}$)RX"))) return std::string("vcs_repositories.default_branch does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_vcs_repositories_size_bytes(int64_t value) {
    if (value < 0) return std::string("vcs_repositories.size_bytes is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_vcs_repositories_ref_count(int32_t value) {
    if (value < 0) return std::string("vcs_repositories.ref_count is below the minimum");
    return std::nullopt;
}

inline const char* vcs_refs_table = "vcs_refs";
inline const std::vector<std::string> vcs_refs_columns = { "id", "repository_id", "ref_name", "ref_type", "target_revision", "is_default", "meta_data", "created_at", "updated_at" };
inline const char* vcs_refs_select_sql = R"SQL(select
      id::text as id,
      repository_id::text as repository_id,
      ref_name,
      ref_type,
      target_revision,
      is_default,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from vcs_refs)SQL";

enum class VcsRefsRefType { Branch, Tag, Bookmark, Head, Other };
inline std::string vcs_refs_ref_type_to_string(VcsRefsRefType value) {
    switch (value) {
        case VcsRefsRefType::Branch: return "branch";
        case VcsRefsRefType::Tag: return "tag";
        case VcsRefsRefType::Bookmark: return "bookmark";
        case VcsRefsRefType::Head: return "head";
        case VcsRefsRefType::Other: return "other";
    }
    return "";
}
inline std::optional<VcsRefsRefType> parse_vcs_refs_ref_type(const std::string& value) {
    if (value == "branch") return VcsRefsRefType::Branch;
    if (value == "tag") return VcsRefsRefType::Tag;
    if (value == "bookmark") return VcsRefsRefType::Bookmark;
    if (value == "head") return VcsRefsRefType::Head;
    if (value == "other") return VcsRefsRefType::Other;
    return std::nullopt;
}

struct VcsRefsRow {
    std::string id;
    std::string repository_id;
    std::string ref_name;
    std::string ref_type;
    std::string target_revision;
    bool is_default;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline VcsRefsRow vcs_refs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    VcsRefsRow row;
    (void)is_null;
    row.id = get(0);
    row.repository_id = get(1);
    row.ref_name = get(2);
    row.ref_type = get(3);
    row.target_revision = get(4);
    row.is_default = (get(5) == "t");
    row.meta_data = get(6);
    row.created_at = get(7);
    row.updated_at = get(8);
    return row;
}
inline std::optional<std::string> validate_vcs_refs_ref_name(const std::string& value) {
    if (value.size() > 255) return std::string("vcs_refs.ref_name must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_vcs_refs_target_revision(const std::string& value) {
    if (value.size() > 120) return std::string("vcs_refs.target_revision must be at most 120 characters");
    return std::nullopt;
}

inline const char* vcs_operations_table = "vcs_operations";
inline const std::vector<std::string> vcs_operations_columns = { "id", "repository_id", "vcs_kind", "op_type", "status", "params", "result_summary", "error", "duration_ms", "requested_by", "created_at", "updated_at" };
inline const char* vcs_operations_select_sql = R"SQL(select
      id::text as id,
      repository_id::text as repository_id,
      vcs_kind,
      op_type,
      status,
      params::text as params_json,
      result_summary::text as result_summary_json,
      error,
      duration_ms,
      requested_by,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from vcs_operations)SQL";

enum class VcsOperationsVcsKind { Git, Hg, Svn, Fossil };
inline std::string vcs_operations_vcs_kind_to_string(VcsOperationsVcsKind value) {
    switch (value) {
        case VcsOperationsVcsKind::Git: return "git";
        case VcsOperationsVcsKind::Hg: return "hg";
        case VcsOperationsVcsKind::Svn: return "svn";
        case VcsOperationsVcsKind::Fossil: return "fossil";
    }
    return "";
}
inline std::optional<VcsOperationsVcsKind> parse_vcs_operations_vcs_kind(const std::string& value) {
    if (value == "git") return VcsOperationsVcsKind::Git;
    if (value == "hg") return VcsOperationsVcsKind::Hg;
    if (value == "svn") return VcsOperationsVcsKind::Svn;
    if (value == "fossil") return VcsOperationsVcsKind::Fossil;
    return std::nullopt;
}

enum class VcsOperationsOpType { Mirror, Fetch, Refs, Log, Show, Diff, Tree, Blob, Probe, Remove };
inline std::string vcs_operations_op_type_to_string(VcsOperationsOpType value) {
    switch (value) {
        case VcsOperationsOpType::Mirror: return "mirror";
        case VcsOperationsOpType::Fetch: return "fetch";
        case VcsOperationsOpType::Refs: return "refs";
        case VcsOperationsOpType::Log: return "log";
        case VcsOperationsOpType::Show: return "show";
        case VcsOperationsOpType::Diff: return "diff";
        case VcsOperationsOpType::Tree: return "tree";
        case VcsOperationsOpType::Blob: return "blob";
        case VcsOperationsOpType::Probe: return "probe";
        case VcsOperationsOpType::Remove: return "remove";
    }
    return "";
}
inline std::optional<VcsOperationsOpType> parse_vcs_operations_op_type(const std::string& value) {
    if (value == "mirror") return VcsOperationsOpType::Mirror;
    if (value == "fetch") return VcsOperationsOpType::Fetch;
    if (value == "refs") return VcsOperationsOpType::Refs;
    if (value == "log") return VcsOperationsOpType::Log;
    if (value == "show") return VcsOperationsOpType::Show;
    if (value == "diff") return VcsOperationsOpType::Diff;
    if (value == "tree") return VcsOperationsOpType::Tree;
    if (value == "blob") return VcsOperationsOpType::Blob;
    if (value == "probe") return VcsOperationsOpType::Probe;
    if (value == "remove") return VcsOperationsOpType::Remove;
    return std::nullopt;
}

enum class VcsOperationsStatus { Pending, Running, Success, Error };
inline std::string vcs_operations_status_to_string(VcsOperationsStatus value) {
    switch (value) {
        case VcsOperationsStatus::Pending: return "pending";
        case VcsOperationsStatus::Running: return "running";
        case VcsOperationsStatus::Success: return "success";
        case VcsOperationsStatus::Error: return "error";
    }
    return "";
}
inline std::optional<VcsOperationsStatus> parse_vcs_operations_status(const std::string& value) {
    if (value == "pending") return VcsOperationsStatus::Pending;
    if (value == "running") return VcsOperationsStatus::Running;
    if (value == "success") return VcsOperationsStatus::Success;
    if (value == "error") return VcsOperationsStatus::Error;
    return std::nullopt;
}

struct VcsOperationsRow {
    std::string id;
    std::optional<std::string> repository_id;
    std::string vcs_kind;
    std::string op_type;
    std::string status;
    std::string params;
    std::string result_summary;
    std::optional<std::string> error;
    std::optional<int32_t> duration_ms;
    std::optional<std::string> requested_by;
    std::string created_at;
    std::string updated_at;
};

inline VcsOperationsRow vcs_operations_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    VcsOperationsRow row;
    (void)is_null;
    row.id = get(0);
    row.repository_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.vcs_kind = get(2);
    row.op_type = get(3);
    row.status = get(4);
    row.params = get(5);
    row.result_summary = get(6);
    row.error = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.duration_ms = is_null(8) ? std::nullopt : std::optional<int32_t>(std::stoi(get(8)));
    row.requested_by = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.created_at = get(10);
    row.updated_at = get(11);
    return row;
}
inline std::optional<std::string> validate_vcs_operations_duration_ms(int32_t value) {
    if (value < 0) return std::string("vcs_operations.duration_ms is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_vcs_operations_requested_by(const std::string& value) {
    if (value.size() > 200) return std::string("vcs_operations.requested_by must be at most 200 characters");
    return std::nullopt;
}

inline const char* agents_table = "ai_agent_bridge.agents";
inline const std::vector<std::string> agents_columns = { "id", "agent_key", "display_name", "kind", "host", "meta_data", "created_at", "updated_at" };
inline const char* agents_select_sql = R"SQL(select
      id::text as id,
      agent_key,
      display_name,
      kind,
      host,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from ai_agent_bridge.agents)SQL";

enum class AgentsKind { Claude, Codex, Human, Other };
inline std::string agents_kind_to_string(AgentsKind value) {
    switch (value) {
        case AgentsKind::Claude: return "claude";
        case AgentsKind::Codex: return "codex";
        case AgentsKind::Human: return "human";
        case AgentsKind::Other: return "other";
    }
    return "";
}
inline std::optional<AgentsKind> parse_agents_kind(const std::string& value) {
    if (value == "claude") return AgentsKind::Claude;
    if (value == "codex") return AgentsKind::Codex;
    if (value == "human") return AgentsKind::Human;
    if (value == "other") return AgentsKind::Other;
    return std::nullopt;
}

struct AgentsRow {
    std::string id;
    std::string agent_key;
    std::string display_name;
    std::string kind;
    std::optional<std::string> host;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline AgentsRow agents_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    AgentsRow row;
    (void)is_null;
    row.id = get(0);
    row.agent_key = get(1);
    row.display_name = get(2);
    row.kind = get(3);
    row.host = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.meta_data = get(5);
    row.created_at = get(6);
    row.updated_at = get(7);
    return row;
}
inline std::optional<std::string> validate_agents_agent_key(const std::string& value) {
    if (value.size() > 120) return std::string("agents.agent_key must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:-]{1,120}$)RX"))) return std::string("agents.agent_key does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_agents_display_name(const std::string& value) {
    if (value.size() > 200) return std::string("agents.display_name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_agents_host(const std::string& value) {
    if (value.size() > 255) return std::string("agents.host must be at most 255 characters");
    return std::nullopt;
}

inline const char* channels_table = "ai_agent_bridge.channels";
inline const std::vector<std::string> channels_columns = { "id", "slug", "topic", "topic_summary", "embedding_model", "embedding", "embedding_dimensions", "status", "created_by", "meta_data", "created_at", "updated_at" };
inline const char* channels_select_sql = R"SQL(select
      id::text as id,
      slug,
      topic,
      topic_summary,
      embedding_model,
      embedding::text as embedding_json,
      embedding_dimensions,
      status,
      created_by,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from ai_agent_bridge.channels)SQL";

enum class ChannelsStatus { Active, Archived };
inline std::string channels_status_to_string(ChannelsStatus value) {
    switch (value) {
        case ChannelsStatus::Active: return "active";
        case ChannelsStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<ChannelsStatus> parse_channels_status(const std::string& value) {
    if (value == "active") return ChannelsStatus::Active;
    if (value == "archived") return ChannelsStatus::Archived;
    return std::nullopt;
}

struct ChannelsRow {
    std::string id;
    std::string slug;
    std::string topic;
    std::optional<std::string> topic_summary;
    std::string embedding_model;
    std::string embedding;
    int32_t embedding_dimensions;
    std::string status;
    std::string created_by;
    std::string meta_data;
    std::string created_at;
    std::string updated_at;
};

inline ChannelsRow channels_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    ChannelsRow row;
    (void)is_null;
    row.id = get(0);
    row.slug = get(1);
    row.topic = get(2);
    row.topic_summary = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.embedding_model = get(4);
    row.embedding = get(5);
    row.embedding_dimensions = std::stoi(get(6));
    row.status = get(7);
    row.created_by = get(8);
    row.meta_data = get(9);
    row.created_at = get(10);
    row.updated_at = get(11);
    return row;
}
inline std::optional<std::string> validate_channels_slug(const std::string& value) {
    if (value.size() > 120) return std::string("channels.slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9._-]{0,119}$)RX"))) return std::string("channels.slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_channels_embedding_model(const std::string& value) {
    if (value.size() > 120) return std::string("channels.embedding_model must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_channels_embedding_dimensions(int32_t value) {
    if (value < 0) return std::string("channels.embedding_dimensions is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_channels_created_by(const std::string& value) {
    if (value.size() > 120) return std::string("channels.created_by must be at most 120 characters");
    return std::nullopt;
}

inline const char* messages_table = "ai_agent_bridge.messages";
inline const std::vector<std::string> messages_columns = { "id", "channel_slug", "channel_id", "seq", "from_agent_key", "role", "content", "meta_data", "created_at" };
inline const char* messages_select_sql = R"SQL(select
      id::text as id,
      channel_slug,
      channel_id::text as channel_id,
      seq,
      from_agent_key,
      role,
      content,
      meta_data::text as meta_data_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from ai_agent_bridge.messages)SQL";

enum class MessagesRole { User, Assistant, System, Tool };
inline std::string messages_role_to_string(MessagesRole value) {
    switch (value) {
        case MessagesRole::User: return "user";
        case MessagesRole::Assistant: return "assistant";
        case MessagesRole::System: return "system";
        case MessagesRole::Tool: return "tool";
    }
    return "";
}
inline std::optional<MessagesRole> parse_messages_role(const std::string& value) {
    if (value == "user") return MessagesRole::User;
    if (value == "assistant") return MessagesRole::Assistant;
    if (value == "system") return MessagesRole::System;
    if (value == "tool") return MessagesRole::Tool;
    return std::nullopt;
}

struct MessagesRow {
    std::string id;
    std::string channel_slug;
    std::optional<std::string> channel_id;
    int64_t seq;
    std::string from_agent_key;
    std::string role;
    std::string content;
    std::string meta_data;
    std::string created_at;
};

inline MessagesRow messages_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    MessagesRow row;
    (void)is_null;
    row.id = get(0);
    row.channel_slug = get(1);
    row.channel_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.seq = std::stoll(get(3));
    row.from_agent_key = get(4);
    row.role = get(5);
    row.content = get(6);
    row.meta_data = get(7);
    row.created_at = get(8);
    return row;
}
inline std::optional<std::string> validate_messages_channel_slug(const std::string& value) {
    if (value.size() > 120) return std::string("messages.channel_slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9._-]{0,119}$)RX"))) return std::string("messages.channel_slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_messages_seq(int64_t value) {
    if (value < 1) return std::string("messages.seq is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_messages_from_agent_key(const std::string& value) {
    if (value.size() > 120) return std::string("messages.from_agent_key must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:-]{1,120}$)RX"))) return std::string("messages.from_agent_key does not match the required pattern");
    return std::nullopt;
}

inline const char* channel_members_table = "ai_agent_bridge.channel_members";
inline const std::vector<std::string> channel_members_columns = { "id", "channel_slug", "channel_id", "agent_key", "role", "joined_at", "last_seen_at", "meta_data" };
inline const char* channel_members_select_sql = R"SQL(select
      id::text as id,
      channel_slug,
      channel_id::text as channel_id,
      agent_key,
      role,
      to_char(joined_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as joined_at,
      to_char(last_seen_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_seen_at,
      meta_data::text as meta_data_json
    from ai_agent_bridge.channel_members)SQL";

enum class ChannelMembersRole { Owner, Member, Observer };
inline std::string channel_members_role_to_string(ChannelMembersRole value) {
    switch (value) {
        case ChannelMembersRole::Owner: return "owner";
        case ChannelMembersRole::Member: return "member";
        case ChannelMembersRole::Observer: return "observer";
    }
    return "";
}
inline std::optional<ChannelMembersRole> parse_channel_members_role(const std::string& value) {
    if (value == "owner") return ChannelMembersRole::Owner;
    if (value == "member") return ChannelMembersRole::Member;
    if (value == "observer") return ChannelMembersRole::Observer;
    return std::nullopt;
}

struct ChannelMembersRow {
    std::string id;
    std::string channel_slug;
    std::optional<std::string> channel_id;
    std::string agent_key;
    std::string role;
    std::string joined_at;
    std::string last_seen_at;
    std::string meta_data;
};

inline ChannelMembersRow channel_members_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    ChannelMembersRow row;
    (void)is_null;
    row.id = get(0);
    row.channel_slug = get(1);
    row.channel_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.agent_key = get(3);
    row.role = get(4);
    row.joined_at = get(5);
    row.last_seen_at = get(6);
    row.meta_data = get(7);
    return row;
}
inline std::optional<std::string> validate_channel_members_channel_slug(const std::string& value) {
    if (value.size() > 120) return std::string("channel_members.channel_slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9._-]{0,119}$)RX"))) return std::string("channel_members.channel_slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_channel_members_agent_key(const std::string& value) {
    if (value.size() > 120) return std::string("channel_members.agent_key must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:-]{1,120}$)RX"))) return std::string("channel_members.agent_key does not match the required pattern");
    return std::nullopt;
}

inline const char* shared_context_table = "ai_agent_bridge.shared_context";
inline const std::vector<std::string> shared_context_columns = { "id", "channel_slug", "channel_id", "ctx_key", "value", "version", "updated_by", "created_at", "updated_at" };
inline const char* shared_context_select_sql = R"SQL(select
      id::text as id,
      channel_slug,
      channel_id::text as channel_id,
      ctx_key,
      value::text as value_json,
      version,
      updated_by,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from ai_agent_bridge.shared_context)SQL";

struct SharedContextRow {
    std::string id;
    std::optional<std::string> channel_slug;
    std::optional<std::string> channel_id;
    std::string ctx_key;
    std::string value;
    int32_t version;
    std::string updated_by;
    std::string created_at;
    std::string updated_at;
};

inline SharedContextRow shared_context_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SharedContextRow row;
    (void)is_null;
    row.id = get(0);
    row.channel_slug = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.channel_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.ctx_key = get(3);
    row.value = get(4);
    row.version = std::stoi(get(5));
    row.updated_by = get(6);
    row.created_at = get(7);
    row.updated_at = get(8);
    return row;
}
inline std::optional<std::string> validate_shared_context_channel_slug(const std::string& value) {
    if (value.size() > 120) return std::string("shared_context.channel_slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9._-]{0,119}$)RX"))) return std::string("shared_context.channel_slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_shared_context_ctx_key(const std::string& value) {
    if (value.size() > 200) return std::string("shared_context.ctx_key must be at most 200 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[A-Za-z0-9._:/-]{1,200}$)RX"))) return std::string("shared_context.ctx_key does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_shared_context_version(int32_t value) {
    if (value < 1) return std::string("shared_context.version is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_shared_context_updated_by(const std::string& value) {
    if (value.size() > 120) return std::string("shared_context.updated_by must be at most 120 characters");
    return std::nullopt;
}

inline const char* sync_clock_table = "fiducia.sync_clock";
inline const std::vector<std::string> sync_clock_columns = { "singleton", "last_sequence" };
inline const char* sync_clock_select_sql = R"SQL(select
      singleton,
      last_sequence
    from fiducia.sync_clock)SQL";

struct SyncClockRow {
    bool singleton;
    int64_t last_sequence;
};

inline SyncClockRow sync_clock_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SyncClockRow row;
    (void)is_null;
    row.singleton = (get(0) == "t");
    row.last_sequence = std::stoll(get(1));
    return row;
}
inline std::optional<std::string> validate_sync_clock_last_sequence(int64_t value) {
    if (value < 0) return std::string("sync_clock.last_sequence is below the minimum");
    return std::nullopt;
}

inline const char* sync_tombstones_table = "fiducia.sync_tombstones";
inline const std::vector<std::string> sync_tombstones_columns = { "sequence", "table_name", "row_id", "tenant_id", "owner_user_id", "row_version", "deleted_at" };
inline const char* sync_tombstones_select_sql = R"SQL(select
      sequence,
      table_name,
      row_id,
      tenant_id::text as tenant_id,
      owner_user_id::text as owner_user_id,
      row_version,
      to_char(deleted_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as deleted_at
    from fiducia.sync_tombstones)SQL";

struct SyncTombstonesRow {
    int64_t sequence;
    std::string table_name;
    std::string row_id;
    std::optional<std::string> tenant_id;
    std::optional<std::string> owner_user_id;
    int64_t row_version;
    std::string deleted_at;
};

inline SyncTombstonesRow sync_tombstones_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SyncTombstonesRow row;
    (void)is_null;
    row.sequence = std::stoll(get(0));
    row.table_name = get(1);
    row.row_id = get(2);
    row.tenant_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.owner_user_id = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.row_version = std::stoll(get(5));
    row.deleted_at = get(6);
    return row;
}
inline std::optional<std::string> validate_sync_tombstones_sequence(int64_t value) {
    if (value < 1) return std::string("sync_tombstones.sequence is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_sync_tombstones_row_version(int64_t value) {
    if (value < 1) return std::string("sync_tombstones.row_version is below the minimum");
    return std::nullopt;
}

inline const char* orgs_table = "fiducia.orgs";
inline const std::vector<std::string> orgs_columns = { "id", "slug", "name", "created_at", "updated_at", "version", "sync_sequence" };
inline const char* orgs_select_sql = R"SQL(select
      id::text as id,
      slug,
      name,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      version,
      sync_sequence
    from fiducia.orgs)SQL";

struct OrgsRow {
    std::string id;
    std::string slug;
    std::string name;
    std::string created_at;
    std::string updated_at;
    int64_t version;
    int64_t sync_sequence;
};

inline OrgsRow orgs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    OrgsRow row;
    (void)is_null;
    row.id = get(0);
    row.slug = get(1);
    row.name = get(2);
    row.created_at = get(3);
    row.updated_at = get(4);
    row.version = std::stoll(get(5));
    row.sync_sequence = std::stoll(get(6));
    return row;
}
inline std::optional<std::string> validate_orgs_slug(const std::string& value) {
    if (value.size() > 120) return std::string("orgs.slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9-]{1,118}[a-z0-9]$)RX"))) return std::string("orgs.slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_orgs_name(const std::string& value) {
    if (value.size() > 200) return std::string("orgs.name must be at most 200 characters");
    return std::nullopt;
}

inline const char* projects_table = "fiducia.projects";
inline const std::vector<std::string> projects_columns = { "id", "org_id", "slug", "name", "created_at", "updated_at", "version", "sync_sequence" };
inline const char* projects_select_sql = R"SQL(select
      id::text as id,
      org_id::text as org_id,
      slug,
      name,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      version,
      sync_sequence
    from fiducia.projects)SQL";

struct ProjectsRow {
    std::string id;
    std::string org_id;
    std::string slug;
    std::string name;
    std::string created_at;
    std::string updated_at;
    int64_t version;
    int64_t sync_sequence;
};

inline ProjectsRow projects_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    ProjectsRow row;
    (void)is_null;
    row.id = get(0);
    row.org_id = get(1);
    row.slug = get(2);
    row.name = get(3);
    row.created_at = get(4);
    row.updated_at = get(5);
    row.version = std::stoll(get(6));
    row.sync_sequence = std::stoll(get(7));
    return row;
}
inline std::optional<std::string> validate_projects_slug(const std::string& value) {
    if (value.size() > 120) return std::string("projects.slug must be at most 120 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z0-9][a-z0-9-]{1,118}[a-z0-9]$)RX"))) return std::string("projects.slug does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_projects_name(const std::string& value) {
    if (value.size() > 200) return std::string("projects.name must be at most 200 characters");
    return std::nullopt;
}

inline const char* users_table = "fiducia.users";
inline const std::vector<std::string> users_columns = { "id", "supabase_user_id", "email", "created_at" };
inline const char* users_select_sql = R"SQL(select
      id::text as id,
      supabase_user_id::text as supabase_user_id,
      email,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from fiducia.users)SQL";

struct UsersRow {
    std::string id;
    std::string supabase_user_id;
    std::string email;
    std::string created_at;
};

inline UsersRow users_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    UsersRow row;
    (void)is_null;
    row.id = get(0);
    row.supabase_user_id = get(1);
    row.email = get(2);
    row.created_at = get(3);
    return row;
}
inline std::optional<std::string> validate_users_email(const std::string& value) {
    if (value.size() > 320) return std::string("users.email must be at most 320 characters");
    return std::nullopt;
}

inline const char* org_members_table = "fiducia.org_members";
inline const std::vector<std::string> org_members_columns = { "org_id", "user_id", "role", "created_at" };
inline const char* org_members_select_sql = R"SQL(select
      org_id::text as org_id,
      user_id::text as user_id,
      role,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from fiducia.org_members)SQL";

enum class OrgMembersRole { Owner, Admin, Member };
inline std::string org_members_role_to_string(OrgMembersRole value) {
    switch (value) {
        case OrgMembersRole::Owner: return "owner";
        case OrgMembersRole::Admin: return "admin";
        case OrgMembersRole::Member: return "member";
    }
    return "";
}
inline std::optional<OrgMembersRole> parse_org_members_role(const std::string& value) {
    if (value == "owner") return OrgMembersRole::Owner;
    if (value == "admin") return OrgMembersRole::Admin;
    if (value == "member") return OrgMembersRole::Member;
    return std::nullopt;
}

struct OrgMembersRow {
    std::string org_id;
    std::string user_id;
    std::string role;
    std::string created_at;
};

inline OrgMembersRow org_members_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    OrgMembersRow row;
    (void)is_null;
    row.org_id = get(0);
    row.user_id = get(1);
    row.role = get(2);
    row.created_at = get(3);
    return row;
}

inline const char* project_members_table = "fiducia.project_members";
inline const std::vector<std::string> project_members_columns = { "project_id", "user_id", "role", "created_at" };
inline const char* project_members_select_sql = R"SQL(select
      project_id::text as project_id,
      user_id::text as user_id,
      role,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from fiducia.project_members)SQL";

enum class ProjectMembersRole { Admin, Operator, Viewer };
inline std::string project_members_role_to_string(ProjectMembersRole value) {
    switch (value) {
        case ProjectMembersRole::Admin: return "admin";
        case ProjectMembersRole::Operator: return "operator";
        case ProjectMembersRole::Viewer: return "viewer";
    }
    return "";
}
inline std::optional<ProjectMembersRole> parse_project_members_role(const std::string& value) {
    if (value == "admin") return ProjectMembersRole::Admin;
    if (value == "operator") return ProjectMembersRole::Operator;
    if (value == "viewer") return ProjectMembersRole::Viewer;
    return std::nullopt;
}

struct ProjectMembersRow {
    std::string project_id;
    std::string user_id;
    std::string role;
    std::string created_at;
};

inline ProjectMembersRow project_members_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    ProjectMembersRow row;
    (void)is_null;
    row.project_id = get(0);
    row.user_id = get(1);
    row.role = get(2);
    row.created_at = get(3);
    return row;
}

inline const char* api_keys_table = "fiducia.api_keys";
inline const std::vector<std::string> api_keys_columns = { "id", "key_id", "org_id", "project_id", "created_by_user_id", "name", "secret_hash", "scopes", "env", "require_idempotency", "mtls_required", "revoked", "created_at", "updated_at", "version", "sync_sequence", "last_used_at", "expires_at" };
inline const char* api_keys_select_sql = R"SQL(select
      id::text as id,
      key_id,
      org_id::text as org_id,
      project_id::text as project_id,
      created_by_user_id::text as created_by_user_id,
      name,
      secret_hash,
      scopes::text as scopes_json,
      env,
      require_idempotency,
      mtls_required,
      revoked,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      version,
      sync_sequence,
      to_char(last_used_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_used_at,
      to_char(expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as expires_at
    from fiducia.api_keys)SQL";

enum class ApiKeysEnv { Live, Test };
inline std::string api_keys_env_to_string(ApiKeysEnv value) {
    switch (value) {
        case ApiKeysEnv::Live: return "live";
        case ApiKeysEnv::Test: return "test";
    }
    return "";
}
inline std::optional<ApiKeysEnv> parse_api_keys_env(const std::string& value) {
    if (value == "live") return ApiKeysEnv::Live;
    if (value == "test") return ApiKeysEnv::Test;
    return std::nullopt;
}

struct ApiKeysRow {
    std::string id;
    std::string key_id;
    std::string org_id;
    std::optional<std::string> project_id;
    std::optional<std::string> created_by_user_id;
    std::string name;
    std::string secret_hash;
    std::string scopes;
    std::string env;
    bool require_idempotency;
    bool mtls_required;
    bool revoked;
    std::string created_at;
    std::string updated_at;
    int64_t version;
    int64_t sync_sequence;
    std::optional<std::string> last_used_at;
    std::optional<std::string> expires_at;
};

inline ApiKeysRow api_keys_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    ApiKeysRow row;
    (void)is_null;
    row.id = get(0);
    row.key_id = get(1);
    row.org_id = get(2);
    row.project_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.created_by_user_id = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.name = get(5);
    row.secret_hash = get(6);
    row.scopes = get(7);
    row.env = get(8);
    row.require_idempotency = (get(9) == "t");
    row.mtls_required = (get(10) == "t");
    row.revoked = (get(11) == "t");
    row.created_at = get(12);
    row.updated_at = get(13);
    row.version = std::stoll(get(14));
    row.sync_sequence = std::stoll(get(15));
    row.last_used_at = is_null(16) ? std::nullopt : std::optional<std::string>(get(16));
    row.expires_at = is_null(17) ? std::nullopt : std::optional<std::string>(get(17));
    return row;
}
inline std::optional<std::string> validate_api_keys_key_id(const std::string& value) {
    if (value.size() > 64) return std::string("api_keys.key_id must be at most 64 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_api_keys_name(const std::string& value) {
    if (value.size() > 200) return std::string("api_keys.name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_api_keys_secret_hash(const std::string& value) {
    if (value.size() > 255) return std::string("api_keys.secret_hash must be at most 255 characters");
    return std::nullopt;
}

inline const char* mtls_client_certs_table = "fiducia.mtls_client_certs";
inline const std::vector<std::string> mtls_client_certs_columns = { "id", "org_id", "project_id", "name", "subject", "sha256_fingerprint", "not_before", "not_after", "revoked", "created_at", "updated_at", "version", "sync_sequence" };
inline const char* mtls_client_certs_select_sql = R"SQL(select
      id::text as id,
      org_id::text as org_id,
      project_id::text as project_id,
      name,
      subject,
      sha256_fingerprint,
      to_char(not_before at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as not_before,
      to_char(not_after at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as not_after,
      revoked,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      version,
      sync_sequence
    from fiducia.mtls_client_certs)SQL";

struct MtlsClientCertsRow {
    std::string id;
    std::string org_id;
    std::optional<std::string> project_id;
    std::string name;
    std::string subject;
    std::string sha256_fingerprint;
    std::optional<std::string> not_before;
    std::optional<std::string> not_after;
    bool revoked;
    std::string created_at;
    std::string updated_at;
    int64_t version;
    int64_t sync_sequence;
};

inline MtlsClientCertsRow mtls_client_certs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    MtlsClientCertsRow row;
    (void)is_null;
    row.id = get(0);
    row.org_id = get(1);
    row.project_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.name = get(3);
    row.subject = get(4);
    row.sha256_fingerprint = get(5);
    row.not_before = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.not_after = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.revoked = (get(8) == "t");
    row.created_at = get(9);
    row.updated_at = get(10);
    row.version = std::stoll(get(11));
    row.sync_sequence = std::stoll(get(12));
    return row;
}
inline std::optional<std::string> validate_mtls_client_certs_name(const std::string& value) {
    if (value.size() > 200) return std::string("mtls_client_certs.name must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mtls_client_certs_subject(const std::string& value) {
    if (value.size() > 500) return std::string("mtls_client_certs.subject must be at most 500 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_mtls_client_certs_sha256_fingerprint(const std::string& value) {
    if (value.size() > 95) return std::string("mtls_client_certs.sha256_fingerprint must be at most 95 characters");
    return std::nullopt;
}

inline const char* customer_preferences_table = "fiducia.customer_preferences";
inline const std::vector<std::string> customer_preferences_columns = { "user_id", "density", "timezone", "region", "notify_key_rotation", "notify_lock_contention", "notify_mfa", "updated_at", "version", "sync_sequence" };
inline const char* customer_preferences_select_sql = R"SQL(select
      user_id::text as user_id,
      density,
      timezone,
      region,
      notify_key_rotation,
      notify_lock_contention,
      notify_mfa,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      version,
      sync_sequence
    from fiducia.customer_preferences)SQL";

enum class CustomerPreferencesDensity { Comfortable, Compact };
inline std::string customer_preferences_density_to_string(CustomerPreferencesDensity value) {
    switch (value) {
        case CustomerPreferencesDensity::Comfortable: return "comfortable";
        case CustomerPreferencesDensity::Compact: return "compact";
    }
    return "";
}
inline std::optional<CustomerPreferencesDensity> parse_customer_preferences_density(const std::string& value) {
    if (value == "comfortable") return CustomerPreferencesDensity::Comfortable;
    if (value == "compact") return CustomerPreferencesDensity::Compact;
    return std::nullopt;
}

struct CustomerPreferencesRow {
    std::string user_id;
    std::string density;
    std::string timezone;
    std::string region;
    bool notify_key_rotation;
    bool notify_lock_contention;
    bool notify_mfa;
    std::string updated_at;
    int64_t version;
    int64_t sync_sequence;
};

inline CustomerPreferencesRow customer_preferences_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    CustomerPreferencesRow row;
    (void)is_null;
    row.user_id = get(0);
    row.density = get(1);
    row.timezone = get(2);
    row.region = get(3);
    row.notify_key_rotation = (get(4) == "t");
    row.notify_lock_contention = (get(5) == "t");
    row.notify_mfa = (get(6) == "t");
    row.updated_at = get(7);
    row.version = std::stoll(get(8));
    row.sync_sequence = std::stoll(get(9));
    return row;
}
inline std::optional<std::string> validate_customer_preferences_timezone(const std::string& value) {
    if (value.size() > 64) return std::string("customer_preferences.timezone must be at most 64 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_customer_preferences_region(const std::string& value) {
    if (value.size() > 16) return std::string("customer_preferences.region must be at most 16 characters");
    return std::nullopt;
}

inline const char* customer_sessions_table = "fiducia.customer_sessions";
inline const std::vector<std::string> customer_sessions_columns = { "id", "user_id", "device", "location", "last_seen", "status", "updated_at", "version", "sync_sequence" };
inline const char* customer_sessions_select_sql = R"SQL(select
      id::text as id,
      user_id::text as user_id,
      device,
      location,
      to_char(last_seen at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_seen,
      status,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      version,
      sync_sequence
    from fiducia.customer_sessions)SQL";

enum class CustomerSessionsStatus { Active, Verified, Revoked };
inline std::string customer_sessions_status_to_string(CustomerSessionsStatus value) {
    switch (value) {
        case CustomerSessionsStatus::Active: return "active";
        case CustomerSessionsStatus::Verified: return "verified";
        case CustomerSessionsStatus::Revoked: return "revoked";
    }
    return "";
}
inline std::optional<CustomerSessionsStatus> parse_customer_sessions_status(const std::string& value) {
    if (value == "active") return CustomerSessionsStatus::Active;
    if (value == "verified") return CustomerSessionsStatus::Verified;
    if (value == "revoked") return CustomerSessionsStatus::Revoked;
    return std::nullopt;
}

struct CustomerSessionsRow {
    std::string id;
    std::string user_id;
    std::string device;
    std::optional<std::string> location;
    std::string last_seen;
    std::string status;
    std::string updated_at;
    int64_t version;
    int64_t sync_sequence;
};

inline CustomerSessionsRow customer_sessions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    CustomerSessionsRow row;
    (void)is_null;
    row.id = get(0);
    row.user_id = get(1);
    row.device = get(2);
    row.location = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.last_seen = get(4);
    row.status = get(5);
    row.updated_at = get(6);
    row.version = std::stoll(get(7));
    row.sync_sequence = std::stoll(get(8));
    return row;
}
inline std::optional<std::string> validate_customer_sessions_device(const std::string& value) {
    if (value.size() > 200) return std::string("customer_sessions.device must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_customer_sessions_location(const std::string& value) {
    if (value.size() > 200) return std::string("customer_sessions.location must be at most 200 characters");
    return std::nullopt;
}

inline const char* audit_log_table = "fiducia.audit_log";
inline const std::vector<std::string> audit_log_columns = { "id", "org_id", "project_id", "actor_user_id", "actor_key_id", "actor", "action", "target", "request_id", "source_ip", "user_agent", "meta", "created_at", "retention_expires_at" };
inline const char* audit_log_select_sql = R"SQL(select
      id::text as id,
      org_id::text as org_id,
      project_id::text as project_id,
      actor_user_id::text as actor_user_id,
      actor_key_id::text as actor_key_id,
      actor,
      action,
      target,
      request_id,
      source_ip,
      user_agent,
      meta::text as meta_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(retention_expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as retention_expires_at
    from fiducia.audit_log)SQL";

struct AuditLogRow {
    std::string id;
    std::optional<std::string> org_id;
    std::optional<std::string> project_id;
    std::optional<std::string> actor_user_id;
    std::optional<std::string> actor_key_id;
    std::optional<std::string> actor;
    std::string action;
    std::optional<std::string> target;
    std::optional<std::string> request_id;
    std::optional<std::string> source_ip;
    std::optional<std::string> user_agent;
    std::string meta;
    std::string created_at;
    std::optional<std::string> retention_expires_at;
};

inline AuditLogRow audit_log_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    AuditLogRow row;
    (void)is_null;
    row.id = get(0);
    row.org_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.project_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.actor_user_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.actor_key_id = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.actor = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.action = get(6);
    row.target = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.request_id = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.source_ip = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.user_agent = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.meta = get(11);
    row.created_at = get(12);
    row.retention_expires_at = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    return row;
}
inline std::optional<std::string> validate_audit_log_actor(const std::string& value) {
    if (value.size() > 320) return std::string("audit_log.actor must be at most 320 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_audit_log_action(const std::string& value) {
    if (value.size() > 120) return std::string("audit_log.action must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_audit_log_target(const std::string& value) {
    if (value.size() > 320) return std::string("audit_log.target must be at most 320 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_audit_log_request_id(const std::string& value) {
    if (value.size() > 120) return std::string("audit_log.request_id must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_audit_log_source_ip(const std::string& value) {
    if (value.size() > 64) return std::string("audit_log.source_ip must be at most 64 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_audit_log_user_agent(const std::string& value) {
    if (value.size() > 500) return std::string("audit_log.user_agent must be at most 500 characters");
    return std::nullopt;
}

inline const char* customer_notifications_table = "fiducia.customer_notifications";
inline const std::vector<std::string> customer_notifications_columns = { "id", "user_id", "org_id", "kind", "severity", "title", "body", "link", "read_at", "created_at", "updated_at", "version", "sync_sequence" };
inline const char* customer_notifications_select_sql = R"SQL(select
      id::text as id,
      user_id::text as user_id,
      org_id::text as org_id,
      kind,
      severity,
      title,
      body,
      link,
      to_char(read_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as read_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      version,
      sync_sequence
    from fiducia.customer_notifications)SQL";

enum class CustomerNotificationsSeverity { Info, Success, Warning, Critical };
inline std::string customer_notifications_severity_to_string(CustomerNotificationsSeverity value) {
    switch (value) {
        case CustomerNotificationsSeverity::Info: return "info";
        case CustomerNotificationsSeverity::Success: return "success";
        case CustomerNotificationsSeverity::Warning: return "warning";
        case CustomerNotificationsSeverity::Critical: return "critical";
    }
    return "";
}
inline std::optional<CustomerNotificationsSeverity> parse_customer_notifications_severity(const std::string& value) {
    if (value == "info") return CustomerNotificationsSeverity::Info;
    if (value == "success") return CustomerNotificationsSeverity::Success;
    if (value == "warning") return CustomerNotificationsSeverity::Warning;
    if (value == "critical") return CustomerNotificationsSeverity::Critical;
    return std::nullopt;
}

struct CustomerNotificationsRow {
    std::string id;
    std::string user_id;
    std::optional<std::string> org_id;
    std::string kind;
    std::string severity;
    std::string title;
    std::string body;
    std::optional<std::string> link;
    std::optional<std::string> read_at;
    std::string created_at;
    std::string updated_at;
    int64_t version;
    int64_t sync_sequence;
};

inline CustomerNotificationsRow customer_notifications_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    CustomerNotificationsRow row;
    (void)is_null;
    row.id = get(0);
    row.user_id = get(1);
    row.org_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.kind = get(3);
    row.severity = get(4);
    row.title = get(5);
    row.body = get(6);
    row.link = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.read_at = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.created_at = get(9);
    row.updated_at = get(10);
    row.version = std::stoll(get(11));
    row.sync_sequence = std::stoll(get(12));
    return row;
}
inline std::optional<std::string> validate_customer_notifications_kind(const std::string& value) {
    if (value.size() > 40) return std::string("customer_notifications.kind must be at most 40 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z][a-z0-9_.]{1,38}[a-z0-9]$)RX"))) return std::string("customer_notifications.kind does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_customer_notifications_title(const std::string& value) {
    if (value.size() > 200) return std::string("customer_notifications.title must be at most 200 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_customer_notifications_body(const std::string& value) {
    if (value.size() > 2000) return std::string("customer_notifications.body must be at most 2000 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_customer_notifications_link(const std::string& value) {
    if (value.size() > 500) return std::string("customer_notifications.link must be at most 500 characters");
    return std::nullopt;
}

inline const char* sync_idempotency_keys_table = "fiducia.sync_idempotency_keys";
inline const std::vector<std::string> sync_idempotency_keys_columns = { "key", "request_fingerprint", "committed_version", "created_at" };
inline const char* sync_idempotency_keys_select_sql = R"SQL(select
      key,
      request_fingerprint,
      committed_version,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from fiducia.sync_idempotency_keys)SQL";

struct SyncIdempotencyKeysRow {
    std::string key;
    std::string request_fingerprint;
    std::optional<int64_t> committed_version;
    std::string created_at;
};

inline SyncIdempotencyKeysRow sync_idempotency_keys_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SyncIdempotencyKeysRow row;
    (void)is_null;
    row.key = get(0);
    row.request_fingerprint = get(1);
    row.committed_version = is_null(2) ? std::nullopt : std::optional<int64_t>(std::stoll(get(2)));
    row.created_at = get(3);
    return row;
}
inline std::optional<std::string> validate_sync_idempotency_keys_request_fingerprint(const std::string& value) {
    if (value.size() > 64) return std::string("sync_idempotency_keys.request_fingerprint must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9a-f]{64}$)RX"))) return std::string("sync_idempotency_keys.request_fingerprint does not match the required pattern");
    return std::nullopt;
}

inline const char* billing_customers_table = "fiducia.billing_customers";
inline const std::vector<std::string> billing_customers_columns = { "id", "org_id", "provider", "provider_customer_id", "email", "created_at", "updated_at" };
inline const char* billing_customers_select_sql = R"SQL(select
      id::text as id,
      org_id::text as org_id,
      provider,
      provider_customer_id,
      email,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from fiducia.billing_customers)SQL";

enum class BillingCustomersProvider { Stripe, Paypal };
inline std::string billing_customers_provider_to_string(BillingCustomersProvider value) {
    switch (value) {
        case BillingCustomersProvider::Stripe: return "stripe";
        case BillingCustomersProvider::Paypal: return "paypal";
    }
    return "";
}
inline std::optional<BillingCustomersProvider> parse_billing_customers_provider(const std::string& value) {
    if (value == "stripe") return BillingCustomersProvider::Stripe;
    if (value == "paypal") return BillingCustomersProvider::Paypal;
    return std::nullopt;
}

struct BillingCustomersRow {
    std::string id;
    std::string org_id;
    std::string provider;
    std::string provider_customer_id;
    std::optional<std::string> email;
    std::string created_at;
    std::string updated_at;
};

inline BillingCustomersRow billing_customers_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BillingCustomersRow row;
    (void)is_null;
    row.id = get(0);
    row.org_id = get(1);
    row.provider = get(2);
    row.provider_customer_id = get(3);
    row.email = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.created_at = get(5);
    row.updated_at = get(6);
    return row;
}
inline std::optional<std::string> validate_billing_customers_provider_customer_id(const std::string& value) {
    if (value.size() > 255) return std::string("billing_customers.provider_customer_id must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_billing_customers_email(const std::string& value) {
    if (value.size() > 320) return std::string("billing_customers.email must be at most 320 characters");
    return std::nullopt;
}

inline const char* payment_methods_table = "fiducia.payment_methods";
inline const std::vector<std::string> payment_methods_columns = { "id", "org_id", "billing_customer_id", "provider", "provider_payment_method_id", "kind", "brand", "last4", "exp_month", "exp_year", "is_default", "created_at", "updated_at" };
inline const char* payment_methods_select_sql = R"SQL(select
      id::text as id,
      org_id::text as org_id,
      billing_customer_id::text as billing_customer_id,
      provider,
      provider_payment_method_id,
      kind,
      brand,
      last4,
      exp_month,
      exp_year,
      is_default,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from fiducia.payment_methods)SQL";

enum class PaymentMethodsProvider { Stripe, Paypal };
inline std::string payment_methods_provider_to_string(PaymentMethodsProvider value) {
    switch (value) {
        case PaymentMethodsProvider::Stripe: return "stripe";
        case PaymentMethodsProvider::Paypal: return "paypal";
    }
    return "";
}
inline std::optional<PaymentMethodsProvider> parse_payment_methods_provider(const std::string& value) {
    if (value == "stripe") return PaymentMethodsProvider::Stripe;
    if (value == "paypal") return PaymentMethodsProvider::Paypal;
    return std::nullopt;
}

struct PaymentMethodsRow {
    std::string id;
    std::string org_id;
    std::string billing_customer_id;
    std::string provider;
    std::string provider_payment_method_id;
    std::string kind;
    std::optional<std::string> brand;
    std::optional<std::string> last4;
    std::optional<int32_t> exp_month;
    std::optional<int32_t> exp_year;
    bool is_default;
    std::string created_at;
    std::string updated_at;
};

inline PaymentMethodsRow payment_methods_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    PaymentMethodsRow row;
    (void)is_null;
    row.id = get(0);
    row.org_id = get(1);
    row.billing_customer_id = get(2);
    row.provider = get(3);
    row.provider_payment_method_id = get(4);
    row.kind = get(5);
    row.brand = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.last4 = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.exp_month = is_null(8) ? std::nullopt : std::optional<int32_t>(std::stoi(get(8)));
    row.exp_year = is_null(9) ? std::nullopt : std::optional<int32_t>(std::stoi(get(9)));
    row.is_default = (get(10) == "t");
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}
inline std::optional<std::string> validate_payment_methods_provider_payment_method_id(const std::string& value) {
    if (value.size() > 255) return std::string("payment_methods.provider_payment_method_id must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_payment_methods_kind(const std::string& value) {
    if (value.size() > 32) return std::string("payment_methods.kind must be at most 32 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_payment_methods_brand(const std::string& value) {
    if (value.size() > 32) return std::string("payment_methods.brand must be at most 32 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_payment_methods_last4(const std::string& value) {
    if (value.size() > 4) return std::string("payment_methods.last4 must be at most 4 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9]{4}$)RX"))) return std::string("payment_methods.last4 does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_payment_methods_exp_month(int32_t value) {
    if (value < 1) return std::string("payment_methods.exp_month is below the minimum");
    if (value > 12) return std::string("payment_methods.exp_month is above the maximum");
    return std::nullopt;
}

inline const char* billing_subscriptions_table = "fiducia.billing_subscriptions";
inline const std::vector<std::string> billing_subscriptions_columns = { "id", "org_id", "billing_customer_id", "provider", "provider_subscription_id", "plan", "status", "current_period_start", "current_period_end", "cancel_at_period_end", "canceled_at", "created_at", "updated_at" };
inline const char* billing_subscriptions_select_sql = R"SQL(select
      id::text as id,
      org_id::text as org_id,
      billing_customer_id::text as billing_customer_id,
      provider,
      provider_subscription_id,
      plan,
      status,
      to_char(current_period_start at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as current_period_start,
      to_char(current_period_end at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as current_period_end,
      cancel_at_period_end,
      to_char(canceled_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as canceled_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from fiducia.billing_subscriptions)SQL";

enum class BillingSubscriptionsProvider { Stripe, Paypal };
inline std::string billing_subscriptions_provider_to_string(BillingSubscriptionsProvider value) {
    switch (value) {
        case BillingSubscriptionsProvider::Stripe: return "stripe";
        case BillingSubscriptionsProvider::Paypal: return "paypal";
    }
    return "";
}
inline std::optional<BillingSubscriptionsProvider> parse_billing_subscriptions_provider(const std::string& value) {
    if (value == "stripe") return BillingSubscriptionsProvider::Stripe;
    if (value == "paypal") return BillingSubscriptionsProvider::Paypal;
    return std::nullopt;
}

enum class BillingSubscriptionsStatus { Trialing, Active, PastDue, Canceled, Unpaid, Incomplete, IncompleteExpired, Paused };
inline std::string billing_subscriptions_status_to_string(BillingSubscriptionsStatus value) {
    switch (value) {
        case BillingSubscriptionsStatus::Trialing: return "trialing";
        case BillingSubscriptionsStatus::Active: return "active";
        case BillingSubscriptionsStatus::PastDue: return "past_due";
        case BillingSubscriptionsStatus::Canceled: return "canceled";
        case BillingSubscriptionsStatus::Unpaid: return "unpaid";
        case BillingSubscriptionsStatus::Incomplete: return "incomplete";
        case BillingSubscriptionsStatus::IncompleteExpired: return "incomplete_expired";
        case BillingSubscriptionsStatus::Paused: return "paused";
    }
    return "";
}
inline std::optional<BillingSubscriptionsStatus> parse_billing_subscriptions_status(const std::string& value) {
    if (value == "trialing") return BillingSubscriptionsStatus::Trialing;
    if (value == "active") return BillingSubscriptionsStatus::Active;
    if (value == "past_due") return BillingSubscriptionsStatus::PastDue;
    if (value == "canceled") return BillingSubscriptionsStatus::Canceled;
    if (value == "unpaid") return BillingSubscriptionsStatus::Unpaid;
    if (value == "incomplete") return BillingSubscriptionsStatus::Incomplete;
    if (value == "incomplete_expired") return BillingSubscriptionsStatus::IncompleteExpired;
    if (value == "paused") return BillingSubscriptionsStatus::Paused;
    return std::nullopt;
}

struct BillingSubscriptionsRow {
    std::string id;
    std::string org_id;
    std::string billing_customer_id;
    std::string provider;
    std::string provider_subscription_id;
    std::string plan;
    std::string status;
    std::optional<std::string> current_period_start;
    std::optional<std::string> current_period_end;
    bool cancel_at_period_end;
    std::optional<std::string> canceled_at;
    std::string created_at;
    std::string updated_at;
};

inline BillingSubscriptionsRow billing_subscriptions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BillingSubscriptionsRow row;
    (void)is_null;
    row.id = get(0);
    row.org_id = get(1);
    row.billing_customer_id = get(2);
    row.provider = get(3);
    row.provider_subscription_id = get(4);
    row.plan = get(5);
    row.status = get(6);
    row.current_period_start = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.current_period_end = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.cancel_at_period_end = (get(9) == "t");
    row.canceled_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.created_at = get(11);
    row.updated_at = get(12);
    return row;
}
inline std::optional<std::string> validate_billing_subscriptions_provider_subscription_id(const std::string& value) {
    if (value.size() > 255) return std::string("billing_subscriptions.provider_subscription_id must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_billing_subscriptions_plan(const std::string& value) {
    if (value.size() > 120) return std::string("billing_subscriptions.plan must be at most 120 characters");
    return std::nullopt;
}

inline const char* invoices_table = "fiducia.invoices";
inline const std::vector<std::string> invoices_columns = { "id", "org_id", "billing_customer_id", "subscription_id", "provider", "provider_invoice_id", "status", "amount_due_cents", "amount_paid_cents", "currency", "period_start", "period_end", "hosted_invoice_url", "created_at", "updated_at" };
inline const char* invoices_select_sql = R"SQL(select
      id::text as id,
      org_id::text as org_id,
      billing_customer_id::text as billing_customer_id,
      subscription_id::text as subscription_id,
      provider,
      provider_invoice_id,
      status,
      amount_due_cents,
      amount_paid_cents,
      currency,
      to_char(period_start at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as period_start,
      to_char(period_end at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as period_end,
      hosted_invoice_url,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from fiducia.invoices)SQL";

enum class InvoicesProvider { Stripe, Paypal };
inline std::string invoices_provider_to_string(InvoicesProvider value) {
    switch (value) {
        case InvoicesProvider::Stripe: return "stripe";
        case InvoicesProvider::Paypal: return "paypal";
    }
    return "";
}
inline std::optional<InvoicesProvider> parse_invoices_provider(const std::string& value) {
    if (value == "stripe") return InvoicesProvider::Stripe;
    if (value == "paypal") return InvoicesProvider::Paypal;
    return std::nullopt;
}

enum class InvoicesStatus { Draft, Open, Paid, Void, Uncollectible };
inline std::string invoices_status_to_string(InvoicesStatus value) {
    switch (value) {
        case InvoicesStatus::Draft: return "draft";
        case InvoicesStatus::Open: return "open";
        case InvoicesStatus::Paid: return "paid";
        case InvoicesStatus::Void: return "void";
        case InvoicesStatus::Uncollectible: return "uncollectible";
    }
    return "";
}
inline std::optional<InvoicesStatus> parse_invoices_status(const std::string& value) {
    if (value == "draft") return InvoicesStatus::Draft;
    if (value == "open") return InvoicesStatus::Open;
    if (value == "paid") return InvoicesStatus::Paid;
    if (value == "void") return InvoicesStatus::Void;
    if (value == "uncollectible") return InvoicesStatus::Uncollectible;
    return std::nullopt;
}

struct InvoicesRow {
    std::string id;
    std::string org_id;
    std::optional<std::string> billing_customer_id;
    std::optional<std::string> subscription_id;
    std::string provider;
    std::string provider_invoice_id;
    std::string status;
    int64_t amount_due_cents;
    int64_t amount_paid_cents;
    std::string currency;
    std::optional<std::string> period_start;
    std::optional<std::string> period_end;
    std::optional<std::string> hosted_invoice_url;
    std::string created_at;
    std::string updated_at;
};

inline InvoicesRow invoices_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    InvoicesRow row;
    (void)is_null;
    row.id = get(0);
    row.org_id = get(1);
    row.billing_customer_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.subscription_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.provider = get(4);
    row.provider_invoice_id = get(5);
    row.status = get(6);
    row.amount_due_cents = std::stoll(get(7));
    row.amount_paid_cents = std::stoll(get(8));
    row.currency = get(9);
    row.period_start = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.period_end = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.hosted_invoice_url = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.created_at = get(13);
    row.updated_at = get(14);
    return row;
}
inline std::optional<std::string> validate_invoices_provider_invoice_id(const std::string& value) {
    if (value.size() > 255) return std::string("invoices.provider_invoice_id must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_invoices_amount_due_cents(int64_t value) {
    if (value < 0) return std::string("invoices.amount_due_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_invoices_amount_paid_cents(int64_t value) {
    if (value < 0) return std::string("invoices.amount_paid_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_invoices_currency(const std::string& value) {
    if (value.size() > 3) return std::string("invoices.currency must be at most 3 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z]{3}$)RX"))) return std::string("invoices.currency does not match the required pattern");
    return std::nullopt;
}

inline const char* payments_table = "fiducia.payments";
inline const std::vector<std::string> payments_columns = { "id", "org_id", "invoice_id", "payment_method_id", "provider", "provider_payment_id", "status", "amount_cents", "currency", "failure_code", "created_at", "updated_at" };
inline const char* payments_select_sql = R"SQL(select
      id::text as id,
      org_id::text as org_id,
      invoice_id::text as invoice_id,
      payment_method_id::text as payment_method_id,
      provider,
      provider_payment_id,
      status,
      amount_cents,
      currency,
      failure_code,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from fiducia.payments)SQL";

enum class PaymentsProvider { Stripe, Paypal };
inline std::string payments_provider_to_string(PaymentsProvider value) {
    switch (value) {
        case PaymentsProvider::Stripe: return "stripe";
        case PaymentsProvider::Paypal: return "paypal";
    }
    return "";
}
inline std::optional<PaymentsProvider> parse_payments_provider(const std::string& value) {
    if (value == "stripe") return PaymentsProvider::Stripe;
    if (value == "paypal") return PaymentsProvider::Paypal;
    return std::nullopt;
}

enum class PaymentsStatus { Pending, Processing, Succeeded, Failed, Canceled, Refunded, PartiallyRefunded };
inline std::string payments_status_to_string(PaymentsStatus value) {
    switch (value) {
        case PaymentsStatus::Pending: return "pending";
        case PaymentsStatus::Processing: return "processing";
        case PaymentsStatus::Succeeded: return "succeeded";
        case PaymentsStatus::Failed: return "failed";
        case PaymentsStatus::Canceled: return "canceled";
        case PaymentsStatus::Refunded: return "refunded";
        case PaymentsStatus::PartiallyRefunded: return "partially_refunded";
    }
    return "";
}
inline std::optional<PaymentsStatus> parse_payments_status(const std::string& value) {
    if (value == "pending") return PaymentsStatus::Pending;
    if (value == "processing") return PaymentsStatus::Processing;
    if (value == "succeeded") return PaymentsStatus::Succeeded;
    if (value == "failed") return PaymentsStatus::Failed;
    if (value == "canceled") return PaymentsStatus::Canceled;
    if (value == "refunded") return PaymentsStatus::Refunded;
    if (value == "partially_refunded") return PaymentsStatus::PartiallyRefunded;
    return std::nullopt;
}

struct PaymentsRow {
    std::string id;
    std::string org_id;
    std::optional<std::string> invoice_id;
    std::optional<std::string> payment_method_id;
    std::string provider;
    std::string provider_payment_id;
    std::string status;
    int64_t amount_cents;
    std::string currency;
    std::optional<std::string> failure_code;
    std::string created_at;
    std::string updated_at;
};

inline PaymentsRow payments_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    PaymentsRow row;
    (void)is_null;
    row.id = get(0);
    row.org_id = get(1);
    row.invoice_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.payment_method_id = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.provider = get(4);
    row.provider_payment_id = get(5);
    row.status = get(6);
    row.amount_cents = std::stoll(get(7));
    row.currency = get(8);
    row.failure_code = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.created_at = get(10);
    row.updated_at = get(11);
    return row;
}
inline std::optional<std::string> validate_payments_provider_payment_id(const std::string& value) {
    if (value.size() > 255) return std::string("payments.provider_payment_id must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_payments_amount_cents(int64_t value) {
    if (value < 0) return std::string("payments.amount_cents is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_payments_currency(const std::string& value) {
    if (value.size() > 3) return std::string("payments.currency must be at most 3 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[a-z]{3}$)RX"))) return std::string("payments.currency does not match the required pattern");
    return std::nullopt;
}
inline std::optional<std::string> validate_payments_failure_code(const std::string& value) {
    if (value.size() > 120) return std::string("payments.failure_code must be at most 120 characters");
    return std::nullopt;
}

inline const char* billing_webhook_events_table = "fiducia.billing_webhook_events";
inline const std::vector<std::string> billing_webhook_events_columns = { "id", "provider", "provider_event_id", "event_type", "signature_verified", "payload_sha256", "received_at", "processed_at", "process_error" };
inline const char* billing_webhook_events_select_sql = R"SQL(select
      id::text as id,
      provider,
      provider_event_id,
      event_type,
      signature_verified,
      payload_sha256,
      to_char(received_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as received_at,
      to_char(processed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as processed_at,
      process_error
    from fiducia.billing_webhook_events)SQL";

enum class BillingWebhookEventsProvider { Stripe, Paypal };
inline std::string billing_webhook_events_provider_to_string(BillingWebhookEventsProvider value) {
    switch (value) {
        case BillingWebhookEventsProvider::Stripe: return "stripe";
        case BillingWebhookEventsProvider::Paypal: return "paypal";
    }
    return "";
}
inline std::optional<BillingWebhookEventsProvider> parse_billing_webhook_events_provider(const std::string& value) {
    if (value == "stripe") return BillingWebhookEventsProvider::Stripe;
    if (value == "paypal") return BillingWebhookEventsProvider::Paypal;
    return std::nullopt;
}

struct BillingWebhookEventsRow {
    std::string id;
    std::string provider;
    std::string provider_event_id;
    std::string event_type;
    bool signature_verified;
    std::string payload_sha256;
    std::string received_at;
    std::optional<std::string> processed_at;
    std::optional<std::string> process_error;
};

inline BillingWebhookEventsRow billing_webhook_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    BillingWebhookEventsRow row;
    (void)is_null;
    row.id = get(0);
    row.provider = get(1);
    row.provider_event_id = get(2);
    row.event_type = get(3);
    row.signature_verified = (get(4) == "t");
    row.payload_sha256 = get(5);
    row.received_at = get(6);
    row.processed_at = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.process_error = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    return row;
}
inline std::optional<std::string> validate_billing_webhook_events_provider_event_id(const std::string& value) {
    if (value.size() > 255) return std::string("billing_webhook_events.provider_event_id must be at most 255 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_billing_webhook_events_event_type(const std::string& value) {
    if (value.size() > 120) return std::string("billing_webhook_events.event_type must be at most 120 characters");
    return std::nullopt;
}
inline std::optional<std::string> validate_billing_webhook_events_payload_sha256(const std::string& value) {
    if (value.size() > 64) return std::string("billing_webhook_events.payload_sha256 must be at most 64 characters");
    if (!std::regex_match(value, std::regex(R"RX(^[0-9a-f]{64}$)RX"))) return std::string("billing_webhook_events.payload_sha256 does not match the required pattern");
    return std::nullopt;
}

inline const char* transcriptions_table = "t2v.transcriptions";
inline const std::vector<std::string> transcriptions_columns = { "id", "source", "provider", "model", "text", "language", "sample_rate", "duration_ms", "created_at" };
inline const char* transcriptions_select_sql = R"SQL(select
      id::text as id,
      source,
      provider,
      model,
      text,
      language,
      sample_rate,
      duration_ms,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from t2v.transcriptions)SQL";

struct TranscriptionsRow {
    std::string id;
    std::string source;
    std::string provider;
    std::string model;
    std::string text;
    std::optional<std::string> language;
    std::optional<int32_t> sample_rate;
    std::optional<int64_t> duration_ms;
    std::string created_at;
};

inline TranscriptionsRow transcriptions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    TranscriptionsRow row;
    (void)is_null;
    row.id = get(0);
    row.source = get(1);
    row.provider = get(2);
    row.model = get(3);
    row.text = get(4);
    row.language = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.sample_rate = is_null(6) ? std::nullopt : std::optional<int32_t>(std::stoi(get(6)));
    row.duration_ms = is_null(7) ? std::nullopt : std::optional<int64_t>(std::stoll(get(7)));
    row.created_at = get(8);
    return row;
}
inline std::optional<std::string> validate_transcriptions_sample_rate(int32_t value) {
    if (value < 4000) return std::string("transcriptions.sample_rate is below the minimum");
    if (value > 384000) return std::string("transcriptions.sample_rate is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_transcriptions_duration_ms(int64_t value) {
    if (value < 0) return std::string("transcriptions.duration_ms is below the minimum");
    return std::nullopt;
}

inline const char* syntheses_table = "t2v.syntheses";
inline const std::vector<std::string> syntheses_columns = { "id", "text", "voice", "provider", "model", "format", "audio_bytes", "created_at" };
inline const char* syntheses_select_sql = R"SQL(select
      id::text as id,
      text,
      voice,
      provider,
      model,
      format,
      audio_bytes,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from t2v.syntheses)SQL";

struct SynthesesRow {
    std::string id;
    std::string text;
    std::string voice;
    std::string provider;
    std::string model;
    std::string format;
    int64_t audio_bytes;
    std::string created_at;
};

inline SynthesesRow syntheses_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SynthesesRow row;
    (void)is_null;
    row.id = get(0);
    row.text = get(1);
    row.voice = get(2);
    row.provider = get(3);
    row.model = get(4);
    row.format = get(5);
    row.audio_bytes = std::stoll(get(6));
    row.created_at = get(7);
    return row;
}
inline std::optional<std::string> validate_syntheses_audio_bytes(int64_t value) {
    if (value < 0) return std::string("syntheses.audio_bytes is below the minimum");
    return std::nullopt;
}

inline const char* translations_table = "t2v.translations";
inline const std::vector<std::string> translations_columns = { "id", "source_text", "translated_text", "source_lang", "target_lang", "provider", "model", "latency_ms", "created_at" };
inline const char* translations_select_sql = R"SQL(select
      id::text as id,
      source_text,
      translated_text,
      source_lang,
      target_lang,
      provider,
      model,
      latency_ms,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from t2v.translations)SQL";

struct TranslationsRow {
    std::string id;
    std::string source_text;
    std::string translated_text;
    std::optional<std::string> source_lang;
    std::string target_lang;
    std::string provider;
    std::string model;
    int64_t latency_ms;
    std::string created_at;
};

inline TranslationsRow translations_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    TranslationsRow row;
    (void)is_null;
    row.id = get(0);
    row.source_text = get(1);
    row.translated_text = get(2);
    row.source_lang = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.target_lang = get(4);
    row.provider = get(5);
    row.model = get(6);
    row.latency_ms = std::stoll(get(7));
    row.created_at = get(8);
    return row;
}
inline std::optional<std::string> validate_translations_latency_ms(int64_t value) {
    if (value < 0) return std::string("translations.latency_ms is below the minimum");
    return std::nullopt;
}

inline const char* vapi_calls_table = "t2v.vapi_calls";
inline const std::vector<std::string> vapi_calls_columns = { "id", "vapi_call_id", "status", "ended_reason", "transcript", "summary", "created_at", "updated_at" };
inline const char* vapi_calls_select_sql = R"SQL(select
      id::text as id,
      vapi_call_id,
      status,
      ended_reason,
      transcript,
      summary,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from t2v.vapi_calls)SQL";

struct VapiCallsRow {
    std::string id;
    std::string vapi_call_id;
    std::string status;
    std::optional<std::string> ended_reason;
    std::optional<std::string> transcript;
    std::optional<std::string> summary;
    std::string created_at;
    std::string updated_at;
};

inline VapiCallsRow vapi_calls_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    VapiCallsRow row;
    (void)is_null;
    row.id = get(0);
    row.vapi_call_id = get(1);
    row.status = get(2);
    row.ended_reason = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.transcript = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.summary = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.created_at = get(6);
    row.updated_at = get(7);
    return row;
}

inline const char* vapi_events_table = "t2v.vapi_events";
inline const std::vector<std::string> vapi_events_columns = { "id", "vapi_call_id", "event_type", "payload", "created_at" };
inline const char* vapi_events_select_sql = R"SQL(select
      id::text as id,
      vapi_call_id,
      event_type,
      payload::text as payload_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from t2v.vapi_events)SQL";

struct VapiEventsRow {
    std::string id;
    std::optional<std::string> vapi_call_id;
    std::string event_type;
    std::string payload;
    std::string created_at;
};

inline VapiEventsRow vapi_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    VapiEventsRow row;
    (void)is_null;
    row.id = get(0);
    row.vapi_call_id = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.event_type = get(2);
    row.payload = get(3);
    row.created_at = get(4);
    return row;
}

inline const char* fab_plans_table = "daedalus.fab_plans";
inline const std::vector<std::string> fab_plans_columns = { "id", "owner_email", "title", "goal", "process_family", "status", "document", "created_at", "updated_at" };
inline const char* fab_plans_select_sql = R"SQL(select
      id::text as id,
      owner_email,
      title,
      goal,
      process_family,
      status,
      document::text as document_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from daedalus.fab_plans)SQL";

enum class FabPlansProcessFamily { Additive, Subtractive, Hybrid };
inline std::string fab_plans_process_family_to_string(FabPlansProcessFamily value) {
    switch (value) {
        case FabPlansProcessFamily::Additive: return "additive";
        case FabPlansProcessFamily::Subtractive: return "subtractive";
        case FabPlansProcessFamily::Hybrid: return "hybrid";
    }
    return "";
}
inline std::optional<FabPlansProcessFamily> parse_fab_plans_process_family(const std::string& value) {
    if (value == "additive") return FabPlansProcessFamily::Additive;
    if (value == "subtractive") return FabPlansProcessFamily::Subtractive;
    if (value == "hybrid") return FabPlansProcessFamily::Hybrid;
    return std::nullopt;
}

enum class FabPlansStatus { Draft, Planning, Planned, Released, Archived };
inline std::string fab_plans_status_to_string(FabPlansStatus value) {
    switch (value) {
        case FabPlansStatus::Draft: return "draft";
        case FabPlansStatus::Planning: return "planning";
        case FabPlansStatus::Planned: return "planned";
        case FabPlansStatus::Released: return "released";
        case FabPlansStatus::Archived: return "archived";
    }
    return "";
}
inline std::optional<FabPlansStatus> parse_fab_plans_status(const std::string& value) {
    if (value == "draft") return FabPlansStatus::Draft;
    if (value == "planning") return FabPlansStatus::Planning;
    if (value == "planned") return FabPlansStatus::Planned;
    if (value == "released") return FabPlansStatus::Released;
    if (value == "archived") return FabPlansStatus::Archived;
    return std::nullopt;
}

struct FabPlansRow {
    std::string id;
    std::string owner_email;
    std::string title;
    std::string goal;
    std::string process_family;
    std::string status;
    std::optional<std::string> document;
    std::string created_at;
    std::string updated_at;
};

inline FabPlansRow fab_plans_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    FabPlansRow row;
    (void)is_null;
    row.id = get(0);
    row.owner_email = get(1);
    row.title = get(2);
    row.goal = get(3);
    row.process_family = get(4);
    row.status = get(5);
    row.document = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.created_at = get(7);
    row.updated_at = get(8);
    return row;
}

inline const char* fab_designs_table = "daedalus.fab_designs";
inline const std::vector<std::string> fab_designs_columns = { "id", "plan_id", "filename", "format", "storage_uri", "size_bytes", "content_hash", "geometry", "created_at" };
inline const char* fab_designs_select_sql = R"SQL(select
      id::text as id,
      plan_id::text as plan_id,
      filename,
      format,
      storage_uri,
      size_bytes,
      content_hash,
      geometry::text as geometry_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from daedalus.fab_designs)SQL";

enum class FabDesignsFormat { Step, Stl, 3mf, Dxf, Iges, Obj };
inline std::string fab_designs_format_to_string(FabDesignsFormat value) {
    switch (value) {
        case FabDesignsFormat::Step: return "step";
        case FabDesignsFormat::Stl: return "stl";
        case FabDesignsFormat::3mf: return "3mf";
        case FabDesignsFormat::Dxf: return "dxf";
        case FabDesignsFormat::Iges: return "iges";
        case FabDesignsFormat::Obj: return "obj";
    }
    return "";
}
inline std::optional<FabDesignsFormat> parse_fab_designs_format(const std::string& value) {
    if (value == "step") return FabDesignsFormat::Step;
    if (value == "stl") return FabDesignsFormat::Stl;
    if (value == "3mf") return FabDesignsFormat::3mf;
    if (value == "dxf") return FabDesignsFormat::Dxf;
    if (value == "iges") return FabDesignsFormat::Iges;
    if (value == "obj") return FabDesignsFormat::Obj;
    return std::nullopt;
}

struct FabDesignsRow {
    std::string id;
    std::string plan_id;
    std::string filename;
    std::string format;
    std::string storage_uri;
    int64_t size_bytes;
    std::optional<std::string> content_hash;
    std::string geometry;
    std::string created_at;
};

inline FabDesignsRow fab_designs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    FabDesignsRow row;
    (void)is_null;
    row.id = get(0);
    row.plan_id = get(1);
    row.filename = get(2);
    row.format = get(3);
    row.storage_uri = get(4);
    row.size_bytes = std::stoll(get(5));
    row.content_hash = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.geometry = get(7);
    row.created_at = get(8);
    return row;
}
inline std::optional<std::string> validate_fab_designs_size_bytes(int64_t value) {
    if (value < 0) return std::string("fab_designs.size_bytes is below the minimum");
    return std::nullopt;
}

inline const char* fab_instructions_table = "daedalus.fab_instructions";
inline const std::vector<std::string> fab_instructions_columns = { "id", "plan_id", "revision", "machine_profile", "dialect", "storage_uri", "content_hash", "validated", "validation", "released_by_email", "released_at", "created_at" };
inline const char* fab_instructions_select_sql = R"SQL(select
      id::text as id,
      plan_id::text as plan_id,
      revision,
      machine_profile,
      dialect,
      storage_uri,
      content_hash,
      validated,
      validation::text as validation_json,
      released_by_email,
      to_char(released_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as released_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from daedalus.fab_instructions)SQL";

enum class FabInstructionsDialect { Gcode, Nc, Apt, Proprietary };
inline std::string fab_instructions_dialect_to_string(FabInstructionsDialect value) {
    switch (value) {
        case FabInstructionsDialect::Gcode: return "gcode";
        case FabInstructionsDialect::Nc: return "nc";
        case FabInstructionsDialect::Apt: return "apt";
        case FabInstructionsDialect::Proprietary: return "proprietary";
    }
    return "";
}
inline std::optional<FabInstructionsDialect> parse_fab_instructions_dialect(const std::string& value) {
    if (value == "gcode") return FabInstructionsDialect::Gcode;
    if (value == "nc") return FabInstructionsDialect::Nc;
    if (value == "apt") return FabInstructionsDialect::Apt;
    if (value == "proprietary") return FabInstructionsDialect::Proprietary;
    return std::nullopt;
}

struct FabInstructionsRow {
    std::string id;
    std::string plan_id;
    int32_t revision;
    std::string machine_profile;
    std::string dialect;
    std::string storage_uri;
    std::optional<std::string> content_hash;
    bool validated;
    std::string validation;
    std::optional<std::string> released_by_email;
    std::optional<std::string> released_at;
    std::string created_at;
};

inline FabInstructionsRow fab_instructions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    FabInstructionsRow row;
    (void)is_null;
    row.id = get(0);
    row.plan_id = get(1);
    row.revision = std::stoi(get(2));
    row.machine_profile = get(3);
    row.dialect = get(4);
    row.storage_uri = get(5);
    row.content_hash = is_null(6) ? std::nullopt : std::optional<std::string>(get(6));
    row.validated = (get(7) == "t");
    row.validation = get(8);
    row.released_by_email = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.released_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.created_at = get(11);
    return row;
}
inline std::optional<std::string> validate_fab_instructions_revision(int32_t value) {
    if (value < 1) return std::string("fab_instructions.revision is below the minimum");
    return std::nullopt;
}

inline const char* fab_runs_table = "daedalus.fab_runs";
inline const std::vector<std::string> fab_runs_columns = { "id", "instructions_id", "status", "machine_id", "operator_email", "progress", "as_built", "error", "started_at", "finished_at", "created_at" };
inline const char* fab_runs_select_sql = R"SQL(select
      id::text as id,
      instructions_id::text as instructions_id,
      status,
      machine_id,
      operator_email,
      progress,
      as_built::text as as_built_json,
      error,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(finished_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as finished_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from daedalus.fab_runs)SQL";

enum class FabRunsStatus { Queued, Running, Succeeded, Failed, Aborted };
inline std::string fab_runs_status_to_string(FabRunsStatus value) {
    switch (value) {
        case FabRunsStatus::Queued: return "queued";
        case FabRunsStatus::Running: return "running";
        case FabRunsStatus::Succeeded: return "succeeded";
        case FabRunsStatus::Failed: return "failed";
        case FabRunsStatus::Aborted: return "aborted";
    }
    return "";
}
inline std::optional<FabRunsStatus> parse_fab_runs_status(const std::string& value) {
    if (value == "queued") return FabRunsStatus::Queued;
    if (value == "running") return FabRunsStatus::Running;
    if (value == "succeeded") return FabRunsStatus::Succeeded;
    if (value == "failed") return FabRunsStatus::Failed;
    if (value == "aborted") return FabRunsStatus::Aborted;
    return std::nullopt;
}

struct FabRunsRow {
    std::string id;
    std::string instructions_id;
    std::string status;
    std::string machine_id;
    std::optional<std::string> operator_email;
    int32_t progress;
    std::string as_built;
    std::optional<std::string> error;
    std::optional<std::string> started_at;
    std::optional<std::string> finished_at;
    std::string created_at;
};

inline FabRunsRow fab_runs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    FabRunsRow row;
    (void)is_null;
    row.id = get(0);
    row.instructions_id = get(1);
    row.status = get(2);
    row.machine_id = get(3);
    row.operator_email = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.progress = std::stoi(get(5));
    row.as_built = get(6);
    row.error = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    row.started_at = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.finished_at = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.created_at = get(10);
    return row;
}
inline std::optional<std::string> validate_fab_runs_progress(int32_t value) {
    if (value < 0) return std::string("fab_runs.progress is below the minimum");
    if (value > 100) return std::string("fab_runs.progress is above the maximum");
    return std::nullopt;
}

inline const char* web_sessions_table = "daedalus.web_sessions";
inline const std::vector<std::string> web_sessions_columns = { "id", "token_hash", "user_id", "owner_email", "access_ciphertext", "access_nonce", "refresh_ciphertext", "refresh_nonce", "created_at", "last_seen_at", "idle_expires_at", "absolute_expires_at", "revoked_at" };
inline const char* web_sessions_select_sql = R"SQL(select
      id::text as id,
      token_hash,
      user_id::text as user_id,
      owner_email,
      access_ciphertext,
      access_nonce,
      refresh_ciphertext,
      refresh_nonce,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(last_seen_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_seen_at,
      to_char(idle_expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as idle_expires_at,
      to_char(absolute_expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as absolute_expires_at,
      to_char(revoked_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as revoked_at
    from daedalus.web_sessions)SQL";

struct WebSessionsRow {
    std::string id;
    std::string token_hash;
    std::string user_id;
    std::string owner_email;
    std::string access_ciphertext;
    std::string access_nonce;
    std::string refresh_ciphertext;
    std::string refresh_nonce;
    std::string created_at;
    std::string last_seen_at;
    std::string idle_expires_at;
    std::string absolute_expires_at;
    std::optional<std::string> revoked_at;
};

inline WebSessionsRow web_sessions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    WebSessionsRow row;
    (void)is_null;
    row.id = get(0);
    row.token_hash = get(1);
    row.user_id = get(2);
    row.owner_email = get(3);
    row.access_ciphertext = get(4);
    row.access_nonce = get(5);
    row.refresh_ciphertext = get(6);
    row.refresh_nonce = get(7);
    row.created_at = get(8);
    row.last_seen_at = get(9);
    row.idle_expires_at = get(10);
    row.absolute_expires_at = get(11);
    row.revoked_at = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    return row;
}

inline const char* principals_table = "shared_auth.principals";
inline const std::vector<std::string> principals_columns = { "shared_user_id", "email", "email_verified", "phone", "display_name", "status", "profile", "created_at", "updated_at", "last_seen_at" };
inline const char* principals_select_sql = R"SQL(select
      shared_user_id::text as shared_user_id,
      email,
      email_verified,
      phone,
      display_name,
      status,
      profile::text as profile_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      to_char(last_seen_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_seen_at
    from shared_auth.principals)SQL";

enum class PrincipalsStatus { Active, Disabled, Deleted };
inline std::string principals_status_to_string(PrincipalsStatus value) {
    switch (value) {
        case PrincipalsStatus::Active: return "active";
        case PrincipalsStatus::Disabled: return "disabled";
        case PrincipalsStatus::Deleted: return "deleted";
    }
    return "";
}
inline std::optional<PrincipalsStatus> parse_principals_status(const std::string& value) {
    if (value == "active") return PrincipalsStatus::Active;
    if (value == "disabled") return PrincipalsStatus::Disabled;
    if (value == "deleted") return PrincipalsStatus::Deleted;
    return std::nullopt;
}

struct PrincipalsRow {
    std::string shared_user_id;
    std::optional<std::string> email;
    bool email_verified;
    std::optional<std::string> phone;
    std::optional<std::string> display_name;
    std::string status;
    std::string profile;
    std::string created_at;
    std::string updated_at;
    std::string last_seen_at;
};

inline PrincipalsRow principals_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    PrincipalsRow row;
    (void)is_null;
    row.shared_user_id = get(0);
    row.email = is_null(1) ? std::nullopt : std::optional<std::string>(get(1));
    row.email_verified = (get(2) == "t");
    row.phone = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.display_name = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.status = get(5);
    row.profile = get(6);
    row.created_at = get(7);
    row.updated_at = get(8);
    row.last_seen_at = get(9);
    return row;
}

inline const char* provider_identities_table = "shared_auth.provider_identities";
inline const std::vector<std::string> provider_identities_columns = { "provider_identity_id", "shared_user_id", "provider", "provider_tenant", "provider_subject", "email", "email_verified", "metadata", "created_at", "updated_at", "last_seen_at" };
inline const char* provider_identities_select_sql = R"SQL(select
      provider_identity_id::text as provider_identity_id,
      shared_user_id::text as shared_user_id,
      provider,
      provider_tenant,
      provider_subject,
      email,
      email_verified,
      metadata::text as metadata_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      to_char(last_seen_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_seen_at
    from shared_auth.provider_identities)SQL";

struct ProviderIdentitiesRow {
    std::string provider_identity_id;
    std::string shared_user_id;
    std::string provider;
    std::string provider_tenant;
    std::string provider_subject;
    std::optional<std::string> email;
    bool email_verified;
    std::string metadata;
    std::string created_at;
    std::string updated_at;
    std::string last_seen_at;
};

inline ProviderIdentitiesRow provider_identities_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    ProviderIdentitiesRow row;
    (void)is_null;
    row.provider_identity_id = get(0);
    row.shared_user_id = get(1);
    row.provider = get(2);
    row.provider_tenant = get(3);
    row.provider_subject = get(4);
    row.email = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.email_verified = (get(6) == "t");
    row.metadata = get(7);
    row.created_at = get(8);
    row.updated_at = get(9);
    row.last_seen_at = get(10);
    return row;
}

inline const char* local_credentials_table = "shared_auth.local_credentials";
inline const std::vector<std::string> local_credentials_columns = { "shared_user_id", "password_hash", "password_changed_at", "failed_attempts", "locked_until", "created_at", "updated_at" };
inline const char* local_credentials_select_sql = R"SQL(select
      shared_user_id::text as shared_user_id,
      password_hash,
      to_char(password_changed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as password_changed_at,
      failed_attempts,
      to_char(locked_until at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as locked_until,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from shared_auth.local_credentials)SQL";

struct LocalCredentialsRow {
    std::string shared_user_id;
    std::string password_hash;
    std::string password_changed_at;
    int32_t failed_attempts;
    std::optional<std::string> locked_until;
    std::string created_at;
    std::string updated_at;
};

inline LocalCredentialsRow local_credentials_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    LocalCredentialsRow row;
    (void)is_null;
    row.shared_user_id = get(0);
    row.password_hash = get(1);
    row.password_changed_at = get(2);
    row.failed_attempts = std::stoi(get(3));
    row.locked_until = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.created_at = get(5);
    row.updated_at = get(6);
    return row;
}
inline std::optional<std::string> validate_local_credentials_failed_attempts(int32_t value) {
    if (value < 0) return std::string("local_credentials.failed_attempts is below the minimum");
    return std::nullopt;
}

inline const char* sessions_table = "shared_auth.sessions";
inline const std::vector<std::string> sessions_columns = { "session_id", "shared_user_id", "refresh_token_hash", "provider", "provider_tenant", "provider_subject", "auth_level", "auth_methods", "created_at", "updated_at", "last_seen_at", "expires_at", "revoked_at", "rotated_from" };
inline const char* sessions_select_sql = R"SQL(select
      session_id::text as session_id,
      shared_user_id::text as shared_user_id,
      refresh_token_hash,
      provider,
      provider_tenant,
      provider_subject,
      auth_level,
      auth_methods::text as auth_methods_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at,
      to_char(last_seen_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as last_seen_at,
      to_char(expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as expires_at,
      to_char(revoked_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as revoked_at,
      rotated_from::text as rotated_from
    from shared_auth.sessions)SQL";

enum class SessionsAuthLevel { 1, 2 };
inline std::string sessions_auth_level_to_string(SessionsAuthLevel value) {
    switch (value) {
        case SessionsAuthLevel::1: return "1";
        case SessionsAuthLevel::2: return "2";
    }
    return "";
}
inline std::optional<SessionsAuthLevel> parse_sessions_auth_level(const std::string& value) {
    if (value == "1") return SessionsAuthLevel::1;
    if (value == "2") return SessionsAuthLevel::2;
    return std::nullopt;
}

struct SessionsRow {
    std::string session_id;
    std::string shared_user_id;
    std::string refresh_token_hash;
    std::string provider;
    std::string provider_tenant;
    std::string provider_subject;
    std::string auth_level;
    std::string auth_methods;
    std::string created_at;
    std::string updated_at;
    std::string last_seen_at;
    std::string expires_at;
    std::optional<std::string> revoked_at;
    std::optional<std::string> rotated_from;
};

inline SessionsRow sessions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    SessionsRow row;
    (void)is_null;
    row.session_id = get(0);
    row.shared_user_id = get(1);
    row.refresh_token_hash = get(2);
    row.provider = get(3);
    row.provider_tenant = get(4);
    row.provider_subject = get(5);
    row.auth_level = get(6);
    row.auth_methods = get(7);
    row.created_at = get(8);
    row.updated_at = get(9);
    row.last_seen_at = get(10);
    row.expires_at = get(11);
    row.revoked_at = is_null(12) ? std::nullopt : std::optional<std::string>(get(12));
    row.rotated_from = is_null(13) ? std::nullopt : std::optional<std::string>(get(13));
    return row;
}

inline const char* magic_link_tokens_table = "shared_auth.magic_link_tokens";
inline const std::vector<std::string> magic_link_tokens_columns = { "token_hash", "otp_hash", "shared_user_id", "identifier_hash", "failed_attempts", "created_at", "expires_at", "consumed_at" };
inline const char* magic_link_tokens_select_sql = R"SQL(select
      token_hash,
      otp_hash,
      shared_user_id::text as shared_user_id,
      identifier_hash,
      failed_attempts,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as expires_at,
      to_char(consumed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as consumed_at
    from shared_auth.magic_link_tokens)SQL";

struct MagicLinkTokensRow {
    std::string token_hash;
    std::string otp_hash;
    std::string shared_user_id;
    std::string identifier_hash;
    int32_t failed_attempts;
    std::string created_at;
    std::string expires_at;
    std::optional<std::string> consumed_at;
};

inline MagicLinkTokensRow magic_link_tokens_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    MagicLinkTokensRow row;
    (void)is_null;
    row.token_hash = get(0);
    row.otp_hash = get(1);
    row.shared_user_id = get(2);
    row.identifier_hash = get(3);
    row.failed_attempts = std::stoi(get(4));
    row.created_at = get(5);
    row.expires_at = get(6);
    row.consumed_at = is_null(7) ? std::nullopt : std::optional<std::string>(get(7));
    return row;
}
inline std::optional<std::string> validate_magic_link_tokens_failed_attempts(int32_t value) {
    if (value < 0) return std::string("magic_link_tokens.failed_attempts is below the minimum");
    if (value > 5) return std::string("magic_link_tokens.failed_attempts is above the maximum");
    return std::nullopt;
}

inline const char* mfa_sms_challenges_table = "shared_auth.mfa_sms_challenges";
inline const std::vector<std::string> mfa_sms_challenges_columns = { "challenge_id", "shared_user_id", "phone_e164", "created_at", "expires_at", "verified_at" };
inline const char* mfa_sms_challenges_select_sql = R"SQL(select
      challenge_id::text as challenge_id,
      shared_user_id::text as shared_user_id,
      phone_e164,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as expires_at,
      to_char(verified_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as verified_at
    from shared_auth.mfa_sms_challenges)SQL";

struct MfaSmsChallengesRow {
    std::string challenge_id;
    std::string shared_user_id;
    std::string phone_e164;
    std::string created_at;
    std::string expires_at;
    std::optional<std::string> verified_at;
};

inline MfaSmsChallengesRow mfa_sms_challenges_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    MfaSmsChallengesRow row;
    (void)is_null;
    row.challenge_id = get(0);
    row.shared_user_id = get(1);
    row.phone_e164 = get(2);
    row.created_at = get(3);
    row.expires_at = get(4);
    row.verified_at = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    return row;
}
inline std::optional<std::string> validate_mfa_sms_challenges_phone_e164(const std::string& value) {
    if (!std::regex_match(value, std::regex(R"RX(^\+[1-9][0-9]{7,14}$)RX"))) return std::string("mfa_sms_challenges.phone_e164 does not match the required pattern");
    return std::nullopt;
}

inline const char* roles_table = "shared_auth.roles";
inline const std::vector<std::string> roles_columns = { "role_id", "shared_user_id", "role_name", "granted_at", "granted_by" };
inline const char* roles_select_sql = R"SQL(select
      role_id::text as role_id,
      shared_user_id::text as shared_user_id,
      role_name,
      to_char(granted_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as granted_at,
      granted_by::text as granted_by
    from shared_auth.roles)SQL";

struct RolesRow {
    std::string role_id;
    std::string shared_user_id;
    std::string role_name;
    std::string granted_at;
    std::optional<std::string> granted_by;
};

inline RolesRow roles_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    RolesRow row;
    (void)is_null;
    row.role_id = get(0);
    row.shared_user_id = get(1);
    row.role_name = get(2);
    row.granted_at = get(3);
    row.granted_by = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    return row;
}
inline std::optional<std::string> validate_roles_role_name(const std::string& value) {
    if (!std::regex_match(value, std::regex(R"RX(^[a-z][a-z0-9:_-]{0,63}$)RX"))) return std::string("roles.role_name does not match the required pattern");
    return std::nullopt;
}

inline const char* webhook_events_table = "shared_auth.webhook_events";
inline const std::vector<std::string> webhook_events_columns = { "event_id", "provider", "event_type", "received_at", "payload_sha256" };
inline const char* webhook_events_select_sql = R"SQL(select
      event_id::text as event_id,
      provider,
      event_type,
      to_char(received_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as received_at,
      payload_sha256
    from shared_auth.webhook_events)SQL";

struct WebhookEventsRow {
    std::string event_id;
    std::string provider;
    std::string event_type;
    std::string received_at;
    std::string payload_sha256;
};

inline WebhookEventsRow webhook_events_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    WebhookEventsRow row;
    (void)is_null;
    row.event_id = get(0);
    row.provider = get(1);
    row.event_type = get(2);
    row.received_at = get(3);
    row.payload_sha256 = get(4);
    return row;
}

inline const char* fab_jobs_table = "daedalus.fab_jobs";
inline const std::vector<std::string> fab_jobs_columns = { "job_id", "request_id", "kind", "status", "ok", "severity", "summary", "artifact_count", "payload", "created_at", "updated_at" };
inline const char* fab_jobs_select_sql = R"SQL(select
      job_id,
      request_id,
      kind,
      status,
      ok,
      severity,
      summary,
      artifact_count,
      payload::text as payload_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from daedalus.fab_jobs)SQL";

struct FabJobsRow {
    std::string job_id;
    std::string request_id;
    std::string kind;
    std::string status;
    bool ok;
    std::string severity;
    std::string summary;
    int32_t artifact_count;
    std::string payload;
    std::string created_at;
    std::string updated_at;
};

inline FabJobsRow fab_jobs_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    FabJobsRow row;
    (void)is_null;
    row.job_id = get(0);
    row.request_id = get(1);
    row.kind = get(2);
    row.status = get(3);
    row.ok = (get(4) == "t");
    row.severity = get(5);
    row.summary = get(6);
    row.artifact_count = std::stoi(get(7));
    row.payload = get(8);
    row.created_at = get(9);
    row.updated_at = get(10);
    return row;
}
inline std::optional<std::string> validate_fab_jobs_artifact_count(int32_t value) {
    if (value < 0) return std::string("fab_jobs.artifact_count is below the minimum");
    return std::nullopt;
}

inline const char* fab_learning_outcomes_table = "daedalus.fab_learning_outcomes";
inline const std::vector<std::string> fab_learning_outcomes_columns = { "outcome_id", "request_id", "job_id", "objective", "machine_kind", "assembly_strategy", "success", "reward", "payload", "created_at" };
inline const char* fab_learning_outcomes_select_sql = R"SQL(select
      outcome_id,
      request_id,
      job_id,
      objective,
      machine_kind,
      assembly_strategy,
      success,
      reward,
      payload::text as payload_json,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at
    from daedalus.fab_learning_outcomes)SQL";

struct FabLearningOutcomesRow {
    std::string outcome_id;
    std::string request_id;
    std::optional<std::string> job_id;
    std::optional<std::string> objective;
    std::optional<std::string> machine_kind;
    std::optional<std::string> assembly_strategy;
    bool success;
    double reward;
    std::string payload;
    std::string created_at;
};

inline FabLearningOutcomesRow fab_learning_outcomes_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    FabLearningOutcomesRow row;
    (void)is_null;
    row.outcome_id = get(0);
    row.request_id = get(1);
    row.job_id = is_null(2) ? std::nullopt : std::optional<std::string>(get(2));
    row.objective = is_null(3) ? std::nullopt : std::optional<std::string>(get(3));
    row.machine_kind = is_null(4) ? std::nullopt : std::optional<std::string>(get(4));
    row.assembly_strategy = is_null(5) ? std::nullopt : std::optional<std::string>(get(5));
    row.success = (get(6) == "t");
    row.reward = std::stod(get(7));
    row.payload = get(8);
    row.created_at = get(9);
    return row;
}

inline const char* fabrication_job_executions_table = "daedalus.fabrication_job_executions";
inline const std::vector<std::string> fabrication_job_executions_columns = { "job_id", "tenant_id", "request_id", "idempotency_key", "kind", "state", "current_stage", "checkpoint_version", "checkpoint", "request_payload", "result_payload", "attempt_count", "max_attempts", "priority", "lease_owner", "lease_expires_at", "fiducia_fencing_token", "next_attempt_at", "last_error_code", "last_error_message", "started_at", "completed_at", "created_at", "updated_at" };
inline const char* fabrication_job_executions_select_sql = R"SQL(select
      job_id::text as job_id,
      tenant_id,
      request_id,
      idempotency_key,
      kind,
      state,
      current_stage,
      checkpoint_version,
      checkpoint::text as checkpoint_json,
      request_payload::text as request_payload_json,
      result_payload::text as result_payload_json,
      attempt_count,
      max_attempts,
      priority,
      lease_owner,
      to_char(lease_expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as lease_expires_at,
      fiducia_fencing_token,
      to_char(next_attempt_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as next_attempt_at,
      last_error_code,
      last_error_message,
      to_char(started_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as started_at,
      to_char(completed_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as completed_at,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from daedalus.fabrication_job_executions)SQL";

enum class FabricationJobExecutionsState { Queued, Running, RetryWait, Succeeded, Failed, Cancelled };
inline std::string fabrication_job_executions_state_to_string(FabricationJobExecutionsState value) {
    switch (value) {
        case FabricationJobExecutionsState::Queued: return "queued";
        case FabricationJobExecutionsState::Running: return "running";
        case FabricationJobExecutionsState::RetryWait: return "retry_wait";
        case FabricationJobExecutionsState::Succeeded: return "succeeded";
        case FabricationJobExecutionsState::Failed: return "failed";
        case FabricationJobExecutionsState::Cancelled: return "cancelled";
    }
    return "";
}
inline std::optional<FabricationJobExecutionsState> parse_fabrication_job_executions_state(const std::string& value) {
    if (value == "queued") return FabricationJobExecutionsState::Queued;
    if (value == "running") return FabricationJobExecutionsState::Running;
    if (value == "retry_wait") return FabricationJobExecutionsState::RetryWait;
    if (value == "succeeded") return FabricationJobExecutionsState::Succeeded;
    if (value == "failed") return FabricationJobExecutionsState::Failed;
    if (value == "cancelled") return FabricationJobExecutionsState::Cancelled;
    return std::nullopt;
}

struct FabricationJobExecutionsRow {
    std::string job_id;
    std::string tenant_id;
    std::string request_id;
    std::string idempotency_key;
    std::string kind;
    std::string state;
    std::string current_stage;
    int64_t checkpoint_version;
    std::string checkpoint;
    std::string request_payload;
    std::optional<std::string> result_payload;
    int32_t attempt_count;
    int32_t max_attempts;
    int32_t priority;
    std::optional<std::string> lease_owner;
    std::optional<std::string> lease_expires_at;
    std::optional<int64_t> fiducia_fencing_token;
    std::string next_attempt_at;
    std::optional<std::string> last_error_code;
    std::optional<std::string> last_error_message;
    std::optional<std::string> started_at;
    std::optional<std::string> completed_at;
    std::string created_at;
    std::string updated_at;
};

inline FabricationJobExecutionsRow fabrication_job_executions_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    FabricationJobExecutionsRow row;
    (void)is_null;
    row.job_id = get(0);
    row.tenant_id = get(1);
    row.request_id = get(2);
    row.idempotency_key = get(3);
    row.kind = get(4);
    row.state = get(5);
    row.current_stage = get(6);
    row.checkpoint_version = std::stoll(get(7));
    row.checkpoint = get(8);
    row.request_payload = get(9);
    row.result_payload = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.attempt_count = std::stoi(get(11));
    row.max_attempts = std::stoi(get(12));
    row.priority = std::stoi(get(13));
    row.lease_owner = is_null(14) ? std::nullopt : std::optional<std::string>(get(14));
    row.lease_expires_at = is_null(15) ? std::nullopt : std::optional<std::string>(get(15));
    row.fiducia_fencing_token = is_null(16) ? std::nullopt : std::optional<int64_t>(std::stoll(get(16)));
    row.next_attempt_at = get(17);
    row.last_error_code = is_null(18) ? std::nullopt : std::optional<std::string>(get(18));
    row.last_error_message = is_null(19) ? std::nullopt : std::optional<std::string>(get(19));
    row.started_at = is_null(20) ? std::nullopt : std::optional<std::string>(get(20));
    row.completed_at = is_null(21) ? std::nullopt : std::optional<std::string>(get(21));
    row.created_at = get(22);
    row.updated_at = get(23);
    return row;
}
inline std::optional<std::string> validate_fabrication_job_executions_checkpoint_version(int64_t value) {
    if (value < 0) return std::string("fabrication_job_executions.checkpoint_version is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_fabrication_job_executions_attempt_count(int32_t value) {
    if (value < 0) return std::string("fabrication_job_executions.attempt_count is below the minimum");
    return std::nullopt;
}
inline std::optional<std::string> validate_fabrication_job_executions_max_attempts(int32_t value) {
    if (value < 1) return std::string("fabrication_job_executions.max_attempts is below the minimum");
    if (value > 100) return std::string("fabrication_job_executions.max_attempts is above the maximum");
    return std::nullopt;
}
inline std::optional<std::string> validate_fabrication_job_executions_fiducia_fencing_token(int64_t value) {
    if (value < 0) return std::string("fabrication_job_executions.fiducia_fencing_token is below the minimum");
    return std::nullopt;
}

inline const char* fabrication_job_outbox_table = "daedalus.fabrication_job_outbox";
inline const std::vector<std::string> fabrication_job_outbox_columns = { "event_id", "job_id", "subject", "event_type", "message_id", "payload", "available_at", "publish_attempts", "claim_owner", "claim_expires_at", "published_at", "last_error", "created_at", "updated_at" };
inline const char* fabrication_job_outbox_select_sql = R"SQL(select
      event_id::text as event_id,
      job_id::text as job_id,
      subject,
      event_type,
      message_id,
      payload::text as payload_json,
      to_char(available_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as available_at,
      publish_attempts,
      claim_owner,
      to_char(claim_expires_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as claim_expires_at,
      to_char(published_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as published_at,
      last_error,
      to_char(created_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as created_at,
      to_char(updated_at at time zone 'utc', 'YYYY-MM-DD"T"HH24:MI:SS"Z"') as updated_at
    from daedalus.fabrication_job_outbox)SQL";

struct FabricationJobOutboxRow {
    std::string event_id;
    std::string job_id;
    std::string subject;
    std::string event_type;
    std::string message_id;
    std::string payload;
    std::string available_at;
    int32_t publish_attempts;
    std::optional<std::string> claim_owner;
    std::optional<std::string> claim_expires_at;
    std::optional<std::string> published_at;
    std::optional<std::string> last_error;
    std::string created_at;
    std::string updated_at;
};

inline FabricationJobOutboxRow fabrication_job_outbox_row_of_row(const std::function<std::string(int)>& get, const std::function<bool(int)>& is_null) {
    FabricationJobOutboxRow row;
    (void)is_null;
    row.event_id = get(0);
    row.job_id = get(1);
    row.subject = get(2);
    row.event_type = get(3);
    row.message_id = get(4);
    row.payload = get(5);
    row.available_at = get(6);
    row.publish_attempts = std::stoi(get(7));
    row.claim_owner = is_null(8) ? std::nullopt : std::optional<std::string>(get(8));
    row.claim_expires_at = is_null(9) ? std::nullopt : std::optional<std::string>(get(9));
    row.published_at = is_null(10) ? std::nullopt : std::optional<std::string>(get(10));
    row.last_error = is_null(11) ? std::nullopt : std::optional<std::string>(get(11));
    row.created_at = get(12);
    row.updated_at = get(13);
    return row;
}
inline std::optional<std::string> validate_fabrication_job_outbox_publish_attempts(int32_t value) {
    if (value < 0) return std::string("fabrication_job_outbox.publish_attempts is below the minimum");
    return std::nullopt;
}

}  // namespace dd_pg_defs
