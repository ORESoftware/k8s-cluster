# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingBudgetForecasts do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_budget_forecasts"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :campaign_id, :binary_id
    field :forecast_kind, :string, default: "monthly"
    field :period_start, :string
    field :period_end, :string
    field :status, :string, default: "draft"
    field :revenue_cents, :integer, default: 0
    field :media_spend_cents, :integer, default: 0
    field :labor_cost_cents, :integer, default: 0
    field :vendor_cost_cents, :integer, default: 0
    field :gross_margin_cents, :integer, default: 0
    field :assumptions, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(client_id period_start period_end)a
  @optional_fields ~w(campaign_id forecast_kind status revenue_cents media_spend_cents labor_cost_cents vendor_cost_cents gross_margin_cents assumptions)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:forecast_kind, ["monthly", "quarterly", "campaign", "annual"])
    |> validate_format(:period_start, ~r/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)
    |> validate_length(:period_start, max: 10)
    |> validate_format(:period_end, ~r/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)
    |> validate_length(:period_end, max: 10)
    |> validate_inclusion(:status, ["draft", "approved", "locked", "archived"])
    |> validate_number(:revenue_cents, greater_than_or_equal_to: 0)
    |> validate_number(:media_spend_cents, greater_than_or_equal_to: 0)
    |> validate_number(:labor_cost_cents, greater_than_or_equal_to: 0)
    |> validate_number(:vendor_cost_cents, greater_than_or_equal_to: 0)
  end
end
