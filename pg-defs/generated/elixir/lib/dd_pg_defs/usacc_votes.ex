# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.UsaccVotes do
  use Ecto.Schema
  import Ecto.Changeset

  @table "usacc_votes"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :election_id, :binary_id
    field :case_id, :binary_id
    field :voter_user_id, :binary_id
    field :vote_kind, :string, default: "choice"
    field :vote_value, :string
    field :weight_micros, :integer, default: 1000000
    field :commitment_hash, :string
    field :sealed_payload, :map
    field :revealed_at, :utc_datetime_usec
    field :contract_digest, :string
    field :meta_data, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(election_id voter_user_id vote_value)a
  @optional_fields ~w(case_id vote_kind weight_micros commitment_hash sealed_payload revealed_at contract_digest meta_data)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:vote_kind, ["choice", "priority_dollar_weighted", "verdict", "approval", "assignment_response"])
    |> validate_format(:vote_value, ~r/^[A-Za-z0-9._:\/-]{1,80}$/)
    |> validate_length(:vote_value, max: 80)
    |> validate_number(:weight_micros, greater_than_or_equal_to: 0, less_than_or_equal_to: 1000000000)
    |> validate_format(:commitment_hash, ~r/^[A-Za-z0-9._:\/-]{1,128}$/)
    |> validate_length(:commitment_hash, max: 128)
    |> validate_length(:contract_digest, max: 160)
  end
end
