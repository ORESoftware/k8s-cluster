# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingProjectTasks do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_project_tasks"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :campaign_id, :binary_id
    field :content_asset_id, :binary_id
    field :status, :string, default: "todo"
    field :priority, :string, default: "normal"
    field :title, :string
    field :description, :string
    field :assigned_to, :binary_id
    field :due_on, :string
    field :sla_due_at, :utc_datetime_usec
    field :time_spent_minutes, :integer, default: 0
    field :meta_data, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(client_id title)a
  @optional_fields ~w(campaign_id content_asset_id status priority description assigned_to due_on sla_due_at time_spent_minutes meta_data)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:status, ["todo", "in_progress", "blocked", "done", "canceled"])
    |> validate_inclusion(:priority, ["low", "normal", "high", "urgent"])
    |> validate_length(:title, max: 240)
    |> validate_format(:due_on, ~r/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)
    |> validate_length(:due_on, max: 10)
    |> validate_number(:time_spent_minutes, greater_than_or_equal_to: 0)
  end
end
