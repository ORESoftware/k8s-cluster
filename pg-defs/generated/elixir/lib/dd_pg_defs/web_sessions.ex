# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.WebSessions do
  use Ecto.Schema
  import Ecto.Changeset

  @table "web_sessions"
  @schema_prefix "daedalus"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :token_hash, :string
    field :user_id, :binary_id
    field :owner_email, :string
    field :access_ciphertext, :string
    field :access_nonce, :string
    field :refresh_ciphertext, :string
    field :refresh_nonce, :string
    field :last_seen_at, :utc_datetime_usec
    field :idle_expires_at, :utc_datetime_usec
    field :absolute_expires_at, :utc_datetime_usec
    field :revoked_at, :utc_datetime_usec
  end

  @required_fields ~w(token_hash user_id owner_email access_ciphertext access_nonce refresh_ciphertext refresh_nonce idle_expires_at absolute_expires_at)a
  @optional_fields ~w(last_seen_at revoked_at)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
  end
end
