# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingCommissionEntries do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_commission_entries"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :opportunity_id, :binary_id
    field :user_id, :binary_id
    field :status, :string, default: "pending"
    field :commission_kind, :string, default: "deal"
    field :basis_cents, :integer, default: 0
    field :rate_micros, :integer, default: 0
    field :amount_cents, :integer, default: 0
    field :earned_on, :string
    field :paid_at, :utc_datetime_usec
    field :meta_data, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(user_id)a
  @optional_fields ~w(client_id opportunity_id status commission_kind basis_cents rate_micros amount_cents earned_on paid_at meta_data)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:status, ["pending", "approved", "paid", "void"])
    |> validate_inclusion(:commission_kind, ["deal", "retainer", "renewal", "upsell", "appointment"])
    |> validate_number(:basis_cents, greater_than_or_equal_to: 0)
    |> validate_number(:rate_micros, greater_than_or_equal_to: 0, less_than_or_equal_to: 1000000)
    |> validate_number(:amount_cents, greater_than_or_equal_to: 0)
    |> validate_format(:earned_on, ~r/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)
    |> validate_length(:earned_on, max: 10)
  end
end
