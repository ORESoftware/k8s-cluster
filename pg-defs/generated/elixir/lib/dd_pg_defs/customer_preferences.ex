# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.CustomerPreferences do
  use Ecto.Schema
  import Ecto.Changeset

  @table "customer_preferences"
  @schema_prefix "fiducia"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :density, :string, default: "comfortable"
    field :timezone, :string, default: "UTC"
    field :region, :string, default: "auto"
    field :notify_key_rotation, :boolean, default: true
    field :notify_lock_contention, :boolean, default: true
    field :notify_mfa, :boolean, default: true
    field :version, :integer, default: 1
    field :sync_sequence, :integer
  end

  @required_fields ~w(sync_sequence)a
  @optional_fields ~w(density timezone region notify_key_rotation notify_lock_contention notify_mfa version)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:density, ["comfortable", "compact"])
    |> validate_length(:timezone, max: 64)
    |> validate_length(:region, max: 16)
  end
end
