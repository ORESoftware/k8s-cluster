# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.Payments do
  use Ecto.Schema
  import Ecto.Changeset

  @table "payments"
  @schema_prefix "fiducia"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :org_id, :binary_id
    field :invoice_id, :binary_id
    field :payment_method_id, :binary_id
    field :provider, :string, default: nil
    field :provider_payment_id, :string
    field :status, :string, default: nil
    field :amount_cents, :integer
    field :currency, :string
    field :failure_code, :string
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(org_id provider provider_payment_id status amount_cents currency)a
  @optional_fields ~w(invoice_id payment_method_id failure_code)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:provider, ["stripe", "paypal"])
    |> validate_length(:provider_payment_id, max: 255)
    |> validate_inclusion(:status, ["pending", "processing", "succeeded", "failed", "canceled", "refunded", "partially_refunded"])
    |> validate_number(:amount_cents, greater_than_or_equal_to: 0)
    |> validate_format(:currency, ~r/^[a-z]{3}$/)
    |> validate_length(:currency, max: 3)
    |> validate_length(:failure_code, max: 120)
  end
end
