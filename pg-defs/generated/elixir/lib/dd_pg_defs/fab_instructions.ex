# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.FabInstructions do
  use Ecto.Schema
  import Ecto.Changeset

  @table "fab_instructions"
  @schema_prefix "daedalus"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :plan_id, :binary_id
    field :revision, :integer, default: 1
    field :machine_profile, :string
    field :dialect, :string, default: "gcode"
    field :storage_uri, :string
    field :content_hash, :string
    field :validated, :boolean, default: false
    field :validation, :map, default: %{}
    field :released_by_email, :string
    field :released_at, :utc_datetime_usec
  end

  @required_fields ~w(plan_id machine_profile storage_uri)a
  @optional_fields ~w(revision dialect content_hash validated validation released_by_email released_at)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_number(:revision, greater_than_or_equal_to: 1)
    |> validate_inclusion(:dialect, ["gcode", "nc", "apt", "proprietary"])
  end
end
