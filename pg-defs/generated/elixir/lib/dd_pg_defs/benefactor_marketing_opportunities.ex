# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingOpportunities do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_opportunities"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :lead_id, :binary_id
    field :status, :string, default: "open"
    field :stage, :string, default: "prospecting"
    field :name, :string
    field :amount_cents, :integer, default: 0
    field :probability_micros, :integer, default: 0
    field :expected_close_on, :string
    field :owner_user_id, :binary_id
    field :meta_data, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(client_id name)a
  @optional_fields ~w(lead_id status stage amount_cents probability_micros expected_close_on owner_user_id meta_data)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:status, ["open", "won", "lost", "paused"])
    |> validate_inclusion(:stage, ["prospecting", "qualified", "meeting", "proposal", "negotiation", "closed"])
    |> validate_length(:name, max: 220)
    |> validate_number(:amount_cents, greater_than_or_equal_to: 0)
    |> validate_number(:probability_micros, greater_than_or_equal_to: 0, less_than_or_equal_to: 1000000)
    |> validate_format(:expected_close_on, ~r/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)
    |> validate_length(:expected_close_on, max: 10)
  end
end
