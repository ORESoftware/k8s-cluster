# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.AgentRemoteDevBreadcrumbs do
  use Ecto.Schema
  import Ecto.Changeset

  @table "agent_remote_dev_breadcrumbs"

  @primary_key {:id, :id, autogenerate: true}

  schema @table do
    field :thread_id, :binary_id
    field :task_id, :binary_id
    field :kind, :string
    field :payload, :map, default: %{}
    field :emitted_at, :utc_datetime_usec
    field :pod_name, :string
    field :branch, :string
    field :provider, :string
  end

  @required_fields ~w(thread_id kind)a
  @optional_fields ~w(task_id payload emitted_at pod_name branch provider)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_format(:kind, ~r/^[A-Za-z0-9._:-]{1,80}$/)
    |> validate_length(:kind, min: 1, max: 80)
    |> validate_length(:pod_name, max: 253)
    |> validate_length(:branch, max: 120)
    |> validate_length(:provider, max: 60)
  end
end
