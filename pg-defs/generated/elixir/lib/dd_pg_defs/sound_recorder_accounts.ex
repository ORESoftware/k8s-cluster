# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.SoundRecorderAccounts do
  use Ecto.Schema
  import Ecto.Changeset

  @table "sound_recorder_accounts"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :status, :string, default: "active"
    field :external_subject, :string
    field :display_name, :string
    field :legal_region, :string
    field :retention_hours, :integer, default: 500
    field :retention_policy_version, :string, default: "sound-recorder-retention-v1"
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w()a
  @optional_fields ~w(status external_subject display_name legal_region retention_hours retention_policy_version)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_inclusion(:status, ["active", "paused", "locked", "deleted"])
    |> validate_length(:external_subject, max: 240)
    |> validate_length(:display_name, max: 160)
    |> validate_format(:legal_region, ~r/^[A-Za-z0-9._:\/-]{1,64}$/)
    |> validate_length(:legal_region, max: 64)
    |> validate_number(:retention_hours, greater_than_or_equal_to: 1, less_than_or_equal_to: 500)
    |> validate_format(:retention_policy_version, ~r/^[A-Za-z0-9._:\/-]{1,80}$/)
    |> validate_length(:retention_policy_version, max: 80)
  end
end
