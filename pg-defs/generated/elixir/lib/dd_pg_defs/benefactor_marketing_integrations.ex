# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingIntegrations do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_integrations"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :platform, :string, default: nil
    field :status, :string, default: "connected"
    field :auth_kind, :string, default: "manual"
    field :external_account_id, :string
    field :sync_cursor, :string
    field :config, :map, default: %{}
    field :last_sync_at, :utc_datetime_usec
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(platform)a
  @optional_fields ~w(client_id status auth_kind external_account_id sync_cursor config last_sync_at)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:platform, ["salesforce", "hubspot", "apollo", "zoominfo", "google_analytics", "google_ads", "linkedin_ads", "meta_ads", "mailchimp", "sendgrid", "scraper", "custom"])
    |> validate_inclusion(:status, ["connected", "disabled", "error"])
    |> validate_inclusion(:auth_kind, ["oauth2", "api_key", "webhook", "manual"])
    |> validate_length(:external_account_id, max: 200)
  end
end
