# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingClients do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_clients"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :status, :string, default: "onboarding"
    field :name, :string
    field :slug, :string
    field :industry, :string
    field :website_url, :string
    field :billing_email, :string
    field :owner_user_id, :binary_id
    field :service_package, :string
    field :onboarding_stage, :string, default: "intake"
    field :portal_enabled, :boolean, default: true
    field :meta_data, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(name slug)a
  @optional_fields ~w(status industry website_url billing_email owner_user_id service_package onboarding_stage portal_enabled meta_data)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:status, ["onboarding", "active", "paused", "archived"])
    |> validate_length(:name, max: 200)
    |> validate_format(:slug, ~r/^[a-z0-9][a-z0-9-]{1,218}[a-z0-9]$/)
    |> validate_length(:slug, max: 220)
    |> validate_length(:industry, max: 120)
    |> validate_length(:billing_email, max: 240)
    |> validate_length(:service_package, max: 120)
    |> validate_format(:onboarding_stage, ~r/^[A-Za-z0-9._:\/-]{1,80}$/)
    |> validate_length(:onboarding_stage, max: 80)
  end
end
