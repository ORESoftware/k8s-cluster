# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingContentAssets do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_content_assets"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :campaign_id, :binary_id
    field :status, :string, default: "draft"
    field :asset_kind, :string, default: nil
    field :title, :string
    field :channel, :string
    field :body, :string
    field :asset_uri, :string
    field :seo_keywords, {:array, :map}, default: []
    field :approval_status, :string, default: "pending"
    field :publish_at, :utc_datetime_usec
    field :meta_data, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(client_id asset_kind title)a
  @optional_fields ~w(campaign_id status channel body asset_uri seo_keywords approval_status publish_at meta_data)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:status, ["draft", "in_review", "approved", "scheduled", "published", "archived"])
    |> validate_inclusion(:asset_kind, ["blog", "social_post", "email", "landing_page", "ad_creative", "video", "script", "proposal", "report"])
    |> validate_length(:title, max: 240)
    |> validate_length(:channel, max: 64)
    |> validate_inclusion(:approval_status, ["pending", "approved", "rejected", "changes_requested"])
  end
end
