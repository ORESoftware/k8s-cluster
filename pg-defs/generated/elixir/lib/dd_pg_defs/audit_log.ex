# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.AuditLog do
  use Ecto.Schema
  import Ecto.Changeset

  @table "audit_log"
  @schema_prefix "fiducia"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :org_id, :binary_id
    field :project_id, :binary_id
    field :actor_user_id, :binary_id
    field :actor_key_id, :binary_id
    field :actor, :string
    field :action, :string
    field :target, :string
    field :request_id, :string
    field :source_ip, :string
    field :user_agent, :string
    field :meta, :map, default: %{}
    field :retention_expires_at, :utc_datetime_usec
  end

  @required_fields ~w(action)a
  @optional_fields ~w(org_id project_id actor_user_id actor_key_id actor target request_id source_ip user_agent meta retention_expires_at)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_length(:actor, max: 320)
    |> validate_length(:action, max: 120)
    |> validate_length(:target, max: 320)
    |> validate_length(:request_id, max: 120)
    |> validate_length(:source_ip, max: 64)
    |> validate_length(:user_agent, max: 500)
  end
end
