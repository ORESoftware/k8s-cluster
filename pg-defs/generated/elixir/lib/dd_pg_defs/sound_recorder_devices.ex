# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.SoundRecorderDevices do
  use Ecto.Schema
  import Ecto.Changeset

  @table "sound_recorder_devices"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :account_id, :binary_id
    field :platform, :string, default: nil
    field :status, :string, default: "active"
    field :install_id, :string
    field :device_label, :string
    field :app_version, :string
    field :os_version, :string
    field :token_hash, :string
    field :token_last4, :string
    field :consent_version, :string
    field :consent_accepted_at, :utc_datetime_usec
    field :recording_indicator_acknowledged, :boolean, default: false
    field :last_seen_at, :utc_datetime_usec
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(account_id platform install_id token_hash token_last4 consent_version consent_accepted_at)a
  @optional_fields ~w(status device_label app_version os_version recording_indicator_acknowledged last_seen_at)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:platform, ["ios", "android"])
    |> validate_inclusion(:status, ["active", "revoked", "lost", "replaced", "deleted"])
    |> validate_length(:install_id, max: 160)
    |> validate_length(:device_label, max: 160)
    |> validate_length(:app_version, max: 80)
    |> validate_length(:os_version, max: 80)
    |> validate_format(:token_hash, ~r/^[a-f0-9]{64}$/)
    |> validate_length(:token_hash, max: 64)
    |> validate_format(:token_last4, ~r/^[A-Za-z0-9_-]{4}$/)
    |> validate_length(:token_last4, max: 4)
    |> validate_format(:consent_version, ~r/^[A-Za-z0-9._:\/-]{1,80}$/)
    |> validate_length(:consent_version, max: 80)
  end
end
