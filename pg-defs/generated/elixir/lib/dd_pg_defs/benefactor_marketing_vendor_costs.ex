# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingVendorCosts do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_vendor_costs"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :campaign_id, :binary_id
    field :vendor_name, :string
    field :category, :string, default: nil
    field :status, :string, default: "planned"
    field :amount_cents, :integer
    field :incurred_on, :string
    field :invoice_ref, :string
    field :meta_data, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(vendor_name category amount_cents)a
  @optional_fields ~w(client_id campaign_id status incurred_on invoice_ref meta_data)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_length(:vendor_name, max: 200)
    |> validate_inclusion(:category, ["ads", "creative", "data", "software", "contractor", "events", "other"])
    |> validate_inclusion(:status, ["planned", "approved", "incurred", "invoiced", "paid", "canceled"])
    |> validate_number(:amount_cents, greater_than_or_equal_to: 0)
    |> validate_format(:incurred_on, ~r/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)
    |> validate_length(:incurred_on, max: 10)
    |> validate_length(:invoice_ref, max: 120)
  end
end
