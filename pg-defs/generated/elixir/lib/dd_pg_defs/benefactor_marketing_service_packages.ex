# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingServicePackages do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_service_packages"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :status, :string, default: "active"
    field :code, :string
    field :name, :string
    field :channel_mix, {:array, :map}, default: []
    field :deliverables, {:array, :map}, default: []
    field :monthly_budget_cents, :integer, default: 0
    field :retainer_cents, :integer, default: 0
    field :meta_data, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(code name)a
  @optional_fields ~w(status channel_mix deliverables monthly_budget_cents retainer_cents meta_data)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:status, ["active", "retired"])
    |> validate_format(:code, ~r/^[A-Za-z0-9._:\/-]{1,120}$/)
    |> validate_length(:code, max: 120)
    |> validate_length(:name, max: 200)
    |> validate_number(:monthly_budget_cents, greater_than_or_equal_to: 0)
    |> validate_number(:retainer_cents, greater_than_or_equal_to: 0)
  end
end
