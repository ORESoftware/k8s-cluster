# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.DesSoccerLearningSetPlayRuns do
  use Ecto.Schema
  import Ecto.Changeset

  @table "des_soccer_learning_set_play_runs"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :policy_version_id, :binary_id
    field :primary_restart, :string, default: nil
    field :team, :string, default: nil
    field :spot_x_micros, :integer
    field :spot_y_micros, :integer
    field :duration_seconds_micros, :integer
    field :episode_count, :integer
    field :goals, :integer
    field :goal_rate_micros, :integer
    field :first_window_goal_rate_micros, :integer
    field :last_window_goal_rate_micros, :integer
    field :goal_rate_delta_micros, :integer
  end

  @required_fields ~w(policy_version_id primary_restart team spot_x_micros spot_y_micros duration_seconds_micros episode_count goals goal_rate_micros first_window_goal_rate_micros last_window_goal_rate_micros goal_rate_delta_micros)a
  @optional_fields ~w()a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:primary_restart, ["direct-free-kick", "indirect-free-kick"])
    |> validate_inclusion(:team, ["home", "away"])
    |> validate_number(:duration_seconds_micros, greater_than_or_equal_to: 0)
    |> validate_number(:episode_count, greater_than_or_equal_to: 0)
    |> validate_number(:goals, greater_than_or_equal_to: 0)
    |> validate_number(:goal_rate_micros, greater_than_or_equal_to: 0, less_than_or_equal_to: 1000000)
  end
end
