{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dd.dev/schemas/nats/dataset-labeling.schema.json",
  "title": "DatasetLabelingNats",
  "description": "NATS subjects for the dataset labeling pipeline: labeling task requests, annotator/label events, weak-supervision aggregation results, and downstream Spark/Airflow training-data materialization triggers.",
  "type": "object",
  "$dd:nats": {
    "service": "dd-dataset-labeling",
    "summary": "Labeling task creation, human/model annotation, weak-supervision labeling functions, label aggregation/agreement, gold-label export, and big-data training-data pipeline handoff subjects.",
    "subjects": [
      {
        "name": "DatasetLabelingTaskRequests",
        "description": "Inbound labeling task/annotation requests accepted over NATS. Payloads mirror the HTTP /tasks, /labels, and /functions/apply contracts.",
        "kind": "static",
        "subject": "dd.remote.dataset_labeling.task.requests",
        "queueGroup": "dd-dataset-labeling",
        "direction": "both"
      },
      {
        "name": "DatasetLabelingLabelEvents",
        "description": "Per-label annotation events emitted when a human annotator, model, or labeling function submits a label for a task item.",
        "kind": "static",
        "subject": "dd.remote.dataset_labeling.label.events",
        "direction": "publish"
      },
      {
        "name": "DatasetLabelingResults",
        "description": "Aggregation, inter-annotator agreement, and gold-label export results from labeling runs.",
        "kind": "static",
        "subject": "dd.remote.dataset_labeling.results",
        "direction": "publish"
      },
      {
        "name": "DatasetLabelingPipelineJobs",
        "description": "Spark/Airflow pipeline job intents that materialize aggregated gold labels into training datasets for downstream model training.",
        "kind": "static",
        "subject": "dd.remote.dataset_labeling.pipeline.jobs",
        "direction": "both"
      }
    ],
    "queueGroups": [
      {
        "name": "DatasetLabelingWorkersQueueGroup",
        "value": "dd-dataset-labeling",
        "description": "Shared queue group used by dd-dataset-labeling replicas so each queued labeling task/annotation request is processed once."
      }
    ]
  }
}
