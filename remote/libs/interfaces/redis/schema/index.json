{
  "$comment": "Index of every redis schema file. The generator iterates this list (alphabetised) so adding a new schema is a one-line change.",
  "schemas": [
    "agent-thread-breadcrumb-cache.schema.json",
    "container-pool-affinity-lock.schema.json",
    "runtime-config-redis.schema.json",
    "vapi-phone-call-cache.schema.json"
  ]
}
