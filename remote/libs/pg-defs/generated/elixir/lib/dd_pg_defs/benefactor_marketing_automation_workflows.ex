# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingAutomationWorkflows do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_automation_workflows"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :status, :string, default: "draft"
    field :name, :string
    field :trigger_kind, :string, default: nil
    field :trigger_config, :map, default: %{}
    field :action_graph, :map, default: %{}
    field :last_run_at, :utc_datetime_usec
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(client_id name trigger_kind)a
  @optional_fields ~w(status trigger_config action_graph last_run_at)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:status, ["draft", "active", "paused", "archived"])
    |> validate_length(:name, max: 220)
    |> validate_inclusion(:trigger_kind, ["lead_created", "score_changed", "form_submit", "email_event", "campaign_event", "manual", "schedule", "webhook"])
  end
end
