# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingAttributionEvents do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_attribution_events"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :campaign_id, :binary_id
    field :lead_id, :binary_id
    field :event_type, :string, default: nil
    field :source_platform, :string
    field :source_event_id, :string
    field :occurred_at, :utc_datetime_usec
    field :value_cents, :integer, default: 0
    field :payload, :map, default: %{}
  end

  @required_fields ~w(client_id event_type)a
  @optional_fields ~w(campaign_id lead_id source_platform source_event_id occurred_at value_cents payload)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:event_type, ["impression", "click", "form_submit", "email_open", "email_click", "meeting_booked", "opportunity_created", "deal_won", "revenue"])
    |> validate_length(:source_platform, max: 64)
    |> validate_length(:source_event_id, max: 200)
    |> validate_number(:value_cents, greater_than_or_equal_to: 0)
  end
end
