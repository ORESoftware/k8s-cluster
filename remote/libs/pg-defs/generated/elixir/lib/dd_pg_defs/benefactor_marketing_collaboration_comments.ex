# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingCollaborationComments do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_collaboration_comments"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :parent_comment_id, :binary_id
    field :resource_type, :string, default: nil
    field :resource_id, :binary_id
    field :author_user_id, :binary_id
    field :author_contact_id, :binary_id
    field :body, :string
    field :status, :string, default: "open"
    field :visibility, :string, default: "client_portal"
    field :meta_data, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(client_id resource_type body)a
  @optional_fields ~w(parent_comment_id resource_id author_user_id author_contact_id status visibility meta_data)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:resource_type, ["client", "campaign", "content_asset", "approval", "ticket", "document", "report", "meeting"])
    |> validate_inclusion(:status, ["open", "resolved", "archived"])
    |> validate_inclusion(:visibility, ["internal", "client_portal"])
  end
end
