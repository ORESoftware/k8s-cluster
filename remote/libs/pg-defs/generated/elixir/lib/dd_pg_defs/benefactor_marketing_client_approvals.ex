# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingClientApprovals do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_client_approvals"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :campaign_id, :binary_id
    field :content_asset_id, :binary_id
    field :requested_by, :binary_id
    field :status, :string, default: "pending"
    field :approval_kind, :string, default: nil
    field :title, :string
    field :request_payload, :map, default: %{}
    field :response_note, :string
    field :due_at, :utc_datetime_usec
    field :decided_at, :utc_datetime_usec
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(client_id approval_kind title)a
  @optional_fields ~w(campaign_id content_asset_id requested_by status request_payload response_note due_at decided_at)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:status, ["pending", "approved", "rejected", "expired", "canceled"])
    |> validate_inclusion(:approval_kind, ["campaign_launch", "content_publish", "budget_change", "report_send", "lead_list"])
    |> validate_length(:title, max: 240)
  end
end
