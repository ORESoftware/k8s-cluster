# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingOutreachTouchpoints do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_outreach_touchpoints"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :sequence_id, :binary_id
    field :enrollment_id, :binary_id
    field :campaign_id, :binary_id
    field :lead_id, :binary_id
    field :contact_id, :binary_id
    field :channel, :string, default: nil
    field :direction, :string, default: "outbound"
    field :status, :string, default: "planned"
    field :subject, :string
    field :body_excerpt, :string
    field :external_message_id, :string
    field :occurred_at, :utc_datetime_usec
    field :payload, :map, default: %{}
  end

  @required_fields ~w(client_id channel)a
  @optional_fields ~w(sequence_id enrollment_id campaign_id lead_id contact_id direction status subject body_excerpt external_message_id occurred_at payload)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:channel, ["email", "linkedin", "sms", "phone", "task", "meeting"])
    |> validate_inclusion(:direction, ["outbound", "inbound", "internal"])
    |> validate_inclusion(:status, ["planned", "sent", "delivered", "opened", "clicked", "replied", "failed", "bounced"])
    |> validate_length(:subject, max: 240)
    |> validate_length(:external_message_id, max: 200)
  end
end
