# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.UsaccContractOperations do
  use Ecto.Schema
  import Ecto.Changeset

  @table "usacc_contract_operations"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :case_id, :binary_id
    field :election_id, :binary_id
    field :vote_id, :binary_id
    field :request_id, :string
    field :operation_kind, :string, default: nil
    field :status, :string, default: "pending"
    field :program_id, :string
    field :digest, :string
    field :envelope, :map, default: %{}
    field :response, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(request_id operation_kind)a
  @optional_fields ~w(case_id election_id vote_id status program_id digest envelope response)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_length(:request_id, max: 160)
    |> validate_inclusion(:operation_kind, ["validate_envelope", "simulate_transaction", "send_transaction", "vote_commitment", "escrow_notary"])
    |> validate_inclusion(:status, ["pending", "validated", "simulated", "sent", "failed", "canceled"])
    |> validate_length(:program_id, max: 128)
    |> validate_length(:digest, max: 160)
  end
end
