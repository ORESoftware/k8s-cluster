# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingMeetings do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_meetings"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :lead_id, :binary_id
    field :opportunity_id, :binary_id
    field :status, :string, default: "scheduled"
    field :meeting_kind, :string, default: nil
    field :title, :string
    field :scheduled_at, :utc_datetime_usec
    field :duration_minutes, :integer, default: 30
    field :notes, :string
    field :recording_uri, :string
    field :transcript_summary, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(client_id meeting_kind title scheduled_at)a
  @optional_fields ~w(lead_id opportunity_id status duration_minutes notes recording_uri transcript_summary)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:status, ["scheduled", "completed", "canceled", "no_show"])
    |> validate_inclusion(:meeting_kind, ["onboarding", "report_review", "sales_discovery", "strategy", "content_review", "support"])
    |> validate_length(:title, max: 240)
    |> validate_number(:duration_minutes, greater_than_or_equal_to: 1, less_than_or_equal_to: 1440)
  end
end
