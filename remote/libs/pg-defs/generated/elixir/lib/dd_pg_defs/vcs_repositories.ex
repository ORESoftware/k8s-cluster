# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.VcsRepositories do
  use Ecto.Schema
  import Ecto.Changeset

  @table "vcs_repositories"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :slug, :string
    field :display_name, :string
    field :vcs_kind, :string, default: "git"
    field :remote_url, :string
    field :default_branch, :string, default: "main"
    field :mirror_path, :string
    field :mirror_status, :string, default: "pending"
    field :visibility, :string, default: "private"
    field :last_synced_at, :utc_datetime_usec
    field :last_error, :string
    field :size_bytes, :integer, default: 0
    field :ref_count, :integer, default: 0
    field :meta_data, :map, default: %{}
    field :is_soft_deleted, :boolean, default: false
    field :created_by, :binary_id
    field :updated_by, :binary_id
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(slug display_name remote_url)a
  @optional_fields ~w(vcs_kind default_branch mirror_path mirror_status visibility last_synced_at last_error size_bytes ref_count meta_data is_soft_deleted created_by updated_by)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_format(:slug, ~r/^[a-z0-9][a-z0-9._-]{0,119}$/)
    |> validate_length(:slug, max: 120)
    |> validate_length(:display_name, max: 200)
    |> validate_inclusion(:vcs_kind, ["git", "hg", "svn", "fossil"])
    |> validate_format(:default_branch, ~r/^[A-Za-z0-9._\/-]{1,160}$/)
    |> validate_length(:default_branch, max: 160)
    |> validate_inclusion(:mirror_status, ["pending", "mirroring", "ready", "error", "disabled"])
    |> validate_inclusion(:visibility, ["private", "internal", "public"])
    |> validate_number(:size_bytes, greater_than_or_equal_to: 0)
    |> validate_number(:ref_count, greater_than_or_equal_to: 0)
  end
end
