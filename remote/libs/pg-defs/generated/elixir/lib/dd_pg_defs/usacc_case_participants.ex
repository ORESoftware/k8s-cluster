# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.UsaccCaseParticipants do
  use Ecto.Schema
  import Ecto.Changeset

  @table "usacc_case_participants"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :case_id, :binary_id
    field :user_id, :binary_id
    field :role, :string, default: nil
    field :status, :string, default: "active"
    field :granted_by, :binary_id
    field :granted_by_policy_version, :string
    field :ended_at, :utc_datetime_usec
    field :ended_reason, :string
    field :meta_data, :map, default: %{}
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(case_id user_id role)a
  @optional_fields ~w(status granted_by granted_by_policy_version ended_at ended_reason meta_data)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:role, ["plaintiff", "defendant", "sponsor", "witness", "judge", "panel_juror", "appeal_judge", "presiding_juror", "paralegal", "investigator", "intake_reviewer", "clerk_of_court", "compliance_monitor", "counsel", "oversight_board", "auditor", "ombuds"])
    |> validate_inclusion(:status, ["active", "pending", "declined", "suspended", "ended", "banned"])
    |> validate_format(:granted_by_policy_version, ~r/^[A-Za-z0-9._:\/-]{1,120}$/)
    |> validate_length(:granted_by_policy_version, max: 120)
    |> validate_length(:ended_reason, max: 240)
  end
end
