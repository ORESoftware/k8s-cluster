# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.BenefactorMarketingOutreachEnrollments do
  use Ecto.Schema
  import Ecto.Changeset

  @table "benefactor_marketing_outreach_enrollments"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :client_id, :binary_id
    field :sequence_id, :binary_id
    field :lead_id, :binary_id
    field :contact_id, :binary_id
    field :status, :string, default: "active"
    field :current_step_order, :integer, default: 1
    field :enrollment_context, :map, default: %{}
    field :last_touch_at, :utc_datetime_usec
    field :next_touch_at, :utc_datetime_usec
    field :outcome, :string
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(client_id sequence_id)a
  @optional_fields ~w(lead_id contact_id status current_step_order enrollment_context last_touch_at next_touch_at outcome)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_inclusion(:status, ["active", "paused", "completed", "bounced", "unsubscribed", "failed"])
    |> validate_number(:current_step_order, greater_than_or_equal_to: 1, less_than_or_equal_to: 100)
    |> validate_length(:outcome, max: 64)
  end
end
