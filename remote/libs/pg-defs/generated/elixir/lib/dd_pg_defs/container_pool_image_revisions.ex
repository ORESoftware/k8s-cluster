# Generated by @dd/pg-defs. Do not edit by hand.
# SOURCE OF TRUTH: schema/schema.sql defines the database contract.
# Generated ORM/client code is an adapter only; do not infer migrations from it.
# MIGRATION SAFETY: never run or apply migrations automatically. Require explicit human review and approval before any database write.

defmodule DdPgDefs.ContainerPoolImageRevisions do
  use Ecto.Schema
  import Ecto.Changeset

  @table "container_pool_image_revisions"

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema @table do
    field :image_slug, :string
    field :image_ref, :string
    field :dockerfile_path, :string
    field :build_context, :string
    field :dockerfile_text, :string
    field :dockerfile_sha256, :string
    field :source, :string, default: "user"
    field :notes, :string, default: ""
    field :status, :string, default: "candidate"
    field :meta_data, :map, default: %{}
    field :is_soft_deleted, :boolean, default: false
    field :created_by, :binary_id
    field :updated_by, :binary_id
    timestamps(inserted_at: :created_at, type: :utc_datetime_usec)
  end

  @required_fields ~w(image_slug image_ref dockerfile_path build_context dockerfile_text dockerfile_sha256)a
  @optional_fields ~w(source notes status meta_data is_soft_deleted created_by updated_by)a

  @doc "Builds an Ecto changeset enforcing every constraint exposed in schema.sql."
  def changeset(struct, attrs) do
    struct
    |> cast(attrs, @required_fields ++ @optional_fields)
    |> validate_required(@required_fields)
    |> validate_format(:image_slug, ~r/^[a-z0-9][a-z0-9-]{0,118}[a-z0-9]$/)
    |> validate_length(:image_slug, max: 120)
    |> validate_format(:dockerfile_sha256, ~r/^[0-9a-f]{64}$/)
    |> validate_length(:dockerfile_sha256, max: 64)
    |> validate_inclusion(:source, ["disk-default", "user", "system"])
    |> validate_inclusion(:status, ["candidate", "active", "archived"])
  end
end
