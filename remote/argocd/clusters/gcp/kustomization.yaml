apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      dd.dev/cloud-provider: gcp

resources:
  - storageclass.yaml
  - applications.yaml
  - ai-agent-coordinator.applications.yaml
  # Registration is safe before activation: all six child Applications are manual.
  - ../../multicloud-data-plane/clusters/gcp
  - root.application.yaml
