apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: spark-worker-available
  namespace: big-data
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: spark
      app.kubernetes.io/component: worker
