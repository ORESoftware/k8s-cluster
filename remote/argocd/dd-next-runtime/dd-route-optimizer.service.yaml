apiVersion: v1
kind: Service
metadata:
  name: dd-route-optimizer
  namespace: default
  labels:
    app: dd-route-optimizer
spec:
  selector:
    app: dd-route-optimizer
  ports:
    - name: http
      port: 8132
      targetPort: http
