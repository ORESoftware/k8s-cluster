apiVersion: v1
kind: Service
metadata:
  name: dd-runtime-config
  namespace: default
  labels:
    app: dd-runtime-config
spec:
  type: ClusterIP
  selector:
    app: dd-runtime-config
  ports:
    - name: http
      port: 8110
      targetPort: 8110
