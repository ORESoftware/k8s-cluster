apiVersion: v1
kind: Service
metadata:
  name: dd-athleto-backend
  namespace: default
  labels:
    app: dd-athleto-backend
spec:
  type: ClusterIP
  selector:
    app: dd-athleto-backend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
