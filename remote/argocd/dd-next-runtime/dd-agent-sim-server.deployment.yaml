apiVersion: apps/v1
kind: Deployment
metadata:
  name: dd-agent-sim-server
  namespace: default
  labels:
    app: dd-agent-sim-server
spec:
  # Scaled to 0 by default (like dd-mdp-optimizer) to spare the single-node
  # cluster a cold-start rustc build storm; scale up to exercise the service.
  replicas: 0
  minReadySeconds: 5
  progressDeadlineSeconds: 1800
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: dd-agent-sim-server
  template:
    metadata:
      annotations:
        dd.dev/telemetry-revision: '2026-06-09-fun-servers'
      labels:
        app: dd-agent-sim-server
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 20
      containers:
        - name: agent-sim-server
          image: docker.io/library/rust:1.90-bookworm
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          command:
            - /bin/bash
            - -lc
          args:
            - |
              set -euo pipefail
              export PATH=/usr/local/cargo/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
              cd /opt/dd-next-1/remote/deployments/agent-sim-server-rs
              exec /usr/local/cargo/bin/cargo run --release
          env:
            - name: HOST
              value: 0.0.0.0
            - name: PORT
              value: '8133'
            - name: NATS_URL
              value: nats://dd-nats.messaging.svc.cluster.local:4222
            - name: AGENT_SIM_SIMULATE_SUBJECT
              value: dd.remote.agent_sim.simulate.requests
            - name: AGENT_SIM_QUEUE_GROUP
              value: dd-agent-sim-server
            - name: AGENT_SIM_FRAME_SUBJECT
              value: dd.remote.agent_sim.frames
            - name: AGENT_SIM_RESULT_SUBJECT
              value: dd.remote.agent_sim.simulate.results
            - name: AGENT_SIM_WS_SUBJECT
              value: dd.remote.websocket.events
            - name: AGENT_SIM_EVENT_SUBJECT
              value: dd.remote.events
            - name: AGENT_SIM_MAX_INFLIGHT
              value: '8'
            # Optional app-level auth: populate this secret key to require an
            # 'x-server-auth: <secret>' header on the compute endpoint (open if unset).
            - name: AGENT_SIM_AUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: dd-agent-secrets
                  key: AGENT_SIM_AUTH_SECRET
                  optional: true
            # runtime-config wiring: registers this pod with the control plane
            # so it receives /internal/update-runtime-config pushes every 5 min
            # plus on-demand admin pushes.
            - name: RUNTIME_CONFIG_SERVICE_NAME
              value: dd-agent-sim-server
            - name: RUNTIME_CONFIG_SCOPE
              value: dd-agent-sim-server
            - name: RUNTIME_CONFIG_ENV
              value: stage
            - name: RUNTIME_CONFIG_REGISTER_URL
              value: http://dd-runtime-config.default.svc.cluster.local:8110/subscribers
            - name: RUNTIME_CONFIG_APPLY_URL
              value: http://dd-agent-sim-server.default.svc.cluster.local:8133/internal/update-runtime-config
            - name: RUNTIME_CONFIG_SERVER_SECRET
              valueFrom:
                secretKeyRef:
                  name: dd-agent-secrets
                  key: SERVER_AUTH_SECRET
                  optional: true
          ports:
            - name: http
              containerPort: 8133
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: '1'
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: repo
              mountPath: /opt/dd-next-1
      volumes:
        - name: repo
          hostPath:
            path: /home/ec2-user/codes/dd/dd-next-1
            type: Directory
