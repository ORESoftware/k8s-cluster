apiVersion: v1
kind: Service
metadata:
  name: dd-webrtc-media
  namespace: default
  labels:
    app: dd-webrtc-media
spec:
  type: ClusterIP
  selector:
    app: dd-webrtc-media
  ports:
    - name: http
      port: 8125
      targetPort: http
