apiVersion: v1
kind: Service
metadata:
  name: dd-raft-consensus
  namespace: default
  labels:
    app: dd-raft-consensus
spec:
  selector:
    app: dd-raft-consensus
  ports:
    - name: http
      port: 8135
      targetPort: http
