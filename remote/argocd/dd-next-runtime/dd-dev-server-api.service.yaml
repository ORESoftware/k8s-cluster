apiVersion: v1
kind: Service
metadata:
  name: dd-dev-server-api
  namespace: default
  labels:
    app: dd-dev-server-api
spec:
  type: ClusterIP
  selector:
    app: dd-dev-server-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
