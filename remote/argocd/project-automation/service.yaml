apiVersion: v1
kind: Service
metadata:
  name: dd-project-automation
  namespace: default
  labels:
    app.kubernetes.io/name: dd-project-automation
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dd-project-automation
  ports:
    - name: http
      port: 8787
      targetPort: http
      protocol: TCP
