apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Disabled catalog component. No live Argo CD or root kustomization references
# this directory, and the BackendTrafficPolicy target is deliberately missing.
resources:
  - backend-traffic-policy.yaml
