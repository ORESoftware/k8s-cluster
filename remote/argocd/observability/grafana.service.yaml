apiVersion: v1
kind: Service
metadata:
  name: dd-grafana
  namespace: observability
  labels:
    app: dd-grafana
spec:
  type: ClusterIP
  selector:
    app: dd-grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
