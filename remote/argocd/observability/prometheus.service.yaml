apiVersion: v1
kind: Service
metadata:
  name: dd-prometheus
  namespace: observability
  labels:
    app: dd-prometheus
spec:
  type: ClusterIP
  selector:
    app: dd-prometheus
  ports:
    - name: http
      port: 9090
      targetPort: 9090
