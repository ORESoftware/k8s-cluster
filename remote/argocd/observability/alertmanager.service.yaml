apiVersion: v1
kind: Service
metadata:
  name: dd-alertmanager
  namespace: observability
  labels:
    app: dd-alertmanager
spec:
  type: ClusterIP
  selector:
    app: dd-alertmanager
  ports:
    - name: http
      port: 9093
      targetPort: 9093
