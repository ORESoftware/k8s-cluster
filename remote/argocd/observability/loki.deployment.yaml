apiVersion: apps/v1
kind: Deployment
metadata:
  name: dd-loki
  namespace: observability
  labels:
    app: dd-loki
  annotations:
    # Pick up limits/query-config edits without a manual rollout.
    configmap.reloader.stakater.com/reload: "dd-loki-config"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dd-loki
  template:
    metadata:
      labels:
        app: dd-loki
    spec:
      automountServiceAccountToken: false
      containers:
        - name: loki
          image: grafana/loki:3.2.1
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          args:
            - -config.file=/etc/loki/loki.yaml
          ports:
            - name: http
              containerPort: 3100
          readinessProbe:
            httpGet:
              path: /ready
              port: 3100
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          volumeMounts:
            - name: config
              mountPath: /etc/loki
            - name: data
              mountPath: /loki
      volumes:
        - name: config
          configMap:
            name: dd-loki-config
        - name: data
          # Hard backstop: even if retention lags, Loki can never consume more
          # than this of the node root disk (kubelet evicts the pod if exceeded),
          # so it can't re-trip node disk-pressure. Pairs with retention_period.
          emptyDir:
            sizeLimit: 40Gi
