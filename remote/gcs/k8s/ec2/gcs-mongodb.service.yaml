apiVersion: v1
kind: Service
metadata:
  name: gcs-mongodb
  namespace: default
  labels:
    app: gcs-mongodb
spec:
  type: ClusterIP
  selector:
    app: gcs-mongodb
  ports:
    - name: mongodb
      port: 27017
      targetPort: mongodb
