apiVersion: apps/v1
kind: Deployment
metadata:
  name: dd-evolution-optimizer-master
  namespace: ai-ml
  labels:
    app: dd-evolution-optimizer-master
    app.kubernetes.io/component: master
spec:
  replicas: 1
  minReadySeconds: 5
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: dd-evolution-optimizer-master
  template:
    metadata:
      labels:
        app: dd-evolution-optimizer-master
        app.kubernetes.io/component: master
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      containers:
        - name: optimizer
          image: REPLACE_ME_ECR_URI/dd-evolution-optimizer:latest
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          env:
            - name: HOST
              value: 0.0.0.0
            - name: PORT
              value: '8131'
            - name: EVOLUTION_NODE_ROLE
              value: master
            - name: NATS_URL
              value: nats://dd-nats.messaging.svc.cluster.local:4222
            # Optional: when present, /optimize requires this as a bearer token.
            - name: EVOLUTION_AUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: dd-agent-secrets
                  key: EVOLUTION_AUTH_SECRET
                  optional: true
          ports:
            - name: http
              containerPort: 8131
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: '1'
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
