apiVersion: v1
kind: Service
metadata:
  name: dd-evolution-optimizer
  namespace: ai-ml
  labels:
    app: dd-evolution-optimizer-master
    app.kubernetes.io/component: master
spec:
  type: ClusterIP
  selector:
    app: dd-evolution-optimizer-master
  ports:
    - name: http
      port: 8131
      targetPort: http
