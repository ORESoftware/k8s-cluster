apiVersion: v1
kind: Service
metadata:
  name: dd-gleamlang-ws-server
  namespace: default
  labels:
    app: dd-gleamlang-ws-server
spec:
  selector:
    app: dd-gleamlang-ws-server
  ports:
    - name: http
      port: 8081
      targetPort: 8081
  type: ClusterIP
