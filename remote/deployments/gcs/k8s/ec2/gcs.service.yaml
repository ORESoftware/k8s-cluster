apiVersion: v1
kind: Service
metadata:
  name: gcs
  namespace: default
  labels:
    app: gcs
spec:
  type: ClusterIP
  selector:
    app: gcs
  ports:
    - name: rest
      port: 3000
      targetPort: rest
    - name: ws
      port: 3001
      targetPort: ws
