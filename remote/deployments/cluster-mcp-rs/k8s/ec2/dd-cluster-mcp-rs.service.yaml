apiVersion: v1
kind: Service
metadata:
  name: dd-cluster-mcp-rs
  namespace: default
  labels:
    app: dd-cluster-mcp-rs
spec:
  selector:
    app: dd-cluster-mcp-rs
  ports:
    - name: http
      port: 8091
      targetPort: 8091
  type: ClusterIP
