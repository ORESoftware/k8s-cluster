apiVersion: v1
kind: Service
metadata:
  name: dd-embeddings-rs
  namespace: default
  labels:
    app: dd-embeddings-rs
spec:
  type: ClusterIP
  selector:
    app: dd-embeddings-rs
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
