{
  "service": "dd-embeddings-rs",
  "version": "0.1.0",
  "description": "Multi-provider embedding gateway and RAG indexing service.",
  "notes": [
    "Provider `anthropic` is an alias for `voyage`: Anthropic has no first-party embeddings API and recommends Voyage AI.",
    "Providers are opt-in: only those with credentials in the secret are registered."
  ],
  "routes": [
    { "method": "GET", "path": "/healthz", "auth": false, "summary": "Liveness probe (no dependencies)." },
    { "method": "GET", "path": "/readyz", "auth": false, "summary": "Readiness probe; checks Qdrant reachability." },
    { "method": "GET", "path": "/api/docs.json", "auth": false, "summary": "This document, as JSON." },
    { "method": "GET", "path": "/api/docs", "auth": false, "summary": "Human-readable API docs." },
    { "method": "GET", "path": "/api/providers", "auth": true, "summary": "List configured embedding providers, default models, and aliases." },
    { "method": "POST", "path": "/api/embeddings", "auth": true, "summary": "Generate embeddings. Body: { provider, input[], model?, dimensions?, input_type? }." },
    { "method": "POST", "path": "/api/rag/index", "auth": true, "summary": "Embed documents and upsert into a Qdrant collection. Body: { collection, provider, documents[], model?, dimensions?, distance? }." },
    { "method": "POST", "path": "/api/rag/search", "auth": true, "summary": "Embed a query and retrieve nearest neighbors. Body: { collection, provider, query, top_k?, model?, dimensions? }." }
  ]
}
