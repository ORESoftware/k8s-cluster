/* PLACEHOLDER — replace with the real htmx 1.9.12 minified build.
 * Vendored locally (not a CDN) so the Content-Security-Policy stays self-only.
 * Fetch the real file at release time:
 *   curl -fsSL https://unpkg.com/htmx.org@1.9.12/dist/htmx.min.js -o assets/htmx.min.js
 * The pinned version lives in src/views.rs (HTMX_VERSION); keep them in lockstep.
 * This stub exists so the crate compiles and tests run offline. */
window.htmx = window.htmx || { version: "1.9.12-placeholder" };
