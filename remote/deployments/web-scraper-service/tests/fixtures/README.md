# Browser-agent field fixtures

- `safe-application-fields.html` contains ordinary job-application fields that the agent may fill.
- `sensitive-fields.html` contains credential, government-ID, banking, payment-card, and MFA fields used to verify fail-closed behavior.

Fixtures contain no real personal data or credentials.
