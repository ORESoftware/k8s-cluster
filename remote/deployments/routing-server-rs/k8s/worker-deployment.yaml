apiVersion: apps/v1
kind: Deployment
metadata:
  name: dd-routing-server-worker
  namespace: ai-ml
  labels:
    app: dd-routing-server-worker
    app.kubernetes.io/component: worker
spec:
  replicas: 1
  minReadySeconds: 5
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: dd-routing-server-worker
  template:
    metadata:
      labels:
        app: dd-routing-server-worker
        app.kubernetes.io/component: worker
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 60
      containers:
        - name: routing
          image: REPLACE_ME_ECR_URI/dd-routing-server:latest
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          env:
            - name: HOST
              value: 0.0.0.0
            - name: PORT
              value: '8132'
            - name: ROUTING_NODE_ROLE
              value: worker
            - name: NATS_URL
              value: nats://dd-nats.messaging.svc.cluster.local:4222
            - name: ROUTING_ACK_WAIT_SECONDS
              value: '300'
            - name: ROUTING_MAX_ACK_PENDING
              value: '16'
          ports:
            - name: http
              containerPort: 8132
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: '2'
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
