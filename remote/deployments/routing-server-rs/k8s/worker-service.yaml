apiVersion: v1
kind: Service
metadata:
  name: dd-routing-server-worker
  namespace: ai-ml
  labels:
    app: dd-routing-server-worker
    app.kubernetes.io/component: worker
spec:
  type: ClusterIP
  selector:
    app: dd-routing-server-worker
  ports:
    - name: http
      port: 8132
      targetPort: http
