apiVersion: v1
kind: Service
metadata:
  name: dd-gleamlang-server
  namespace: default
  labels:
    app: dd-gleamlang-server
spec:
  selector:
    app: dd-gleamlang-server
  ports:
    - name: http
      port: 8081
      targetPort: 8081
  type: ClusterIP
