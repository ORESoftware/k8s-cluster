apiVersion: v1
kind: Service
metadata:
  name: dd-threefa-web-server
  namespace: default
  labels:
    app: dd-threefa-web-server
spec:
  type: ClusterIP
  selector:
    app: dd-threefa-web-server
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
