apiVersion: v1
kind: Service
metadata:
  name: dd-billing-server
  namespace: default
  labels:
    app: dd-billing-server
spec:
  type: ClusterIP
  selector:
    app: dd-billing-server
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
