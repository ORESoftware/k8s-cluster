# Invalid reconciliation run: 31033274687

The GitHub Actions run `31033274687` concluded `success`, but its committed organization reconciliation evidence is **not valid acceptance evidence**.

## Why the run is quarantined

After GitHub API rate limiting began, multiple generated rows:

- embedded a REST `403` rate-limit response as `canonical_org`;
- derived `project_title` from that error payload;
- left Project, pull-request, and governance-issue identifiers or URLs blank;
- still reported `status: ok` and synthetic creation/update actions.

The script invoked `reconcile_org` directly in an `if` condition. Bash disables `errexit` semantics inside commands whose status is being tested, including the nested function body, so failed API parsing and assertions could continue to the final success recorder.

## Repair evidence

- Semantic reconciliation workflow run `31037342125` merged the current `main` rate-limit retry and audit implementation with the fail-closed validation work from PR #990.
- The workflow completed successfully, ran shell syntax plus both validator test suites, removed its one-time workflow, and pushed merged head `0a7f5644a629bb349b36ad7c3c8f7247b20f52c0`.
- This repair evidence proves the branch reconciliation and regression tests only. It does **not** convert run `31033274687` into valid fleet acceptance evidence or claim that all 64 organizations have been reconciled.

## Evidence policy

- Do not use any row from this run to claim that a GitHub Project, organization `.github` repository, documentation PR, governance issue, or Project item was created or verified.
- Preserve the files as incident and regression-test input; do not rewrite history to make the run appear valid.
- A corrected run must pass `scripts/ops/validate_org_project_docs_evidence.py`, contain exactly 64 unique requested organizations, and have complete internally consistent landing evidence for every successful row.
- The corrected reconciler must fail closed on REST/GraphQL errors, rate limits, malformed owner identities, blank URLs, duplicate owners, and incomplete PR or governance-issue evidence.

Tracking: [DEN-2242](https://linear.app/denman/issue/DEN-2242/reconcile-github-projects-and-linear-documentation-across)
