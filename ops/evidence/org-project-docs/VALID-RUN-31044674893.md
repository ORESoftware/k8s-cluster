# Valid reconciliation evidence: run 31044674893, attempt 1

**This directory contains two evidence sets. This one is authoritative.**

The files under [`run-31044674893-attempt-1/`](run-31044674893-attempt-1/) are the
validator-approved evidence of the completed 64-organization GitHub Project and
documentation reconciliation (DEN-2242). The top-level `results.json`,
`results.jsonl`, `README.md`, and `audit.json` in this directory belong to the
**quarantined invalid run `31033274687`** (see
[`INVALID-RUN-31033274687.md`](INVALID-RUN-31033274687.md)) and are retained
unchanged as incident and regression-test input. Per standing policy, superseded
artifacts are documented and kept, not deleted; no quarantined file was modified
by the commit that added this document.

## Provenance

- Workflow: `.github/workflows/ops-sync-org-project-docs-rate-aware-once.yml`
  ("Reconcile organization Projects and docs; audit sealed repository gaps once")
- Run: [`31044674893`](https://github.com/ORESoftware/k8s-cluster/actions/runs/31044674893),
  **attempt 1**, started `2026-08-05T20:34:03Z`, completed `success`
  `2026-08-05T20:48:26Z`, on trusted head
  `c213a9378145724a23b15ffa56657d207b274c49` (an ancestor of `main`).
- Every step succeeded, including "Reconcile all registered organization
  Projects and docs" and "Revalidate exact 64-organization Project evidence".
- Actions artifact: `fleet-reconciliation-31044674893`, **artifact id
  `8946374819`**, 93,466 bytes, created `2026-08-05T20:48:24Z`. Unexpired at
  landing time; under default 90-day retention it expires on or about
  **2026-11-03**, which is why the evidence is committed here. The artifact
  additionally contains per-organization logs, checkpoints, and the advisory
  sealed-fleet audit output not copied into this directory.

### Why the run shows "failure" in the Actions UI

Run `31044674893` was re-run on `2026-08-08T03:58:39Z` (attempt 2). That attempt
failed at "Receive encrypted credential after GitHub API quota recovers" because
no ciphertext was delivered for its handshake nonce, and it performed no
mutation. GitHub surfaces the latest attempt's conclusion, so the run-level
status reads `failure` and masks the successful attempt 1. Attempt-level state
is preserved at
`GET /repos/ORESoftware/k8s-cluster/actions/runs/31044674893/attempts/1`
(`conclusion: success`). Attempt 2 uploaded a separate 350-byte advisory
artifact (id `9016915464`); it does not replace the attempt-1 artifact above.

## Landed files (byte-exact copies from artifact id 8946374819)

| File | SHA-256 |
|---|---|
| `run-31044674893-attempt-1/results.json` | `17dc5fca61aa0b4fe0e1dcd5c6c913dc37c7adf54e20c11db12339e7b6eba184` |
| `run-31044674893-attempt-1/results.jsonl` | `eb4efed7f44474019efacd5f183acab84ee849b50344931c124e289adf37ccf0` |
| `run-31044674893-attempt-1/README.md` | `35cae02195d5f748b08d2fce0924988a1e40e889c22a6e588ec40467b9f9a8da` |

## Validation

```
$ python3 scripts/ops/validate_org_project_docs_evidence.py \
    ops/evidence/org-project-docs/run-31044674893-attempt-1/results.json 64
validated 64 organization reconciliation rows
$ echo $?
0
```

Content summary: 64 rows, 64 unique requested/canonical organizations exactly
matching `ops/portfolio/github-linear-project-registry.tsv`; every row
`status: ok` with a single `run_stamp 20260805T203509Z`; zero rate-limit
payloads; every row carries the canonical owner, an org-owned Project
number/title/URL, a governance issue with `project_item_action: added`, a Linear
URL, and documentation-PR evidence (42 `merged-squash`, 22 `not-needed` no-ops).

## Scope and non-claims

- The advisory sealed private hypesiege/StreemPilot repository audit inside the
  same run recorded `outcome: failure` with `blocking: false`
  (`streempilot/streempilot-cli.rs` push verification returned HTTP 422). By the
  workflow contract that audit never blocks Project/docs acceptance; remediation
  is tracked in DEN-2328 and DEN-1549, not here.
- The retry-registry contract added by PR #1184 (`audit.json`-driven, asserting
  26 invalid + 38 missing organizations) describes the quarantined run
  `31033274687` only. This evidence set refutes the premise that 64/64
  organizations still need a retry run for Project/docs reconciliation.
  Retiring or re-pointing that contract, and the quarantine/acceptance branch in
  `.github/workflows/test-org-project-docs-evidence.yml`, is a deliberate
  follow-up decision left to review; this commit only adds files and changes no
  existing byte.

Tracking: [DEN-2242](https://linear.app/denman/issue/DEN-2242/reconcile-github-projects-and-linear-documentation-across)
