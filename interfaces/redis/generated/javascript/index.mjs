// AUTOGENERATED by remote/libs/interfaces/redis/src/generate.mjs
// Do not edit by hand; edit the JSON Schema under schema/ and regenerate.

/**
 * @typedef {object} AgentThreadBreadcrumb
 * @property {number} id
 * @property {string} threadId
 * @property {string} [taskId]
 * @property {string} kind
 * @property {*} [payload]
 * @property {string} emittedAt
 * @property {string} [podName]
 * @property {string} [branch]
 * @property {string} [provider]
 */

/**
 * @typedef {object} AgentThreadBreadcrumbTail
 * @property {string} threadId
 * @property {number} tailMaxItems
 * @property {AgentThreadBreadcrumb[]} items
 * @property {string} generatedAt
 * @property {string} [source]
 */

/**
 * @typedef {object} VapiPhoneCallerContext
 * @property {string} callerHash
 * @property {number} recentCallCount
 * @property {string} [lastCallId]
 * @property {string} [lastSignal]
 * @property {string} [lastReason]
 * @property {string} [lastEventAt]
 * @property {number} generatedAtMs
 * @property {string} [source]
 */

/**
 * @typedef {object} VapiPhoneCallSignal
 * @property {string} callId
 * @property {string} [callerHash]
 * @property {string} [calledNumberHash]
 * @property {string} signal
 * @property {string} [callerKind]
 * @property {string} [reason]
 * @property {number} recordedAtMs
 */

export function AgentThreadBreadcrumbTailKey(prefix, threadId) {
  return `${prefix}:${threadId}`;
}
export const AGENT_THREAD_BREADCRUMB_TAIL_KEY_DEFAULT_PREFIX = "dd:agent:breadcrumb-tail";
export function ContainerPoolAffinityLockKey(prefix, poolSlug, threadId) {
  return `${prefix}:${poolSlug}:${threadId}`;
}
export const CONTAINER_POOL_AFFINITY_LOCK_KEY_DEFAULT_PREFIX = "dd:container-pool:affinity";
export function MusicDailyGenerationTargetKey(prefix, yyyyMmDd) {
  return `${prefix}:daily:${yyyyMmDd}:target`;
}
export const MUSIC_DAILY_GENERATION_TARGET_KEY_DEFAULT_PREFIX = "dd:music";
export function MusicGenerationRunLockKey(prefix) {
  return `${prefix}:generate:lock`;
}
export const MUSIC_GENERATION_RUN_LOCK_KEY_DEFAULT_PREFIX = "dd:music";
export function MusicSongVisitorVoteKey(prefix, songId, visitorHash) {
  return `${prefix}:song:${songId}:visitor:${visitorHash}:vote`;
}
export const MUSIC_SONG_VISITOR_VOTE_KEY_DEFAULT_PREFIX = "dd:music";
export function RuntimeConfigEntryIndexKey(prefix, env) {
  return `${prefix}:${env}:entry-index`;
}
export const RUNTIME_CONFIG_ENTRY_INDEX_KEY_DEFAULT_PREFIX = "dd:rc";
export function RuntimeConfigEntryKey(prefix, env, scope, key) {
  return `${prefix}:${env}:entry:${scope}:${key}`;
}
export const RUNTIME_CONFIG_ENTRY_KEY_DEFAULT_PREFIX = "dd:rc";
export function RuntimeConfigGenerationKey(prefix, env) {
  return `${prefix}:${env}:generation`;
}
export const RUNTIME_CONFIG_GENERATION_KEY_DEFAULT_PREFIX = "dd:rc";
export function RuntimeConfigSubscriberIndexKey(prefix, env) {
  return `${prefix}:${env}:subs-index`;
}
export const RUNTIME_CONFIG_SUBSCRIBER_INDEX_KEY_DEFAULT_PREFIX = "dd:rc";
export function RuntimeConfigSubscriberKey(prefix, env, name) {
  return `${prefix}:${env}:subs:${name}`;
}
export const RUNTIME_CONFIG_SUBSCRIBER_KEY_DEFAULT_PREFIX = "dd:rc";
export function VapiPhoneCallerContextKey(prefix, callerHash) {
  return `${prefix}:caller:${callerHash}`;
}
export const VAPI_PHONE_CALLER_CONTEXT_KEY_DEFAULT_PREFIX = "dd:vapi-phone";
export function VapiPhoneCallSignalKey(prefix, callId) {
  return `${prefix}:call:${callId}:signal`;
}
export const VAPI_PHONE_CALL_SIGNAL_KEY_DEFAULT_PREFIX = "dd:vapi-phone";
