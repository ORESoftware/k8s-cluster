// AUTOGENERATED by remote/libs/interfaces/redis/src/generate.mjs
// Do not edit by hand; edit the JSON Schema under schema/ and regenerate.
module DdRedisInterfaces

type AgentThreadBreadcrumb =
    { AgentThreadBreadcrumbId: int
      AgentThreadBreadcrumbThreadId: string
      AgentThreadBreadcrumbTaskId: string option
      AgentThreadBreadcrumbKind: string
      AgentThreadBreadcrumbPayload: string option
      AgentThreadBreadcrumbEmittedAt: string
      AgentThreadBreadcrumbPodName: string option
      AgentThreadBreadcrumbBranch: string option
      AgentThreadBreadcrumbProvider: string option }

and AgentThreadBreadcrumbTail =
    { AgentThreadBreadcrumbTailThreadId: string
      AgentThreadBreadcrumbTailTailMaxItems: int
      AgentThreadBreadcrumbTailItems: AgentThreadBreadcrumb list
      AgentThreadBreadcrumbTailGeneratedAt: string
      AgentThreadBreadcrumbTailSource: string option }

and VapiPhoneCallerContext =
    { VapiPhoneCallerContextCallerHash: string
      VapiPhoneCallerContextRecentCallCount: int
      VapiPhoneCallerContextLastCallId: string option
      VapiPhoneCallerContextLastSignal: string option
      VapiPhoneCallerContextLastReason: string option
      VapiPhoneCallerContextLastEventAt: string option
      VapiPhoneCallerContextGeneratedAtMs: int
      VapiPhoneCallerContextSource: string option }

and VapiPhoneCallSignal =
    { VapiPhoneCallSignalCallId: string
      VapiPhoneCallSignalCallerHash: string option
      VapiPhoneCallSignalCalledNumberHash: string option
      VapiPhoneCallSignalSignal: string
      VapiPhoneCallSignalCallerKind: string option
      VapiPhoneCallSignalReason: string option
      VapiPhoneCallSignalRecordedAtMs: int }

let agentThreadBreadcrumbTailKey (prefix: string) (thread_id: string) : string = prefix + ":" + thread_id
let agentThreadBreadcrumbTailKeyDefaultPrefix : string = "dd:agent:breadcrumb-tail"
let containerPoolAffinityLockKey (prefix: string) (pool_slug: string) (thread_id: string) : string = prefix + ":" + pool_slug + ":" + thread_id
let containerPoolAffinityLockKeyDefaultPrefix : string = "dd:container-pool:affinity"
let musicDailyGenerationTargetKey (prefix: string) (yyyy_mm_dd: string) : string = prefix + ":daily:" + yyyy_mm_dd + ":target"
let musicDailyGenerationTargetKeyDefaultPrefix : string = "dd:music"
let musicGenerationRunLockKey (prefix: string) : string = prefix + ":generate:lock"
let musicGenerationRunLockKeyDefaultPrefix : string = "dd:music"
let musicSongVisitorVoteKey (prefix: string) (song_id: string) (visitor_hash: string) : string = prefix + ":song:" + song_id + ":visitor:" + visitor_hash + ":vote"
let musicSongVisitorVoteKeyDefaultPrefix : string = "dd:music"
let runtimeConfigEntryIndexKey (prefix: string) (env: string) : string = prefix + ":" + env + ":entry-index"
let runtimeConfigEntryIndexKeyDefaultPrefix : string = "dd:rc"
let runtimeConfigEntryKey (prefix: string) (env: string) (scope: string) (key: string) : string = prefix + ":" + env + ":entry:" + scope + ":" + key
let runtimeConfigEntryKeyDefaultPrefix : string = "dd:rc"
let runtimeConfigGenerationKey (prefix: string) (env: string) : string = prefix + ":" + env + ":generation"
let runtimeConfigGenerationKeyDefaultPrefix : string = "dd:rc"
let runtimeConfigSubscriberIndexKey (prefix: string) (env: string) : string = prefix + ":" + env + ":subs-index"
let runtimeConfigSubscriberIndexKeyDefaultPrefix : string = "dd:rc"
let runtimeConfigSubscriberKey (prefix: string) (env: string) (name: string) : string = prefix + ":" + env + ":subs:" + name
let runtimeConfigSubscriberKeyDefaultPrefix : string = "dd:rc"
let vapiPhoneCallerContextKey (prefix: string) (caller_hash: string) : string = prefix + ":caller:" + caller_hash
let vapiPhoneCallerContextKeyDefaultPrefix : string = "dd:vapi-phone"
let vapiPhoneCallSignalKey (prefix: string) (call_id: string) : string = prefix + ":call:" + call_id + ":signal"
let vapiPhoneCallSignalKeyDefaultPrefix : string = "dd:vapi-phone"
