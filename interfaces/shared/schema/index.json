{
  "$comment": "Index of every JSON Schema file in this directory. The generator iterates this list (alphabetised) so adding a new schema is a one-line change.",
  "schemas": [
    "agent-orchestration.schema.json",
    "fabrication-cad-conversion.schema.json",
    "fabrication-instruction-generation.schema.json",
    "runtime-config.schema.json"
  ]
}
