{
  "name": "fiducia-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "check:gitops": "node tools/gitops-release.mjs check",
    "test": "npm run check:gitops && node --test tests/*.test.mjs"
  }
}
